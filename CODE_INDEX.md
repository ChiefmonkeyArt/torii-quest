# Torii Quest — Code Index

> Lightweight developer/agent index. Keep this practical and update it as systems are touched.
> Purpose: help future debugging, SDK extraction, FOSS contribution, and AI handoff speed.

Current version: `v0.2.244-alpha`  
Live site: [torii-quest.pplx.app](https://torii-quest.pplx.app)  
**Active focus: 15-hour proof-of-concept route** — shooter is maintenance-only unless demo-breaking; the active MVP is the freedom-tech loop (gateway/NAP-to-NAP preview, Plebeian/Nostr product panel proof, leaderboard preview, torii.quest GitHub update-check). Retrospective polish after PoC validation. See `strategy.md` → "15-Hour Proof-of-Concept Route".

**Task queue split (v0.2.244+):** `quest-todo.md` is the Torii Quest active task source of truth; `continuum-todo.md` is the Torii Continuum active task source of truth; `todo.md` is a legacy pointer (continuity gate + Continuum parser only). Both active task files are safely editable via `npm run md:patch` (`tools/mdPatch.mjs` — whitelist-confined, `.bak` backup, no network, no arbitrary writes).

---

## Index Rule

When a bug fix or feature pass stabilises a system, update at least one of:

- SDK/API seam
- `window.ToriiDebug` hook
- regression/smoke check
- this index

Do not abstract imaginary systems. Index proven systems and extract boundaries from working code.

---

## Core Runtime Areas

| Area | Current location / seam | Notes |
|---|---|---|
| Version/config | `src/config.js`, `index.html`, `tools/regression-check.mjs`, `package.json`, `public/sw.js` | Version must bump every deploy. `godMode` must remain false. v0.2.130 added the frozen `TUNING` snapshot (JSON-serialisable mirror of the balance constants) surfaced via `ToriiDebug.snapshot().config`. **v0.2.137:** `package.json` `version` is now a tracked marker — it must equal `EXPECTED_VERSION` with the leading `v` stripped (valid semver, e.g. `0.2.137-alpha`); regression-check [5] asserts it so package metadata can't drift from runtime `VERSION` again. **v0.2.218:** regression-check [5] also guards `package.json` `"private": true` — fails if package privacy is ever dropped, so this static web-app/game can never be accidentally `npm publish`-ed (security-review advisory). **v0.2.219:** `public/sw.js` `CACHE_VERSION` now TRACKS the version (`tq-<EXPECTED_VERSION>` instead of the static `tq-v1`) — regression-check [5] reads the literal and fails if it does not embed the current version, so every version bump busts the service-worker cache (no stale assets after an asset-changing deploy). `public/sw.js` is copied verbatim by Vite (no module system), so the literal is kept in lockstep via the guard, not an import. |
| Debug/handoff | `src/engine/debug/toriiDebug.js`, `src/engine/debug/snapshot.js`, `src/engine/debug/shellReport.js`, `src/engine/debug/proofSurfaceCheck.js` | `window.ToriiDebug` inspection surface (ships unconditionally in alpha). **v0.2.137:** `engine/debug/shellReport.js` adds read-only reports over the v0.2.136 VIEW shells — `gatewayReport`/`productReport`/`leaderboardReport`/`buildShellReport` (+ `DEMO_GATEWAY`/`DEMO_PRODUCT`/`DEMO_SCORES` fixtures) surfaced on `ToriiDebug.shells.{gateway,product,leaderboard,report}`. Only reads the shells' pure return values — NO signer, NO relay/publish, NO navigation; pure/node-safe; `tests/shell-report.test.js`. **v0.2.145:** `shellReport.js` adds `shellsSummary(inputs)` (surfaced at `ToriiDebug.shells.summary()`) — a one-call discoverability aggregate of the four MVP proof surfaces (gateway/product/leaderboard/update previews) framed by the MVP loop: each surface's `lean`/`step`/`sdk`/`shell`/`title` + inert `invariants` (readOnly/actionable/signed/published) READ from the live `buildShellReport` output, plus an `allInert` gate and `network:false`/`autoUpdate:false`. Pure/read-only; see `SDK_DEBUG_INDEX.md`. **v0.2.146:** `gatewayPreview.js` adds `readOnly:true` (surfaced through `gatewayPreviewReport` + `shellsSummary`) so all four proof surfaces expose symmetric `readOnly`+`actionable`; `shellReport.js` adds `shellsDiff(a,b)` (surfaced at `ToriiDebug.shells.diff()`) — a pure diff of two `summary()` outputs that classifies each invariant flip and flags the ones that *loosen* inertness (`actionable→true`/`readOnly→false`/`signed|published→true`/`allInert→false`/`network|autoUpdate→true`) into a `loosened[]` checklist with a single `safe` boolean, for mechanical preview→live promotion review. No network/actions/DOM/THREE. **v0.2.147:** `engine/world/proofSurfaceSpecs.js` adds a pure LAYOUT/SPEC layer for the future in-world proof meshes (surfaced at `ToriiDebug.shells.surfaceSpecs()`) — `PROOF_SURFACE_SPECS` (4 frozen specs: gateway-portal-panel/product-stall-panel/leaderboard-board/update-prompt-board, each with step/lean/title/kind/previewSdk/shell/anchor + plain `position`/`size`/`yawRad` placement data in the NAP zone + inert `invariants`) + `proofSurfaceLayout()` (badge/anchorZone/bounds/`allInert`/`rendered:false`/`actionable:false`). PLAIN data only — no THREE/DOM/renderer/gameplay. SDK `proofSurfaceSpecs` (experimental). **v0.2.148:** `engine/debug/proofSurfaceCheck.js` adds a pure CROSS-CHECK (surfaced at `ToriiDebug.shells.surfaceSpecCheck(map?,specs?)`) — `checkProofSurfaceSpecs()` verifies each proof-surface spec stays ALIGNED with the live registries before any future mesh pass: `previewSdk` must name a real SDK experimental namespace (`SDK_SURFACE`; unknown→error, known-but-non-experimental→warning), `shell` must name a real `ToriiDebug.shells` report (a `buildShellReport()` key), the inert invariants must hold, and no live-action key may have crept onto a spec. Returns `{ok,badge,checked,errors,warnings,surfaces}`; deterministic; reads only static metadata + demo-fixture output — NO render/network/DOM/THREE. Lives in `engine/debug` (not the SDK barrel) to avoid a barrel↔leaf import cycle; `tests/proof-surface-check.test.js`. **v0.2.130:** pure `engine/debug/snapshot.js` builds the JSON-serialisable status object — `ToriiDebug.snapshot()` (version, phase, run state, player pos, combat last shot/hit/miss, physics+crate summary, `TUNING`) plus focused `ToriiDebug.combat.report()` / `ToriiDebug.physics.report()`. Every field reads behind `safe()` so the surface NEVER throws, even at the title screen or before physics loads. Providers are injected from `main.js` (`getPhase`/`getState`/`getCrateSummary`/`config` + existing getters); the snapshot module imports nothing (no Three/Rapier/DOM) and is unit-tested. **v0.2.343:** `engine/debug/flyCamera.js` adds a dev/debug free-fly camera surfaced at `ToriiDebug.fly.{enable,disable,toggle,enabled}`. It detaches the shared `camera` from `playerObj` (`scene.attach`, world-transform preserving; `rotation.order='YXZ'` for FPS look), then drives it with WASD on the yaw plane + Space/C(Ctrl) vertical + Shift boost (18/60 u/s), reusing the EXISTING pointer-lock yaw/pitch from `input.js` (no second lock; pitch clamped ±~89°). `player.js tickPlayer` early-returns on `isFlyEnabled()` so it skips its per-frame camera write and fly wins; disable re-parents to `playerObj` and the next player tick snaps back to the eye. `arenaRuntime` calls `initFlyCamera({camera,scene,playerObj,onToggle})` + `tickFly(dt)` in the loop, binds **F** to toggle (only when the gateway isn't armed, so one press never does both), and enables it at ENTER when `state.flyMode` is true. Title screen: `#btn-fly-toggle` (three-free DOM) sets `state.flyMode`; `onToggle` shows a HUD toast + keeps the button label/`fly-on` accent in sync. Allocation-free update (module-scope scratch vectors). |
| Physics interaction / raycast SDK | `src/engine/physics/interactions.js`, `src/engine/physics/raycastService.js` | **v0.2.130 first slices.** `interactions.js` — pure, allocation-free `nudgeImpulse(dx,dy,dz,strength,lift,out)` + `applyNudge(body,…,impOut,ptOut)` (forwards to any `applyImpulseAtPoint(imp,pt,wake)` body) and the bullet→crate tuning (`CRATE_IMPULSE`/`CRATE_LIFT`, moved here from `weapons.js`, which now calls `applyNudge` with its existing scratch). `raycastService.js` — `createRaycastService(impl)` injectable facade (`ray`/`rayStatic`/`lineOfSight`; missing impl degrades safely: null/null/true) + default `raycastService` wired to the live Rapier raycast layer, surfaced on `ToriiDebug.physics.service`. **v0.2.132:** all live raycast call sites migrated to the facade — bots LOS via `lineOfSight` (v0.2.131), and `player.js shoot()` aim ray + `weapons.js` recordPlayerShot aim/pred and tickWeapons player/bot bullet rays via `raycastService.ray`/`.rayStatic` (behaviour-identical; the default service wraps the same `raycast.js` functions, barrel→crosshair rule preserved). **v0.2.133:** the last direct `castRay` consumer, the read-only reticle preview (`targetReticle.js`), migrated to `raycastService.ray(...)` too (behaviour-identical; collider forwarded as the exclude arg) — no module now imports `castRay` directly outside the service. Injected-fake-world tests added pinning the ray/LOS contract without Rapier. Both modules import no Three/Rapier and are unit-tested. |
| Game state | `src/state.js` | v0.2.115 added the explicit state machine: `PHASE` + `GAME_EVENT` + transition table; `transition(event)` is the only write path; predicates `isTitle/isPlaying/isPaused/isDead/isGameover/isLive`. All phase reads/writes route through this seam. Check 7 guards against direct `state.phase =` writes elsewhere. v0.2.116: `transition()` also emits `EV.PHASE_CHANGE`. **v0.2.130:** pure weapon-state predicates `canShoot(s=state)` / `canReload(s=state)` extracted here (the firing/reload gates, behaviour-identical to the old inline guards in `player.js`); adopted by `player.js shoot()`/`startReload()`. Dead `state.paused` field removed (no reads/writes anywhere). **v0.2.131:** pure pointer-lock predicates `isEngaged(s=state)` (PLAYING && pointerLocked) / `needsPointerLock(s=state)` (PLAYING && !pointerLocked) extracted here; `needsPointerLock()` adopted at the main.js canvas-click re-lock guard (behaviour-identical fold of the `pointerLocked` boolean — ARS-4 slice). **v0.2.132:** reload sub-state folded into the FSM — `isReloading(s=state)` predicate + `tickReload(dt,s=state)` transition (counts `reloadTimer` down; on completion clears `reloading` and refills `ammo=MAX_AMMO`, returns whether it finished this tick). Adopted by `player.js` (reload tick → `if (tickReload(dt)) emit(EV.HUD_UPDATE)`), `weapons.js` (viewmodel gate `isReloading()`), and `main.js` (HUD/model). ARS-4 slice. **v0.2.133:** GAMEOVER edge wired — `GAME_EVENT.END` added (PLAYING/DEAD → GAMEOVER) plus a thin `endRun()` helper (`transition(END)`); GAMEOVER stays terminal (no outgoing edges yet). Behaviour-preserving: no live call site fires END, so the endless die→respawn flow is unchanged; the edge + helper are the named entry point for a future end-of-run screen. ARS-4 slice. |
| Event bus | `src/events.js` | Decoupling seam — modules publish/subscribe by name instead of importing each other. `EV` is the canonical registry; `on/off/emit`. Imports nothing (no cycles). v0.2.116 wired `EV.PHASE_CHANGE` (payload `{from,to,event}`) from `state.transition()` and added check 8 (every `EV.<NAME>` reference must be defined here). v0.2.117 migrated the bot-hit bridge onto the bus: weapons.js emits `EV.BOT_HIT_BY_PLAYER` (`{bot,dmg}`), main.js subscribes (`hitBot`+`flashCross`); the legacy `window._onBotHit` global is now a deprecated debug-tap alias that forwards onto the bus, and check 9 forbids internal `window._onBotHit(` calls. v0.2.118 moved the foliage shader materials off `window` into a module-scope registry in `arena-foliage.js` (`tickFoliage`/`getGrassMat`/`getFlowerMat`); v0.2.119 moved the mirror Reflector handle off `window` into a `mirror.js` accessor (`getMirror()`); `window._grassMat`/`_flowerMat`/`_mirrorMesh` remain only as deprecated debug aliases and check 10 forbids internal reads of them. **No functional `window.*` globals remain as internal wiring.** v0.2.121 added the FIRST real `PHASE_CHANGE` subscriber: main.js subscribes and drives top-level screen visibility (title/HUD/pause modal) via the pure `engine/ui/phaseScreens.js` map, replacing the imperative `classList` toggles that were scattered across the ENTER/PAUSE/RESUME/HOME call sites. Dormant seams remaining: `WS_PLAYER_HIT`, `WS_CHAT`. |
| Scene/rendering | `src/scene.js`, mirror modules, Three.js renderer | Mirror and first-person camera regressions should be checked manually. **v0.2.343:** the aurora sky dome was re-tessellated from `SphereGeometry(500,16,8)` to `(500,64,32)` to kill two straight pale-blue band seams (an "X" through the zenith) that appeared as great-circle chords across the coarse 16-segment facets; the fragment shader already renormalises `dir` per fragment, so raising the segment count is the fix (one draw call, `frustumCulled=false`, art unchanged). |
| Player | `src/engine/entities/player.js` (boundary), `src/player.js` (runtime), `src/firstPersonBody.js`, `playerObj` | v0.2.114 began the boundary: geometry, spawn shape, and look-down POV math. v0.2.123 added the movement **heading basis** (`forwardX`/`forwardZ`/`rightX`/`rightZ(yaw)` — pure scalars the movement tick writes into its scratch vectors; LOS/allocation-free) and narrowed the module's import to the pure `engine/physics/bodies.js` leaf so it (and its tests) stay free of the Three/Rapier chain. Stateful movement tick, combat, lifecycle, and body-state still in `src/player.js` (next slice). Covered by `tests/player-boundary.test.js`. |
| Weapons/combat | `src/weapons.js`, `src/engine/weapons/muzzle.js`, `src/engine/combat/classifier.js`, `src/engine/combat/shotDiagnostics.js`, `src/engine/combat/damage.js`, `src/engine/combat/aim.js`, `src/engine/weapons/reloadPose.js`, `src/targetReticle.js`, `src/hud.js` | **v0.2.129 — muzzle origin side fixed.** `getGunBarrelWorld` built the barrel offset basis from the camera's LOCAL quaternion, but the FP camera is a CHILD of `playerObj` (which holds the YAW; the camera holds only pitch), so the +0.12 right offset pointed in a fixed WORLD direction regardless of facing — as the player turned, the bullet/tracer origin swung to the wrong (notably LEFT) side. Barrel math extracted to the pure `engine/weapons/muzzle.js` (`muzzlePoint`/`barrelWorldFromCamera` + `MUZZLE_FORWARD/RIGHT/UP`); the basis is now built from `camera.getWorldQuaternion()` so the +right offset tracks yaw and the muzzle stays on the visible right-hand gun. Barrel→crosshair DIRECTION unchanged — only the origin side corrected. `tests/muzzle.test.js` locks the +right side convention and the world-quaternion yaw-tracking (origin stays on the camera world-right at every facing). **v0.2.127 — reload viewmodel feel reworked to "click down, clack snap back" via the pure `engine/weapons/reloadPose.js` (`reloadDip`); `RELOAD_TIME` and audio unchanged.** **v0.2.126 — barrel-origin projectile aimed THROUGH the crosshair (current firing rule).** `player.js shoot()` casts the camera/crosshair ray to find the point the reticle is on (first hit, or `CONVERGE_DIST` 80 m fallback), spawns the bullet at the gun BARREL (`getGunBarrelWorld`), and fires it along **barrel → that crosshair point** (pure `engine/combat/aim.js`: `crosshairPoint`/`aimDirection`/`CONVERGE_DIST`). The projectile passes through the exact spot the reticle classified, so a previewed headshot lands as a headshot — convergence happens at the ACTUAL aimed point at any range, not a fixed distance. This **supersedes the v0.2.125 camera-origin experiment** (bullet == camera ray, muzzle nudged off the gun): that killed parallax but moved the muzzle off the barrel, so the new rule restores the gun-barrel origin while keeping the shot honest. Diagnostics still compare the camera aim line vs the bullet line. Damage mapping extracted to the pure `engine/combat/damage.js` (`shotDamage(isHead)`→9/3, `applyDamage`, `isLethal`); `EV.BOT_HIT_BY_PLAYER` now also carries `isHead`. Contract locked: headshot 9 ≥ `BOT_HP` 5 (one-shot), body 3 (two-shot). v0.2.113 introduced the shared headshot classifier for bullets and HUD preview. v0.2.120 extracted it into a pure module `engine/combat/classifier.js` (`isInHeadSphere`/`classifyHeadshot` + derived `HEAD_BOTTOM`/`HEAD_PROX`; imports only the geometry constants from `bodies.js`, no Three/Rapier) so it is unit-tested; `weapons.js` re-exports it unchanged. **v0.2.124 — target-practice diagnostics.** The reticle preview (`targetReticle.js`) is an instantaneous hitscan ray from the CAMERA, but the player bullet is a projectile fired from the gun BARREL toward an 80 m convergence point (`player.js shoot()`) — so a green/👌 reticle can still miss, especially at range (target moves during flight, and the barrel/convergence offset only zeroes at ~80 m). `weapons.js` now records a per-shot diagnostic: at fire time `recordPlayerShot()` casts both the aim line (camera) and the bullet line and stores what each is on; at resolution the actual outcome is captured and a miss reason derived via the pure `engine/combat/shotDiagnostics.js` (`classifyShotOutcome(aim,outcome) -> {reason,label}`; categories: `head`/`body`/`head-to-body`/`blocked`/`moved-or-offset`/`aim-off`). Surfaced via `ToriiDebug.combat.lastShot`/`lastMiss` (and `lastHit`). Per-shot allocations only (never per-frame). No gameplay change — diagnostics only. |
| Tests | `tests/*.test.js`, `vite.config.js` (`test` block), `npm test` | v0.2.120 added Vitest (node env). Suites: `state.test.js` (FSM transitions/guards/predicates), `events.test.js` (bus on/emit/off/no-op/ordering), `classifier.test.js` (head-vs-body geometry), `phaseScreens.test.js` (v0.2.121 — phase→screen map + the PHASE_CHANGE subscriber integration), `bot-agent.test.js` (v0.2.122 — BotAgent decision helpers + decideActions facade), `player-boundary.test.js` (v0.2.123 — movement heading basis + look-down POV math + geometry constants), `shot-diagnostics.test.js` (v0.2.124 — aim-vs-outcome miss-reason classifier), `combat-damage.test.js` (v0.2.125 — head/body damage + kill-threshold contract vs `BOT_HP`), `aim.test.js` (v0.2.126 — barrel→crosshair aiming: `crosshairPoint`/`aimDirection`/convergence + the guarantee that a barrel-fired bullet passes through the aimed point), `reload-pose.test.js` (v0.2.127 — `reloadDip` "click down, clack snap back" curve: drop/hold/snap-back/settle phases, rest at p=0 and p=1, overshoot bounds). v0.2.128 added 7 head-zone realignment cases to `classifier.test.js` (face shot scores; crown ~1.70 still in; above-crown 1.85/1.95 NOT head; torso stays body; lateral shoulder not promoted; outright head-collider resolve still wins). v0.2.129 added `tests/muzzle.test.js` (11 cases — barrel +right side convention; `barrelWorldFromCamera` keeps the origin on the camera WORLD-right across yaws, the local-quaternion regression). v0.2.130 added `tests/snapshot.test.js` (snapshot/combat-report/physics-report incl. missing + throwing providers, JSON round-trip, safe-before-init), `tests/interactions.test.js` (`nudgeImpulse` math + `applyNudge` against a fake body; null/incapable body returns false), `tests/raycast-service.test.js` (facade delegation + safe degradation when impls absent), and `canShoot`/`canReload` truth-table cases in `tests/state.test.js`. **v0.2.131** added `tests/sdk.test.js` (public SDK entrypoint — `SDK_VERSION`==config VERSION, valid stability tiers, every non-internal surface re-exported / internals forward-declared only, representative exports resolve to real module fns) and `isEngaged`/`needsPointerLock` pointer-lock predicate cases in `tests/state.test.js`. **v0.2.132** added `tests/component.test.js` (component contract — manifest validation, `isComponent` shape, `defineComponent` idempotent mount/unmount + SDK `component` exposure), `isReloading`/`tickReload` reload-fold cases in `tests/state.test.js`, and default-`raycastService` production-wiring cases in `tests/raycast-service.test.js`. **v0.2.133** added `tests/torii-gateway.test.js` (reference Torii gateway component — contract validity, gateway manifest, config→manifest flow, symmetric idempotent mount/unmount, SDK experimental exposure), `END`/`endRun()` GAMEOVER-edge cases in `tests/state.test.js`, and an injected-fake-world ray/LOS contract block in `tests/raycast-service.test.js`. **v0.2.134** added `tests/travel-intent.test.js` (gateway-protocol URL handoff — build/validate/serialise/parse a travel intent + round-trip + SDK exposure), `tests/product-display.test.js` (read-only product component — contract validity, safe `validateProduct`, no checkout/pay/zap surface, SDK exposure), and `tests/leaderboard.test.js` (Nostr leaderboard — `buildScore`/`validateScore`/`buildScoreEventTemplate` unsigned event, headshots≤kills, indexable tags). **v0.2.135** added `tests/registry.test.js` (component loader/registry — register-by-factory probe+validate, index by id/kind, `load` returns fresh contract-valid instance, unknown/incompatible degrades safely, built-in registry, SDK exposure), `tests/gateway-handoff.test.js` (gateway portal/handoff shell — `gatewayDestination`/`planGatewayTravel`/`gatewayTravelUrl` map a gateway component onto a validated travel intent/URL, round-trip via `parseTravelUrl`, non-gateway rejected, SDK exposure), `tests/product-panel.test.js` (product panel view-model — `priceLabel` rules, flat render-ready view, invalid degrades, no checkout surface, SDK exposure), and `tests/leaderboard-publisher.test.js` (publisher adapter — build-only/signer-only/signer+publisher paths via injected fakes, failures captured not thrown, invalid score still throws, SDK exposure). **v0.2.136** added `tests/gateway-portal.test.js` (gateway portal view shell — `shortKey` truncation, `destinationLabel` fallbacks, `gatewayPortalView` armed/ready with a parseable URL preview, custom prompt+base, not-armed on malformed player npub, not-a-gateway, SDK exposure), `tests/product-panel-shell.test.js` (product panel render shell — ordered Price/Seller/reward lines, reward row omitted when absent, display-only `actionable:false` footer, NO commerce action in `actions[]` or JSON, invalid degrades to `panel:null`, SDK exposure), and `tests/leaderboard-view.test.js` (leaderboard view — `accuracyLabel`, `rankScores` deterministic order with a skipped invalid row, safe on null, default mock mode + counts, build mode + VIEW_MODES, rejects 'live' mode, build-only preview with `signed:false`/`published:false` + bad rows captured, SDK exposure). **v0.2.137** added `tests/shell-report.test.js` (read-only debug reports over the v0.2.136 shells — armed demo gateway / not-a-gateway, product read-only with empty `actions[]`, deterministic leaderboard rank with `signed:false`/`published:false`, `buildShellReport` composite + overrides, frozen demo fixtures). **v0.2.138** added `tests/update-check.test.js` (torii.quest GitHub update-check — `compareVersions` numeric/v-prefix/prerelease ordering, `parseRelease` normalisation + bad-input safety, `evaluateUpdate` update-available/up-to-date/unknown + default runtime VERSION, `updateCheckView` inert view-model with `actionable:false` + capped notes preview + no fetch/update/navigate/install keys). **v0.2.139** added `tests/gateway-preview.test.js` (gateway/NAP-to-NAP visible preview — `statusText` label map + fallback, `previewUrl` cap/safety, `gatewayPreviewBlock` armed/ready with intent + capped URL, render-ready label/value rows, inert `actionable:false` + no clickable/action keys, not-armed on invalid plan, NO GATEWAY for non-gateway, SDK exposure, frozen status table). **v0.2.140** added `tests/product-preview.test.js` (Plebeian/Nostr product/market visible preview — `shortNpub` truncation/safety, `previewUrl` cap/safety, `productPreviewBlock` render-ready Product/Price/Seller-npub/reward/Marketplace/Link rows with shortened seller, reward row omitted when absent, inert `readOnly:true`/`actionable:false` + no checkout/pay/zap/clickable keys, invalid product degrades to `ok:false`+errors, SDK exposure). **v0.2.141** added `tests/leaderboard-preview.test.js` (local/mock leaderboard visible preview — `shortNpub` truncation/safety, `modeLabel` mapping, `formatRankRow` format, `leaderboardPreviewBlock` inert local-mock block with `signed:false`/`published:false`/`actionable:false`/`readOnly:true`, deterministic desc rank, kind-30000/#torii-quest proof shape surfaced, ordered Mode/Signer/Status/Event framing + ranked `#n` rows, npub identity flavour shortened, limit caps shown rows but counts all, empty → "NO LOCAL SCORES", invalid scores degrade into `skipped` without throwing, no sign/publish/submit/relay keys, SDK exposure). **v0.2.142** added `tests/update-preview.test.js` (torii.quest update-check visible preview — `statusLabel` mapping + frozen `STATUS_TEXT`, `updatePreviewBlock` inert update-available block with `actionable:false`/`readOnly:true`, ordered Version/Latest/Status/Source/Notes rows, GitHub releases-page path surfaced as display-only text, up-to-date when runtime matches the release, draft/unparseable degrades to UNKNOWN without throwing, default runtime VERSION, capped single-line notes preview, no fetch/install/update/navigate keys, SDK exposure). **v0.2.143** added `tests/mvp-loop.test.js` (MVP-loop clarity header — frozen ordered `MVP_LOOP_STEPS` TRAVEL/MARKET/SCORE/UPDATE with n 1..4 + LEAN ids, `MVP_LOOP_FLOW` === 'Travel → Market → Score → Update', `mvpLoopSummary` inert render-ready block with `readOnly:true`/`actionable:false`, lines labels '1 · TRAVEL'..'4 · UPDATE' mapped to Gateway/Product/Leaderboard/Update check, default + override version, never exposes fetch/navigate/href/onClick/sign/publish/update/checkout keys, SDK experimental exposure). **v0.2.145** added 4 `shellsSummary()` cases to `tests/shell-report.test.js` (the discoverability aggregate — four proof surfaces in loop order with LEAN/step/sdk/shell keys; version + flow tied to the MVP loop; every surface inert with `actionable`/`signed`/`published` never true + `allInert`/`network:false`/`autoUpdate:false`; no live-action keys leak). **v0.2.146** added 6 cases to `tests/shell-report.test.js` (the gateway preview + `shellsSummary` now report symmetric `readOnly:true`/`actionable:false` across all four surfaces; `shellsDiff()` — identical summaries are unchanged+safe, a surface flip to `actionable:true` is flagged in `loosened[]` with `safe:false` alongside the `allInert` flip, a tightening flip is `safe:true`, added/removed surfaces never loosen, no live-action keys leak). **v0.2.147** added `tests/proof-surface-specs.test.js` (9 cases — the pure in-world proof-surface SPEC layer: 4 frozen specs in loop order with ids/steps TRAVEL/MARKET/SCORE/UPDATE + LEAN-2..5; `position`/`size`/`yawRad` are PLAIN finite-number data, never THREE Vector3/Matrix4, with x within the NAP zone and positive sizes; every spec inert `readOnly:true`/`actionable:false` and never signed-or-published-true (leaderboard explicitly `signed:false`/`published:false`); no live-action keys (fetch/navigate/href/sign/publish/checkout/onClick/mesh/geometry); `getProofSurfaceSpec` by id + null on unknown; `proofSurfaceLayout()` count 4 / anchorZone nap-zone / `allInert:true` / `rendered:false` / `actionable:false` / bounds within the NAP zone). **v0.2.148** added `tests/proof-surface-check.test.js` (14 cases — the spec↔registry cross-check: all four current specs pass against the real SDK experimental + `buildShellReport()` shells registries (default + injected map), deterministic; unknown/missing `previewSdk` and unknown/missing `shell` are errors; a known-but-non-experimental SDK reference is a WARNING (not an error); loosened invariants (readOnly/actionable/signed/published) are flagged; every forbidden live-action key on a spec is flagged; the real specs carry none). **v0.2.149** added `tests/anchor-transforms.test.js` (14 cases — the pure anchor→transform contract: the registry exposes exactly the four anchor ids each with a plain ground `origin` (y:0)/`parent`/`zone`; `getAnchor` returns the frozen anchor or null; `resolveAnchorTransform` binds each current spec to a plain-data descriptor (origin/position/offset/size/yawRad, JSON round-trips, no THREE) with `origin+offset===position`; unknown anchor / bad input → null; no live-action keys on the descriptor; `resolveAllAnchors` resolves all four with `ok:true`/empty `unresolved`, is deterministic, reports unresolved anchors instead of throwing, and defaults to an empty report for a non-array; every spec anchor maps to exactly one registry entry). **v0.2.150** added `tests/proof-surface-render-plan.test.js` (the pure render plan — default plan opens both gates with one panel per spec in loop order; each panel is plain finite position/size/yawRad data with a numeric colour and a plain Object prototype; readOnly:true/actionable:false + no live-action keys; JSON round-trips; deterministic; panel position/size/yaw/label/sublabel stay consistent with the specs + `resolveAllAnchors`; injected failing anchors → ok:false + 'anchors-unresolved' + empty panels; injected failing check → ok:false + 'spec-check-failed'; both fail → both reasons) and `tests/proof-surface-meshes.test.js` (adapter guard paths, DOM-free — initial `not-built` state; a failing plan refuses to build without throwing and carries the gate reasons; a null scene → `no-scene`). **v0.2.151** added `tests/proof-surface-parent-binding.test.js` (the pure scene-graph parent binding — the parent-hint → node-name / group-name maps; the live plan binds all four panels into the `torii-gate` (gateway) + `nap-zone-floor` (product/leaderboard/update) groups in plan order; every panel id partitions across exactly one group; deterministic + JSON-serialisable with no live-action keys; falls back to the anchor registry when a panel omits `parent`; unknown anchor → `unbound`; null/empty plan degrades without throwing) + a panel-`parent` assertion in `tests/proof-surface-render-plan.test.js`. **v0.2.152** added `tests/proof-surface-gate.test.js` (7 cases — the promotion/regression gate `proofSurfaceGate()`: the LIVE gate is `ok` with all three sub-gates green + counts 4/2/4/0 + empty reasons; deterministic, JSON-serialisable, inert (`rendered`/`actionable` false, no live-action keys); an injected failing spec-check fails fast (specCheck false, cascades to renderPlan) with the spec error in `reasons`; injected unresolved anchors → renderPlan false + 'render-plan: anchors-unresolved'; an explicitly not-ok plan fails renderPlan; an `ok` plan carrying an orphan panel → parentBinding false + 'parent-binding: unbound orphan' + unbound count; an injected not-ok binding is reported directly). **v0.2.153** added `tests/bundle-sizes.test.js` (16 cases — the pure bundle-size formatting/classification in `tools/bundleSizes.mjs`: `formatBytes` B/KB/MB rounding + `n/a` on invalid/negative; `classifyAsset` hash-agnostic stem match for app/three/rapier/runtime/html + `other` fallback + `isJsCategory`; `severityFor` strict over-limit + custom limit + NaN-safe; `summarizeBundle` size-desc sort + per-category/JS-vs-html totals + over-limit warnings + deterministic JSON-serialisable output + empty/missing-gzip/custom-warnLimit edge cases). **v0.2.154** added `tests/doc-consistency.test.js` (18 cases — the pure docs/status consistency checks in `tools/docConsistency.mjs`: `versionInText` exact-match + bad-input safety; `findVersionMarkers` order + empty; `staleLiveVersionLines` flags a `live/published version: vX` status line off the current version but NOT a deploy-task line that merely mentions "live"+a version; `checkDocConsistency` passes when continuity docs carry the version, HARD FAILS on continuity-doc drift / missing core doc / explicit `present:false` / no-version, only WARNS on advisory-doc lag + stale live lines, deterministic + JSON-serialisable, exposes the doc lists). **v0.2.155** added 5 cases to `tests/doc-consistency.test.js` (the quoted/changelog-prose suppression — `staleLiveVersionLines` strips backtick inline-code spans + double-quoted spans before matching, so a line that QUOTES `` `Live published version: vX` `` or `"live/published version: vX"` is no longer false-flagged, while a plainly-stated `Live published version: vX` status line and a `deployed version = vX` assertion are still flagged). **v0.2.156** added `tests/handoff-status.test.js` (14 cases — the pure AI-handoff status assembly/formatting in `tools/handoffStatus.mjs`: `stripV` leading-`v` strip + bad-input safety; `versionAgreement` config↔package sync + drift; `buildHandoffStatus` assembles a complete JSON-serialisable status, reports missing core docs, degrades safely when git/dist absent, surfaces version drift, deterministic, does not mutate caller arrays; `formatHandoffStatus` renders VERSION/pkg/commit/live-URL/checks, flags DRIFT + MISSING docs, notes no-dist, safe on null). 505 tests / 44 files. Run with `npm test`; `npm run check` stays separate but check [11] statically guards the scaffold exists, check [12] RUNS the proof-surface gate, check [13] reports the advisory bundle baseline, and check [14] guards docs/status consistency. |
| Docs/status consistency | `tools/docConsistency.mjs`, `tools/regression-check.mjs` (check [14]) | **v0.2.154-alpha (DOC-GUARD).** Keeps the cross-model handoff docs from drifting off the runtime VERSION. `tools/docConsistency.mjs` is PURE/node-safe (no fs/network/THREE/DOM): `CONTINUITY_DOCS` (`todo.md`/`progress.md`/`HANDOFF.md` — hard-fail on version drift), `ADVISORY_DOCS` (`SDK_DEBUG_INDEX.md`/`CODE_INDEX.md` — warn only), `versionInText(version,text)`, `findVersionMarkers(text)`, `staleLiveVersionLines(text,version)` (narrowly matches a `live/published/deployed … version <marker>` status assertion — task/changelog lines mentioning "live"+a version are NOT flagged; v0.2.155 also strips backtick inline-code + double-quoted spans first so changelog/prose that QUOTES the pattern is not false-flagged), `checkDocConsistency({version,files,present})` → `{ok,version,errors,warnings,checked}`. Regression-check `[14]` reads the docs and runs the checker: HARD FAILS on continuity-doc current-version drift / missing core doc; ADVISORY warnings (never fail) for advisory-doc lag + stale live-version lines. Build-time only — never imported by the game; reads files only (no network/install). `tests/doc-consistency.test.js`. **v0.2.155-alpha:** the stale-live ADVISORY now strips backtick inline-code + double-quoted spans before matching, so changelog/prose that QUOTES the pattern is no longer false-flagged. |
| AI-handoff auto-summary | `tools/handoffSummary.mjs`, `tools/handoff-summary.mjs` (`npm run handoff:summary`) | **v0.2.190-alpha (HANDOFF-SUMMARY).** A concise AI handoff brief for the NEXT agent/model that folds the existing local readiness signals into one block — version, git commit, live URL, current gate status, regression/test counts, latest reports, next safe task, key constraints, and the exact release-verification commands. `tools/handoffSummary.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM): `HANDOFF_SUMMARY_BADGE`/`HANDOFF_SUMMARY_SCHEMA` ('torii.handoff-summary')/`HANDOFF_SUMMARY_SCHEMA_VERSION` (=1)/`HANDOFF_SUMMARY_LIVE_URL`, frozen `VERIFY_COMMANDS` (npm run check / npm test / npm run release:status / npm run test:release), frozen `KEY_CONSTRAINTS` (godMode false / version bump every deploy / no new setTimeout / no new Vector3-Matrix4 in hot paths / comments use 'nostrich' / Chiefmonkey spelling / debug tools ship unconditionally / ESC pause + panel-click fire safety), `DEFAULT_NEXT_SAFE_TASK`; `buildHandoffSummary({version,packageVersion,gitCommit,liveUrl,release,nextSafeTask,constraints,verifyCommands,latestReports,generatedAt})` folds a `buildReleaseReadiness()` summary into a JSON-serialisable `{schema,schemaVersion,generatedAt,badge,version,packageVersion,gitCommit,liveUrl,gate:{status,statusLabel,ready,gateCommand,blockers,unknowns,regression,testProfiles},nextSafeTask,constraints,verifyCommands,latestReports}`; a null/garbled release degrades to an honest `gate.status:'unknown'` ('NO RELEASE SUMMARY', ready:false) and never throws. Deterministic by design: `generatedAt` is the ONLY non-deterministic field (omit → null, reproducible). `formatHandoffSummary(summary)` → text block, `formatHandoffSummaryMarkdown(summary)` → markdown, both safe on null. CLI `tools/handoff-summary.mjs` reuses `gatherReleaseReadiness()` (`realpathSync` run-guard — silent on import) + reads config/package/git; modes default text / `--json` / `--markdown`; READ-ONLY/local/no-network and NEVER writes unless an explicit `--write[=path]` flag is supplied (markdown to `handoff-summary.md`). `tests/handoff-summary.test.js` (13). |
| Agent handoff readiness export | `tools/agentHandoff.mjs`, `tools/agent-handoff.mjs` (`npm run handoff:agent`) | **v0.2.199-alpha (AGENT-HANDOFF-READINESS).** An agent-friendly handoff export so future agents/models (incl. non-Perplexity tools like DeepSeek/Perplexica/Routstr-style handoffs) can continue WITHOUT reading the whole repo — a SUPERSET layer that COMPOSES the existing `buildHandoffSummary` brief + the v0.2.198 `runMvpReadiness` rollup, adding the two pieces the summary lacked: the MVP readiness pct/status and the smoke-harness inventory. `tools/agentHandoff.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM): `AGENT_HANDOFF_BADGE`, `AGENT_HANDOFF_SCHEMA` ('torii.agent-handoff'), `AGENT_HANDOFF_SCHEMA_VERSION` (=1), `AGENT_HANDOFF_WRITE_FILENAME` (=`HANDOFF.generated.md`), frozen `SMOKE_HARNESSES` (5: readHealth/gatewayTravelSmoke/updateFlowSmoke/hostRouteSmoke/mvpReadiness, each with SDK name / `ToriiDebug.shells.*(o?)` shell / rollup `signalKey`); `buildAgentHandoff({handoffSummary,mvpReadiness,smokeHarnesses,generatedAt})` folds the summary + rollup into `{schema,schemaVersion,generatedAt,badge,version,packageVersion,gitCommit,liveUrl,gate,readiness:{pct,status,ok,summary,reasons},harnesses(annotated live status by signalKey),nextSafeTask(prefers rollup task, falls back to summary string),constraints,verifyCommands,latestReports}`; null/garbled inputs degrade to honest UNKNOWNs, never throws. `formatAgentHandoff(handoff)` → text, `formatAgentHandoffMarkdown(handoff)` → markdown (harness table + a "the curated `HANDOFF.md` stays the source of truth" note), both safe on null. CLI `tools/agent-handoff.mjs` reuses `gatherReleaseReadiness()` + `buildHandoffSummary()` + `runMvpReadiness()` behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown` / `--write[=path]` (the ONLY writer; defaults to `HANDOFF.generated.md`, confined in-repo via the SHARED `resolveHandoffWritePath`); READ-ONLY/local/no-network, always exits 0. The curated `HANDOFF.md` is NOT replaced; `HANDOFF.generated.md` is in neither the continuity nor advisory doc list (no gate touches it). `tests/agent-handoff.test.js` (13). |
| Machine-readable next-action state | `tools/nextActionState.mjs`, `tools/next-action-state.mjs` (`npm run handoff:next`) | **v0.2.217-alpha (NEXT-ACTION-STATE · LOCAL · READ-ONLY).** A compact machine-readable next-action/handoff state so an agent (GPT/Claude/DeepSeek/other) can pick up the safe MVP pipeline WITHOUT reading the whole repo — a FLATTENING of the existing `buildAgentHandoff()` export, NOT a second task list. `tools/nextActionState.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM; never throws): `NEXT_ACTION_STATE_BADGE` ('NEXT-ACTION STATE · LOCAL · READ-ONLY'), `NEXT_ACTION_STATE_SCHEMA` ('torii.next-action-state'), `NEXT_ACTION_STATE_SCHEMA_VERSION` (=1), `NEXT_ACTION_STATE_WRITE_FILENAME` (=`NEXT_ACTION_STATE.json`), frozen `NEXT_ACTION_STATE_REQUIRED_KEYS` (schema/schemaVersion/badge/version/gitCommit/liveUrl/release/readiness/tests/manualBlocker/nextSafeTask/docs/reports/safety). `buildNextActionState({agentHandoff,manualValidation,testStatus,docs,generatedAt})` flattens the handoff into `{schema,schemaVersion,badge,generatedAt,version,packageVersion,gitCommit,liveUrl,release:{ready,gateStatus,gateCommand,blockers,regression:{count,expected}},readiness:{pct,status},tests:{passing,files},manualBlocker:{pending,statusLabel,pill},nextSafeTask:{title,why,kind},constraints[],docs[],reports[],safety:{deploy/publish/push/tag/networkWrite/nostrWrite/godMode=false}}`; `manualBlocker.pending` = `manualValidation.pill !== 'no-blocker'` (same source `build-continuum.mjs` uses), null when no manual card; null/garbled inputs degrade to honest nulls / `gateStatus:'UNKNOWN'` and never throw. `formatNextActionState(state)` → text, `formatNextActionStateMarkdown(state)` → markdown, both safe on null. CLI `tools/next-action-state.mjs` reuses `gatherReleaseReadiness()` + `buildHandoffSummary()` + `buildAgentHandoff()` + `runMvpReadiness()` + `buildManualValidationModel()`/`CURRENT_TEST_STATUS` (continuumData) + `CORE_DOCS` (handoffStatus) behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `NEXT_ACTION_STATE.json`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; READ-ONLY/local/no-network, always exits 0 otherwise. `tests/next-action-state.test.js` (13; required keys always present, no stale version vs config VERSION + CURRENT_TEST_STATUS, manual-blocker derivation, degraded/garbled inputs, all-false safety, formatters). Composes shipped signals only — no new gameplay/physics/Nostr/network behaviour. |
| Dashboard-smoke state | `tools/dashboardSmokeState.mjs`, `tools/dashboard-smoke-state.mjs` (`npm run dashboard:smoke`), `DASHBOARD_SMOKE_STATE.json` | **v0.2.232-alpha (DASHBOARD-SMOKE STATE · LOCAL · READ-ONLY · OBSERVED ON THE DEPLOYED DASHBOARD).** A single auditable record of the latest LIVE cloud-browser smoke of the deployed Continuum DASHBOARD (`/continuum.html`), surfaced alongside the app-entry [[live-smoke-state]]. `tools/dashboardSmokeState.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM; never throws): `DASHBOARD_SMOKE_BADGE`, `DASHBOARD_SMOKE_SCHEMA` ('torii.dashboard-smoke-state'), `DASHBOARD_SMOKE_SCHEMA_VERSION` (=1), `DASHBOARD_SMOKE_FILE` (=`DASHBOARD_SMOKE_STATE.json`), `DASHBOARD_SMOKE_RESULTS` ({PASS,FAIL,UNKNOWN}), `DASHBOARD_SMOKE_CHECK_OUTCOMES` (`['pass','fail','skip']`), frozen `DASHBOARD_SMOKE_REQUIRED_KEYS`. `buildDashboardSmokeState({result,version,commit,dashboardUrl,surface,smokedAt,smokedBy,checks,notes,generatedAt})` → a JSON-serialisable record; `result` is COERCED (anything not exactly `'pass'`/`'fail'` → `'unknown'`), checks are normalised (idless dropped, unknown outcome → `'skip'`), adds a `surface` field (the page observed), and the `safety` block is pinned all-false incl. BOTH `impliesApproval:false` AND `impliesPlaytestComplete:false`. `validateDashboardSmokeState(state)` → `{ok,errors,warnings}` is the PASS-REQUIRES-EVIDENCE floor: a `pass` verdict ERRORs unless ≥1 check, NO failed check, a concrete version marker, AND a `smokedAt`. `isDashboardSmokePass(state)` is strict. `formatDashboardSmokeState(state)` → text (null-safe); `summarizeDashboardSmokeForState(state)` → `{result,pass,version,surface,smokedAt,checks,passed,failed,impliesApproval:false,impliesPlaytestComplete:false}` (folded into `nextActionState.dashboardSmoke`). CLI `tools/dashboard-smoke-state.mjs` reshapes/reads the committed `DASHBOARD_SMOKE_STATE.json` (text / `--json`) and re-persists a NORMALISED record only under a flag-gated in-repo `--write`. The committed record holds the **v0.2.231-alpha** dashboard smoke (4/4 checks: page loads; Continuum version visible; LIVE SMOKE v0.2.230 PASS evidence visible; active-slice visible). The recorded version legitimately LAGS config `VERSION`; `tests/dashboard-smoke-state.test.js` asserts the floor + a non-staleness guard (recorded ≤ config VERSION). Surfaced as a **Dashboard smoke** Continuum metric row + folded into `NEXT_ACTION_STATE.json`. STATE ONLY — dashboard smoke passing is NOT MVP approval ([[mvp-approval-state]]) and does NOT imply the manual playtest is complete; no gameplay/physics/Nostr/network behaviour. |
| Handoff / release control panel | `src/engine/status/handoffControlPanel.js`, surfaced by `src/engine/dashboard/continuumData.js` + folded into `tools/nextActionState.mjs` | **v0.2.233-alpha (HANDOFF / RELEASE CONTROL PANEL · LOCAL · READ-ONLY).** PURE/browser-safe single-source-of-truth (no fs/network/child_process/THREE/DOM; never throws): `HANDOFF_LIVE_URL`/`HANDOFF_DASHBOARD_URL`, `HANDOFF_CONTROL_PANEL_BADGE`, frozen `PROJECT_PRINCIPLES` (11 NON-RELIGIOUS practical operating principles — self-sovereignty, consent, privacy by default, open protocols, FOSS, no lock-in, local circular economics, no dark patterns, reversible user-controlled actions, community agency, truthful status reporting), `HANDOFF_DO_NOT` (the exact actions NOT to take without user input), `ETHICS_NOTE`, frozen `RELIGIOUS_DENYLIST` (word-boundary; intentionally EXCLUDES the brand vocabulary torii/gate/shrine/⛩ and the bare `god`/godMode flag), `findReligiousLanguage`/`containsReligiousLanguage`, `HANDOFF_CONTROL_PANEL_REQUIRED_KEYS`. `buildHandoffControlPanel({version,liveUrl,dashboardUrl,entrySmoke,dashboardSmoke,manualBlocker,mvpApproval,nextSafeTask,principles,doNot,ethicsNote})` → a normalised panel; `manualBlocker.pending` is a strict tri-state (true/false/null). `validateHandoffControlPanel(panel)` → `{ok,errors,warnings}` is the GREEN-REQUIRES-EVIDENCE floor: a green/complete surface ERRORs without a current version marker, both live URLs, PASSING entry- AND dashboard-smoke evidence (≥1 check, all passing, a version marker), an EXPLICIT manual-blocker boolean, AND non-religious ethics copy (any flagged term is an ERROR); a pending blocker is a WARNING, not an error. `isHandoffPanelGreen(panel)` is strict. `summarizeHandoffControlPanelForState(panel)` folds into `nextActionState.controlPanel` (green/version/URLs/smoke/manualBlockerPending/ethicsNonReligious + `impliesApproval:false`/`impliesPlaytestComplete:false`). `buildHandoffControlPanelCard(panel)` → the Continuum dashboard card (band incomplete/ready-pending/ready-clear; pill `manual` when incomplete or pending, `no-blocker` when green+clear). Surfaced as the **Handoff / release control panel** section at the TOP of `/continuum.html` (consolidates the former at-a-glance Live/Dashboard smoke rows) + folded into `NEXT_ACTION_STATE.json`. STATE/SURFACE ONLY — GREEN means the handoff SURFACE is complete + trustworthy, NOT that the MVP is approved ([[mvp-approval-state]]) or the playtest complete; no gameplay/physics/Nostr/network behaviour. `tests/handoff-control-panel.test.js` + extended `tests/continuum-dashboard.test.js`/`tests/next-action-state.test.js`. **v0.2.237** added frozen `WORKFLOW_INVARIANTS` (the standing process rule **finish a useful in-progress job rather than cancelling it halfway** + its 4 exceptions: explicit user cancel, immediate conflict, safely resumable, stale/hung & already shipped), folded through the panel builder/validator (a panel with no invariants is now a validator ERROR), `HANDOFF_CONTROL_PANEL_REQUIRED_KEYS`, `summarizeHandoffControlPanelForState` (count), a new **Workflow invariants** card metric, AND carried verbatim into `NEXT_ACTION_STATE.json` as a top-level `workflowInvariants` array. Workflow guidance ONLY — implies no approval/deploy/runtime change. |
| Live-smoke state | `tools/liveSmokeState.mjs`, `tools/live-smoke-state.mjs` (`npm run smoke:state`), `LIVE_SMOKE_STATE.json` | **v0.2.231-alpha (LIVE-SMOKE-STATE · LOCAL · READ-ONLY · OBSERVED ON THE DEPLOYED SITE).** A single auditable record of the latest LIVE cloud-browser smoke of the deployed site — the one posture local gates (`npm run test:release`) can NEVER prove, since it observes the production URL after a manual deploy. `tools/liveSmokeState.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM; never throws): `LIVE_SMOKE_BADGE`, `LIVE_SMOKE_SCHEMA` ('torii.live-smoke-state'), `LIVE_SMOKE_SCHEMA_VERSION` (=1), `LIVE_SMOKE_FILE` (=`LIVE_SMOKE_STATE.json`), `LIVE_SMOKE_RESULTS` ({PASS,FAIL,UNKNOWN}), `LIVE_SMOKE_CHECK_OUTCOMES` (`['pass','fail','skip']`), frozen `LIVE_SMOKE_REQUIRED_KEYS`. `buildLiveSmokeState({result,version,commit,liveUrl,smokedAt,smokedBy,checks,notes,generatedAt})` → a JSON-serialisable record; `result` is COERCED (anything not exactly `'pass'`/`'fail'` → `'unknown'`, so a typo can never read green), checks are normalised (idless dropped, unknown outcome → `'skip'`), and the `safety` block is pinned all-false incl. `impliesApproval:false`. `validateLiveSmokeState(state)` → `{ok,errors,warnings}` is the PASS-REQUIRES-EVIDENCE floor: a `pass` verdict ERRORs unless ≥1 check, NO failed check, a concrete version marker, AND a `smokedAt`. `isLiveSmokePass(state)` is strict (result `'pass'` AND validate ok). `formatLiveSmokeState(state)` → text (null-safe); `summarizeLiveSmokeForState(state)` → `{result,pass,version,smokedAt,checks,passed,failed,impliesApproval:false}` (folded into `nextActionState.liveSmoke`). CLI `tools/live-smoke-state.mjs` reshapes/reads the committed `LIVE_SMOKE_STATE.json` (text / `--json`) and re-persists a NORMALISED record only under a flag-gated in-repo `--write`; read-only/local/no-network otherwise. The committed record holds the **v0.2.230-alpha** smoke (3/3 checks: version visible; ENTER ARENA → `Engine still loading — reload the page if this persists.`; LOGIN WITH NOSTR → `NIP-07 extension not found`). The recorded version legitimately LAGS config `VERSION` (a smoke can only observe a deployed build); `tests/live-smoke-state.test.js` (16) asserts the floor + a non-staleness guard (recorded ≤ config VERSION, never leads). Surfaced as a **Live smoke** Continuum metric row + folded into `NEXT_ACTION_STATE.json`. STATE ONLY — `impliesApproval` pinned false (a green smoke is NOT MVP approval, see [[mvp-approval-state]]); no gameplay/physics/Nostr/network behaviour. |
| MVP manual-approval state | `tools/mvpApproval.mjs`, `tools/mvp-approval-state.mjs` (`npm run approval:state`), `MVP_APPROVAL_STATE.json` | **v0.2.220-alpha (MVP-APPROVAL-STATE · LOCAL · READ-ONLY · PENDING UNTIL EXPLICIT USER APPROVAL).** One clean, auditable place to record MVP approval when the user explicitly says "MVP approved", instead of hand-editing many docs or guessing. `tools/mvpApproval.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM; never throws): `MVP_APPROVAL_BADGE`, `MVP_APPROVAL_SCHEMA` ('torii.mvp-approval-state'), `MVP_APPROVAL_SCHEMA_VERSION` (=1), `MVP_APPROVAL_FILE` (=`MVP_APPROVAL_STATE.json`), `MVP_APPROVAL_STATUSES` ({PENDING,APPROVED}), `APPROVAL_REQUIRED_FIELDS` (`approved_by`,`approved_at`), `MVP_APPROVAL_PENDING_NOTE`. `buildApprovalState({status,version,commit,approved_by,approved_at,notes,generatedAt})` → `{kind,schemaVersion,status,version,commit,approved_by,approved_at,notes,safety:{deploy/publish/push/tag/networkWrite/nostrWrite/godMode=false}}`, coercing any `status` ≠ `'approved'` to `'pending'` (never silently approves) and carrying provenance verbatim (trimmed→null). `validateApprovalState(state)` → `{ok,errors,warnings}` ERRORing on wrong kind/schemaVersion/status, bad version marker, non-null/non-string commit, an APPROVED record missing any required field or a version marker, OR a PENDING record carrying approver fields. `isApproved(state)` is strict (status `'approved'` AND validate ok). `formatApprovalState(state)` → text; `summarizeApprovalForState(state)` → `{status,approved,approvedBy,approvedAt,version}` (consumed by `nextActionState`). The committed `MVP_APPROVAL_STATE.json` defaults to PENDING (approver fields null). CLI `tools/mvp-approval-state.mjs` reads/renders the record (text / `--json`) and under `--write` emits ONLY a PENDING record for the current config version — NO `--approve` path in this slice; always exits 0. `tests/mvp-approval-state.test.js`. Composes/records local state only — no gameplay/physics/Nostr/network behaviour; status STAYS pending until explicit user approval. **v0.2.221-alpha** surfaces this state VISIBLY on the Continuum dashboard via `buildMvpApprovalModel()` (continuumData.js) — a compact MVP-approval card placed ABOVE Manual validation showing PENDING status, version, no approver, and the explicit next-step (user playtest + say "MVP approved"); strict approved-only-with-provenance rendering, `manual` pill reused (no new CSP/pill/script-hash), live state gathered in `build-continuum.mjs`. **v0.2.223-alpha** adds a sibling **Playtest results** card via `buildPlaytestResultsCardModel()` (continuumData.js; `PLAYTESTRESULTS_BADGE` + frozen `PLAYTESTRESULTS_LASTKNOWN` not-run fallback) placed BETWEEN the MVP-approval card and Manual validation — it surfaces the v0.2.222 playtest-results state (status not-run/incomplete/attention/complete/unknown, recorded yes/no, item counts, failing ids, next step) read from `MVP_PLAYTEST_RESULTS.md` and re-shaped through the pure `summarizePlaytestForState` ([[playtest-results-state]]) in `build-continuum.mjs`; `approvalImplied` HARD-pinned false in every branch (a recorded playtest is necessary but NOT sufficient — approval stays a separate explicit gate), `manual`/`open-edge`/`no-blocker` pills reused (no new CSP/pill/script-hash). |
| Generated commit-stamp wording | `tools/commitStamp.mjs` (consumed by `tools/agentHandoff.mjs`, `tools/handoffSummary.mjs`, `tools/releasePackage.mjs`, `tools/releaseNotes.mjs`, `tools/playtestChecklist.mjs`, `tools/githubReleaseDryRun.mjs`) | **v0.2.209-alpha (COMMIT-STAMP-CLARITY).** A tiny shared PURE/node-safe wording helper (no fs/git/network/THREE/DOM) that fixes a cosmetic-but-misleading detail flagged in security review: every generated artifact is WRITTEN before the commit that will contain it, so the short hash stamped on it is repo HEAD at generation time — the PARENT of the file's own commit — yet bare `commit` / `Git commit` labels imply final-commit equality. Exports `SOURCE_COMMIT_NOTE` (the one-line explanation), `sourceCommitLabel(commit)` → `<hash> (source commit at generation — precedes this file's own commit)` or `(unavailable)`, and `sourceCommitInline(commit)` → ` @ <hash> (source)` or `''`. The six generators above now render commit metadata through these helpers (text via `sourceCommitInline`, markdown via `sourceCommitLabel`) so the wording no longer implies the stamped hash is the file's own commit; `tools/playtestResults.mjs` is deliberately untouched (its rendered `| Commit | … |` row is asserted verbatim by `tests/playtest-results.test.js`). Wording-only — NO behaviour, schema, or model-field change (the underlying `gitCommit` model fields are unchanged, so all model-field assertions still pass). Covered by 5 cases in `tests/agent-handoff.test.js`. |
| Stale-doc detector | `tools/staleDocs.mjs`, `tools/stale-docs.mjs` (`npm run docs:stale`) | **v0.2.191-alpha (STALE-DOC, ADVISORY).** Catches docs/status/version drift earlier and more clearly than the gated docConsistency guard [14]. `tools/staleDocs.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM) and REUSES the docConsistency primitives (`CONTINUITY_DOCS`/`findVersionMarkers`/`versionInText`): `STALE_DOCS_BADGE`; `staleVersionHeaderLines(text,version)` flags version-HEADER lines citing a non-current marker (narrow `VERSION_HEADER_RE` = `version` + ONLY separator/markup chars + marker, after stripping backtick/double-quote spans, so changelog prose like "version drift fixed (v0.2.137)" and quoted examples are NOT flagged); `testCountsInText(text)` → every `<N> passing` count; `reportVersionToken(version)` → marker minus prerelease tag (`v0.2.191-alpha`→`v0.2.191`); `detectStaleDocs({version,docs,reports})` → `{ok,version,badge,issues,counts,summary}` checking (A) per-continuity-doc version-header-drift + version-missing + doc-unavailable, (B) newest report not linked by any continuity doc, (C) newest report lagging the current version, no-reports, (D) disagreeing test counts across continuity docs. `ok` is true unless NO version is provided (the only error); every drift signal is a WARNING — advisory by design. `formatStaleDocs(report)` → text block, safe on null. CLI `tools/stale-docs.mjs` reads the continuity-doc contents + config VERSION + mtime-sorted `torii-*report.md` filenames behind a `realpathSync` run-guard; text default / `--json`; READ-ONLY/local/no-network and ALWAYS exits 0 — NOT in `npm run check` (the hard gate stays docConsistency [14]). `tests/stale-docs.test.js` (25). |
| Release/update metadata | `tools/releaseMeta.mjs`, `tools/release-meta.mjs` (`npm run release:meta`) | **v0.2.192-alpha (RELEASE METADATA · NO AUTO-UPDATE).** Prepares the static metadata a FUTURE torii.quest / VPS update-checker reads to surface an inert "update available" notice, with NO live update and NO runtime network. `tools/releaseMeta.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM): `RELEASE_META_BADGE`, `METADATA_SCHEMA_VERSION`, `RELEASE_META_KIND`, `RELEASE_META_FILE` (=`public/release-metadata.json`), `UPDATE_CHANNELS`, `DEFAULT_SOURCE`, `DIST_SPEC`, `REQUIRED_FILES`, `REQUIRED_CHECKS`, `CONSENT_TEXT`, `UPDATE_NOTICE`; `channelForVersion(version)` derives stable/alpha/beta/rc/unknown from the prerelease tag; `releaseUrlsFor(owner,repo)` builds the documentation-only https GitHub endpoints (mirrors `RELEASE_SOURCE` in `src/engine/update/updateCheck.js`); `buildReleaseMeta({version,commit,owner,repo,generatedAt})` → `{kind,schemaVersion,generatedAt,channel,version,commit,source,dist,requiredFiles,requiredChecks,update}`; `validateReleaseMeta(meta)` → `{ok,errors,warnings}` — the SAFETY FLOOR: it ERRORs if `update.autoUpdate` or `update.actionable` is anything but `false` (machine-enforced no-auto-update contract), plus shape/channel/url/required-array checks; never throws, safe on null/non-object/array. `formatReleaseMeta(meta)` → text block, safe on null. CLI `tools/release-meta.mjs` reads config VERSION + best-effort git short commit behind a `realpathSync` run-guard; text default / `--json` / `--write` (emits the DETERMINISTIC `public/release-metadata.json` — no commit/timestamp baked in so re-runs never churn) / `--stamp` (bakes live commit+ISO time for a deploy step); READ-ONLY by default, writes ONLY the in-repo safe path under `--write`, ALWAYS exits 0. Spec mirrored into `UPDATE_CHECK.md` §5 + `VPS_INSTALL.md` §12. `tests/release-meta.test.js` (23). |
| VPS install dry-run | `tools/vpsDryRun.mjs`, `tools/vps-dry-run.mjs` (`npm run vps:dry-run`) | **v0.2.193-alpha (VPS INSTALL DRY-RUN · LOCAL · READ-ONLY).** A local readiness checklist an operator runs BEFORE deploying torii.quest to a VPS/static host — with NO SSH, network, DNS, or server change; a readiness GUARD, not a deploy step. `tools/vpsDryRun.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM) and REUSES the shipped pure guards (`validateReleaseMeta`/`DEFAULT_SOURCE` from `releaseMeta.mjs`, `fallbackEvidence` from `zoneFallbackReadiness.mjs`): `VPS_DRY_RUN_BADGE`, `REQUIRED_DOCS` (`VPS_INSTALL.md`/`UPDATE_CHECK.md`/`HANDOFF.md`), `REQUIRED_VPS_SECTIONS` (build/manual update/rollback/security), `REQUIRED_BUILD_COMMANDS` (`npm run build`/`npm run check`), `REAL_REPO_SLUG` (`ChiefmonkeyArt/torii-gate`), `LIVE_URLS` (`torii.quest`/`torii-quest.pplx.app`); 11 per-row check fns — `checkDistBundle` (SKIP if no build, FAIL if no index.html, WARN if index but no release-metadata.json, PASS if both), `checkReleaseMetaPresent`, `checkReleaseMetaManualOnly` (reuses `validateReleaseMeta`), `checkRealRepoMetadata`, `checkZoneFallbackDocs` (reuses `fallbackEvidence`), `checkVpsSections`, `checkBuildCommands`, `checkRollbackSafety`, `checkServiceWorkerCaveat`, `checkLiveUrls`; `runVpsDryRun({docs,dist,releaseMeta})` → `{ok,badge,checks,summary:{pass,fail,warn,skip,total},errors,warnings}` (`ok` true iff NO check FAILED — warn/skip never flip it; never throws on null/degraded input), `formatVpsDryRun(result)` → text block (safe on null). CLI `tools/vps-dry-run.mjs` reads `REQUIRED_DOCS` contents + `public/release-metadata.json` + `dist/` paths behind a `realpathSync` run-guard; text default / `--json`; READ-ONLY/local/no-network and exits non-zero ONLY on a blocking FAIL — NOT wired into `npm run check` (standalone operator tool). Documented in `VPS_INSTALL.md` §13 + §9. `tests/vps-dry-run.test.js` (43). |
| Host route / static-host smoke | `src/engine/host/hostRouteSmoke.js` | **v0.2.197-alpha (HOST-ROUTE-SMOKE).** PURE node-safe HOST ROUTE + ASSET SMOKE HARNESS that proves the torii.quest static-host route + asset readiness contracts hold WITHOUT touching a server/shell/network (no THREE/Rapier/window/fs/child_process/network; never throws). `HOST_ROUTE_SMOKE_VERSION`=1; `HOST_ROUTE_SMOKE_BADGE`='HOST ROUTE SMOKE · READ-ONLY · NO DEPLOY'; `REQUIRED_ASSETS`=['index.html','continuum.html','continuum-data.json','release-metadata.json']; frozen LOCAL fixtures `SAMPLE_DIST_PATHS` (Vite-build-shape path list — index.html, assets/*.js, continuum.html, continuum-data.json, release-metadata.json, sw.js), `SAMPLE_FALLBACK_DOCS` (VPS_INSTALL.md/HANDOFF.md with try_files/index.html + /zone/ text), `SAMPLE_ZONE_SLUG`='plebeian-market-bazaar', `HOSTILE_ZONE_PATHS` (absolute scheme / protocol-relative / dot-dot / sub-path / uppercase+underscore / empty slug / percent-encoding / `javascript:`). `runHostRouteSmoke(opts?)` composes the shipped pure readiness helpers (`checkFallbackDocs`/`checkDistRoutes`/`zonePathsInDist`/`ZONE_ROUTE_PREFIX`/`REQUIRED_FALLBACK_DOCS` from `tools/zoneFallbackReadiness.mjs`, `parseZoneRoute`/`isValidZoneSlug`/`ZONE_ROUTE_KIND`/`DEMO_ZONE_ROUTE` from `engine/gateway/zoneRoute.js`, `buildReleaseMeta`/`validateReleaseMeta`/`DIST_SPEC`/`REQUIRED_FILES`/`RELEASE_META_FILE` from `tools/releaseMeta.mjs`) into TEN read-only signals — root `index.html` present; `DIST_SPEC.expectedArtifacts` (index.html+assets) present; the `/continuum.html` dashboard asset present; the `release-metadata.json` update asset present; the `REQUIRED_FILES` floor documented; the `/zone/*` SPA fallback documented (`checkFallbackDocs`); NO built file shadows the `/zone/<slug>` fallback (`zonePathsInDist` empty); an unknown `/zone/<slug>` is served `index.html` by host config while NOT a built file; the route parser keeps the slug SAFE (`parseZoneRoute`→`ZONE`) and rejects the whole `HOSTILE_ZONE_PATHS` fixture; no host-side action (every report pins `served/deployed/navigated/performed/external/network/wrote/fetched=false` and exposes NO `serve`/`deploy`/`publish`/`upload`/`fetch`/`write`/`navigate`/`exec`/`spawn`/`run`/`ssh`/`connect` CALLABLE) — folding into `{version,badge,ok,signals,summary,safety,reasons,rendered:false,actionable:false}` (a broken injected fixture degrades to `ok:false` with concrete `reasons`, never throws). `formatHostRouteSmoke(result)` renders one stable text block. Opts (`distPaths`/`fallbackDocs`/`zoneSlug`/`hostile`) injectable. Composes ONLY already-shipped pure helpers — surfaces NO serve/deploy/publish/upload/fetch/write/navigate method of its own; NOT a VPS deployment — touches no real server/DNS/SSH/remote command/network. SDK `hostRouteSmoke` (experimental) + read-only `ToriiDebug.shells.hostRouteSmoke(opts?)` (new `hostRouteSmokeReport` in `shellReport.js`, added to `buildShellReport`). `tests/host-route-smoke.test.js` (+17). Pure local/non-network coverage, no runtime behaviour change. |
| MVP release-readiness rollup | `src/engine/status/mvpReadiness.js` | **v0.2.198-alpha (MVP-READINESS-ROLLUP).** PURE node-safe MVP RELEASE-READINESS ROLLUP that folds the already-pure local readiness signals into ONE read-only verdict with an MVP percentage/status + next safe task, so the user sees how close the read-only MVP proof is WITHOUT manually digging through every harness, doc, and gate (no THREE/Rapier/window/location/fs/child_process/network/socket; never throws). `MVP_READINESS_VERSION`=1; `MVP_READINESS_BADGE`='MVP READINESS ROLLUP · READ-ONLY · NO DEPLOY'; curated frozen defaults `DEFAULT_TEST_STATUS` (1246/78, profile full, ok; v0.2.200 keeps it in lock-step with the dashboard `CURRENT_TEST_STATUS` via a cross-capture test, NOT a cross-import), `DEFAULT_VPS_DRY_RUN` (ok), `DEFAULT_DOCS_STATUS` (ok), `NEXT_SAFE_TASK` (no-runtime-risk infra/tooling/docs; SEC-gated work parked behind SEC-1/2/3). `runMvpReadiness(opts?)` composes the already-pure harnesses (`runReadHealth` from `engine/nostr/readHealth.js`, `runGatewayTravelSmoke` from `engine/gateway/travelSmoke.js`, `runUpdateFlowSmoke` from `engine/update/updateFlowSmoke.js`, `runHostRouteSmoke` from `engine/host/hostRouteSmoke.js`, `buildReleaseMeta`/`validateReleaseMeta` from `tools/releaseMeta.mjs`, `VERSION` from `config.js`) into NINE read-only signals — version marker valid; Nostr read-path health; gateway travel smoke; update-flow smoke; host-route smoke; release-metadata safety floor (valid AND a tampered `autoUpdate:true` REJECTED); the injected last-known test-suite verdict (`opts.tests`); the injected VPS manual-deploy dry-run verdict (`opts.vpsDryRun`); the injected docs/handoff freshness verdict (`opts.docs`) — folding into `{version,badge,ok,mvpPct,status,currentVersion,signals,summary,safety,reasons,nextSafeTask,rendered:false,actionable:false}` (`mvpPct` = share of passing signals 0..100; `status` = READY/NEAR/ATTENTION; a broken injected fixture degrades to a fail with concrete `reasons`, never throws). The four live smoke verdicts are computed from the pure harnesses; the fs-backed signals (tests/VPS/docs) are INJECTED via opts with curated last-known defaults (like the dashboard ship/health models) so the module stays pure. Every report pins `served/deployed/published/navigated/performed/fetched/wrote/network=false`. `formatMvpReadiness(result)` renders one stable text block (safe on null). Override via opts (`tests`/`vpsDryRun`/`docs`/`nextSafeTask`). SDK `mvpReadiness` (experimental) + read-only `ToriiDebug.shells.mvpReadiness(opts?)` (new `mvpReadinessReport` in `shellReport.js`, added to `buildShellReport`). `tests/mvp-readiness-rollup.test.js` (+14). Pure local/non-network coverage, no runtime behaviour change. |
| GitHub MVP release dry-run | `tools/githubReleaseDryRun.mjs`, `tools/github-release-dry-run.mjs` (`npm run release:dry-run`) | **v0.2.207-alpha (GITHUB-RELEASE-DRY-RUN · LOCAL · READ-ONLY · NO TAG / NO RELEASE).** A LOCAL dry-run that validates the prerequisites for a FUTURE GitHub MVP proof release WITHOUT creating it — generated from frozen local data + injected verdicts, NOT a release/tag/deploy and NOT a live browser test. `tools/githubReleaseDryRun.mjs` is pure/node-safe (no fs/network/child_process/THREE/DOM; never throws): `GITHUB_RELEASE_DRY_RUN_SCHEMA` ('torii.github-release-dry-run')/`GITHUB_RELEASE_DRY_RUN_SCHEMA_VERSION` (=1)/`GITHUB_RELEASE_DRY_RUN_BADGE` ('GITHUB RELEASE DRY-RUN · LOCAL · READ-ONLY · NO TAG / NO RELEASE')/`GITHUB_RELEASE_DRY_RUN_WRITE_FILENAME` ('GITHUB_RELEASE_DRY_RUN.md')/`GITHUB_RELEASE_DRY_RUN_TITLE`/`GITHUB_RELEASE_APPROVAL_GATE`; frozen `GITHUB_RELEASE_DRY_RUN_PREREQUISITES` (9 `{key,label,gating}`: version[gating], version-sync[gating], clean-tree[soft], pushed[soft], release-notes[gating], release-package[gating], gate-ready[gating], live-url[gating], no-auto-update[gating]) + frozen `GITHUB_RELEASE_DRY_RUN_ADVISORIES` (3: rapier bundle / SDK_DEBUG / alpha). `buildGithubReleaseDryRunModel({version,packageVersion,gitCommit,cleanTree,pushed,releaseNotesPresent,releasePackagePresent,gateReady,liveUrl,autoUpdateActionable,advisories,generatedAt})` → `{schema,schemaVersion,generatedAt,badge,title,dryRun:true,version,packageVersion,gitCommit,status('ready'/'near'/'blocked'),statusLabel,ready,prerequisites[] (each {key,label,gating,state∈ok/blocked/pending/unknown,detail}),missing[],advisories[],approvalGate,approvalRequired:true,futureCommands[] (git tag / git push / gh release — each note demands approval),safety:{tagged/released/pushed/published/deployed/announced/served/navigated/wrote/network=false},rendered:false,actionable:false}`. Verdict: a gating prereq `blocked` → BLOCKED; a gating `unknown` or a soft-pending (expected dirty tree / unpushed HEAD before the parent pushes) → NEAR; else READY — even READY reads "(pending manual approval)" because the manual-approval gate is a standing always-shown requirement, never a scored item. Null/garbled inputs degrade to honest unknowns and never throw. `formatGithubReleaseDryRun(model)` → text, `formatGithubReleaseDryRunMarkdown(model)` → markdown, both safe on null. CLI `tools/github-release-dry-run.mjs` stamps `configVersion()` + `packageVersion()` + best-effort `git rev-parse --short HEAD` + read-only git (`git status --porcelain` for clean-tree; `git rev-parse HEAD` vs `@{u}` for pushed — NO fetch) + `HANDOFF_SUMMARY_LIVE_URL` + the `public/release-metadata.json` autoUpdate/actionable read, building with `gateReady:null` (the RC gate is deliberately NOT run, holding gate-ready at `unknown`→NEAR), behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `GITHUB_RELEASE_DRY_RUN.md`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; READ-ONLY/local/no-network, always exits 0 otherwise. `tests/github-release-dry-run.test.js` (16). DRY-RUN ONLY — runs NO `git tag`, NO `git push`, NO `gh release`, NO deploy/publish/announce, reaches NO network/server; suggested future commands are TEXT ONLY, each demanding explicit user approval — no new gameplay/physics/Nostr/network behaviour. |
| Release artifact integrity manifest | `tools/releaseManifest.mjs`, `tools/release-manifest.mjs` (`npm run release:manifest`) | **v0.2.212-alpha (RELEASE ARTIFACT MANIFEST · LOCAL · READ-ONLY).** Records the RC package artifacts a future GitHub release / VPS self-update flow must verify, with present/missing status + stable sha256 checksums — NOT a gate, NOT a release, NOT a network self-update. `tools/releaseManifest.mjs` is pure/node-safe (no fs/network/crypto/child_process/THREE/DOM; never throws): `RELEASE_MANIFEST_SCHEMA` ('torii.release-manifest')/`RELEASE_MANIFEST_SCHEMA_VERSION` (=1)/`RELEASE_MANIFEST_BADGE` ('RELEASE ARTIFACT MANIFEST · LOCAL · READ-ONLY')/`RELEASE_MANIFEST_WRITE_FILENAME` ('RELEASE_ARTIFACT_MANIFEST.md')/`RELEASE_MANIFEST_TITLE`/`RELEASE_MANIFEST_STATES` (frozen `['COMPLETE','INCOMPLETE']`); frozen `RELEASE_MANIFEST_REQUIRED` (6 `{key,file,label,category}`: RELEASE_NOTES_DRAFT.md, MVP_RELEASE_PACKAGE.md, GITHUB_RELEASE_DRY_RUN.md, public/release-metadata.json, package.json, index.html) + frozen `RELEASE_MANIFEST_OPTIONAL` (6: MVP_RC_SNAPSHOT.md, MVP_PLAYTEST_CHECKLIST.md, MVP_PLAYTEST_RESULTS_TEMPLATE.md, HANDOFF.md, VPS_INSTALL.md, public/continuum-data.json) + frozen `RELEASE_MANIFEST_NOTES` (4); plus (v0.2.212) the pure recent-report discovery helper `selectRecentReports(names, cap=RELEASE_MANIFEST_REPORT_CAP)` + exported `RELEASE_MANIFEST_REPORT_RE` (`/^torii-v.*-report\.md$/`) / `RELEASE_MANIFEST_REPORT_CAP` (6) — filters report-shaped names, sorts lexicographically (deterministic), caps to the most recent; null-safe, never throws. `buildReleaseManifestModel({version,packageVersion,gitCommit,liveUrl,artifacts,required,optional,notes,reports,generatedAt})` folds a CLI-injected `{key:{present,sha256,bytes}}` map into `{schema,schemaVersion,generatedAt,badge,title,manifest:true,status,complete,version,packageVersion,gitCommit,liveUrl,required[],optional[],missingRequired[],counts:{required,requiredPresent,requiredMissing,optional,optionalPresent,hashed},notes,latestReports,safety:{released/tagged/published/selfUpdated/served/wrote/network/hashedSecrets=false},rendered:false,actionable:false}`. Verdict COMPLETE iff no REQUIRED artifact is explicitly absent (`present===false`); an unknown (null) present flag is never a blocker; an injected non-64-hex sha256 is rejected to null. Null/garbled inputs degrade honestly and never throw. `formatReleaseManifest(model)` → text, `formatReleaseManifestMarkdown(model)` → markdown (artifact tables), both safe on null. CLI `tools/release-manifest.mjs` reads each REQUIRED+OPTIONAL file, computes a sha256 + byte size via `node:crypto` (`createHash('sha256')`), stamps `configVersion()` + `packageVersion()` + best-effort `git rev-parse --short HEAD` + the SHARED live URL, discovers recent `torii-v*-report.md` via `selectRecentReports(readdirSync(ROOT))` (v0.2.212 — was an `execSync('ls …')` shell glob; `execSync` now only for the read-only `git rev-parse`), behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `RELEASE_ARTIFACT_MANIFEST.md`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; READ-ONLY/local/no-network, always exits 0 otherwise — VISIBILITY, not a gate (the authority stays `npm run test:release`). Checksums cover in-repo text docs + small served build-metadata JSON only — no secrets, no large binaries. `tests/release-manifest.test.js` (19; asserts every REQUIRED artifact exists on disk so a missing RC artifact is caught locally, and covers `selectRecentReports` filtering/deterministic-sort/cap/null-safety). MANIFEST ONLY — no GitHub release/tag/push/announce/network/deploy/publish/self-update; composes injected fs hashes only — no new gameplay/physics/Nostr/network behaviour. |
| MVP RC snapshot / freeze-candidate summary | `tools/rcSnapshot.mjs`, `tools/rc-snapshot.mjs` (`npm run rc:snapshot`) | **v0.2.210-alpha (RC-SNAPSHOT · FREEZE-CANDIDATE · LOCAL · READ-ONLY).** Captures the current release-candidate state in ONE freeze-candidate document by COMPOSING the already-pure local verdicts — no re-derivation, no network, no GitHub release, no tag. `tools/rcSnapshot.mjs` is pure/node-safe (no fs/network/child_process/THREE/DOM; never throws): `RC_SNAPSHOT_SCHEMA` ('torii.rc-snapshot')/`RC_SNAPSHOT_SCHEMA_VERSION` (=1)/`RC_SNAPSHOT_BADGE` ('MVP RC SNAPSHOT · FREEZE CANDIDATE · LOCAL · READ-ONLY')/`RC_SNAPSHOT_WRITE_FILENAME` ('MVP_RC_SNAPSHOT.md')/`RC_SNAPSHOT_TITLE`/`RC_SNAPSHOT_STATES` (frozen `['FREEZE-CANDIDATE','NEAR','BLOCKED']`); frozen `RC_SNAPSHOT_DOC_REFS` (7 `{key,file,label}` RC package docs: RELEASE_NOTES_DRAFT.md, MVP_RELEASE_PACKAGE.md, GITHUB_RELEASE_DRY_RUN.md, MVP_PLAYTEST_CHECKLIST.md, MVP_PLAYTEST_RESULTS_TEMPLATE.md, HANDOFF.md, VPS_INSTALL.md), frozen `RC_SNAPSHOT_MANUAL_VALIDATION` (live-browser items the snapshot can't self-verify), frozen `RC_SNAPSHOT_RELEASE_STEPS` (the user-approved steps to cut a real GitHub release/tag later), frozen `RC_SNAPSHOT_ADVISORIES` (3). `rcSnapshotVersionConsistency({version,packageVersion,rcGate,mvpReadiness,dryRun})` → `{ok,version,sources,mismatches}` (ok iff a resolved version exists and every PRESENT folded source matches; package coerced to v-prefixed). `buildRcSnapshotModel({version,packageVersion,gitCommit,liveUrl,rcGate,mvpReadiness,dryRun,testStatus,regression,present,advisories,manualValidation,releaseSteps,reports,generatedAt})` folds a `buildMvpRcGate()` verdict + a `runMvpReadiness()` rollup + a `buildGithubReleaseDryRunModel()` + a doc present-map into `{schema,schemaVersion,generatedAt,badge,title,snapshot:true,status,freezeCandidate,version,packageVersion,gitCommit,liveUrl,versionConsistency,rcGate,mvpReadiness,releaseDryRun,tests,regression,docs,advisories,manualValidation,releaseSteps,latestReports,safety:{released/tagged/published/announced/served/navigated/wrote/network=false},rendered:false,actionable:false}`. Verdict: BLOCKED if the RC gate is BLOCKED or the dry-run is blocked; else FREEZE-CANDIDATE iff the RC gate is a candidate AND the dry-run is not blocked; else NEAR. Null/garbled inputs degrade to honest unknowns and never throw. `formatRcSnapshot(model)` → text, `formatRcSnapshotMarkdown(model)` → markdown, both safe on null. CLI `tools/rc-snapshot.mjs` reuses `gatherReleaseReadiness()` + `runMvpReadiness()` + `buildHandoffSummary()` + `buildMvpRcGate()` + `buildGithubReleaseDryRunModel()`, stamps `configVersion()` + `packageVersion()` + best-effort short commit + read-only git (`git status --porcelain` clean-tree, `git rev-parse HEAD` vs `@{u}` pushed — NO fetch) + the SHARED live URL + the `public/release-metadata.json` autoUpdate read, stat-s each RC doc for present/missing, discovers recent `torii-v*-report.md` via the SHARED pure `selectRecentReports(readdirSync(ROOT))` from `releaseManifest.mjs` (v0.2.213 — was an `execSync('ls …')` shell glob; `execSync` now only for the read-only `git`), behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `MVP_RC_SNAPSHOT.md`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; READ-ONLY/local/no-network, always exits 0 otherwise — VISIBILITY, not a gate (the authority stays `npm run test:release`). `tests/rc-snapshot.test.js` (19; asserts every referenced RC doc exists on disk so the list cannot silently rot). SNAPSHOT ONLY — no GitHub release/tag/push/announce/network/deploy/publish; composes shipped signals + fs existence only — no new gameplay/physics/Nostr/network behaviour. |
| MVP release package index | `tools/releasePackage.mjs`, `tools/release-package.mjs` (`npm run release:package`) | **v0.2.206-alpha (RELEASE-PACKAGE · INDEX · LOCAL · READ-ONLY).** A single discoverability INDEX for the MVP proof-of-concept candidate so humans and future agents can find every relevant file fast — generated from frozen local data + injected fs existence, NOT a gameplay change and NOT a live browser test. `tools/releasePackage.mjs` is pure/node-safe (no fs/network/child_process/THREE/DOM; never throws): `RELEASE_PACKAGE_SCHEMA` ('torii.release-package')/`RELEASE_PACKAGE_SCHEMA_VERSION` (=1)/`RELEASE_PACKAGE_BADGE` ('MVP RELEASE PACKAGE INDEX · LOCAL · READ-ONLY')/`RELEASE_PACKAGE_WRITE_FILENAME` ('MVP_RELEASE_PACKAGE.md')/`RELEASE_PACKAGE_TITLE`; frozen `RELEASE_PACKAGE_ENTRIES` (10 entries `{key,file,label,category}` across Release & handoff / Playtest / Status & planning / Ops readiness → RELEASE_NOTES_DRAFT.md, HANDOFF.generated.md, HANDOFF.md, MVP_PLAYTEST_CHECKLIST.md, MVP_PLAYTEST_RESULTS_TEMPLATE.md, progress.md, todo.md, UPDATE_CHECK.md, VPS_INSTALL.md, ZONE_FALLBACK_READINESS.md); frozen `RELEASE_PACKAGE_ADVISORIES` (3) + `RELEASE_PACKAGE_DEFAULT_NEXT_ACTION`. `buildReleasePackageModel({version,gitCommit,liveUrl,testStatus,regression,advisories,nextAction,reports,present,generatedAt})` → `{schema,schemaVersion,generatedAt,badge,title,index:true,version,gitCommit,liveUrl,tests,regression,entries (each {key,file,label,category,present}),advisories,latestReports,nextAction,safety:{released/tagged/published/announced/served/navigated/wrote/network=false},rendered:false,actionable:false}`; the `present` map (key→bool) is INJECTED by the CLI so the helper stays fs-free, and an absent flag → `present:null` (unknown). Null/garbled inputs degrade honestly and never throw. `formatReleasePackage(model)` → text grouped by category with a present/MISSING/unknown mark, `formatReleasePackageMarkdown(model)` → markdown, both safe on null. CLI `tools/release-package.mjs` stamps `configVersion()` + best-effort `git rev-parse --short HEAD` + `HANDOFF_SUMMARY_LIVE_URL` + the curated `CURRENT_TEST_STATUS`, stat-s each indexed file via `existsSync` for the `present` map, and discovers recent `torii-v*-report.md` via the SHARED pure `selectRecentReports(readdirSync(ROOT))` from `releaseManifest.mjs` (v0.2.213 — was an `execSync('ls …')` shell glob; `execSync` now only for the read-only `git rev-parse` commit stamp), behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `MVP_RELEASE_PACKAGE.md`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; READ-ONLY/local/no-network, always exits 0 otherwise. `tests/release-package.test.js` (12). INDEX ONLY — no GitHub release/tag/announce/network/deploy/publish; composes only frozen local data + fs existence — no new gameplay/physics/Nostr/network behaviour. |
| MVP playtest results state | `tools/playtestResultsState.mjs`, `tools/playtest-results-status.mjs` (`npm run playtest:status`), `MVP_PLAYTEST_RESULTS.md` | **v0.2.222-alpha (PLAYTEST-RESULTS-STATE · LOCAL · READ-ONLY · NOT AN APPROVAL).** Maps the v0.2.204 results summary into ONE compact pipeline-friendly STATE answering "has the human MVP playtest actually been recorded, and what did it say?" without scattering notes — the canonical recording file `MVP_PLAYTEST_RESULTS.md` is source-controlled and hand-edited (ships BLANK, so a fresh checkout reads `not-run`; NO-CLOBBER — never regenerated by the artifact sequence). `tools/playtestResultsState.mjs` is pure/node-safe (no fs/network/child_process/process/THREE/DOM; never throws) and imports `summarizePlaytestResults` from `./playtestResults.mjs`: `PLAYTEST_RESULTS_STATE_SCHEMA` ('torii.playtest-results-state')/`PLAYTEST_RESULTS_STATE_SCHEMA_VERSION` (=1)/`PLAYTEST_RESULTS_STATE_BADGE`/`PLAYTEST_RESULTS_STATE_FILE` (=`MVP_PLAYTEST_RESULTS.md`)/frozen `PLAYTEST_RESULTS_STATUSES` (unknown/not-run/incomplete/attention/complete). `summarizePlaytestForState(input)` accepts a results markdown string OR a `summarizePlaytestResults()` object OR null → `{schema,schemaVersion,status,verdict,ran,complete,pending,approvalImplied:false,total,counts:{total,pass,fail,na,blank,other},fails[]}`; status: no summary/total 0 → unknown, all blank → not-run, any fail → attention, any blank/other → incomplete, else complete. **HARD INVARIANT: `approvalImplied` is pinned false in EVERY branch** — a recorded playtest is necessary but NOT sufficient; explicit user "MVP approved" (`MVP_APPROVAL_STATE.json`, see [[mvp-approval-state]]) is a separate gate. `formatPlaytestResultsState(state)` → text, null-safe. CLI `tools/playtest-results-status.mjs` reads `MVP_PLAYTEST_RESULTS.md` + stamps version/commit behind a run-guard; text default / `--json`; READ-ONLY/local/no-network, never writes. Folded into the next-action state (`nextActionState.playtestResults`, always `approvalImplied:false`). `tests/playtest-results-state.test.js` (11). STATE ONLY — no browser/automation/approval; composes the parser output only — no new gameplay/physics/Nostr/network behaviour. |
| MVP playtest note capture | `tools/playtestNoteCapture.mjs`, `tools/playtest-capture.mjs` (`npm run playtest:capture`), `PLAYTEST_NOTE_CAPTURE.md` | **v0.2.224-alpha (PLAYTEST-NOTE-CAPTURE · LOCAL · READ-ONLY · NOT AN APPROVAL).** Makes it easy to turn the user's rough manual-test notes into the canonical `MVP_PLAYTEST_RESULTS.md` ([[playtest-results-state]]) later WITHOUT guessing or implying approval. `tools/playtestNoteCapture.mjs` is pure/node-safe (no fs/network/child_process/process/THREE/DOM; never throws) and imports `parsePlaytestResults`/`summarizePlaytestResults`/`PLAYTEST_RESULTS_META_FIELDS`/`PLAYTEST_RESULTS_ITEM_FIELDS` from `./playtestResults.mjs` + `summarizePlaytestForState` from `./playtestResultsState.mjs` (single source of truth — no second vocabulary): `PLAYTEST_NOTE_CAPTURE_SCHEMA` ('torii.playtest-note-capture')/`PLAYTEST_NOTE_CAPTURE_SCHEMA_VERSION` (=1)/`PLAYTEST_NOTE_CAPTURE_BADGE` (carries 'READ-ONLY' + 'NOT AN APPROVAL')/frozen `CAPTURE_FOLLOWUP_FIELDS` (`['severity','repro','nextAction']`). `explainPlaytestCapture(text)` accepts a results markdown string (or null) → `{schema,schemaVersion,badge,status,ran,complete,pending,approvalImplied:false,total,recorded,blank,counts,fails,meta:{filled,blank},items:[{id,result,recorded,missingFields,needsFollowup}],followups,nextSteps,note}`: it reuses the canonical parser/state for status (unknown/not-run/incomplete/attention/complete) and LAYERS per-item field-completeness — which meta fields (build/commit/liveUrl/tester/date/environment/overall) and per-item fields are still blank, and for any FAIL the follow-up fields a tester still owes (`severity`+`nextAction`; media optional). **HARD INVARIANT: `approvalImplied` is pinned false in EVERY branch** — capture is necessary but NEVER sufficient; explicit user "MVP approved" stays a separate gate ([[mvp-approval-state]]). `formatPlaytestCaptureExplain(explain)` → text (note→result mapping + "implies approval: NO"), null-safe. CLI `tools/playtest-capture.mjs` is STRICTLY READ-ONLY (NO `--write` at all — so the no-clobber `MVP_PLAYTEST_RESULTS.md` is structurally untouchable): reads `MVP_PLAYTEST_RESULTS.md` by default or a `--file=` confined in-repo (rejects absolute / `..` → exit 2) behind a run-guard; text default / `--json`; local/no-network, exits 0. New how-to doc `PLAYTEST_NOTE_CAPTURE.md`. **v0.2.225-alpha — `--file=` PATH HARDENING:** added a pure, exported `safeRepoRelPath(raw)` → `{ok:true,rel}` else `{ok:false,reason}` (rejects empty/non-string, absolute paths POSIX `/`/Windows `\`/`X:` drive, `..` traversal, AND percent-encoded separators/dots via `ENCODED_SEP_RE = /%2[fF]|%5[cC]|%2[eE]/` literally PLUS a defense-in-depth `decodeURIComponent` re-check — malformed `%`-escape → reject; never throws); `tools/playtest-capture.mjs` `readTarget()` now wires the guard (dropped the inline `isAbsolute`/`normalize` import), still read-only and exits 2 on a rejected path. Resolves the v0.2.224 security-review NON-BLOCKING advisory (Node `fs` never URL-decodes, so `%2F`/`%5C` were not exploitable locally). NOTE: `tools/playtest-results-status.mjs` carries the same `readTarget` guard, OUT OF SCOPE this slice (possible follow-up). `tests/playtest-note-capture.test.js` (11 → 16). EXPLAINER ONLY — no browser/automation/approval/write; composes the parser/state output only — no new gameplay/physics/Nostr/network behaviour. |
| MVP manual playtest results intake template | `tools/playtestResults.mjs`, `tools/playtest-results.mjs` (`npm run playtest:results`) | **v0.2.204-alpha (PLAYTEST-RESULTS · MANUAL · LOCAL · READ-ONLY).** A structured RESULTS-INTAKE template for recording manual playtest results from the v0.2.203 `MVP_PLAYTEST_CHECKLIST.md` and feeding failures back into todo/progress/handoff — generated from frozen local data, NOT a browser test and NOT a gameplay change. `tools/playtestResults.mjs` is pure/node-safe (no fs/network/child_process/THREE/DOM; never throws) and imports `PLAYTEST_CHECKLIST_SECTIONS`/`PLAYTEST_SEVERITIES` from `./playtestChecklist.mjs` so the template tracks the checklist as the single source of truth: `PLAYTEST_RESULTS_SCHEMA` ('torii.playtest-results')/`PLAYTEST_RESULTS_SCHEMA_VERSION` (=1)/`PLAYTEST_RESULTS_SUMMARY_SCHEMA` ('torii.playtest-results-summary')/`PLAYTEST_RESULTS_BADGE` ('MVP PLAYTEST RESULTS INTAKE · LOCAL · READ-ONLY')/`PLAYTEST_RESULTS_WRITE_FILENAME` ('MVP_PLAYTEST_RESULTS_TEMPLATE.md')/`PLAYTEST_RESULTS_TITLE`; frozen `PLAYTEST_RESULT_VALUES` (`['PASS','FAIL','N/A']`), `PLAYTEST_RESULTS_META_FIELDS` (7: build/commit/liveUrl/tester/date/environment/overall), `PLAYTEST_RESULTS_ITEM_FIELDS` (5: result/severity/repro/media/nextAction), `PLAYTEST_RESULTS_HOWTO`. `playtestResultsItemCount()` sums the sections; `buildPlaytestResultsTemplate({version,gitCommit,liveUrl,generatedAt})` → `{schema,schemaVersion,generatedAt,badge,title,manual:true,version,gitCommit,liveUrl,resultValues,severities,howTo,metaFields,itemFields,sections (derived from the checklist: {key,title,items:[{id,title,severity,expected}]}),itemCount,safety:{automated/served/navigated/deployed/published/wrote/network=false},rendered:false,actionable:false}`. `formatPlaytestResultsTemplate(model)` → blank text template, `formatPlaytestResultsTemplateMarkdown(model)` → blank markdown template (build table + per-item Field/Value tables), both safe on null. A tolerant pure parser/summary reads a COMPLETED results markdown back: `parsePlaytestResults(text)` → `{schema,items:[{id,result,raw}],total}` (heading-anchored, reads the Result row's value cell, strips emphasis + parenthetical hints, never throws); `summarizePlaytestResults(parsedOrText)` → `{schema,total,counts:{total,pass,fail,na,blank,other},fails:[ids],verdict}` (verdict EMPTY/INCOMPLETE/ATTENTION/COMPLETE, accepts a raw string or parsed object); `formatPlaytestResultsSummary(summary)` → text, safe on null. CLI `tools/playtest-results.mjs` builds the model from `configVersion()`/`gitCommit()`/`HANDOFF_SUMMARY_LIVE_URL` behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `MVP_PLAYTEST_RESULTS_TEMPLATE.md`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; `--summarize[=path]` reads a completed results file in-repo (rejects absolute / `..`, exits 2) and prints a text/`--json` summary; READ-ONLY/local/no-network, always exits 0 otherwise. `tests/playtest-results.test.js` (16). RESULTS INTAKE ONLY — no browser/automation; composes only frozen local data + the checklist sections — no new gameplay/physics/Nostr/network behaviour. |
| MVP manual playtest acceptance checklist | `tools/playtestChecklist.mjs`, `tools/playtest-checklist.mjs` (`npm run playtest:checklist`) | **v0.2.203-alpha (PLAYTEST-CHECKLIST · MANUAL · LOCAL · READ-ONLY).** A MANUAL playtest / MVP acceptance checklist the user runs against the live build — generated from frozen local data, NOT a browser test and NOT a gameplay change. `tools/playtestChecklist.mjs` is pure/node-safe (no fs/network/child_process/THREE/DOM; never throws): `PLAYTEST_CHECKLIST_SCHEMA` ('torii.playtest-checklist')/`PLAYTEST_CHECKLIST_SCHEMA_VERSION` (=1)/`PLAYTEST_CHECKLIST_BADGE` ('MVP MANUAL PLAYTEST CHECKLIST · LOCAL · READ-ONLY')/`PLAYTEST_CHECKLIST_WRITE_FILENAME` ('MVP_PLAYTEST_CHECKLIST.md')/`PLAYTEST_CHECKLIST_TITLE`; frozen `PLAYTEST_SEVERITIES` (`['blocker','major','minor']`); frozen `PLAYTEST_CHECKLIST_SECTIONS` (13 areas keyed launch/shooter/movement/aim/reload/gun/mirror/crates/nap/continuum/update/nostr/gateway — 17 items total, each `{id, title, steps[], expected, severity, ifFailed}`); frozen `PLAYTEST_CHECKLIST_ADVISORIES` (4 known deferred/non-blocking) + `PLAYTEST_CHECKLIST_HOWTO`. `playtestItemCount()` sums the sections; `buildPlaytestChecklistModel({version,gitCommit,liveUrl,generatedAt})` → `{schema,schemaVersion,generatedAt,badge,title,manual:true,version,gitCommit,liveUrl,severities,howTo,sections (deep-copied),itemCount,advisories,safety:{automated/served/navigated/deployed/published/wrote/network=false},rendered:false,actionable:false}`; null/garbled inputs degrade safely and never throw. `formatPlaytestChecklist(model)` → text (checklist with `result: ____` fields), `formatPlaytestChecklistMarkdown(model)` → markdown (checkboxes + Result/Notes tables), both safe on null. CLI `tools/playtest-checklist.mjs` builds the model from `configVersion()`/`gitCommit()`/`HANDOFF_SUMMARY_LIVE_URL` behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `MVP_PLAYTEST_CHECKLIST.md`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; READ-ONLY/local/no-network, always exits 0 otherwise. `tests/playtest-checklist.test.js` (15). MANUAL CHECKLIST ONLY — no browser/automation; composes only frozen local data — no new gameplay/physics/Nostr/network behaviour. |
| MVP proof release-notes draft | `tools/releaseNotes.mjs`, `tools/release-notes.mjs` (`npm run release:notes`) | **v0.2.202-alpha (RELEASE-NOTES-DRAFT · LOCAL · READ-ONLY).** A DRAFT release-notes artifact for the first MVP proof-of-concept candidate, COMPOSED from the already-pure local readiness signals only — no re-derivation, no network, no GitHub release, no tag, no announcement. `tools/releaseNotes.mjs` is pure/node-safe (no fs/network/child_process/THREE/DOM; never throws): `RELEASE_NOTES_SCHEMA` ('torii.release-notes')/`RELEASE_NOTES_SCHEMA_VERSION` (=1)/`RELEASE_NOTES_BADGE` ('MVP PROOF RELEASE NOTES · DRAFT · LOCAL · READ-ONLY')/`RELEASE_NOTES_WRITE_FILENAME` ('RELEASE_NOTES_DRAFT.md')/`RELEASE_NOTES_TITLE`; frozen `RELEASE_NOTES_SECTIONS` (7 curated "what's built" sections — shooter proof loop; Nostr read/profile/leaderboard proof surfaces; gateway travel shell; update/VPS readiness; Continuum dashboard; SDK/debug handoff surfaces; tests/guardrails) + frozen `RELEASE_NOTES_ADVISORIES` (3 known non-blocking advisories). `buildReleaseNotesModel({rcGate,mvpReadiness,handoff,version,gitCommit,liveUrl,reports,generatedAt})` folds a `buildMvpRcGate()` verdict + a `runMvpReadiness()` rollup + a `buildHandoffSummary()` brief into `{schema,schemaVersion,generatedAt,badge,title,draft:true,version,gitCommit,liveUrl,candidate,readiness,sections,advisories,latestReports,safety,rendered:false,actionable:false}`; reports prefer explicit `reports` else `handoff.latestReports`; version resolves version→rollup.currentVersion→gate.version→handoff.version; `safety` pins `released/tagged/published/announced/served/navigated/wrote/network=false`. Null/garbled inputs degrade honestly and never throw. `formatReleaseNotes(model)` → text, `formatReleaseNotesMarkdown(model)` → markdown, both safe on null. CLI `tools/release-notes.mjs` reuses `gatherReleaseReadiness()` + `runMvpReadiness()` + `buildHandoffSummary()` + `buildMvpRcGate()` behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; `--write[=path]` (default `RELEASE_NOTES_DRAFT.md`, confined in-repo via `resolveHandoffWritePath`, exits 2 on rejected path) is the ONLY write; READ-ONLY/local/no-network, creates no release, cuts no tag, always exits 0 otherwise. `tests/release-notes.test.js` (13). Composes shipped signals only — no new gameplay/physics/Nostr/network behaviour. |
| MVP release-candidate gate | `tools/mvpRcGate.mjs`, `tools/mvp-rc-gate.mjs` (`npm run rc:gate`) | **v0.2.201-alpha (MVP-RC-GATE).** PURE node-safe MVP RELEASE-CANDIDATE GATE that answers ONE question — is this build ready to call an MVP proof-of-concept *release candidate*? — by COMPOSING the existing readiness signals only (no re-derivation). `tools/mvpRcGate.mjs` is pure (no fs/network/child_process/THREE/DOM; never throws): `MVP_RC_GATE_SCHEMA` ('torii.mvp-rc-gate')/`MVP_RC_GATE_SCHEMA_VERSION` (=1)/`MVP_RC_GATE_BADGE` ('MVP RELEASE-CANDIDATE GATE · LOCAL · READ-ONLY')/`MVP_RC_GATE_COMMAND` ('npm run test:release')/`MVP_RC_STATES` (frozen `['READY','NEAR','BLOCKED']`); `buildMvpRcGate({mvpReadiness,releaseReadiness,handoff,generatedAt})` folds a `runMvpReadiness()` rollup + a `buildReleaseReadiness()` summary (+ a `buildHandoffSummary()` brief as the next-task fallback) into `{schema,schemaVersion,generatedAt,badge,gateCommand,version,gitCommit,status,isCandidate,pct,reasons,nextTasks,components:{mvpReadiness,releaseReadiness},safety,rendered:false,actionable:false}`. Verdict: READY iff release ready AND MVP rollup green AND no release unknowns; BLOCKED if inputs missing / a release blocker exists / ≥2 MVP signals fail; else NEAR. `pct` blends passing MVP signals + the five required release signals (versionSync/tests/regression/zoneFallback/docs); `reasons` lists `release:<key>` / `mvp:<key>: <detail>` / `release:<key> (not checked this pass)`; `nextTasks` leads with `Clear top blocker: …` then the safe task, deduped + capped at 2; `safety` pins `served/deployed/published/navigated/released/tagged/wrote/network=false`. Null/garbled inputs degrade to an honest BLOCKED at 0% and never throw. `formatMvpRcGate(gate)` → text block, `formatMvpRcGateMarkdown(gate)` → markdown, both safe on null. CLI `tools/mvp-rc-gate.mjs` reuses `gatherReleaseReadiness()` + `runMvpReadiness()` + `buildHandoffSummary()` behind a `realpathSync` run-guard; modes default text / `--json` / `--markdown`; READ-ONLY/local/no-network, NEVER writes, creates no release, cuts no git tag, always exits 0. `tests/mvp-rc-gate.test.js` (14). Composes shipped signals only — no new gameplay/physics/Nostr/network behaviour. |
| AI-handoff status | `tools/handoffStatus.mjs`, `tools/handoff-status.mjs` (`npm run handoff:status`) | **v0.2.156-alpha (HANDOFF-STATUS).** One-glance handoff snapshot so a future AI/dev sees the project state without reading every file. `tools/handoffStatus.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM, reuses `formatBytes` from `bundleSizes.mjs`): `LIVE_URL`, `CORE_DOCS`, `CHECK_COMMANDS`, `stripV(version)`, `versionAgreement(configVersion,packageVersion)` (mirrors check [5] config↔package tie), `buildHandoffStatus({version,packageVersion,gitCommit,docsPresent,latestReports,bundle,liveUrl})` → JSON-serialisable `{badge,version,packageVersion,versionMatch,gitCommit,liveUrl,checks,docs:{present,missing},latestReports,bundle}`, `formatHandoffStatus(status)` → text block. CLI `tools/handoff-status.mjs` does the fs reads + best-effort `git rev-parse --short HEAD` (falls back to null) + optional dist gzip sizing and always exits 0 — visibility only, NOT a deploy gate (drift is covered by [5]/[14]). Network-free, no secrets. `tests/handoff-status.test.js`. |
| Release readiness | `tools/releaseReadiness.mjs`, `tools/release-readiness.mjs` (`npm run release:status`) | **v0.2.187-alpha (RELEASE-READINESS).** One concise ship verdict for AI handoff + rapid shipping, aggregating the local network-free signals so a shipper doesn't re-read every tool. `tools/releaseReadiness.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM; reuses `versionAgreement` from `handoffStatus.mjs`, `PROFILES`/`validateProfiles` from `testProfiles.mjs`, `formatBytes` from `bundleSizes.mjs`): `REGRESSION_CHECK_COUNT` (=15), `RELEASE_READINESS_BADGE`, `RELEASE_GATE_COMMAND`, `buildReleaseReadiness({version,packageVersion,gitCommit,existingTests,regression,bundle,zoneFallback,docs,latestReports})` → JSON-serialisable `{badge,gateCommand,status,statusLabel,ready,blockers,unknowns,signals:{versionSync,tests,regression,bundle,zoneFallback,docs},latestReports}` with honest per-signal states (`ok`/`blocked`/`advisory`/`skipped`/`unknown`) and a single verdict (READY / NOT READY · blockers / INCOMPLETE · signals missing); bundle stays ADVISORY (never blocks). `formatReleaseReadiness(summary)` → text block. CLI `tools/release-readiness.mjs` does the fs/git reads + folds the existing pure checks (`checkZoneFallbackReadiness`, `checkDocConsistency`, `summarizeBundle`) + reads the regression `[N]` count read-only, and always exits 0 — visibility only, NOT a gate (the authority stays `npm run check` / `npm run test:release`). Network-free, no writes, no secrets. `tests/release-readiness.test.js` (added to `test:foundation`). **v0.2.188-alpha:** the CLI's fs/git gathering was extracted into an exported `gatherReleaseReadiness(root=process.cwd())` (all fs/git helpers are now closures over `root`) so OTHER build-time tooling — namely `tools/build-continuum.mjs`, which surfaces the verdict on the Torii Continuum **Ship readiness** section via `buildShipModel` — can fold the SAME live signals without duplicating the I/O. The `npm run release:status` CLI behaviour is UNCHANGED: a `realpathSync` run-guard prints the formatted block + exits 0 only when the file is the entry point; imported, it stays silent and side-effect-free. **v0.2.189-alpha:** `tools/releaseReadiness.mjs` gained a PURE `buildReleaseStatusJson(summary,{generatedAt})` envelope (`RELEASE_STATUS_SCHEMA`='torii.release-status', `RELEASE_STATUS_SCHEMA_VERSION`=1) over a `buildReleaseReadiness()` summary → `{schema,schemaVersion,generatedAt,status,statusLabel,ready,badge,gateCommand,blockers,unknowns,version,packageVersion,gitCommit,signals,latestReports}`, so the readiness verdict is machine-readable for the dashboard/handoff/updater/agents WITHOUT parsing the human terminal block. Deterministic by design: `generatedAt` is the ONLY non-deterministic field — OPTIONAL + isolated (omit → `null`, fully reproducible for tests; a non-string stamp is ignored); the CLI passes a real ISO stamp at print time. A missing/garbled summary (incl. arrays) degrades to an honest `{status:'unknown',error:'no-summary'}` envelope; never throws, never mutates the source (signals are deep-cloned, arrays sliced). The CLI gained a `--json` flag (`npm run release:status:json`) that prints `JSON.stringify(buildReleaseStatusJson(...))` to stdout and exits 0 — pure parseable JSON (node tools/release-readiness.mjs --json is pure JSON; plain `npm run` prepends a lifecycle banner to stdout, so scripted consumers use `npm run --silent release:status:json`) — while the default human block + the `realpathSync` run-guard are unchanged. Read-only/local-only/no network. `tests/release-readiness.test.js` (+7). |
| Bundle-size tracking | `tools/bundleSizes.mjs`, `tools/bundle-report.mjs`, `tools/regression-check.mjs` (check [13]), `npm run bundle:report` | **v0.2.153 (BUNDLE-TRACK).** Advisory baseline that turns the recurring Vite "large chunk" warning into a measurable LOCAL report WITHOUT changing the runtime or the existing `manualChunks` splitting. `tools/bundleSizes.mjs` is PURE/node-safe (no fs/zlib/THREE/DOM): `KIB`/`MIB`/`DEFAULT_WARN_LIMIT` (700·KiB, mirrors vite.config.js `chunkSizeWarningLimit`), `formatBytes(bytes,digits)`, `classifyAsset(name)` (hash-agnostic stem → app/three/rapier/runtime/html/other), `isJsCategory`, `severityFor(bytes,limit)`, `summarizeBundle(entries,opts)` → `{warnLimit,assets[sorted desc],totals{count,jsBytes,jsGzip,htmlBytes,allBytes},categories,warnings}`. `tools/bundle-report.mjs` does the fs+`node:zlib` gzip I/O over `dist/assets/*.js`+`dist/index.html` and prints a per-asset/per-category table + advisory flag (exits 0 always; hint+exit 0 when no `dist/`). `regression-check.mjs` check [13] is NON-FAILING (never touches `fails`; one-line summary; skipped when no `dist/`). Build-time only — never imported by the game; reads built artifacts only (no network/install/build). `tests/bundle-sizes.test.js`. |
| Physics | `src/engine/physics/raycast.js`, `src/engine/physics/bodies.js`, `src/physics.js`, `src/main.js` (ENTER bootstrap) | Rapier-backed truth layer for LOS, bullets, crates, bodies. **v0.2.128 — head-sphere geometry lowered: `BOT_HEAD_CENTRE_Y_OFFSET` 1.65→1.55, `BOT_HEAD_RADIUS` 0.22→0.20 → sphere spans [1.35,1.75] hugging the Banker GLB face/crown (was [1.43,1.87], floating 0.17 m above the visible head so players had to aim OVER the model). Bottom still overlaps the body cap (1.52) — no thread-through gap.** **Re-entry fix (v0.2.128): `initPhysics()` builds a BRAND-NEW Rapier world each call; the old ENTER handler re-ran the full bootstrap every entry, orphaning the load-time bot colliders (bound to the discarded world) so a re-entered arena had no bot colliders ("hardly any shots connect"). The ENTER handler now bootstraps physics + colliders + player body + viewmodels exactly ONCE (`_arenaBootstrapped` guard); the single world persists across HOME/ENTER and subsequent entries only `resetRun()` + reset the player to spawn.** |
| Bots/NPCs | `src/bots.js` (runtime), `src/engine/entities/bot-agent.js` (SDK boundary) | v0.2.122 began the BotAgent boundary: pure decision helpers `engageSpeed`/`steerComponent`/`inEngageRange`/`wantsToShoot`, `BOT_ACTION` constants, and a `decideActions(worldState) -> BotAction[]` facade (the `BotAgent.tick` direction). `bots.js` consumes the allocation-free scalar helpers in `tickBots()` (LOS short-circuit preserved: cheap `inEngageRange()` gates the expensive LOS check); `decideActions` is tested-but-unwired (allocates per call). **v0.2.131:** the bot-LOS call now goes through the RaycastService facade — `raycastService.lineOfSight(...)` instead of importing `hasLineOfSight` from `physics.js` directly (ARS-3 first call-site migration; behaviour-identical, both resolve to `raycast.js`). Remaining: migrate the stateful tick/shoot/blowback runtime behind the boundary. |
| Outer-arena boundary (coastline) | `src/terrain/coastline.js` (pure polygon), `src/arena.js` (glass wall), `src/physics.js` (player colliders), `src/bots.js` (containment), `src/terrain/terrainMesh.js` (ground) | **v0.2.342 "Landscaping":** the square ±`ARENA_HALF` footprint is replaced by an ORGANIC coastline. `terrain/coastline.js` is a pure, node-safe, deterministic module (no THREE, no `Math.random`) exporting `ARENA_COASTLINE` (32-vertex wavy ring, max extent ≈18 < 20), `pointInCoastline`, `clampToCoastline(x,z,margin)`, `coastlineRing()`, `coastlineBounds()` — the SINGLE source the wall, colliders and bot clamp all read, so they can never drift. The old orange square `LineSegments` trim is gone; `arena.js` now builds a knee-high (0.5 m) transparent glass ribbon + emissive neon top tube following the ring (`_buildCoastlineWall`), with a gate gap at the east bridge so the NAP walkway stays open. `physics.js` adds ~30 thin yaw-rotated cuboid PLAYER colliders (0.5 m tall — above the 0.3 m autostep, under the ~2 m jump apex) via the new `createStaticYaw` helper (`engine/physics/bodies.js`), same gate gap. **Player wall and bot containment are INDEPENDENT systems:** kinematic bots ignore static colliders, so `bots.js` contains them purely by math — `_pushout` now calls `clampToCoastline(nx,nz,BOT_R)` (full closed polygon, no gap → bots never cross to NAP), spawns sample `coastlineBounds()` rejecting `!pointInCoastline`, dead-bot blowback clamps to the coast. The turquoise overhead PointLight became a cheap `HemisphereLight` fill; arena ground is now sandy `0xb9a06b` with procedural per-vertex colour variation (high roughness, no textures). Covered by `tests/coastline.test.js`. |
| HUD/UI | `src/hud.js`, `src/engine/ui/phaseScreens.js`, `index.html` HUD markup/styles | Reticle states: none, close, body, headshot. v0.2.121: top-level screen visibility (title/HUD/pause modal) is derived from a pure phase→visibility map in `engine/ui/phaseScreens.js` and applied by a single `EV.PHASE_CHANGE` subscriber in main.js. |
| Audio | `src/audio.js` | Reload is WebAudio-scheduled; no new `setTimeout`. |
| World/NAP | `src/world/napZone.js`, `src/world/handoff.js`, `src/identity/presence.js`, `src/engine/gateway/travelIntent.js`, `src/engine/gateway/gatewayHandoff.js`, `src/engine/gateway/gatewayPortal.js`, `src/engine/gateway/gatewayPreview.js`, `src/engine/gateway/gatewayRead.js`, `src/engine/gateway/travelConfirm.js`, `src/engine/gateway/handoffPlan.js`, `src/engine/gateway/handoffExecute.js`, `src/engine/world/proofSurfaceSpecs.js`, `src/engine/world/anchorTransforms.js`, `GATEWAY_PROTOCOL.md` | Skeletons exist; formalise after SDK Layer 1. **v0.2.134:** `GATEWAY_PROTOCOL.md` drafts the n2n spatial-hop wire agreement and `engine/gateway/travelIntent.js` adds the pure URL-handoff helpers (build/parse/validate a travel intent — `to`/`from`/`return`/`spawn`/`zoneType`/`relays`/`player`/`state`); no navigation/relay/signing. **v0.2.135:** `engine/gateway/gatewayHandoff.js` bridges a gateway COMPONENT to a travel INTENT — `gatewayDestination`/`planGatewayTravel`/`gatewayTravelUrl` map the gateway manifest's `{npub,relay,target}` onto a validated intent / URL string (pure return values; still no navigation/relay/signing). `world/handoff.js` is the host seam where a future build acts on a validated intent (the step with browser side effects). **v0.2.136:** `engine/gateway/gatewayPortal.js` adds the pure portal VIEW shell over `gatewayHandoff` — `gatewayPortalView` returns a render-ready view-model (status/armed/destination label/prompt/relay/URL preview) for a portal mesh to display; DISPLAY-ONLY, never navigates. **v0.2.139 (LEAN-2):** `engine/gateway/gatewayPreview.js` makes the preview VISIBLE — `gatewayPreviewBlock` flattens `gatewayPortalView` into a render-ready block of `{label,value}` rows (Destination/Status/Relay/Intent/URL) + `statusText`/`previewUrl` helpers + a `GATEWAY_PREVIEW_BADGE` ("PREVIEW · SAFE · INERT"); every block `actionable:false`. `main.js` renders it into the title-screen `#gateway-preview` card via `textContent` only (no link/navigation/fetch/signing); read-only at `ToriiDebug.shells.gatewayPreview()`. SDK `gatewayPreview` (experimental). **v0.2.147:** `engine/world/proofSurfaceSpecs.js` adds the pure LAYOUT/SPEC layer for the FUTURE in-world proof meshes — `PROOF_SURFACE_SPECS` (4 frozen specs: gateway-portal-panel/product-stall-panel/leaderboard-board/update-prompt-board), `PROOF_SURFACE_IDS`, `getProofSurfaceSpec(id)`, `proofSurfaceLayout()`. Each spec carries identifiers/labels (step/lean/title/kind/previewSdk/shell), an `anchor` + plain `position`/`size`/`yawRad` placement data anchored in the NAP zone (x in [`NAP_X`,`NAP_FAR_X`]), and inert `invariants` (readOnly/actionable, +signed/published on the board). PLAIN data only — NO THREE objects, NO DOM, NO renderer mutation, NO gameplay integration; this is the spec/contract layer the future mesh pass binds against (`rendered:false`). SDK `proofSurfaceSpecs` (experimental); read-only at `ToriiDebug.shells.surfaceSpecs()`; `tests/proof-surface-specs.test.js`. **v0.2.149:** `engine/world/anchorTransforms.js` adds the pure ANCHOR→TRANSFORM contract those specs resolve against — `PROOF_SURFACE_ANCHORS` (4 frozen anchors: torii-gate-threshold/nap-zone-north-stall/nap-zone-far-centre/nap-zone-south-board, each with a plain ground `origin` (y:0) + `parent` hint + NAP `zone`), `ANCHOR_IDS`, `getAnchor(id)`, `resolveAnchorTransform(spec)` (→ a plain descriptor: origin/position/`offset`=position−origin/size/yawRad, with `origin+offset===position`), `resolveAllAnchors(specs)` (→ `{ok,count,resolved,unresolved}`). PLAIN data only — NO THREE/DOM/renderer/gameplay; the single source of truth the future mesh pass reads to place each surface relative to its anchor. SDK `anchorTransforms` (experimental); read-only at `ToriiDebug.shells.anchorTransforms()`; `tests/anchor-transforms.test.js`. **v0.2.150:** the FIRST display-only in-world proof-surface MESH pass, split in two: `engine/world/proofSurfaceRenderPlan.js` (PURE, node-safe) — `buildProofSurfaceRenderPlan(opts?)` runs the `resolveAllAnchors()`+`checkProofSurfaceSpecs()` gates (either injectable via `opts`) and turns the four specs into a plain-data RENDER PLAN `{badge,ok,gates,count,panels,reasons,rendered:false,actionable:false}`, each panel `{id,label,sublabel,kind,anchor,position,size,yawRad,color,readOnly:true,actionable:false}`; NO THREE/DOM. `engine/world/proofSurfaceMeshes.js` (browser-only adapter) — `buildProofSurfaceMeshes(scene,opts?)` consumes the plan and, ONLY when `plan.ok`, builds inert panel meshes (coloured `BoxGeometry` board + `CanvasTexture` label plate, the bitcoin-sun canvas-text pattern) EXACTLY ONCE during scene setup; `arena.js` `_buildNapZone()` calls it after `_buildNapTree`. Idempotent `_built`/`rendered` guard; NO per-frame/hot-path allocation. `proofSurfaceRenderState()` mirrors `{rendered,count,ok,badge,reasons}`. DISPLAY-ONLY/INERT: no click/raycast/navigation/payments/Nostr/live-data/fetch. Read-only at `ToriiDebug.shells.surfaceRender()`; `tests/proof-surface-render-plan.test.js` + `tests/proof-surface-meshes.test.js`. (The plan imports `proofSurfaceCheck.js` → not in the SDK barrel; debug-only.) **v0.2.151:** `engine/world/proofSurfaceParentBinding.js` adds the pure scene-graph PARENT BINDING — `PARENT_NODE_NAMES`/`parentNodeName(parent)` map each anchor `parent` hint (`torii-gate`/`nap-zone-floor`) to the live scene-node name; `parentGroupName(parent)`→`proof-surfaces::<parent>`; `resolveParentBindings(plan)` groups the plan's panels by parent → `{badge,group,count,ok,groups:[{parent,parentNode,groupName,panelIds}],unbound,rendered:false,actionable:false}`. The render plan now carries `parent` on each panel; the mesh adapter builds one named subgroup per parent under the `proof-surfaces` root and mounts each board there (boards keep WORLD positions — structural/discoverability change only, no visual change); `arena.js` `.name`s the live `nap-zone-floor` + `torii-gate` (fallback + GLB) nodes so `scene.getObjectByName` finds them. PURE/node-safe — NO THREE/DOM; builds/parents nothing. Read-only at `ToriiDebug.shells.surfaceBindings()`; `tests/proof-surface-parent-binding.test.js`. **v0.2.152:** `engine/debug/proofSurfaceGate.js` adds the pure promotion/regression GATE — `proofSurfaceGate(opts?)` folds the spec↔registry cross-check + render plan + scene-graph parent binding into one fail-fast `{badge,ok,gates:{specCheck,renderPlan,parentBinding},counts:{panels,groups,bound,unbound},reasons,rendered:false,actionable:false}`; `ok` iff all three hold, `reasons` lists concrete failures, every input (`check`/`anchors`/`plan`/`binding`) injectable. `tools/regression-check.mjs` check [12] RUNS it (`await import` of the pure gate — THREE/DOM-free — fails with the gate's `reasons`); read-only at `ToriiDebug.shells.surfaceGate()`. PURE/node-safe — composes plain data only, renders/acts on nothing. `tests/proof-surface-gate.test.js`. **v0.2.164 (LEAN-2):** `engine/gateway/gatewayRead.js` adds the READ-ONLY gateway DESTINATION-RECORD read proof — `readGateways(input,options)` builds the kind-30078 `#t:torii-gateway` NIP-01 filter (`buildGatewayFilter`), runs injected/sample relay events through relayRead `normalizeRelayEvent`→`validateRelayEvent`→`extractGatewayFromEvent`, and dedupes to the newest record per addressable `pubkey+zoneId` (`dedupeGateways`), returning a read-only `{ok,filter,count,gateways,skipped,duplicates,navigated:false,signed:false,published:false,performed:false,readOnly:true,errors}`. `extractGatewayFromEvent` sanitises every field into a safe travel-preview model (zone id anchored to the `d` tag; text control/markup-stripped; website/banner https-only via profileRead `safeProfileUrl`; relays ws/wss-only + credential-free + deduped via relayRead `validateRelayUrl`; npub via travelIntent `looksLikeNpub`; zoneType ∈ nap/arena/shop/gallery; `trust:'unverified'`). PURE/node-safe — NO navigation/relay I/O/socket/signing/publishing/NIP-07/DOM/network/auto-connect; never throws on event data. SDK `gatewayRead` (experimental); read-only at `ToriiDebug.shells.gatewayRead()` over `DEMO_GATEWAY_EVENTS`; `tests/gateway-read.test.js`. **v0.2.165 (LEAN-2 / GATEWAY-TRAVEL):** `engine/gateway/travelConfirm.js` adds the READ-ONLY gateway travel CONFIRMATION/INTENT behind the v0.2.162 consent gate, consuming the v0.2.164 gatewayRead preview model. `sanitizeDestination(input)→{ok,destination?|errors?}` accepts EITHER a `gatewayRead` preview model OR a plain descriptor (idempotent), anchored to a required `zoneId`, control/markup-stripping text, https-only website via profileRead `safeProfileUrl`, ws/wss credential-free relays via relayRead `validateRelayUrl` (deduped/capped), `looksLikeNpub` npub, 64-hex pubkey, known `zoneType` (nap/arena/shop/gallery); no-zoneId/malformed → `ok:false`, never throws. `summariseTravelConfirm(input)` → one stable preview-only line. `prepareTravelIntent(input,grant)` routes the destination through `evaluateConsent('gateway:travel',grant)` → INERT `{ok,action,destination,consent,summary,navigated:false,performed:false,signed:false,published:false,readOnly:true,errors}` — BLOCKED with no grant (`consent-required`), allowed ONLY with an explicit matching grant (boolean `true`/scoped `{granted,action,token}`; mismatch → `consent-mismatch`), malformed destination `ok:false` even WITH a grant; an allowed grant marks consent allowed but STILL never navigates/signs/publishes/sends/connects (`navigated:false`/`performed:false` pinned). Exposes NO navigate/goto/sign/publish/send/connect/open/apply method. PURE/node-safe — NO browser navigation/world-unload/relay I/O/socket/signing/publishing/NIP-07/DOM/network/auto-connect. SDK `travelConfirm` (experimental); read-only at `ToriiDebug.shells.gatewayTravel(input?,grant?)` over `DEMO_TRAVEL_INPUT` (new `gatewayTravelReport` in `shellReport.js`, added to `buildShellReport`; the 4-surface `shellsSummary` list unchanged); `tests/gateway-travel-confirm.test.js`. The real host navigation/world-unload-reload + the consent UX prompt remain deferred. **v0.2.167 (LEAN-2 / GATEWAY-HANDOFF):** `engine/gateway/handoffPlan.js` adds the host TRAVEL HANDOFF SEAM over `prepareTravelIntent` — the host-side boundary that RECEIVES an allowed `gateway:travel` intent and produces a DRY-RUN handoff + rollback plan. `HANDOFF_PLAN_VERSION`/`HANDOFF_BADGE` ('HANDOFF · DRY-RUN · NO NAVIGATION')/`HANDOFF_STATUS` (ready/blocked/invalid)/`HANDOFF_COMMANDS` (frozen ordered FUTURE command names — preflight/snapshotState/unloadWorld/navigate/loadWorld/spawnPlayer — DECLARED for audit, NEVER invoked, NO method bears these names). `safeRoutePath(raw)` accepts ONLY a single-`/` same-origin path (rejects `//`, schemes, `javascript:`/`data:`, whitespace, control chars, markup, backslashes, length-capped). `handoffRouteFor(destination)` → inert same-site `/zone/<slug>`; `handoffUrlFor(destination)` → https-only external preview via profileRead `safeProfileUrl`. `summariseHandoff(input,grant,hostContext)` → one stable preview-only line. `planHandoff(input,grant,hostContext)` forwards to `prepareTravelIntent`, injects the host's sanitised `currentRoute`/`rollbackRoute` (fallback `/`), and returns an INERT `{version,badge,action,status,ok,reason,targetZoneId,targetRoute,targetUrl,currentRoute,rollbackRoute,preflight:[{check,ok,detail}],commands,destination,consent,summary,dryRun:true,navigated:false,worldReloaded:false,performed:false,signed:false,published:false,readOnly:true,errors}` — INVALID with no destination, BLOCKED on withheld/mismatched consent, READY only for an allowed matching intent over a valid destination; rollback route ALWAYS present even when blocked. Even READY NEVER navigates/unloads/reloads/performs — `ok:true` is proof of what a host COULD execute next, not the act. PURE/node-safe — NO browser navigation/`location`/`window.open`/`history`/router/world unload-reload/relay I/O/socket/signing/publishing/NIP-07/DOM/network/fetch; never throws. SDK `handoffPlan` (experimental); read-only at `ToriiDebug.shells.handoffPlan(input?,grant?,hostContext?)` over `DEMO_HANDOFF_INPUT` (new `handoffPlanReport` in `shellReport.js`, added to `buildShellReport`; the 4-surface `shellsSummary` list unchanged); `tests/handoff-plan.test.js`. The first controlled local/same-site travel that ACTS on a ready plan (v0.2.168) remains deferred. **v0.2.168 (LEAN-2 / GATEWAY-EXECUTE):** `engine/gateway/handoffExecute.js` adds the first controlled SAME-ORIGIN travel EXECUTOR over a v0.2.167 READY plan — the first slice that can actually change a route, but ONLY through an injected host transport and ONLY for a safe same-origin path. `EXECUTE_VERSION`/`EXECUTE_BADGE` ('TRAVEL · SAME-ORIGIN · HOST-TRANSPORT')/`EXECUTE_STATUS` (done/no-op/blocked/failed/rolled-back). `isHostTransport(t)` requires a `navigate` function (snapshot/rollback/log optional). `executeHandoff(plan,transport,opts)` validates the plan is a READY v0.2.167 handoff plan, RE-validates `plan.targetRoute` with `safeRoutePath` (the external `targetUrl` is NEVER executed — preview-only this slice), then hands ONLY the safe same-origin route to the INJECTED `transport.navigate(route)`: default NO-OP with no transport / under `opts.dryRun`, BLOCKED on a non-ready/invalid/unsafe-route plan; `navigated`/`performed` go true ONLY when navigate succeeds; a navigate throw or explicit `false` triggers a single SYNCHRONOUS rollback (NO timers) via `transport.rollback(rollbackRoute)` if present (`failed` vs `rolled-back`, rollback failure captured in `errors`); optional `transport.snapshot()`/`log()` are best-effort (a throw never breaks execution). Returns `{version,badge,action,status,ok,reason,targetRoute,fromRoute,rollbackRoute,steps:[{step,ok,detail}],rollback:{attempted,ok,route},rolledBack,navigated,performed,external:false,worldReloaded:false,signed:false,published:false,network:false,errors}` with the safety flags PINNED LAST (a caller cannot flip `external`/`worldReloaded`/`signed`/`published`/`network` via a tampered plan). `executeHandoffFor(input,grant,transport,opts)` builds the plan then executes. Exposes NO bare browser-navigation method (open/reload/goto/assign/href/pushState/location/unload). PURE/node-safe — NO direct `location`/`window.open`/`history`/router/world unload-reload/relay I/O/socket/signing/publishing/NIP-07/payments/auto-update/timers/DOM/network/fetch; never throws. SDK `handoffExecute` (experimental); DEFAULT-NO-OP `ToriiDebug.shells.handoffExecute(input?,grant?,transport?,opts?)` (new `handoffExecuteReport` in `shellReport.js`, added to `buildShellReport`; with no injected transport the debug shell never navigates the live app; the 4-surface `shellsSummary` list unchanged); `tests/handoff-execute.test.js`. The REAL audited host transport (router/history adapter + same-origin allowlist + CSP) this executor will drive remains deferred. **v0.2.170-alpha (LEAN-2 / GATEWAY-TRANSPORT):** `engine/gateway/hostTransport.js` builds the same-origin HOST TRANSPORT ADAPTER the v0.2.168 executor drives — the controlled, injectable seam between `executeHandoff` and a real router/history, scoped to same-origin route changes ONLY. `HOST_TRANSPORT_VERSION`/`HOST_TRANSPORT_BADGE` ('TRANSPORT · SAME-ORIGIN · HISTORY-PUSHSTATE'). `isRouteHost(host)` accepts a bare callable or an object exposing a `pushState` fn. `createHostTransport(host,{home,onLog})` normalises the host into bound `push`/`replace`/`read` thunks (bare fn → push/replace = fn; object → push from `pushState`, replace from `replaceState` (push fallback), read from `getRoute`) and returns an executor-compatible `{navigate,snapshot,rollback,log}` — or `null` when no usable host (→ executor safely NO-OPs, satisfying "missing host no-op"). `navigate(route)` re-validates via `safeRoutePath` and pushes ONLY a safe same-origin path (external/protocol-relative/unsafe → logged reject + `false`); `snapshot()` reads+sanitises the current route (fallback `home`); `rollback(route?)` replaces to a safe target / saved snapshot / home — `rollback()` with no arg is the back-home escape. `createRecordingHost(initialRoute='/')` is the DEFAULT-SAFE in-memory host (records `pushState`/`replaceState` calls, never really navigates) used by the debug shell + tests. `createBrowserHostTransport(win,opts)` is the runtime SEAM: returns `null` without `win.history.pushState`, builds the host using ONLY `win.history.pushState/replaceState` + `win.location.pathname+search` (NO reload/href/open) — provided but NOT wired into the live app. Exposes NO bare browser-navigation method at module scope. PURE/node-safe — browser APIs fully isolated behind the injected host; NO direct `location`/`window.open`/`history`/router/world unload-reload/relay I/O/socket/signing/publishing/NIP-07/payments/auto-update/timers/DOM/network/fetch; never throws. SDK `hostTransport` (experimental); read-only `ToriiDebug.shells.hostTransport(input?,grant?,opts?)` runs a full plan→execute over a recording host (new `hostTransportReport` in `shellReport.js`, added to `buildShellReport`; the 4-surface `shellsSummary` list unchanged); `tests/host-transport.test.js`. Wiring `createBrowserHostTransport(window)` into `world/handoff.js` (the real router/history adapter + same-origin allowlist + CSP) remains the next deferred step. **v0.2.178-alpha (LEAN-2 / GATEWAY-ACTIVATION):** `engine/gateway/gatewayActivation.js` is the LIVE-WIRE seam that finally lets the v0.2.168 executor ACT on a confirmed same-origin hop. `ACTIVATION_VERSION`=1/`ACTIVATION_BADGE` ('GATEWAY · CONFIRMED · SAME-ORIGIN HOP')/`ACTIVATION_STATUS` (navigated/unconfirmed/no-transport/blocked/rolled-back/failed)/`TRANSPORT_KIND` (browser/host/injected/none). `resolveHostTransport(source,opts)→{transport,kind}` picks a transport WITHOUT navigating: an `isHostTransport` object → `injected`; a window-shaped object (`history.pushState`) → `browser` via `createBrowserHostTransport`; an `isRouteHost` host → `host` via `createHostTransport`; else `none`. `activateGatewayHandoff(input,grant,opts)` ALWAYS builds the dry-run `planHandoff` first (inert/auditable), then enforces THREE ordered gates — (1) `opts.confirmed===true` literal boolean (any truthy-but-not-true → `unconfirmed`, transport NEVER resolved), (2) the consent-gated plan `ok` (missing grant / unidentifiable destination → `blocked`), (3) the planned `targetRoute` passes the optional `routeAllowlist` prefix check (`route-not-allowed` → `blocked`) — and ONLY THEN resolves a transport (from `opts.transport`/`opts.window`/`opts.host`) and calls `executeHandoff`. `live` is true only on the real browser-window path; a failed navigate rolls back to the rollback route (back-home) when supported. Report pins `external`/`worldReloaded`/`signed`/`published`/`network`=false. `DEMO_ACTIVATION_OPTS` is a frozen confirmed+`/zone/`-allowlist preset. Exposes NO bare browser-navigation method at module scope; the window is INJECTED, never reached at module scope. PURE/node-safe — never throws. SDK `gatewayActivation` (experimental); read-only `ToriiDebug.shells.gatewayActivation(input?,grant?,opts?)` drives a `createRecordingHost` so the debug path NEVER live-navigates (new `gatewayActivationReport` in `shellReport.js`, added to `buildShellReport`; the 4-surface `shellsSummary` list unchanged); `tests/gateway-activation.test.js`. Driving this from a real injected host router (app/browser window + CSP-scoped allowlist) + an in-world portal mesh remains the next deferred step. **v0.2.180-alpha (LEAN-2 / GATEWAY-PORTAL-ACTIVATION):** `engine/gateway/gatewayPortalActivation.js` adds the pure PORTAL-BOUNDARY seam that bridges an in-world gateway COMPONENT to the v0.2.178 confirmed same-origin hop. `PORTAL_ACTIVATION_VERSION`=1/`PORTAL_ACTIVATION_BADGE` ('GATEWAY PORTAL · CONFIRMED · SAME-ORIGIN HOP')/`DEFAULT_PORTAL_ALLOWLIST` (frozen `['/zone/']` — a meaningful scoped prefix, NEVER `['/']`)/`PORTAL_STATE` (idle/armed/navigated/blocked). `portalActivationInput(component,context)` maps `gatewayDestination(component).target`→`zoneId` and DROPS the external `website` (so an external profile URL is never built or navigated), carrying title/zoneType/npub/relays only; rejects a non-gateway component or a destination with no target. `sanitizePortalAllowlist(allowlist)` keeps only same-origin string prefixes (`/`-leading, length ≥2) and FOLDS `['/']`→`['/zone/']` (stronger than the executor's fail-closed — the boundary can never be permit-everything). `withinPortalRange(playerPos,portalPos,radius=3)` is a scalar squared-distance compare (NO Vector3 allocation). `activatePortalHandoff(component,context,grant,opts)` builds the input, sanitises the allowlist, and delegates to `activateGatewayHandoff` (so all three v0.2.178 gates still apply — confirmed===true, consent-gated plan.ok, route-allowlist prefix), wrapping the result in a `_portalReport` that pins `external`/`worldReloaded`/`signed`/`published`/`network`=false LAST. `createGatewayPortalBoundary(opts)` captures the injected window/transport/host ONCE at construction and is a one-shot `arm(component,context)`→`confirm(grant,extra)` controller (`cancel()`/`state()`/`armed()`/`routeAllowlist()`/`stagedZoneId()`); `confirm` sets `confirmed:true` and delegates, refusing with reason `not-armed` if not armed. Exposes NO bare browser-navigation method; the window is INJECTED, never reached at module scope. PURE/node-safe — never throws. SDK `gatewayPortalActivation` (experimental); read-only `ToriiDebug.shells.gatewayPortalActivation(component?,context?,grant?,opts?)` drives a `createRecordingHost` so the debug path NEVER live-navigates; `tests/gateway-portal-activation.test.js` (28 tests). Wiring an actual in-world portal MESH + proximity trigger that calls `arm`/`confirm` against a real injected browser host remains the next deferred step. **v0.2.181-alpha (LEAN-2 / GATEWAY-PORTAL-TRIGGER):** `engine/gateway/portalTrigger.js` adds the pure PROXIMITY→CONFIRM controller that finally wires an in-world portal position to the v0.2.180 `createGatewayPortalBoundary` — proximity ARMS/PREVIEWS but never navigates; only an explicit player interaction (KeyF) confirms. `PORTAL_TRIGGER_VERSION`=1/`PORTAL_PROMPT_TEXT` ('Press F to travel'). `createPortalTrigger({boundary,component,context,portalPos,range=3,onPrompt,promptText})` returns `{tick(playerPos),interact(grant=true),isArmed(),inRange(),promptShown(),reset(),portalPos(),range()}`. `tick` uses the v0.2.180 `withinPortalRange` scalar compare (NO Vector3 allocation); on ENTERING range it calls `boundary.arm(component,context)` + emits the prompt, on LEAVING it calls `boundary.cancel()` + hides the prompt — state changes ONLY on transitions, returns `{inRange,armed,changed}`. `interact(grant)` acts ONLY when the boundary is `armed()`, delegates to `boundary.confirm(grant)` (so all three v0.2.178 gates + the v0.2.180 `['/zone/']` allowlist still apply), clears the prompt, returns the activation report or `null`. `reset()` cancels + clears the prompt. Exposes NO bare navigate/open/reload/goto/assign/href/pushState method; NO window/THREE/DOM (the boundary — which captured the injected window ONCE at construction — is injected). `onPrompt` is best-effort (try/catch). PURE/node-safe — never throws. WIRED in `main.js` composition root ONLY: a `createGatewayPortalBoundary({window,routeAllowlist:['/zone/'],...})` + `createPortalTrigger(...)` whose `tick` runs in `update()` while `isPlaying()` (else `reset()`), with a KeyF `onKeyDown` handler calling `interact(true)`; `hud.js` `showPortalPrompt`/`hidePortalPrompt` render the lazy `#portal-prompt` div (opacity crossfade, no setTimeout). SDK `portalTrigger` (experimental); read-only `ToriiDebug.shells.portalTrigger(component?,context?,opts?)` drives a `createRecordingHost` boundary so the debug path NEVER live-navigates (new `portalTriggerReport` in `shellReport.js`); `tests/portal-trigger.test.js`. A dedicated portal MESH + SPA `/zone/<slug>` route handler (so a hard refresh resolves the zone) remains the next deferred infra step. **v0.2.182-alpha (LEAN-2 / GATEWAY-ZONE-ROUTE):** `engine/gateway/zoneRoute.js` adds the pure SPA `/zone/<slug>` ROUTE PARSER — the safe client-side READ of the same-origin URL state the v0.2.181 portal hop pushes, so a refresh/deep-link on `/zone/*` is not brittle. `ZONE_ROUTE_VERSION`=1/`ZONE_ROUTE_BADGE` ('ZONE ROUTE · SAME-ORIGIN · INERT')/`ZONE_ROUTE_PREFIX` ('/zone/')/`ZONE_SLUG_MAX_LEN`=64/`ZONE_ROUTE_KIND` ({HOME,ZONE,INVALID})/`DEMO_ZONE_ROUTE` ('/zone/plebeian-market-bazaar'). `isValidZoneSlug(slug)` (strict: lowercase alnum words joined by single hyphens, ≤64, no lead/trail/double hyphen, non-strings rejected), `humanizeZoneSlug(slug)` (Title Case or ''), `zoneRouteFor(slug)` (`/zone/<slug>` or null), `parseZoneRoute(path)`, `describeZoneRoute(path)`. `parseZoneRoute` runs the route through the v0.2.179-hardened `safeRoutePath` FIRST (non-string/empty/over-length(256)/dot-dot/percent/protocol-relative/`javascript:`/`data:`/markup/control/whitespace/backslash → INVALID), strips a trailing `?query`/`#hash`, then classifies HOME (root `/` or any non-`/zone/` same-origin path, `ok:true`), ZONE (valid slug → INERT `{kind,ok,slug,zoneId,route,title,notice}`), or INVALID (sub-path `/zone/a/b`, malformed slug, empty slug, hostile, non-string; `ok:false` + `errors`). `navigated`/`performed`/`external`/`signed`/`published`/`network` ALL pinned false LAST — it interprets a URL, never acts. Exposes NO bare navigate/open/reload/goto/assign/href/pushState method; NO module-scope `window`/THREE/DOM; never throws. WIRED in `main.js` composition root ONLY: `_applyZoneRoute()` reads `window.location.pathname` once on startup + on a `popstate` listener, calling `hud.js` `showZoneNotice`/`hideZoneNotice` (lazy `#zone-notice` div, opacity crossfade, no setTimeout) for a zone/invalid route and hiding it on home. SDK `zoneRoute` (experimental); read-only `ToriiDebug.shells.zoneRoute(path?)` (new `zoneRouteReport` in `shellReport.js`); `tests/zone-route.test.js` (28 tests). **Hard-refresh deep-link resolution is NOT solvable in app code alone — it requires a static-host SPA fallback (serve `index.html` for `/zone/*`); documented in HANDOFF.md §7 + GATEWAY_PROTOCOL.md, not faked in code.** A dedicated portal MESH remains the next deferred step. **v0.2.183-alpha (LEAN-2 / GATEWAY-PORTAL-MESH):** `engine/gateway/portalMeshPlan.js` (PURE/node-safe) + `engine/gateway/portalMesh.js` (browser-only THREE adapter) add the visible in-world PORTAL MARKER at the v0.2.181 trigger position so a player can SEE the travel point. `PORTAL_MESH_PLAN_VERSION`=1/`PORTAL_MESH_BADGE` ('PORTAL MESH · DISPLAY-ONLY · INERT')/`PORTAL_MESH_GROUP` ('gateway-portal')/`DEMO_PORTAL_MESH_OPTS`. `buildPortalMeshPlan({position,range,title})` turns the trigger geometry into a plain-data plan of four inert marker parts — an **outer ring whose radius EQUALS the proximity range**, an inner accent ring, a faint translucent beam, and a slow-spinning core — each part + the plan pinning `navigated`/`performed`/`external`/`signed`/`published` false + `readOnly:true`/`actionable:false`; degrades to `ok:false` (`invalid-position`) on bad input, never throws. `describePortalMeshPlan` is a one-line audit string. The adapter `buildPortalMesh(scene,opts)` consumes an `ok` plan + scene to build emissive `MeshStandardMaterial` meshes EXACTLY ONCE behind a `_built` guard, mounted under a `gateway-portal` group at the plan anchor; `tickPortalMesh(dt)` mutates ONLY scalars (`rotation.y`, `emissiveIntensity` — NO `Vector3`/`Matrix4`/geometry/material per frame); `disposePortalMesh()` frees GPU resources + resets the guard; `portalMeshRenderState()` mirrors `{rendered,count,ok,badge,reasons,anchor,ringRadius}`. WIRED in `main.js` composition root ONLY: `buildPortalMesh(scene,{position:_portalTrigger.portalPos(),range:_portalTrigger.range(),title})` after the trigger is built + `tickPortalMesh(dt)` in `update()`. DISPLAY-ONLY + INERT — no collider/raycast/click/input, no navigation/relay/sign/publish; the safety model is unchanged (proximity ARMS, KeyF CONFIRMS, same-origin `/zone/` only). SDK `portalMeshPlan` (experimental); read-only `ToriiDebug.shells.portalMeshPlan(opts?)` (plan report) + `ToriiDebug.shells.portalMesh()` (render state); `tests/portal-mesh-plan.test.js` (18 tests). **v0.2.184-alpha (LEAN-2 / GATEWAY-ZONE-LABEL):** `engine/gateway/zoneLabel.js` (PURE/node-safe) adds portal/zone state CLARITY label helpers so the player can tell WHERE the portal goes and WHAT they entered, without touching the navigation-safety model. `ZONE_LABEL_VERSION`=1/`ZONE_LABEL_BADGE` ('ZONE LABEL · DISPLAY-ONLY · INERT')/`DEFAULT_PORTAL_KEY` ('F')/`DEFAULT_ENTERED_PREFIX` ('Entered')/`DEMO_ZONE_LABEL_OPTS` (`{slug:'plebeian-market-bazaar',key:'F'}`). `portalPromptLabel({slug,route,title,key})` builds a target-aware proximity prompt ("Press F to travel to Plebeian Market Bazaar") and falls back to the generic "Press F to travel" when no target is known; `enteredZoneLabel(input,{prefix})` builds the concise post-hop notice ("Entered: Plebeian Market Bazaar"), returning `''` for an unknown/empty target. Both DERIVE their human text from the safe slug via the v0.2.182 `humanizeZoneSlug` (alnum by construction); the internal `_titleFrom` strips the `ZONE_ROUTE_PREFIX` and, for any non-slug/free-form/hostile string, runs an allowlist sanitiser `_safeTitle` (`[A-Za-z0-9 -]` only, collapse whitespace, cap 80) so NO markup/dangerous token can survive even though the HUD sink is `textContent`. WIRED in `main.js` composition root ONLY: the `createPortalTrigger` `promptText` is now `portalPromptLabel({slug:'plebeian-market-bazaar'})`, and the KeyF handler shows `showZoneNotice(enteredZoneLabel(rep.zoneId))` ONLY when the v0.2.180 `confirm()` report returns `navigated:true` with a string `zoneId` (a pushState hop does NOT fire popstate, so the existing `_applyZoneRoute()` never refreshed the notice). DISPLAY-ONLY + INERT — no network/relay/sign/publish/external nav/payments/auto-update/confirmation bypass; proximity still ARMS, KeyF still CONFIRMS, route stays same-origin `/zone/` only. SDK `zoneLabel` (experimental); read-only `ToriiDebug.shells.zoneLabel(opts?)` (label preview + a `safe` flag proving hostile input is stripped, via `zoneLabelReport` in `shellReport.js`); `tests/zone-label.test.js` (15 tests). |
| Update check (torii.quest) | `src/engine/update/updateCheck.js`, `src/engine/update/updatePreview.js`, `src/engine/update/githubReleaseSource.js`, `src/engine/update/updateStatus.js`, `UPDATE_CHECK.md` | **v0.2.158-alpha (LEAN-5 in-game update-status panel).** `engine/update/updateStatus.js` — PURE node-safe in-game UPDATE-STATUS panel that folds the v0.2.157 release source + the v0.2.142 inert preview into ONE render-ready, display-only view. `updateStatusPanel(payload=SAMPLE_RELEASE_FEED,{currentVersion=VERSION,notesMax=120,includePrerelease,includeDraft})` `selectLatestRelease`s the newest eligible release from a single object / `releases` array / manifest (defaults to a DETERMINISTIC LOCAL `SAMPLE_RELEASE_FEED` of two `-alpha` prereleases — never reaches the wire), reuses `updatePreviewBlock` for the verdict/presentation, and returns `{title:'UPDATE STATUS',badge:'STATUS · MANUAL · NO AUTO-UPDATE'(`UPDATE_STATUS_BADGE`),surface:'update-prompt-board'(`UPDATE_SURFACE_ID`),step:'UPDATE',status,statusLabel,currentVersion,latestVersion,updateAvailable,prompt,notesPreview,source:{status,kind,candidates,errors},sourceUrl,lines:[Version/Latest/Status/Source/Releases],readOnly:true,actionable:false}`. Reflects the verdict (update-available/up-to-date/unknown) AND source diagnostics; draft/empty/malformed degrade to UNKNOWN without throwing; exposes NO fetch/install/update/navigate/href/onClick/autoUpdate/sign/publish key. SDK `updateStatus` (experimental) + read-only `ToriiDebug.shells.updateStatus()` (new `updateStatusReport` in `shellReport.js`, also in `buildShellReport`; the 4-surface `shellsSummary` list is unchanged). **NO auto-fetch/auto-update/install/shell/file-mutation/navigation/relay.** `tests/update-status.test.js`. **v0.2.157-alpha (LEAN-5 source adapter).** `engine/update/githubReleaseSource.js` — PURE node-safe GitHub Releases source adapter that prepares the read-only update-check path. `normalizeRelease(raw)` maps a GitHub release object OR a simple manifest (`version|tag`/`url`/`notes`) into the `parseRelease()` shape (null for non-objects/no-tag, strict-boolean draft/prerelease). `selectLatestRelease(payload,{includePrerelease=true,includeDraft=false})` accepts a single `releases/latest` object, a `releases` array, or a manifest → `{status,kind,release,candidates,errors}` (`SOURCE_KIND` latest/list/unknown, `SOURCE_STATUS` ok/empty/malformed), picking the highest-version ELIGIBLE release via `compareVersions`. `evaluateFromSource(payload,opts)` folds that into `evaluateUpdate()` → `{source,...verdict}`, degrading draft/empty/malformed to UNKNOWN. The OPTIONAL `fetchLatestRelease({fetcher,url,timeoutMs,signal,init,...})` is host-only: REQUIRES an injected `fetcher` (no global-fetch fallback), NEVER auto-invoked, honours timeout WITHOUT setTimeout (caller signal or `AbortSignal.timeout()`), JSON/shape-validates, captures thrown/HTTP errors as MALFORMED. **NO auto-update/install/shell/file-mutation/navigation/relay.** SDK `githubReleaseSource` (experimental); `tests/github-release-source.test.js`. **v0.2.138 (LEAN-5).** Pure, node-safe torii.quest GitHub release/update-check architecture. `compareVersions(a,b)` — tolerant semver compare (optional leading `v`, dotted core, single dotted prerelease tag; a prerelease ranks below the same full release). `parseRelease(raw)` — normalises a GitHub-release-shaped object (`tag_name`/`name`/`html_url`/`body`/`draft`/`prerelease`/`published_at`) into `{ok,tag,version,name,url,notes,draft,prerelease,publishedAt,errors}`; never throws. `evaluateUpdate(release, currentVersion=VERSION)→{status,currentVersion,latestVersion,updateAvailable,release}` (compares runtime `VERSION` to the release; draft/unparseable → `UNKNOWN`). `updateCheckView(release,{currentVersion,notesMax})` → an INERT render-ready view-model `{status,currentVersion,latestVersion,updateAvailable,prompt,notesPreview,releaseUrl,releasesPageUrl,actionable:false}`. `RELEASE_SOURCE` documents WHERE the data would be fetched; `UPDATE_STATUS` enum. **NO network fetch, NO auto-update, NO install** — the releases-endpoint fetch + the in-world prompt mesh are deferred host steps. SDK `updateCheck` (experimental). Locked by `tests/update-check.test.js`. See `UPDATE_CHECK.md`. **v0.2.142 (LEAN-5 visible).** `engine/update/updatePreview.js` — pure visible-but-inert torii.quest update-check PREVIEW block over `updateCheckView`. `updatePreviewBlock(release,{currentVersion,notesMax})` flattens the view-model into a render-ready block of `{label,value}` rows (Version/Latest/Status/Source/Notes) + `statusLabel` helper + frozen `STATUS_TEXT` + `UPDATE_PREVIEW_BADGE` ("PREVIEW · MANUAL · NO AUTO-UPDATE"); every block is `actionable:false`/`readOnly:true`; the GitHub releases-page URL is surfaced as display-only TEXT (no link); draft/unparseable releases degrade to UNKNOWN (no throw). **NO network fetch, NO install, NO shell execution, NO auto-update, NO navigation.** `main.js` renders it into the title-screen `#update-preview` card via `textContent` only from a DETERMINISTIC LOCAL SAMPLE release; read-only at `ToriiDebug.shells.updatePreview()`. SDK `updatePreview` (experimental); `tests/update-preview.test.js`. **v0.2.196-alpha (UPDATE-FLOW-SMOKE).** `engine/update/updateFlowSmoke.js` — PURE node-safe UPDATE-FLOW SMOKE HARNESS that proves the torii.quest / VPS self-update contracts hold WITHOUT touching the network/shell/package-manager/DOM (no THREE/Rapier/window/fs/child_process/network; never throws). `UPDATE_SMOKE_VERSION`=1; `UPDATE_SMOKE_BADGE`='UPDATE FLOW SMOKE · READ-ONLY · NO AUTO-UPDATE'; `UPDATE_ACTION`='update:apply'; frozen LOCAL fixtures `SAMPLE_NEWER_FEED` (2-entry releases array, newest `v0.2.999-alpha`), `SAMPLE_CURRENT_RELEASE` (tag = `VERSION` → up-to-date), `MALFORMED_PAYLOADS` (null / 42 / `'not-a-release'` / `{}` / draft / empty list). `runUpdateFlowSmoke(opts?)` composes the shipped pure helpers (`selectLatestRelease`/`evaluateFromSource`/`evaluateUpdate`/`updateCheckView`/`updateStatusPanel`/`buildReleaseMeta`/`validateReleaseMeta`/`evaluateConsent`) into TEN read-only signals — current version read; release metadata shape parses; a strictly-newer release → update-available; a same/older release → up-to-date; every malformed payload degrades to UNKNOWN without throwing; the status panel/view are manual-only (`readOnly:true`/`actionable:false`/`MANUAL` badge); the release-metadata safety floor REJECTS a tampered `autoUpdate:true`; none of the read-only outputs expose a `fetch`/`install`/`update`/`apply`/`exec`/`spawn`/`run`/`download`/`write`/`navigate`/`sign`/`publish`/`deploy` CALLABLE; apply-update is confirmation-gated (`evaluateConsent('update:apply',…)` — no grant → blocked/`CONSENT_REQUIRED`, a grant → allowed/`CONSENT_GRANTED` but STILL `performed:false`); no auto action (every report pins `performed/actionable/autoUpdate/installed/executed/fetched/network/signed/published/navigated=false`, read path synchronous/not a thenable) — folding into `{version,badge,ok,signals,summary,safety,reasons,rendered:false,actionable:false}` (a broken injected fixture degrades to `ok:false` with concrete `reasons`, never throws). `formatUpdateFlowSmoke(result)` renders one stable text block. The single network entry in the update layer (`githubReleaseSource.fetchLatestRelease`) is host-only + refuses without an injected fetcher; this harness never invokes it. Composes ONLY the already-shipped pure helpers — surfaces NO fetch/install/update/apply/exec/spawn/download/navigate method of its own. SDK `updateFlowSmoke` (experimental) + read-only `ToriiDebug.shells.updateFlowSmoke(opts?)` (new `updateFlowSmokeReport` in `shellReport.js`, added to `buildShellReport`). `tests/update-flow-smoke.test.js` (+17). NOT an updater — performs no real update; pure local/non-network coverage, no runtime behaviour change. |
| MVP loop header | `src/engine/mvpLoop.js` | **v0.2.143.** Pure node-safe header that frames the four title-screen preview cards as ONE proof-of-concept loop. `mvpLoopSummary({currentVersion})` → inert `{title:'TORII QUEST · MVP LOOP', badge, flow:'Travel → Market → Score → Update', note, version, steps, lines, readOnly:true, actionable:false}`; `MVP_LOOP_STEPS` (frozen, ordered TRAVEL/MARKET/SCORE/UPDATE ↔ Gateway/Product/Leaderboard/Update check, LEAN-2..5), `MVP_LOOP_FLOW`, `MVP_LOOP_BADGE` ("PREVIEW · READ ONLY · MANUAL"). `main.js` `renderMvpLoop()` writes flow + note into the `#mvp-loop` card via `textContent` only; each card title carries its step (`1 · TRAVEL`…`4 · UPDATE`). **No network, no actions, no navigation.** Read-only at `ToriiDebug.shells.mvpLoop()`. SDK `mvpLoop` (experimental); `tests/mvp-loop.test.js`. |
| Project oversight dashboard | `src/engine/dashboard/continuumData.js`, `tools/build-continuum.mjs`, `tools/continuumParse.mjs`, `public/continuum.html`, `public/continuum-data.json` | **v0.2.171 (PROGRESS-1 / oversight).** PURE node-safe Torii Continuum project-oversight dashboard — the FIRST slice of a broader oversight surface. `continuumData.js` (no THREE/Rapier/DOM/fs/network; renders to a STRING) holds the curated `progress.md` snapshot in one frozen `CONTINUUM` object (`metrics`, `contributors` SEED metric, `tracks`, `leanRoute`, `activeNow`/`next12`/`archive`/`completed24h`, `risks`, `sourceOfTruth`) + `CONTINUUM_VERSION`/`CONTINUUM_BADGE`. Pure helpers: `escapeHtml` (every value escaped), `clampPct` (0..100 or null), `barCells`, `ringDash` (SVG dasharray), `computeTotals(data)` (tasksAhead/activeTasks/completedLast24h/archivedClusters/trackCount/milestoneCount/milestonesAchieved/milestonesInProgress/milestonesAchievedPct/pocProgressPct/buildProgressPct), `buildContinuumModel()`, `continuumDataJSON(model)` (the packaged snapshot), `renderContinuumPage(model)` → a self-contained dark-cyberpunk static HTML doc (CSS bars + 3 SVG donut rings + totals strip + Now/Next/Later + next-12 + struck completed-24h + archive + risks + source-of-truth footer). Renders fully WITHOUT JS; a tiny SAME-ORIGIN-only refresh script re-reads `./continuum-data.json` (no external URL/eval/timers, silent catch). `tools/build-continuum.mjs` (wired into `npm run build` via `build:continuum`) stamps `generatedAt` and writes `public/continuum.html` + `public/continuum-data.json`, so each deploy/refresh shows the latest PACKAGED state. Safe relative title-screen link (`index.html` `#continuum-link` → `./continuum.html`). READ-ONLY: no live writes/auth/signing/relay-publish/admin actions/navigation. SDK `continuum` (experimental); `tests/continuum-dashboard.test.js`. **v0.2.172 (CSP hardening):** the renderer now bakes a strict `Content-Security-Policy` `<meta>` into the generated page via `CONTINUUM_CSP` — `script-src 'self'` + `CONTINUUM_SCRIPT_SHA256` (the sha256 of the single packaged refresh script, exported as `CONTINUUM_REFRESH_SCRIPT`; NO `'unsafe-inline'` for script, closing the inline-script WARN), `style-src 'self' 'unsafe-inline'` (static `<style>` + data-driven `width:N%` track bars — inline style attributes can't be element-hashed, styles can't execute), `connect-src 'self'` (same-origin `continuum-data.json` refresh only), `default-src 'self'` with `object-src`/`base-uri`/`form-action`/`frame-ancestors` → `'none'`. The module stays crypto-free (browser-bundle-safe like `index.html`'s hash); `tests/continuum-dashboard.test.js` recomputes the hash with `node:crypto` so the shipped script and the CSP hash can never drift. Page stays fully static/read-only. **v0.2.174 (dashboard data automation):** new PURE node-safe build-time doc parser `tools/continuumParse.mjs` (`stripInlineMd`/`cleanBullet`/`sectionLines`/`parseNumberedList`/`parseStruckBullets`/`parseBullets`/`countStruck`/`deriveContinuumData`/`summariseTaskTotals`) parses `progress.md` + `todo.md` and DERIVES the dashboard's four list sections (next-12, active-now, completed-24h, archive) + a docs-derived task-count metric (todo `~~struck~~` markers + per-section counts). `continuumData.js` `buildContinuumModel(overrides)` gained a merge seam (`{taskTotals,derived,...dataOverrides}`) that REPLACES the curated arrays with derived ones and recomputes `computeTotals`; `continuumDataJSON`/`renderContinuumPage` carry an optional `taskTotals` + `derived` provenance (DERIVED at-a-glance row + sources/parsed/gap-count footer) — both empty when null so a no-override render is byte-identical to v0.2.173. `tools/build-continuum.mjs` reads both docs via `readSafe` (a missing/garbled section degrades to '' → curated default, reported as a parser `gap`, never failing the build) and merges the derived overrides. **CSP unchanged** — `CONTINUUM_REFRESH_SCRIPT` untouched + no new script-driven `data-k` keys, so the v0.2.172 hash still matches; milestone/% stay explicit curated SEED metrics; the parser is build-time ONLY (never imported by the bundled module, keeping it fs-free). `tests/continuum-parse.test.js` (+15); added to the `test:foundation` profile. **v0.2.175 (engineering health metrics):** `continuumData.js` gained a PURE browser-safe `buildHealthModel(input)` + a frozen `HEALTH_LASTKNOWN` baseline; it runs at module load (curated `CONTINUUM.health` fallback) AND in `tools/build-continuum.mjs` with freshly GENERATED inputs (profile/test-file counts via `tools/testProfiles.mjs` + a `countTestFiles()` readdir, parser-gap count, `config.js` VERSION, `docsInSync` = progress.md+todo.md both carry VERSION). Each metric carries `kind: 'generated' | 'last-known'` rendered as a provenance chip (`.hk-gen`/`.hk-lk`); the new **Engineering health** `<section>` (cards grid + 3 SVG rings + efficiency-loop note: measure · profile · standardise · automate · modularise · document) is server-rendered ESCAPED text with NO new `<script>`, so the v0.2.172 CSP hash is unchanged. GENERATED = profile sizes / parser gaps / version / doc-sync; LAST-KNOWN = total tests / profile timings / bundle baseline / last-green release gate (captured from the most recent green `test:release`). `health` flows as a normal dataOverride through `buildContinuumModel`; `continuumDataJSON` carries `health`. `tests/continuum-dashboard.test.js` (+9). **v0.2.176 (milestone + layout pass):** `continuumData.js` gained a PURE browser-safe `buildMilestoneModel(input)` + a frozen `SEED_MILESTONES` list. The model folds `CONTINUUM.leanRoute` (the 15-hour MVP slices, which ARE the active milestone's tasks) into DERIVED `tasks` counts (`total`/`done`/`active`/`pending` from each slice's `state`), a `donePct` (done/total) and a directional `progressPct` (`_average` of per-slice `progress`, the same number as the PoC ring, labelled an estimate on the page), exposes bullet-ready `counts` strings, and lists the clearly-labelled `SEED_MILESTONES` so a "total milestones" figure is HONEST (1 active + N seed, never pretending seeds carry real task sets; future hook: derive seeds from `strategy.md`). `buildContinuumModel` attaches `milestones` (falling back to `buildMilestoneModel`); `continuumDataJSON` carries `milestones`. The renderer gained `_milestonesSection`/`_milestoneCard`/`_seedMilestoneCards` (an ACTIVE pill + directional % bar + SEED chips) inserted after At-a-glance, plus `_cardValueHtml(value)` which turns a ` · `-joined value of 2+ parts into a compact `<ul class="mini">` bullet list (user preference: bullet lists over dense comma/dot-separated prose) — adopted by the metric rows, health cards, and docs-derived row. All server-rendered ESCAPED text with NO new `<script>` and no new `data-k` key, so the v0.2.172 CSP hash is unchanged. Layout follow-up tracked as **DASHBOARD-LAYOUT-1**. `tests/continuum-dashboard.test.js` (+9). **v0.2.177 (DASHBOARD-LAYOUT-1 first pass):** a layout/hierarchy/responsive readability pass on the renderer — the ACTIVE-milestone headline is PROMOTED above At-a-glance (order: Active focus → Milestones → At a glance → Engineering health → Tracks → 15-hour route → Now/Next/Later → Next 12 → Risk); a new `_h2(title,count)` helper emits a `.h2row` heading row with an item-count `.count` chip, and every section gains a one-line `<div class="lead">` caption; the Now/Archive/Done columns moved into ONE `<div class="cols">` reflowing on a responsive auto-fit grid (`grid-template-columns:repeat(auto-fit,minmax(260px,1fr))`, no hard 3→1 break) each showing a live count; CSS tightened (wider 1080px `main`, larger inter-section margins, subtle `border-color` hover on cards/tracks/totals). All server-rendered ESCAPED text — NO new `<script>` and no new `data-k` key, so the v0.2.172 CSP hash is unchanged; DERIVED/GENERATED/LAST-KNOWN/SEED chips stay visible. A larger visual redesign remains a documented future follow-up. `tests/continuum-dashboard.test.js` (+6 layout/SAFETY). **v0.2.186 (deployment-readiness VISIBILITY):** `continuumData.js` gained a PURE browser-safe `buildReadinessModel(input)` + `READINESS_BADGE` (auto-exported under the SDK `continuum` namespace via `export *`) that folds the v0.2.185 read-only `checkZoneFallbackReadiness({docs,dist})` verdict (passed in as `input.zoneFallback` — the module itself does NO fs/network/THREE/DOM) into a render-ready `{badge,status,statusLabel,checks,errors,warnings,note}` model with honest states — `ready` (READY) / `docs-ready` (DOCS READY · BUILD CHECK PENDING) / `blocked` (NOT READY) / `unknown` (NOT CHECKED) — and a four-row per-check table (SPA `/zone/*` fallback documented, built dist route shape, host fallback configured = MANUAL, auto-update = MANUAL); it NEVER throws (no input → honest NOT-CHECKED) and each check's `state` reuses the existing pill vocabulary (`no-blocker`/`gated`/`manual`/`deferred`) so the new `_readinessSection(readiness)` renderer (a status pill + badge + escaped per-check table, placed after Engineering health) adds NO new CSS. `buildContinuumModel` attaches `readiness` (falling back to a curated NOT-CHECKED `CURATED_READINESS`); `continuumDataJSON` carries `readiness`. `tools/build-continuum.mjs` reads the `REQUIRED_FALLBACK_DOCS` + walks `dist/` at packaging time (absent → dist check SKIPPED; `build:continuum` runs before `vite build`, so regression-check [15] stays the authoritative dist check) and feeds the real verdict in. All server-rendered ESCAPED text with NO new `<script>` and no new `data-k` key, so the v0.2.172 refresh-script sha256 + CSP/XSS guard are unchanged. `tests/continuum-dashboard.test.js` (+8). **v0.2.188-alpha (SHIP-READINESS VISIBILITY):** `continuumData.js` gained a PURE browser-safe `buildShipModel(input={})` + the frozen `SHIP_BADGE` ('SHIP READINESS · LAST GATE · READ-ONLY')/`SHIP_STATUS_COMMAND` ('npm run release:status')/`SHIP_NEXT_SAFE_TASK` (frozen `{title,why,kind:'infra'}`)/`SHIP_LASTKNOWN` (frozen `{status:'ready',statusLabel:'READY',version,signals:[6]}`) — all auto-exported under the SDK `continuum` namespace via `export *`. `buildShipModel` folds the v0.2.187 read-only release-readiness verdict (passed in as `input.readiness` — the module itself does NO fs/git/network/THREE/DOM) into a render-ready ship model: when live signals exist it is `kind:'generated'`, else it degrades to the frozen `SHIP_LASTKNOWN` baseline as `kind:'last-known'`; accepts an `input.nextTask` override and NEVER throws. `_shipSignalRows(signals)` is the null-safe signal-row builder; `SHIP_SIGNAL_PILL` maps a signal status to the existing pill vocabulary (ok→`no-blocker`, blocked→`gated`, advisory→`manual`, skipped/unknown→`deferred`). The new `_shipSection(ship)` renderer (a status pill ready→`no-blocker`/not-ready|blocked→`gated`/incomplete→`manual`/else `deferred`, the `_healthChip(ship.kind)` provenance chip, a **Next safe task** `.focus` block, a six-row signal table, and blockers/unknowns/verdict `.focus` lines — all `escapeHtml`'d) is inserted after the Active-focus section, before Milestones, and adds NO new CSS. `buildContinuumModel` attaches `ship` (falling back to a curated `CURATED_SHIP = buildShipModel()`); `continuumDataJSON` carries `ship`. `tools/release-readiness.mjs` was refactored to EXPORT a reusable `gatherReleaseReadiness(root=process.cwd())` (all fs/git helpers became closures over `root`, behind a `realpathSync` run-guard so the `npm run release:status` CLI behaviour is unchanged); `tools/build-continuum.mjs` imports `buildShipModel` + `gatherReleaseReadiness` and feeds the SAME live verdict in at packaging time (try/catch fallback to the last-known baseline). All server-rendered ESCAPED text with NO new `<script>` and no new `data-k` key, so the v0.2.172 refresh-script sha256 + CSP/XSS guard are unchanged. `tests/continuum-dashboard.test.js` (+10). **v0.2.200-alpha (DASHBOARD-METRIC-FRESHNESS):** resolves the recurring stale LAST-KNOWN test-count drift flagged in security reviews — `HEALTH_LASTKNOWN.totalTests` was a SECOND hand-maintained copy stuck at `'1180 passing'` while the suite was 1246/78, so the engineering-health "Total tests" metric and the metrics "Tests" row had diverged. `continuumData.js` now exports ONE curated source of truth `CURRENT_TEST_STATUS` (`{passing:1246, files:78, fastProfile:5, foundationProfile:25}`, frozen) + a `testCountLabel(status?=CURRENT_TEST_STATUS)` helper (canonical `"<passing> passing / <files> files"`, null/partial-safe); `HEALTH_LASTKNOWN.totalTests` now DERIVES from `testCountLabel()` and the metrics "Tests" row renders `testCountLabel()` + the profile counts, so the two displayed surfaces can no longer drift apart. A new `tests/continuum-dashboard.test.js` block ("test-count freshness — single source of truth", +5) asserts the source is frozen + well-shaped, the curated `files` equals the REAL on-disk `*.test.js` count via `readdirSync(tests/)` (forces a bump when files change), `testCountLabel()` is correct + degrades safely, BOTH surfaces derive from the single source and `totalTests` no longer matches `/1180/`, and `mvpReadiness.DEFAULT_TEST_STATUS` (1246/78) agrees with `CURRENT_TEST_STATUS` (cross-capture, no cross-import). DASHBOARD/STATUS-DATA-ONLY: all server-rendered ESCAPED text, NO new `<script>`/`data-k` key → the v0.2.172 refresh-script sha256 + CSP/XSS guard unchanged; no runtime behaviour change. **v0.2.208-alpha (PROGRESS-PARSER-GAP-CLEANUP):** the build logs flagged `activeNow`/`completed24h` parser gaps (`no usable items parsed from progress.md`), forcing the dashboard to keep curated defaults. Root cause was the per-section bounds in `continuumParse.mjs` `deriveContinuumData()` via `tryList(key, items, {min,max})`, NOT the parsing logic: progress.md's `## Active now` / `## Completed last 24h` are running logs (each shipped slice prepends an entry) that had outgrown the original v0.2.174 ceilings — activeNow 34 top-level bullets vs `max:16`, completed24h 26 struck bullets vs `max:24` — so both were dropped as `gaps`. The running-log format is intended, so the bounds were RAISED to fit it (activeNow/completed24h `max:60`, archive `max:40`, next12 `max:24`) while KEEPING the protective guard (a `>60`-item garbled section still degrades to the curated default). After the fix `build:continuum` derives all four lists from progress.md (`next12 (12), activeNow (34), completed24h (26), archive (11)`; gaps: none). `tests/continuum-parse.test.js` (+2; 15→17): a 34/26 long-but-bounded parse with no gap, and an absurd 61-item list that still falls back. Pure build-time parser change only — no parser CALL site, schema, dashboard render path, or CSP change; `godMode` stays false; no new `setTimeout`/`Vector3`/`Matrix4`. **v0.2.214-alpha (CONTINUUM-RC-STATUS-CARD):** `continuumData.js` gained a PURE browser-safe `buildRcStatusModel(input={})` + the frozen `RCSTATUS_BADGE` ('RC / RELEASE MANIFEST · LOCAL · READ-ONLY')/`RCSTATUS_LASTKNOWN` (auto-exported under the SDK `continuum` namespace via `export *`) that surfaces the LOCAL release-candidate artifact posture as a read-only **RC / release manifest** section just below Ship readiness. It folds plain data — `{version, testLabel, profileSummary, manifest:{status,requiredPresent,required,optionalPresent,optional}, rcDocs:{present,total}, manualValidationRemaining, gateStatusLabel}` — into a render-ready `{badge,kind,band,statusLabel,pill,version,manifestStatus,manifest*Present,rcDocs*,testLabel,profileSummary,manualValidationRemaining,gateStatusLabel,metrics:[7 cards],note}`; with no input it degrades to the frozen `RCSTATUS_LASTKNOWN` (`kind:'last-known'`) and NEVER throws. Band: any required artifact/RC doc missing → `artifacts-incomplete`/`gated`/'ARTIFACTS INCOMPLETE'; complete + `/^READY/` gate → `gates-green`/`manual`/'LOCAL GATES GREEN · MANUAL VALIDATION + APPROVAL PENDING'; else `near`/`manual`/'NEAR · LOCAL GATES'. The module does NO fs/git/network/crypto/THREE/DOM — `tools/build-continuum.mjs` DERIVES the live inputs by stat-ing the frozen `RELEASE_MANIFEST_REQUIRED`/`RELEASE_MANIFEST_OPTIONAL` (`tools/releaseManifest.mjs`) + `RC_SNAPSHOT_DOC_REFS`/`RC_SNAPSHOT_MANUAL_VALIDATION` (`tools/rcSnapshot.mjs`) on disk (cheap `existsSync` only), reuses the curated `CURRENT_TEST_STATUS`/`testCountLabel()` + the already-gathered `ship.statusLabel`, and folds the result via `buildContinuumModel({rcStatus})` (try/catch → curated `CURATED_RCSTATUS` fallback). The new `_rcStatusSection(rc)` renderer (status pill from the existing vocabulary + `_healthChip(kind)` + `_metricRows(rc.metrics)` + escaped note) is inserted after `_shipSection`; `continuumDataJSON` carries `rcStatus`. All server-rendered ESCAPED text with NO new `<script>`/`data-k` key, so the v0.2.172 refresh-script sha256 + CSP/XSS guard are unchanged. `tests/continuum-dashboard.test.js` (+7). DASHBOARD/DOCS/TOOLING-ONLY; no runtime/gameplay/physics/shooter/Rapier change; no Nostr signing/publishing/live network write; no network/deploy/publish/tag/release/self-update; `godMode` stays false; no new `setTimeout`/`Vector3`/`Matrix4`. **v0.2.215-alpha (CONTINUUM-MANUAL-VALIDATION-CARD):** `continuumData.js` gained a PURE browser-safe `buildManualValidationModel(input={})` + the frozen `MANUALVALIDATION_BADGE` ('MANUAL VALIDATION · MVP PLAYTEST · READ-ONLY')/`MANUALVALIDATION_LASTKNOWN` (auto-exported under the SDK `continuum` namespace via `export *`) that surfaces the LOCAL manual-validation / MVP-playtest readiness posture as a read-only **Manual validation** section just below the RC / release-manifest card. It CLEARLY SEPARATES what is no-blocker (the local automated gates, green) from what still needs manual input (the live-browser MVP playtest + explicit user approval), folding plain data — `{sections, items, blocker, major, minor, validationAreas, checklistDocPresent, resultsTemplatePresent, gateStatusLabel, areas}` — into a render-ready `{badge,kind,band,statusLabel,pill,sections,items,blocker,major,minor,validationAreas,checklistDocPresent,resultsTemplatePresent,gateStatusLabel,areas,metrics:[6 cards],note}`; with no input it degrades to the frozen `MANUALVALIDATION_LASTKNOWN` (`kind:'last-known'`) and NEVER throws. Band: checklist/results-template doc missing → `docs-incomplete`/`gated`/'PLAYTEST DOCS INCOMPLETE'; docs present + `/^READY/` gate → `gates-green`/`manual`/'LOCAL GATES GREEN · MANUAL PLAYTEST + APPROVAL PENDING'; else `manual-outstanding`/`manual`/'MANUAL VALIDATION OUTSTANDING'. The module does NO fs/git/network/crypto/THREE/DOM — `tools/build-continuum.mjs` DERIVES the live inputs from `PLAYTEST_CHECKLIST_SECTIONS`/`playtestItemCount()`/`PLAYTEST_SEVERITIES` (`tools/playtestChecklist.mjs`) + `existsSync` of `MVP_PLAYTEST_CHECKLIST.md` / `MVP_PLAYTEST_RESULTS_TEMPLATE.md` + the highest-level `RC_SNAPSHOT_MANUAL_VALIDATION` areas + the already-gathered `ship.statusLabel`, and folds the result via `buildContinuumModel({manualValidation})` (try/catch → curated `CURATED_MANUALVALIDATION` fallback). The new `_manualValidationSection(mv)` renderer (status pill from the existing vocabulary + `_healthChip(kind)` + `_metricRows(mv.metrics)` + escaped note) is inserted after `_rcStatusSection`; `continuumDataJSON` carries `manualValidation`. All server-rendered ESCAPED text with NO new `<script>`/`data-k` key, so the v0.2.172 refresh-script sha256 + CSP/XSS guard are unchanged. `tests/continuum-dashboard.test.js` (+8). DASHBOARD/DOCS/TOOLING-ONLY; no runtime/gameplay/physics/shooter/Rapier change; no Nostr signing/publishing/live network write; no network/deploy/publish/tag/release/self-update; `godMode` stays false; no new `setTimeout`/`Vector3`/`Matrix4`. **v0.2.216-alpha (CONTINUUM-NO-BLOCKER-QUEUE-CARD):** `continuumData.js` gained a PURE browser-safe `buildNoBlockerQueueModel(input={})` + the frozen `NOBLOCKERQUEUE_BADGE` ('NO-BLOCKER QUEUE · SAFE NEXT WORK · READ-ONLY')/`NOBLOCKERQUEUE_LASTKNOWN` (auto-exported under the SDK `continuum` namespace via `export *`) that surfaces a read-only **No-blocker queue** section just below the manual-validation card answering, at a glance, WHAT AN AI AGENT CAN PICK UP NEXT WITHOUT USER INPUT — the next safe no-runtime-risk slice (`SHIP_NEXT_SAFE_TASK`: title/why/kind) plus the active/next/archive queue COUNTS — kept SEPARATE from the one item parked on the human (the live-browser MVP playtest + explicit approval). It folds plain data — `{nextSafeTitle, nextSafeWhy, nextSafeKind, activeNow, nextUp, archiveClusters, completed24h, todoCompletedMarkers, manualPending}` — into a render-ready `{badge,kind,band,statusLabel,pill,nextSafeTitle,nextSafeWhy,nextSafeKind,activeNow,nextUp,archiveClusters,completed24h,todoCompletedMarkers,manualPending,metrics:[6 cards],note}`; with no input it degrades to the frozen `NOBLOCKERQUEUE_LASTKNOWN` (`kind:'last-known'`) and NEVER throws. Band: `manualPending` → `safe-available`/`no-blocker`/'NO-BLOCKER WORK AVAILABLE · MANUAL PLAYTEST AWAITS USER'; else `safe-available-clear`/`no-blocker`/'NO-BLOCKER WORK AVAILABLE'. The module does NO fs/git/network/crypto/THREE/DOM — `tools/build-continuum.mjs` DERIVES the counts from the SAME `taskTotals` already parsed off todo.md/progress.md by `tools/continuumParse.mjs` (`activeNow`/`next12`/`archiveClusters`/`completed24h`/`todoCompletedMarkers` — no second source of truth) + `SHIP_NEXT_SAFE_TASK` + a `manualPending` flag read off `manualValidation.pill !== 'no-blocker'`, and folds the result via `buildContinuumModel({noBlockerQueue})` (try/catch → curated `CURATED_NOBLOCKERQUEUE` fallback). The new `_noBlockerQueueSection(nb)` renderer (status pill from the existing vocabulary + `_healthChip(kind)` + `_metricRows(nb.metrics)` + escaped note) is inserted after `_manualValidationSection`; `continuumDataJSON` carries `noBlockerQueue`. All server-rendered ESCAPED text with NO new `<script>`/`data-k` key, so the v0.2.172 refresh-script sha256 + CSP/XSS guard are unchanged. `tests/continuum-dashboard.test.js` (+8; 1396→1404). DASHBOARD/DOCS/TOOLING-ONLY; no runtime/gameplay/physics/shooter/Rapier change; no Nostr signing/publishing/live network write; no network/deploy/publish/tag/release/self-update; `godMode` stays false; no new `setTimeout`/`Vector3`/`Matrix4`. |
| Test profiles | `tools/testProfiles.mjs`, `tools/test-profile.mjs`, `package.json` (`test:fast`/`test:foundation`/`test:release`) | **v0.2.173 (TESTPROF-1).** Faster inner-loop test profiles that DO NOT weaken the release gate. `tools/testProfiles.mjs` is PURE/node-safe (no fs/network/child_process/THREE/DOM): a frozen `PROFILES` registry — `fast` (5 core files: state/events/classifier/aim/snapshot) and `foundation` (16 files: fast + combat-damage/raycast-service/player-boundary/bot-agent + sdk/registry/component + consent-gate + the doc-consistency/handoff-status/bundle-sizes guard suites) — plus `PROFILE_NAMES`, `isKnownProfile`, `profileBasenames`, `profileFiles` (→ `tests/<file>` vitest args), `validateProfiles(existing)` (every listed test must exist on disk AND `fast ⊆ foundation`), `formatProfileLine`, `formatTiming`. `release` is deliberately NOT a file list — `npm run test:release` = `npm run build && vitest run && npm run check && npm run bundle:report && npm run handoff:status` (the FULL suite + all guardrails, unchanged release safety). The thin CLI `tools/test-profile.mjs` resolves a profile, validates against the real `tests/` dir (a renamed/deleted test fails loudly instead of silently shrinking a profile), spawns `npx vitest run <files>`, and prints a timing footer + a "run test:release before deploy/publish" reminder; `--list` prints the resolved files without running. **Agent workflow: iterate on `test:fast`/`test:foundation`; every deploy/publish/push still requires `test:release` or full parent verification.** Build/dev tooling only — never imported by the game. `tests/test-profiles.test.js`. |
| SDK entrypoint | `src/sdk/index.js` | **v0.2.131 (ARS-5).** The single public import point for external contributors / future CMP marketplace components. Curated namespace re-exports of the node-safe engine leaf modules only (combat `aim`/`classifier`/`damage`, physics `interactions` + `createRaycastService`/`raycastService`, weapons `muzzle`/`reloadPose`, `botAgent`, debug `snapshot`, ui `phaseScreens`) — never pulls `scene.js`/WebGLRenderer, so it imports clean in node. Metadata: `SDK_VERSION` (tracks config VERSION), a `STABILITY` tier enum (`stable`/`experimental`/`internal`), a frozen `SDK_SURFACE` map tagging each surface with a tier + module path (internals forward-declared with `module:null`), `STABILITY_TIERS` set, and `surfacesByTier(tier)`. No runtime wiring. Locked by `tests/sdk.test.js`. **v0.2.132:** added the `component` namespace (re-export of `engine/components/contract.js`) at the `experimental` tier (CMP-1/2). **v0.2.133:** added the `toriiGateway` namespace (re-export of `engine/components/toriiGateway.js`, the reference component) at the `experimental` tier (CMP-8). **v0.2.134:** added the `productDisplay` (read-only product component, CMP-13), `travelIntent` (gateway-protocol URL handoff helpers, GWPROTO-1), and `leaderboard` (Nostr score-event helpers, LB-1) namespaces — all `experimental` tier. **v0.2.135:** added `registry` (component loader/registry, CMP-7), `gatewayHandoff` (gateway portal/handoff shell, CMP-8), `productPanel` (product panel view-model, CMP-13), and `leaderboardPublisher` (leaderboard publisher adapter, LB-1) namespaces — all `experimental` tier. **v0.2.136:** added `gatewayPortal` (gateway portal VIEW shell, CMP-8), `productPanelShell` (product panel RENDER shell, CMP-13), and `leaderboardView` (read-only leaderboard display + build-only preview, LB-1) namespaces — all `experimental` tier. **v0.2.167:** added the `handoffPlan` namespace (host travel handoff seam, LEAN-2) at the `experimental` tier. **v0.2.168:** added the `handoffExecute` namespace (first controlled same-origin travel executor, LEAN-2) at the `experimental` tier. **v0.2.170:** added the `hostTransport` namespace (same-origin host transport adapter the executor drives, LEAN-2) at the `experimental` tier. **v0.2.171:** added the `continuum` namespace (Torii Continuum project-oversight dashboard data + pure renderer, PROGRESS-1) at the `experimental` tier. **v0.2.178:** added the `gatewayActivation` namespace (LIVE-WIRE confirmed same-origin handoff activation seam, LEAN-2) at the `experimental` tier. **v0.2.180:** added the `gatewayPortalActivation` namespace (pure portal-boundary seam bridging an in-world gateway component to the confirmed same-origin hop, LEAN-2) at the `experimental` tier. **v0.2.181:** added the `portalTrigger` namespace (pure proximity→confirm controller wiring an in-world portal position to the portal boundary, LEAN-2) at the `experimental` tier. **v0.2.182:** added the `zoneRoute` namespace (pure SPA `/zone/<slug>` route parser, LEAN-2) at the `experimental` tier. **v0.2.183:** added the `portalMeshPlan` namespace (pure in-world portal-marker render plan, LEAN-2) at the `experimental` tier. **v0.2.184:** added the `zoneLabel` namespace (pure portal/zone state-clarity label helpers, LEAN-2) at the `experimental` tier. |
| Component economy | `src/engine/components/contract.js`, `src/engine/components/registry.js`, `src/engine/components/toriiGateway.js`, `src/engine/components/productDisplay.js`, `src/engine/components/productPanel.js`, `src/engine/components/productPanelShell.js`, `src/engine/components/productPreview.js`, `src/engine/gateway/travelIntent.js`, `src/engine/gateway/gatewayHandoff.js`, `src/engine/gateway/gatewayPortal.js`, `src/engine/nostr/leaderboard.js`, `src/engine/nostr/leaderboardPublisher.js`, `src/engine/nostr/leaderboardView.js`, `src/engine/nostr/leaderboardPreview.js`, `src/engine/nostr/relayRead.js`, `src/engine/nostr/leaderboardRelayRead.js`, `src/engine/nostr/profileRead.js`, `src/engine/nostr/readHealth.js`, `src/engine/consent/consentGate.js`, `src/engine/consent/consentView.js`, `src/engine/leaderboard/submitIntent.js`, `COMPONENTS.md`, `GATEWAY_PROTOCOL.md` | **v0.2.132 (CMP-1/2).** Pure, node-safe component contract — `COMPONENT_CONTRACT_VERSION`, `REQUIRED_MANIFEST_FIELDS` (id/name/version/author/mountTarget), `MOUNT_TARGETS` (scene/hud/panel/zone), `validateManifest(m)→{valid,errors}` (required fields + `author.npub` provenance + mountTarget membership + pricing free-or-positive-sats), `isComponent(obj)` (mount+unmount fn shape), and `defineComponent(def)` (throws on missing mount/unmount or invalid manifest; returns a component with idempotent `mount(scene,options={})`/`unmount()` + a `mounted` flag). No THREE/Rapier/DOM. Surfaced via the SDK `component` namespace. `COMPONENTS.md` is the full manifest spec (provenance, bundle hash, capabilities, deps, assets, config→mount options, sats pricing + zap splits, Nostr listing events, host security/verification rules). Signature/hash/capability ENFORCEMENT is later CMP work. Locked by `tests/component.test.js`. **v0.2.133 (CMP-8).** First reference component — `engine/components/toriiGateway.js`: `createToriiGateway(config)` / default `toriiGateway` / `GATEWAY_VERSION`, built on `defineComponent`. Pure/node-safe SKELETON: the manifest carries `kind:'gateway'`, `mountTarget:'scene'`, provenance `author.npub`, and a `gateway:{npub,relay,target,position}` destination block; mount/unmount are symmetric no-ops (portal mesh + Nostr npub/relay handoff are documented TODOs, not built). Surfaced via the SDK `toriiGateway` namespace (experimental). Locked by `tests/torii-gateway.test.js`. **v0.2.134 lean-MVP batch.** (CMP-13) `engine/components/productDisplay.js` — read-only product display reference component (`createProductDisplay`/`productDisplay`/`validateProduct`/`PRODUCT_DISPLAY_VERSION`); manifest `kind:'product'`, `mountTarget:'panel'`, product block `{title,image,sellerNpub,priceSats,url,reward}`; READ-ONLY (no checkout/pay/zap/publish — links OUT to Plebeian.Market via a validated `https://` URL); `validateProduct` rejects non-https/script/relative links and negative prices; skeleton no-op lifecycle; SDK `productDisplay` (experimental); `tests/product-display.test.js`. (GWPROTO-1) `GATEWAY_PROTOCOL.md` — the n2n spatial-hop protocol DRAFT (relay-first hybrid discovery, URL handoff MVP, world/zone/gateway identity, travel intent fields, return path, signed-event future, security tiers, NIP path; "component is code, protocol is agreement") + `engine/gateway/travelIntent.js` — pure URL-handoff helpers (`TRAVEL_FIELDS`, `buildTravelIntent`/`validateTravelIntent`/`buildTravelUrl`/`parseTravelUrl`/`looksLikeNpub`); no DOM/navigation/relay/signing; SDK `travelIntent` (experimental); `tests/travel-intent.test.js`. (LB-1) `engine/nostr/leaderboard.js` — pure Nostr leaderboard score-event helpers (`LEADERBOARD_KIND` 30000, `SCORE_FIELDS`, `buildScore`/`validateScore`/`buildScoreEventTemplate`); builds the UNSIGNED event template (kind + JSON content + indexable tags) for runId/score/kills/headshots/accuracy/version; no signing/relay/publish; SDK `leaderboard` (experimental); `tests/leaderboard.test.js`. **v0.2.135 loader + handoff batch.** (CMP-7) `engine/components/registry.js` — pure component loader/registry (`createRegistry`/`createBuiltinRegistry`/`builtinRegistry`); `register(factory)` probes a `(config)=>component` factory once (must be a contract component with a valid manifest, else throws; duplicate ids throw) and indexes it by `manifest.id`+`kind`; `has`/`ids`/`kinds`/`byKind`/`size` for discovery; `load(id, config)→{ok,component,manifest,errors}` builds a FRESH instance and re-validates manifest + contract version, degrading safely (never throws) on unknown/incompatible/invalid. **LOCAL built-ins ONLY — no eval, no dynamic import, no remote fetch** (the security boundary; remote-bundle signature/hash verification is later host-side CMP work). SDK `registry` (experimental); `tests/registry.test.js`. (CMP-8 cont.) `engine/gateway/gatewayHandoff.js` — bridges a gateway component to a travel intent: `gatewayDestination(component)` (the manifest's `gateway` block or null), `planGatewayTravel(component, context)→{valid,errors,intent}` (maps `gateway.target`→`to` (falls back to dest npub), `gateway.relay`→`relays[0]`, merges the traveller `context` `{from,player,spawn,return,zoneType,state}`, validates), `gatewayTravelUrl(...)→{valid,errors,url}` (serialises a valid plan via `buildTravelUrl`; '' on invalid). Pure return values — NO window.location/relay/signing. SDK `gatewayHandoff` (experimental); `tests/gateway-handoff.test.js`. (CMP-13 cont.) `engine/components/productPanel.js` — read-only product panel view-model (`productPanelViewModel(product)→{ok,errors,view}`, `priceLabel(priceSats)`); validates via `validateProduct` then returns a flat render-ready bag (`title`/`imageUrl`/`hasImage`/`priceLabel`/`seller`/`linkUrl`/`linkLabel`/`reward`/`hasReward`); no checkout/pay/zap surface; the in-world mesh binds over this. SDK `productPanel` (experimental); `tests/product-panel.test.js`. (LB-1 cont.) `engine/nostr/leaderboardPublisher.js` — publisher ADAPTER shape (`createLeaderboardPublisher({sign,publish})→{publishScore(stats)}`); INJECTED signer/publisher deps (sync or async), build-only with no signer (returns the unsigned template), signed-not-shipped with a signer only, published only with both; captures sign/publish failures in `{ok,errors}` without throwing (only an invalid score throws, before signing); NO key handling/relay/secrets in-module. SDK `leaderboardPublisher` (experimental); `tests/leaderboard-publisher.test.js`. **v0.2.136 visible-shells batch.** Turned the v0.2.135 infrastructure into pure, render-ready VIEW shells (no THREE/Rapier/DOM/scene; no side effects; all SEC gates intact). (CMP-8 cont.) `engine/gateway/gatewayPortal.js` — gateway portal VIEW shell (`gatewayPortalView(component, context, {base,prompt})→{status,isGateway,armed,destination,destinationLabel,relay,prompt,plan,urlPreview,errors}`, `destinationLabel`, `shortKey`, `PORTAL_PROMPT`); builds on `gatewayHandoff` (`planGatewayTravel`/`gatewayTravelUrl`); `armed = plan.valid`, `prompt`+`urlPreview` blank unless armed; DISPLAY-ONLY — never assigns `window.location`, never contacts a relay or signs. SDK `gatewayPortal` (experimental); `tests/gateway-portal.test.js`. (CMP-13 cont.) `engine/components/productPanelShell.js` — product panel RENDER shell (`productPanelShell(product)→{ok,errors,panel}`); builds on `productPanelViewModel`; `panel = {title,imageUrl,lines:[Price,Seller,(In-game reward)],footer:{kind:'link',actionable:false},actions:[],readOnly:true}`; invalid product degrades to `panel:null`; NO checkout/pay/zap/buy surface (`actions[]` always empty, footer non-actionable). SDK `productPanelShell` (experimental); `tests/product-panel-shell.test.js`. (LB-1 cont.) `engine/nostr/leaderboardView.js` — read-only leaderboard DISPLAY + build-only preview (`VIEW_MODES`=['mock','build'], `accuracyLabel`, `rankScores(statsList)→{rows,skipped}` deterministic desc sort + 1-based rank, `leaderboardView(statsList,{mode})` THROWS on any non-mock/build mode — no 'live'/relay path, `async leaderboardPreview(statsList)` runs through `createLeaderboardPublisher()` with NO signer/publisher → `signed:false`/`published:false`). SDK `leaderboardView` (experimental); `tests/leaderboard-view.test.js`. **v0.2.140 (LEAN-3).** `engine/components/productPreview.js` — pure visible-but-inert Plebeian/Nostr product/market PREVIEW block over `productPanelShell`. `productPreviewBlock(product,{urlMax})` flattens the panel shell into a render-ready block of `{label,value}` rows (Product/Price/Seller (npub shortened via `shortNpub`)/(In-game reward)/Marketplace/Link) + `previewUrl` helper + `PRODUCT_PREVIEW_BADGE` ("PREVIEW · READ ONLY · NO CHECKOUT"); every block is `readOnly:true`/`actionable:false` with a non-actionable marketplace link; invalid products degrade to `{ok:false,errors,lines:[Status:UNAVAILABLE]}` (no throw). NO checkout/pay/zap/navigation/fetch. `main.js` renders it into the title-screen `#product-preview` card via `textContent` only; read-only at `ToriiDebug.shells.productPreview()`. SDK `productPreview` (experimental); `tests/product-preview.test.js`. **v0.2.141 (LEAN-4).** `engine/nostr/leaderboardPreview.js` — pure visible-but-inert local/mock leaderboard PREVIEW block over `leaderboardView`. `leaderboardPreviewBlock(statsList,{mode,limit,signerNpub})` flattens the ranked DISPLAY view into a render-ready block of `{label,value}` rows (Mode/Signer (npub shortened via `shortNpub`)/Status (UNSIGNED · NOT PUBLISHED)/Event (kind-30000 + #torii-quest proof shape)/ranked `#n` rows) + `modeLabel`/`formatRankRow` helpers + `LEADERBOARD_PREVIEW_BADGE` ("PREVIEW · LOCAL MOCK · NO PUBLISH"); every block is `signed:false`/`published:false`/`actionable:false`/`readOnly:true`; `limit` caps shown rows (count reflects all valid), invalid scores degrade into `skipped`, empty → "NO LOCAL SCORES" (no throw). NO NIP-07 signing/relay publish/live submission/fetch. `main.js` renders it into the title-screen `#leaderboard-preview` card via `textContent` only; read-only at `ToriiDebug.shells.leaderboardPreview()`. SDK `leaderboardPreview` (experimental); `tests/leaderboard-preview.test.js`. **v0.2.159-alpha (NOSTR-READ).** `engine/nostr/relayRead.js` — PURE node-safe READ-ONLY Nostr relay adapter foundation (no Nostr client/WebSocket/relay I/O/signing/key handling/DOM/network; never throws). `validateRelayUrl(raw)→{valid,errors,url}` (absolute ws://|wss:// only, must have host, rejects `user:pass@` credentials). `normalizeRelayEvent(raw)` → canonical NIP-01 event (`id/pubkey/created_at/kind/tags/content/sig`; tags→array-of-string-arrays, non-array tags dropped, values stringified; null for non-objects). `validateRelayEvent(event)→{valid,errors}` (id/pubkey 64-hex, kind/created_at non-neg ints, tags array-of-string-arrays, content string, sig 128-hex only when present — NO crypto verification). `eventMatchesFilter(event,filter)` pure NIP-01 semantics (AND across conditions, OR within ids/authors/kinds; since/until; `#<letter>` tag filters; `{}` matches all, null matches none; `limit` is a transport hint, ignored). `buildReqMessage(subId,filters)→['REQ',subId,...filters]` + `buildCloseMessage(subId)→['CLOSE',subId]` — pure READ-frame builders (`RELAY_READ_VERBS`=['REQ','CLOSE']; NO EVENT/publish builder; throw only on bad subId/filters). `createReadOnlyRelayAdapter({request})→{read,readOnly:true}` — INJECTED host-only `request(filters,opts)` transport; `read()` normalises→validates→filters into `{ok,events,skipped,count,errors}`, degrades safely (no transport / thrown request / non-event-list) and NEVER throws/signs/publishes/opens-a-socket; the returned object is `Object.freeze`d and exposes NO publish/sign/send/connect/close method. **NO signing/publishing/payments/relay-writes/NIP-07/private-key/auto-connect/navigation.** SDK `relayRead` (experimental); `tests/relay-read.test.js`. The audited host WS transport (REQ→EOSE collector + CSP `connect-src` relay entries + rate-limiting) remains deferred. **v0.2.160-alpha (NOSTR-READ / LB-1).** `engine/nostr/leaderboardRelayRead.js` — PURE node-safe READ-ONLY leaderboard relay-read PROOF on top of the v0.2.159 boundary (no Nostr client/WebSocket/relay I/O/signing/key handling/DOM/network; never throws on event data). `LEADERBOARD_TOPIC`='torii-quest'. `buildScoreFilter({authors,since,until,limit})` builds the kind-30000 `#t:torii-quest` NIP-01 score filter (only well-formed options included). `extractScoreFromEvent(event)→{ok,score?|errors?}` rebuilds a local score from a NORMALISED leaderboard event (authoritative numbers from JSON `content`, indexable-tag fallback, runId anchored to the `d` tag, carries pubkey+created_at), rejecting non-kind-30000 + scores that fail `validateScore`. `dedupeScores(scores)→{scores,dropped}` keeps the newest event per addressable pubkey+runId. `readLeaderboardEvents(input,options)` accepts a relayRead `read()` result / bare events array / local sample, runs each through relayRead `normalizeRelayEvent`→`validateRelayEvent`→`extractScoreFromEvent` (failures → `skipped`), dedupes, ranks via `leaderboardView.rankScores`, returns read-only `{ok,filter,count,rows,scores,skipped,duplicates,signed:false,published:false,readOnly:true,errors}` — degrades an unusable shape to `ok:false`/empty board, never throws/signs/publishes/opens-a-socket, exposes NO publish/sign/send/connect surface. **NO signing/publishing/payments/relay-writes/NIP-07/private-key/auto-connect/navigation/network.** SDK `leaderboardRelayRead` (experimental) + read-only `ToriiDebug.shells.leaderboardRelayRead()` over a deterministic LOCAL `DEMO_RELAY_SCORE_EVENTS` sample (incl. a superseded duplicate + a malformed entry); added to `buildShellReport` (the 4-surface `shellsSummary` list unchanged). `tests/leaderboard-relay-read.test.js`. The audited host WS transport + the write path (NIP-07 signer + relay publish, SEC-1) + the in-world rank-board MESH/HUD remain deferred. **v0.2.161-alpha (NOSTR-READ / IDENTITY).** `engine/nostr/profileRead.js` — PURE node-safe READ-ONLY Nostr identity/profile PROOF on top of the v0.2.159 boundary (no Nostr client/WebSocket/relay I/O/signing/key handling/NIP-07/DOM/network; never throws on event data). `PROFILE_KIND`=0; `PROFILE_FIELDS`=[name,displayName,about,picture,banner,nip05,lud16,website]. `buildProfileFilter({authors,since,until,limit})` builds the kind:0 NIP-01 profile filter (only well-formed options included). `safeProfileUrl(raw)` accepts ONLY https absolute URLs ≤2048 chars (javascript:/data:/relative/http → null) and returns an INERT data string — NO DOM `<img src>` assignment anywhere. `shortPubkey(pubkey)` truncates a long hex key for display. `parseProfileMetadata(content)` parses the JSON metadata or degrades to `{}` (non-string/malformed/non-object). `extractProfileFromEvent(event)→{ok,profile?|errors?}` builds a sanitised display-only identity view-model (`pubkey`/`shortPubkey`/`name`/`displayName`/`about`/`picture`/`banner`/`nip05`/`lud16`/`website`/`created_at`) from a NORMALISED kind:0 event — rejecting non-kind:0 + non-hex pubkeys, sanitising URLs via `safeProfileUrl`, resolving a `displayName` fallback chain (`display_name`→`name`→short pubkey). `selectNewestProfiles(profiles)→{profiles,dropped}` keeps the newest profile per pubkey (kind:0 replaceable semantics). `readProfiles(input,options)` accepts a relayRead `read()` result / bare events array / local sample, runs each through relayRead `normalizeRelayEvent`→`validateRelayEvent`→`extractProfileFromEvent` (failures → `skipped`), selects newest-per-author, returns read-only `{ok,filter,count,profiles,skipped,duplicates,signed:false,published:false,readOnly:true,errors}` — degrades an unusable shape to `ok:false`/empty list, never throws/signs/publishes/opens-a-socket, exposes NO publish/sign/send/connect surface. **NO signing/publishing/payments/relay-writes/NIP-07/private-key/auto-connect/navigation/network.** SDK `profileRead` (experimental) + read-only `ToriiDebug.shells.profileRead()` over a deterministic LOCAL `DEMO_PROFILE_EVENTS` sample (incl. a superseded duplicate, an unsafe-URL profile, and a malformed-JSON entry); added to `buildShellReport` (the 4-surface `shellsSummary` list unchanged). `tests/profile-read.test.js`. The audited host WS transport + the write path (NIP-07 signer + relay publish, SEC-1) + the in-world identity card MESH/HUD remain deferred. **v0.2.162-alpha (CONSENT-1 / SEC-1 precursor).** `engine/consent/consentGate.js` — PURE node-safe explicit consent-gate foundation (no Nostr client/WebSocket/relay I/O/signing/key handling/NIP-07/payments/DOM/network/auto-update/navigation; never throws; never acts). The auditable boundary every future write/sign/publish/update/travel action MUST pass before it may touch the wire. `CONSENT_ACTIONS` is a frozen known-action registry split into a read tier (`leaderboard:read`/`profile:read`/`relay:read` — `requiresConsent:false`, always allowed) and a write tier (`nostr:publish`/`profile:update`/`leaderboard:submit`/`update:apply`/`gateway:travel` — `requiresConsent:true`), each descriptor pinning `kind`/`write`/`signed`/`danger`/`summary`; `ACTION_KINDS`, `CONSENT_REASON` (read-only/consent-granted/consent-required/consent-mismatch/unknown-action/malformed), `CONSENT_GATE_VERSION`. `isKnownAction`/`getActionDescriptor`/`isWriteAction` classify; `buildConsentRequest(id|{action,detail,origin})→{ok,request?|errors?}` builds a flat request (unknown/malformed → ok:false); `summariseConsent(req|id)` → one human-readable line (READ vs ⚠ KIND · label … "(requires explicit consent)"); `evaluateConsent(req|id,grant)` → an INERT `{action,allowed,blocked,reason,requiresConsent,write,signed,danger,summary,performed:false,readOnly:true,errors}` decision — reads always allowed (grant ignored), writes allowed ONLY with an explicit matching grant (boolean `true` or scoped `{granted,action,token}`; a grant for one action never authorises another → `CONSENT_MISMATCH`); `requestConsent(input,grant)` folds build+evaluate+summary into one read-only report. **NEVER performs an action (`performed:false` on every decision) and exposes NO sign/publish/send/connect/submit/apply/travel method** — an `allowed:true` decision is permission for the host to act later behind its own audited transport, never an action taken here. **NO signing/publishing/payments/relay-writes/NIP-07/private-key/auto-connect/navigation/network.** SDK `consentGate` (experimental) + read-only `ToriiDebug.shells.consentGate({grants?})` (walks the registry showing each action's default no-grant decision); added to `buildShellReport` (the 4-surface `shellsSummary` list unchanged). `tests/consent-gate.test.js`. The consent UX (confirm/HUD prompt) + the real signer wire-up (NIP-07 + relay publish) it gates remain deferred. **v0.2.163-alpha (LB-SUBMIT / SEC-1 precursor).** `engine/leaderboard/submitIntent.js` — PURE node-safe leaderboard SUBMIT INTENT/PREVIEW behind the v0.2.162 consent gate (no Nostr client/WebSocket/relay I/O/signing/key handling/NIP-07/payments/DOM/network/auto-connect; never throws; never acts). `SUBMIT_ACTION`='leaderboard:submit'. `sanitizeSubmitMeta(meta)→{name,npub,pubkey,game}` sanitises identity/game metadata (name: strips C0/DEL control chars + HTML `<`/`>`, caps 64 chars; npub: lowercase bech32 `npub1…` only; pubkey: 64-char lowercase hex only; unsafe/absent → null — no unsafe URLs/HTML stored). `buildSubmitDraft(input)→{ok,draft?|errors?}` combines a validated score (`leaderboard.js` `buildScore`/`validateScore`/`buildScoreEventTemplate`) with the sanitised identity into the inert UNSIGNED kind-30000 draft a host WOULD later sign+publish (`{action,kind,score,identity,event,signed:false,published:false}`), degrading a malformed score to `ok:false` with NO template built. `summariseSubmit(input)` → one stable preview-only line. `prepareSubmitIntent(input,grant)` routes the intent through `evaluateConsent('leaderboard:submit',grant)` and returns an INERT `{ok,action,draft,consent,summary,signed:false,published:false,performed:false,readOnly:true,errors}` — BLOCKED with no grant (`consent-required`), allowed ONLY with an explicit matching grant (boolean `true` or scoped `{granted,action,token}`; a grant for a different action → `consent-mismatch`), a malformed score `ok:false` even WITH a grant; with a matching grant consent is marked allowed but the flow STILL never signs/publishes/sends/connects/writes (`performed:false` pinned). Exposes NO sign/publish/send/connect/submit/write method. **NO signing/publishing/payments/relay-writes/NIP-07/private-key/auto-connect/navigation/network.** SDK `submitIntent` (experimental) + read-only `ToriiDebug.shells.leaderboardSubmit(input?,grant?)` over a deterministic `DEMO_SUBMIT_INPUT` (new `leaderboardSubmitReport` in `shellReport.js`, added to `buildShellReport` — the 4-surface `shellsSummary` list unchanged). `tests/leaderboard-submit-intent.test.js`. The real signer wire-up (NIP-07 + relay publish, SEC-1) + the consent UX prompt that mints the grant remain deferred. **v0.2.166-alpha (CONSENT-2).** `engine/consent/consentView.js` — PURE node-safe CONSENT UX VIEW-MODEL foundation over the v0.2.162 consent gate (no THREE/Rapier/DOM/network/WebSocket/relay I/O/signing/key handling/NIP-07/payments/navigation/auto-connect; never throws; never acts). Turns consent-gate requests/decisions into clear, user-facing PROMPT copy + preview rows BEFORE any real confirm-button wiring exists. `CONSENT_VIEW_VERSION`; `CONSENT_PROMPT_BADGE`='CONSENT · PREVIEW · NO ACTION'; `CONSENT_SEVERITY` (info/caution/danger); `REASON_TEXT` (human copy per `CONSENT_REASON`); `ACTION_COPY` (frozen per-action `{headline,actionLabel,cancelLabel}` for the 5 write actions — gateway:travel/leaderboard:submit/profile:update/update:apply/nostr:publish), read-only + unknown falling back. `copyForAction(id,requiresConsent)` → the UX copy bag (ACTION_COPY entry / READ_COPY / UNKNOWN_COPY). `severityFor(decision)` → read/no-consent→info, low-danger write→caution, high-danger write→danger. `consentPromptView(input,grant)` re-shapes a gate decision into an INERT render-ready view `{title,badge,action,kind,severity,headline,bodyLines:[{label,value}],actionLabel,cancelLabel,requiresExplicitConsent,allowed,blocked,reason,reasonText,write,signed,danger,statusLine,performed:false,actionable:false,readOnly:true,errors}` — blocked-by-default for writes, ALLOWED copy under a matching grant but STILL `performed:false`/`actionable:false`, malformed/unknown → a safe blocked view, every free-form string (origin) control/markup-stripped (`/[\x00-\x1f\x7f<>]/g`, capped) so no `<`/`>` can inject markup. `consentPromptRows(grants)` → one compact inert row per known action (ignores a non-object grants arg). Exposes NO confirm/sign/publish/send/connect/travel/navigate/apply method — a rendered "Travel"/"Publish" label is COPY, not a wired button. **NO confirm-button side effects/navigation/signing/publishing/payments/relay-writes/NIP-07/private-key/auto-connect/auto-update/network.** SDK `consentView` (experimental) + read-only `ToriiDebug.shells.consentPrompt({grants?})` (new `consentPromptReport` in `shellReport.js`, added to `buildShellReport` — the 4-surface `shellsSummary` list unchanged). `tests/consent-view.test.js`. The clickable confirm dialog that MINTS a grant + the real signer/navigation wire-up remain deferred. **v0.2.194-alpha (NOSTR-READ / HEALTH).** `engine/nostr/readHealth.js` — PURE node-safe READ-ONLY health model that proves the Nostr surface is read-only at the MVP stage and the live-write path stays deferred (no Nostr client/WebSocket/relay I/O/signing/key handling/NIP-07/payments/DOM/network; never throws on null/empty input). Constants: `READ_HEALTH_BADGE`='NOSTR READ-PATH · READ-ONLY · NO WRITE/SIGN/PUBLISH'; `PUBLISH_VERB`='EVENT'; `FUTURE_GATED_TIERS`=['SEC-1','SEC-2','SEC-3']; deterministic LOCAL `SAMPLE_PROFILE_EVENTS`/`SAMPLE_SCORE_EVENTS`. Six pure signal checks — `checkRelayReadModel` (relayRead `RELAY_READ_VERBS` are CLOSE/REQ only, no EVENT/publish builder), `checkNoPublishVerb` (no EVENT publish verb in the read path), `checkProfileReadPath` (`readProfiles` over the sample yields read-only profiles), `checkLeaderboardReadPath` (`readLeaderboardEvents` over the sample yields a read-only board), `checkWritePathsGated` (consentGate write tier `requiresConsent:true`, reads always allowed), `checkFutureGatedTiers` (SEC-1/2/3 still future-gated) — fold into `runReadHealth({profileEvents,scoreEvents})→{ok,badge,signals:[{key,label,status,detail}],summary:{total:6,ok,fail},readOnly:true,signed:false,published:false,errors}` (degrades safely, never throws); `formatReadHealth(result)` renders one stable text block. Derives EVERY signal from the shipped pure read modules (`relayRead`/`profileRead`/`leaderboardRelayRead`/`consentGate`) — surfaces NO relay/socket/sign/publish method of its own. **NO signing/publishing/payments/relay-writes/NIP-07/private-key/auto-connect/navigation/network.** SDK `nostrReadHealth` (experimental) + read-only `ToriiDebug.shells.readHealth()` (new `readHealthReport` in `shellReport.js`, added to `buildShellReport` — the 4-surface `shellsSummary` list unchanged) + a read-only Torii Continuum dashboard status panel (`buildReadHealthModel`/`READHEALTH_BADGE` in `engine/dashboard/continuumData.js`, rendered via the existing pill vocabulary so the CSP refresh-script hash is unchanged). `tests/nostr-read-health.test.js` + continuum dashboard-panel tests. The real signer wire-up (NIP-07 + relay publish, SEC-1) the model reports as deferred remains deferred. **v0.2.195-alpha (GATEWAY-TRAVEL-SMOKE).** `engine/gateway/travelSmoke.js` — PURE node-safe gateway-travel SMOKE HARNESS that proves the travel-flow contracts hold WITHOUT touching the network/router/DOM (no THREE/Rapier/window/location/relay I/O/socket/signing/publishing/NIP-07/fs/network; never throws). `TRAVEL_SMOKE_VERSION`=1; `TRAVEL_SMOKE_BADGE`='GATEWAY TRAVEL SMOKE · READ-ONLY · DRY-RUN'; `HOSTILE_ROUTES` (frozen 7-entry list of external/protocol-relative/`javascript:`/`data:`/`//`/backslash/dot-dot hostile route strings the boundary must refuse); `demoGatewayComponent()` builds a deterministic in-memory gateway component for the harness. `runGatewayTravelSmoke(opts?)` drives `createGatewayPortalBoundary({dryRun:true})` (NO transport injected → can never navigate) through ten read-only signals — trigger ARMS but never navigates, proximity arm/cancel transitions, same-origin `/zone/<slug>` route shape, route-allowlist sanitised so it can NEVER be `['/']` (folds to `['/zone/']`), external `website` dropped, consent-required block with no grant, consent-gated confirm path, hostile-route rejection across `HOSTILE_ROUTES`, no-arg/degraded opts safe, every report pins `navigated`/`performed`/`external`/`signed`/`published`/`network`=false — folding into `{ok,badge,summary,signals,safety,reasons,rendered:false,actionable:false}` (a broken component degrades to `ok:false` with concrete `reasons`, never throws). `formatGatewayTravelSmoke(result)` renders one stable text block; re-exports `DEFAULT_PORTAL_ALLOWLIST`. Composes ONLY the shipped pure gateway modules — surfaces NO navigate/open/sign/publish/connect method of its own. SDK `travelSmoke` (experimental) + read-only `ToriiDebug.shells.travelSmoke(opts?)` (new `travelSmokeReport` in `shellReport.js`, added to `buildShellReport` — the 4-surface `shellsSummary` list unchanged). `tests/gateway-travel-smoke.test.js` (+12). Pure local/non-network coverage — NO runtime behaviour change; the safety model is unchanged (proximity ARMS, explicit confirm ACTS, same-origin `/zone/` only). |

---

## SDK/API Boundaries

### Stable or started

- **Physics raycast**: `castRay`, `castRayStatic`, `hasLineOfSight`.
- **Physics bodies**: dynamic/static/kinematic/body factory direction; crate collider mapping now supports bullet impulses.
- **Combat targeting**: shared headshot classifier used by both bullet hit result and target reticle preview. Extracted to a pure, dependency-light module `engine/combat/classifier.js` (v0.2.120) and unit-tested. v0.2.126: the bullet is fired from the gun barrel TOWARD the crosshair target point, so the projectile passes through exactly what the reticle classified (classifier itself unchanged).
- **Combat aiming (v0.2.126)**: `engine/combat/aim.js` — pure barrel-to-crosshair math, imports nothing. `crosshairPoint(camO,camDir,dist)` = the point the crosshair is on; `aimDirection(barrel→target, fallback)` = the unit firing direction; `CONVERGE_DIST` (80 m) = open-sky fallback. `player.js shoot()` casts the camera ray for the target distance, takes the barrel origin from `getGunBarrelWorld`, and fires barrel→crosshair-point. Locked by `tests/aim.test.js`.
- **Reload viewmodel pose (v0.2.127)**: `engine/weapons/reloadPose.js` — pure `reloadDip(p)` curve, imports nothing, returns a scalar (no Three / no hot-path alloc). Shapes the FP gun's reload motion as "click down, clack snap back": ease-out DROP (p<`RELOAD_DROP_END` 0.12) → HOLD lowered (→`RELOAD_HOLD_END` 0.68) → fast SNAP-BACK through rest into a small overshoot (→`RELOAD_SETTLE_END` 0.86, `RELOAD_OVERSHOOT` 0.12) → SETTLE to rest by p=1. `weapons.js _tickGun()` feeds `progress = 1 - reloadTimer/RELOAD_TIME` in and scales the rest-offsets (Y −0.22, Z −0.10, roll −0.6) by the dip. `RELOAD_TIME=1.1` unchanged so audio sync is preserved. Locked by `tests/reload-pose.test.js`.
- **Combat damage (started v0.2.125)**: `engine/combat/damage.js` — pure `shotDamage(isHead)` (9 head / 3 body), `applyDamage(hp,dmg)`, `isLethal(hp,dmg)`. Imports nothing. `weapons.js` uses `shotDamage`; the head/body damage + kill-threshold contract is locked against `BOT_HP` by `tests/combat-damage.test.js` (headshot one-shots, body two-shots).
- **Shot diagnostics (started v0.2.124)**: `engine/combat/shotDiagnostics.js` — pure `classifyShotOutcome(aim, outcome) -> {reason,label}` (no Three/Rapier). Names WHY a player shot landed/missed by comparing the crosshair aim line against the bullet's actual outcome. `weapons.js` records a per-shot snapshot (`recordPlayerShot`/`getLastShot`/`getLastMiss`) surfaced via `ToriiDebug.combat.lastShot`/`lastMiss`. Diagnostics only — no gameplay change. Distance-miss root cause documented: reticle is camera-hitscan, bullet is a barrel-fired projectile (offset + travel time), so the two diverge most at range/with moving targets.
- **Vitest suite (started v0.2.120)**: `tests/*.test.js`, node env, `npm test`. Seams covered: state machine, event bus, headshot classifier, phase→screen map, BotAgent decision helpers (v0.2.122), player heading basis + look-down POV (v0.2.123) — all pure, no browser/Three/Rapier. Check [11] guards the scaffold.
- **BotAgent boundary (started v0.2.122)**: `engine/entities/bot-agent.js` — pure decision helpers (`engageSpeed`, `steerComponent`, `inEngageRange`, `wantsToShoot`), `BOT_ACTION` action constants, and a `decideActions(worldState) -> BotAction[]` facade in the `BotAgent.tick` shape. Imports only config tuning constants. `bots.js` consumes the scalar helpers in its hot path (LOS short-circuit intact); `decideActions` is unit-tested but not yet wired (allocates per call). Stateful tick/shoot/blowback runtime still in `src/bots.js`.
- **Player boundary (started v0.2.114)**: `engine/entities/player.js` — pure geometry (`EYE`, `BODY_FROM_EYE`), spawn shape (`SPAWN_X/Y/Z`, `SPAWN_YAW`, `PLAYER_SAFE_CORNER`), allocation-free look-down POV math (`lookDownEyeY`, `lookDownEyeZ`), and (v0.2.123) the movement heading basis (`forwardX`/`forwardZ`/`rightX`/`rightZ(yaw)`). Imports only the pure `engine/physics/bodies.js` leaf — no Three/Rapier. Unit-tested in `tests/player-boundary.test.js`. Stateful tick/combat/lifecycle/body-state still in `src/player.js`.
- **State machine (started v0.2.115)**: `src/state.js` — `GAME_EVENT` event set, frozen `TRANSITIONS` table, `transition(event)`/`canTransition`/`nextPhase`, and phase predicates. The table mirrors the prior `if (phase !== X) return;` guards exactly, so behaviour is unchanged; all 6 call sites (main, player, input, bots, targetReticle, hud) now read via predicates and write via `transition()`.
- **Event bus (live; seam formalised v0.2.116)**: `src/events.js` — `EV` registry + `on/off/emit`. Already the cross-module signalling backbone (HUD, combat, nostr, stats). v0.2.116 documented the registry convention, wired `EV.PHASE_CHANGE` from `state.transition()` (`{from,to,event}`), and added check 8 (no undefined `EV.<NAME>` references). Imports nothing → no dependency cycles.
- **Debug namespace**: `window.ToriiDebug` is the alpha inspection surface. **v0.2.130** added the JSON-serialisable `ToriiDebug.snapshot()` (+ `combat.report()`/`physics.report()`) built by the pure `engine/debug/snapshot.js` — safe to call at any time (never throws before init). Shape: `{version, phase, state, player:{x,y,z}, combat:{lastHit,lastShot,lastMiss}, physics:{ready,bodies,colliders,bots,crates}, config}`.
- **Physics interactions (started v0.2.130)**: `engine/physics/interactions.js` — pure, allocation-free `nudgeImpulse`/`applyNudge` + `CRATE_IMPULSE`/`CRATE_LIFT` tuning. The first slice of a "push something in the world" surface (bullet→crate today; player/body contact, breakables later). `weapons.js` consumes `applyNudge`. Unit-tested with a fake body in `tests/interactions.test.js`.
- **Raycast service (started v0.2.130)**: `engine/physics/raycastService.js` — `createRaycastService(impl)` injectable facade (`ray`/`rayStatic`/`lineOfSight`) + default `raycastService` wired to the live Rapier raycast layer, surfaced on `ToriiDebug.physics.service`. Lets consumers depend on an injected service instead of importing Rapier directly; missing impls degrade safely. **v0.2.131: first live call-site migrated** — `bots.js` bot-LOS now calls `raycastService.lineOfSight(...)` (was a direct `hasLineOfSight` import); behaviour-identical, LOS short-circuit intact. Remaining call-site: the weapons/bullet ray. Unit-tested with fakes in `tests/raycast-service.test.js`.
- **Weapon-state + pointer-lock predicates (started v0.2.130)**: `state.js` `canShoot(s=state)` / `canReload(s=state)` — pure gates for the firing/reload flow, extracted from `player.js` so the rules live in one place and are unit-testable; adopted by `player.js shoot()`/`startReload()`. **v0.2.131** added the pointer-lock predicates `isEngaged(s=state)` / `needsPointerLock(s=state)`, folding the `pointerLocked` boolean into pure phase predicates; `needsPointerLock()` adopted at the main.js canvas re-lock guard. Truth tables in `tests/state.test.js`.
- **Public SDK entrypoint (started v0.2.131, ARS-5)**: `src/sdk/index.js` — curated namespace re-exports of the node-safe engine leaf modules + `SDK_VERSION` / `STABILITY` tier enum / frozen `SDK_SURFACE` tier map / `surfacesByTier()`. The single import point for external contributors and future CMP marketplace components; no runtime wiring, no scene/WebGLRenderer pull. Stability tiers: **stable** = aim, classifier, damage, interactions, raycastService, reloadPose, muzzle; **experimental** = botAgent, snapshot, phaseScreens, component (v0.2.132), toriiGateway (v0.2.133), productDisplay + travelIntent + leaderboard (v0.2.134), registry + gatewayHandoff + productPanel + leaderboardPublisher (v0.2.135), gatewayPortal + productPanelShell + leaderboardView (v0.2.136), updateCheck (v0.2.138), gatewayPreview (v0.2.139), productPreview (v0.2.140), leaderboardPreview (v0.2.141); **internal** (forward-declared, not yet exported) = physicsBodies, physicsRaycast, player, identity. Locked by `tests/sdk.test.js`.

### Next to extract

- **Player boundary (continue)**: pure geometry/spawn/POV (v0.2.114) and the movement heading basis (v0.2.123) are extracted. Remaining: lift the stateful movement/kinematic tick, combat (shoot/reload/recoil), lifecycle (damage/death/respawn), and body-state (`setPlayerBody`/`getPlayerCollider`/`spawnPlayerBody`) behind the boundary; then add dash/zoom shape. Good next pure slices: play-area clamp helpers, reload/respawn timing math.
- **State machine (continue)**: first slice landed v0.2.115 (see Stable/started); v0.2.130 added the pure `canShoot`/`canReload` weapon-state predicates and removed the dead `state.paused` field; v0.2.131 added the `isEngaged`/`needsPointerLock` pointer-lock predicates and adopted `needsPointerLock()` at the main.js canvas re-lock guard. v0.2.132 folded the reload sub-state into the FSM (`isReloading`/`tickReload`); v0.2.133 wired the `GAMEOVER` edge (`GAME_EVENT.END` + `endRun()`, terminal, no live caller yet). Remaining: adopt the pointer-lock predicates at further call sites, and fire `endRun()` from a real end-of-run screen when it lands.
- **Event bus / decoupling (continue)**: first slice formalised v0.2.116; bot-hit bridge migrated v0.2.117 (`EV.BOT_HIT_BY_PLAYER`, `window._onBotHit` now a deprecated forwarding alias, check 9); foliage shader materials moved off `window` into a module-scope registry in `arena-foliage.js` v0.2.118 (`tickFoliage`/`getGrassMat`/`getFlowerMat`, deprecated `_grassMat`/`_flowerMat` aliases, check 10); mirror Reflector handle moved off `window` into `mirror.js` `getMirror()` v0.2.119 (deprecated `_mirrorMesh` alias, check 10 extended). **All functional `window.*` globals are now decoupled** — only deprecated debug aliases remain. v0.2.121 added the first real `PHASE_CHANGE` subscriber (top-level screen visibility via `engine/ui/phaseScreens.js`). Remaining: add further `PHASE_CHANGE` reactions (audio/presence) and emit `WS_*` from the netcode skeleton when it lands.
- **BotAgent (continue)**: boundary started v0.2.122 (pure decision helpers + `decideActions` facade; scalar helpers wired into `bots.js`). Remaining: migrate the stateful runtime — movement tick, shoot/cooldown, blowback/death/respawn, world-state shaping — behind the boundary, and wire `decideActions` once it can run off the hot path (or made allocation-free).
- **Vitest suite (continue)**: seams landed v0.2.120–124 (state machine, event bus, classifier, phase map, BotAgent helpers, player heading basis, shot diagnostics). Next: physics raycast/bodies (with injected mock world), and later kind:0 profile fetch.

---

## Debug Hooks to Use First

| Need | Debug path / check |
|---|---|
| Grab a full runtime snapshot to paste | `JSON.stringify(window.ToriiDebug.snapshot())` — safe any time; `combat.report()`/`physics.report()` for focused sub-reports |
| Confirm running version | `window.ToriiDebug.version` and HUD version label |
| Check bot spawn count | `window.ToriiDebug.bots.count` |
| Inspect combat classification | `window.ToriiDebug.combat.lastHit` |
| Explain a missed shot (target practice) | `window.ToriiDebug.combat.lastShot` / `lastMiss` — compare `.aim` (crosshair ray) vs `.outcome` (what the bullet hit); read `.reason`/`.label` |
| Verify headshot/body damage + kill | `window.ToriiDebug.bots.damage(i, 9)` one-shots; `damage(i, 3)` twice kills. Contract in `tests/combat-damage.test.js` |
| Check mirror presence | `window.ToriiDebug.world.mirror` |
| Check physics/bodies direction | physics raycast/bodies exports and regression markers |
| Check no god mode | `npm run check` |
| Check no disallowed timers | `npm run check` |
| Check no stale version markers | `npm run check` |
| Run logic unit tests (FSM, bus, classifier, BotAgent) | `npm test` |

---

## Common Fault Index

| Symptom | First places to inspect |
|---|---|
| Headshots/body shots feel wrong | shared classifier in combat path, bot head/body colliders, `ToriiDebug.combat.lastHit`, target reticle state |
| Reticle colour mismatches bullet result | ensure reticle uses same classifier as bullets; avoid duplicated aim math |
| Shots "on target" miss, especially at distance | `ToriiDebug.combat.lastShot`/`lastMiss` — `reason: 'moved-or-offset'` ⇒ projectile travel time (moving bot left the path); `'blocked'` ⇒ geometry in the way; `'aim-off'` ⇒ crosshair wasn't on a bot; `'head-to-body'` ⇒ headshot dropped to torso. (v0.2.126: the bullet is fired barrel→crosshair-point, so it passes through exactly what the reticle is on — static parallax is gone; remaining at-range misses are travel-time/lead.) |
| Headshots "take two shots" to kill | Was camera↔barrel parallax dropping the bullet onto the body collider. v0.2.125 fixed it via a camera-origin bullet; **v0.2.126 keeps the fix but restores the gun-barrel origin** by aiming barrel→crosshair-point (`engine/combat/aim.js`), so the bullet still passes through the previewed head point. Verify damage contract via `tests/combat-damage.test.js` / `ToriiDebug.bots.damage(i,9)`; a clean head hit is 9 dmg vs `BOT_HP` 5 (one-shot). If still two-shot on MOVING bots, it's travel-time lead (see COMBAT-HITREG). |
| Mirror player scale or gun orientation regresses | mirror layer/camera logic, player model layer, world gun transform |
| Looking down clips inside neck | first-person body/camera height and body offset logic |
| Footsteps drumroll | movement/grounded footstep cadence accumulator |
| Reload too slow or visually dead | reload time constant, viewmodel reload animation, `playReload()` timing |
| Crates do not react | `raycast.js` hit crate mapping, `bodies.js` collider map, bullet impulse application |
| NAP NPC stuck or mesh splitting | NPC placement, scale, skin/material setup, animation root |
| Live site shows old behaviour | version label, service worker cache, dist version markers |
| Buttons inert / login + enter-arena do nothing on live | `public/sw.js` PRECACHE_ASSETS (must NOT precache HTML shell `'/'` — pins stale hashed bundle), index.html SW-registration controllerchange self-heal |
| ENTER ARENA / LOGIN click registers but no visible response | `src/main.js` `showEntryStatus()` + `#entry-status` in index.html (v0.2.228: feedback must NOT write to the non-existent `#nostr-status`); ENTER bootstrap fully inside the `try` so a Rapier/WASM failure shows a message + re-enables the button instead of a silent no-op / stuck `LOADING PHYSICS…` (v0.2.229: ENTER also shows an IMMEDIATE `Entering arena…` line BEFORE the await, so a STALLED/never-settling WASM bootstrap is still visible; `_doNostrLogin()` await is try/catch-guarded so a throw can't leave `Connecting…` stuck) |
| `YOU DIED` / `Respawning...` shows in the accessibility tree on the TITLE screen | `#death-msg` is ALWAYS in the DOM (opacity:0 until `.show`); v0.2.229 made it `aria-hidden="true"` by default and `src/hud.js` flips `aria-hidden` in lockstep with the `.show` class (false on PLAYER_KILLED, true on PLAYER_RESPAWN) so AT/smoke only see it during an actual death |
| BOTH title buttons are a complete silent no-op on the LIVE site (version label still renders) | the buttons are wired ONLY by the module bundle `/assets/index-<hash>.js`; if it 404s (stale SW shell) OR throws at module-eval (e.g. headless WebGL/renderer init) before the handlers bind, no listener attaches and the source-level fixes never run. v0.2.230 adds a bundle-INDEPENDENT inline IIFE in index.html (attribute-less `<script>`, CSP sha256 recomputed) that binds both `#btn-enter`/`#btn-nostr-centre` and shows visible feedback (+ the `NIP-07 extension not found` no-provider fallback); `src/main.js` sets `window.__toriiEnterReady`/`__toriiLoginReady` after binding so the inline handlers stand down once the real module owns the click. `tests/entry-flow-smoke.test.js` freezes it |

---

## Manual Smoke Checklist

Run on real hardware after publish:

1. Version label shows current version.
2. Enter arena and reach playing state.
3. Aim near bot: reticle turns orange.
4. Aim body: reticle turns green.
5. Aim head: reticle turns green and shows 👌.
6. Shoot body/head and compare damage/classification via `ToriiDebug.combat.lastHit`.
7. Shoot crates and confirm visible nudge without launch.
8. Press reload and confirm fast clunk-clunk-click feel.
9. Look down and confirm no major neck interior.
10. Check mirror player scale and reflected gun handle orientation.
11. Confirm footsteps do not drumroll.
12. Confirm NAP Chiefmonkey NPC is not stuck or splitting.

### Entry-Flow Live Smoke (run FIRST after every deploy — guards the v0.2.226 blocker)

The static title HTML can render even when the app bundle never executed (stale service
worker → 404'd hashed JS), leaving every title button inert. Do these BEFORE the gameplay
checklist above, on a HARD-RELOADED tab and again on a SECOND normal visit (to exercise the
returning-client service-worker path):

1. Open DevTools console — confirm NO `/assets/index-<hash>.js` 404 and no uncaught errors.
2. `LOGIN WITH NOSTR` (`#btn-nostr-centre`) responds — status text updates on click.
3. `ENTER ARENA` (`#btn-enter`) responds — text flips to `LOADING PHYSICS…` then reaches playing.
4. Application → Service Workers: active worker is `torii-quest-tq-v<current>` (matches version label).
5. Application → Cache Storage: the SW cache does NOT contain the HTML app shell (`/` or `index.html`).
6. Second visit / refresh: page self-heals (one controllerchange reload) — buttons still respond.

Automated companions (catch the source + SW sides in CI without a browser):
`tests/entry-flow-smoke.test.js` (buttons exist + bound in main.js) and
`tests/sw-app-shell.test.js` (no shell precache, cache tracks version, self-heal reload).

---

## Living Reports

- `strategy.md` — strategic source of truth: vision, decision rules, architecture direction.
- `HANDOFF.md` — single-page contributor/agent onboarding: repo state, hard constraints, version markers, source-of-truth docs, build/test/check + deploy commands, debug surface, active issues, next-job format (v0.2.130, ARS-7).
- `SDK_DEBUG_INDEX.md` — SDK & debug discoverability index (v0.2.154): SDK namespaces by stability tier, the four MVP proof surfaces + the MVP loop (SDK namespace + `ToriiDebug.shells` report + inert invariants each — all four now symmetric `readOnly`+`actionable`), the full `ToriiDebug.shells.*` report shapes incl. `summary()` + `diff()` (§4.1 promotion-review helper), where the tests live, and how to add a new proof card / promote preview→live. Docs only — points at `src/sdk/index.js` (`SDK_SURFACE`) + `engine/debug/shellReport.js` as sources of truth.
- `VPS_INSTALL.md` — host-side self-hosting guide (v0.2.144): run the static `dist/` build at `torii.quest` on a shared Ubuntu VPS (Caddy/Nginx + HTTPS, DNS checklist, min specs), the manual GitHub update sequence (build → versioned release folder → flip `current` symlink), symlink rollback, security notes (no auto-update / no shell endpoint / least-privilege deploy user / UFW + SSH hardening), and a deferred guarded "update button" sketch. Docs only — no server touched; aligns with `UPDATE_CHECK.md` §4 + `HANDOFF.md` §7.
- `todo.md` — active task source of truth.
- `progress.md` — visual execution dashboard: track bars (Foundation/Combat/Rapier/SDK/Nostr/Deployment), active goals, current sprint table, completed-last-24h, archive, and update rules.
- `torii-source-reconciliation-report.md` — source reconciliation history.
- `torii-foundation-sprint-report.md` — v0.2.110 foundation history.
- `torii-v0.2.111-regression-repair-report.md` — v0.2.111 repair history.
- `torii-v0.2.112-tuning-report.md` — v0.2.112 collision/POV tuning history.
- `torii-v0.2.113-foundation-tuning-report.md` — v0.2.113 combat/HUD/crate/reload tuning history.
- `torii-v0.2.114-player-boundary-report.md` — v0.2.114 player boundary first-slice extraction.
- `torii-v0.2.115-state-machine-report.md` — v0.2.115 state-machine groundwork first slice.
- `torii-v0.2.116-event-bus-report.md` — v0.2.116 event-bus seam formalisation + `PHASE_CHANGE` wiring.
- `torii-v0.2.117-bot-hit-event-report.md` — v0.2.117 bot-hit bridge migrated onto the bus (`EV.BOT_HIT_BY_PLAYER`); `_onBotHit` deprecated to a forwarding alias.
- `torii-v0.2.118-material-registry-report.md` — v0.2.118 foliage shader materials moved off `window` into an arena-foliage.js module-scope registry; `_grassMat`/`_flowerMat` deprecated to debug aliases.
- `torii-v0.2.119-mirror-accessor-report.md` — v0.2.119 mirror Reflector handle moved off `window` into a mirror.js `getMirror()` accessor; `_mirrorMesh` deprecated to a debug alias. Last functional global decoupled.
- `torii-v0.2.120-vitest-foundation-report.md` — v0.2.120 added Vitest + first unit suites (state machine, event bus, headshot classifier); classifier extracted to a pure `engine/combat/classifier.js`; regression check [11] guards the scaffold.
- `torii-v0.2.121-phase-subscriber-report.md` — v0.2.121 added the first real `EV.PHASE_CHANGE` subscriber: top-level screen visibility centralised into a pure `engine/ui/phaseScreens.js` map applied by one main.js subscriber; imperative title/HUD/pause toggles removed from the transition call sites; tests added.
- `torii-v0.2.122-botagent-report.md` — v0.2.122 began the BotAgent SDK boundary: pure `engine/entities/bot-agent.js` (decision helpers + `decideActions` facade); `bots.js` hot path now consumes the scalar helpers (LOS short-circuit preserved); `tests/bot-agent.test.js` added (54 tests total).
- `torii-v0.2.123-player-boundary-report.md` — v0.2.123 continued the player boundary: movement heading basis (`forwardX`/`forwardZ`/`rightX`/`rightZ`) extracted to `engine/entities/player.js` and wired into the movement tick; module import narrowed to the pure `bodies.js` leaf; `tests/player-boundary.test.js` added (70 tests total).
- `torii-v0.2.124-target-practice-report.md` — v0.2.124 added target-practice combat hit-registration diagnostics: pure `engine/combat/shotDiagnostics.js` miss-reason classifier + per-shot `ToriiDebug.combat.lastShot`/`lastMiss` (aim line vs bullet outcome). Documents the distance-miss root cause (camera-hitscan reticle vs barrel-fired projectile). `tests/shot-diagnostics.test.js` added (83 tests total). Diagnostics only — no gameplay change.
- `torii-v0.2.125-headshot-damage-report.md` — v0.2.125 surgical headshot fix: the bullet now flies the exact camera/crosshair ray (removing the barrel→80 m-convergence parallax that dropped previewed headshots onto the body collider, the cause of "two-shot headshots"). Damage mapping extracted to the pure `engine/combat/damage.js`; `tests/combat-damage.test.js` locks the one-shot-headshot / two-shot-body contract vs `BOT_HP` (92 tests total). Travel-time lead on moving targets documented as remaining work. **NOTE: the camera-origin bullet was superseded in v0.2.126.**
- `torii-v0.2.126-barrel-crosshair-report.md` — v0.2.126 revised the firing rule per user request: bullets ORIGINATE at the gun barrel and fly toward the crosshair target point (camera ray's first hit, or `CONVERGE_DIST` fallback). Pure `engine/combat/aim.js` (`crosshairPoint`/`aimDirection`/`CONVERGE_DIST`) + `tests/aim.test.js`. Keeps the v0.2.125 anti-parallax property (bullet passes through the previewed point) while restoring the gun-barrel muzzle origin. 100 tests / 9 files; headless smoke confirms one-shot headshot / two-shot body.
- `torii-v0.2.127-reload-snap-report.md` — v0.2.127 reload viewmodel "click down, clack snap back" polish via pure `engine/weapons/reloadPose.js` (`reloadDip`); `tests/reload-pose.test.js`. 108 tests / 10 files.
- `torii-v0.2.128-headzone-reentry-report.md` — v0.2.128 combat hitbox + re-entry fix. (1) Head sphere lowered (centre 1.65→1.55, radius 0.22→0.20 → [1.35,1.75]) so crosshair-on-face scores instead of resolving the body cap — players no longer aim over the model. (2) ENTER handler bootstraps physics/colliders/viewmodels ONCE (`_arenaBootstrapped`) so the persistent Rapier world keeps its bot colliders across HOME/ENTER (was rebuilding the world each entry, orphaning colliders → no hits on re-entry); per-entry now does `resetRun()` + canonical-spawn reset. +7 head-zone tests (115 total / 10 files).
- `torii-v0.2.129-muzzle-alignment-report.md` — v0.2.129 muzzle origin side fix. `getGunBarrelWorld` applied the barrel offset along the camera LOCAL quaternion (pitch-only; yaw lives on the parent `playerObj`), so the +right offset pointed in a fixed world direction and the bullet/tracer origin drifted to the wrong (left) side as the player turned. Extracted pure `engine/weapons/muzzle.js` and rebuilt the basis from `camera.getWorldQuaternion()` so the muzzle tracks yaw and stays on the visible right-hand gun; barrel→crosshair direction unchanged. +11 `tests/muzzle.test.js` cases (126 total / 11 files).
- `torii-v0.2.132-infrastructure-report.md` — v0.2.132-alpha infrastructure batch. ARS-4 reload sub-state fold (`isReloading`/`tickReload` in `state.js`, adopted in player/weapons/main; +5 state tests). ARS-3 weapons/player bullet+aim ray migration to `raycastService.ray`/`.rayStatic` (behaviour-identical; +3 service-wiring tests). CMP-1 `COMPONENTS.md` manifest spec. CMP-2 pure `engine/components/contract.js` (`validateManifest`/`isComponent`/`defineComponent`) surfaced via the SDK `component` namespace (experimental); `tests/component.test.js` (+14). 185 tests / 16 files; build + `npm run check` green.
- `torii-v0.2.133-gateway-report.md` — v0.2.133-alpha batch (reconciled onto published v0.2.132). ARS-4 GAMEOVER edge wired (`GAME_EVENT.END` + `endRun()` in `state.js`, terminal GAMEOVER; behaviour-preserving — no live caller fires END). ARS-3 final raycast cleanup: reticle preview (`targetReticle.js`) migrated to `raycastService.ray` (no direct `castRay` consumers remain) + injected-fake-world tests. CMP-8 first reference component `engine/components/toriiGateway.js` (`createToriiGateway`/`toriiGateway`, skeleton no-op lifecycle) surfaced via the SDK `toriiGateway` namespace (experimental); `tests/torii-gateway.test.js`. 200 tests / 17 files; build + `npm run check` green.
- `torii-v0.2.134-lean-mvp-report.md` — v0.2.134-alpha lean-MVP foundation batch (safe, no DNS/VPS). GWPROTO-1 `GATEWAY_PROTOCOL.md` n2n spatial-hop protocol DRAFT + pure `engine/gateway/travelIntent.js` URL-handoff helpers (build/validate/parse travel intents; no navigation/relay/signing). CMP-13 read-only `engine/components/productDisplay.js` (Plebeian.Market product card; links out, no checkout/pay/zap; safe `validateProduct`). LB-1 pure `engine/nostr/leaderboard.js` unsigned score-event helpers (kind 30000; build/validate; no signing/publish). All three surfaced via the SDK at experimental tier. +41 tests (241 total / 20 files); build + `npm run check` green. No deploy/publish/push/upload.
- `torii-v0.2.135-loader-handoff-report.md` — v0.2.135-alpha component-loader + gateway-handoff batch (safe, no DNS/VPS). CMP-7 pure `engine/components/registry.js` component loader/registry (register-by-factory probe+validate, `load` returns a fresh contract-valid instance; LOCAL built-ins only — no eval/dynamic-import/remote). CMP-8 cont. `engine/gateway/gatewayHandoff.js` portal/handoff shell (gateway component → validated travel intent/URL; pure return values, no navigation). CMP-13 cont. `engine/components/productPanel.js` read-only panel view-model. LB-1 cont. `engine/nostr/leaderboardPublisher.js` publisher adapter shape (injected signer/publisher, build-only default, no relay/secrets). All four surfaced via the SDK at experimental tier. +33 tests (274 total / 24 files); build + `npm run check` green. No deploy/publish/push/upload.
- `torii-v0.2.136-visible-shells-report.md` — v0.2.136-alpha visible-shells batch (safe, no DNS/VPS; no side effects). Turned the v0.2.135 infrastructure into pure render-ready VIEW shells. CMP-8 `engine/gateway/gatewayPortal.js` portal VIEW shell (`gatewayPortalView`/`destinationLabel`/`shortKey` — render-ready portal view-model with armed/destination/prompt/URL preview; DISPLAY-ONLY, never navigates). CMP-13 `engine/components/productPanelShell.js` product panel RENDER shell (read-only panel layout; `actionable:false` footer + empty `actions[]`; no checkout surface). LB-1 `engine/nostr/leaderboardView.js` read-only leaderboard display + build-only `leaderboardPreview` (no-signer/no-publisher adapter; rejects any non-mock/build mode). All three surfaced via the SDK at experimental tier; SEC-1/2/3 gates preserved; the actual Three.js meshes remain the deferred render step. +23 tests (297 total / 27 files); build + `npm run check` green. No deploy/publish/push/upload.
