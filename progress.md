# Torii Quest — Progress Dashboard

> Visual execution dashboard. See `strategy.md` for vision and decision rules. See `todo.md` for active tasks.
> Current version: **v0.2.154-alpha** | Live: [torii-quest.pplx.app](https://torii-quest.pplx.app)
> **ACTIVE FOCUS: 15-hour proof-of-concept route** — shooter is maintenance-only unless demo-breaking; the active MVP is the freedom-tech loop (gateway/NAP-to-NAP preview, Plebeian/Nostr product panel proof, leaderboard preview, torii.quest GitHub update-check). Retrospective polish after PoC validation. See `strategy.md` → "15-Hour Proof-of-Concept Route".

---

## Track Overview

Baseline totals are marked **[baseline]** — update them as the project grows rather than doing full archaeology.

### Foundation / Agent-Readable Structure

Tasks: ARS-1 through ARS-7 (7 total) | Done: 5 | In progress: 0 | Remaining: 2

```
[####################################..............] 5 / 7
```

Status: ARS-1 (snapshot tooling), ARS-2 (interactions API), ARS-3 (RaycastService), ARS-7 (HANDOFF.md) landed in v0.2.130; ARS-5 (`src/sdk/index.js` public entrypoint + stability tiers) landed in v0.2.131. Remaining: ARS-4 (FSM fold — partial: v0.2.130 canShoot/canReload + v0.2.131 isEngaged/needsPointerLock pointer-lock predicates + v0.2.132 `isReloading`/`tickReload` reload sub-state fold + v0.2.133 real `GAMEOVER` edge `GAME_EVENT.END`/`endRun()`), ARS-6 (ongoing CODE_INDEX upkeep). ARS-3 follow-up: bot-LOS call-site migrated to the facade in v0.2.131; weapons/player bullet+aim ray call-sites migrated to `raycastService.ray`/`.rayStatic` in v0.2.132; the last direct `castRay` consumer (reticle preview) migrated in v0.2.133 + injected-world tests added — ARS-3 cleanup done.

---

### Combat / Game Feel

Tracked fixes (v0.2.100–v0.2.129): 30 [baseline] | Done: 30 | Open: 1 (travel-time lead on moving targets)

```
[##################################################] 30 / 30 landed | 1 open edge
```

Major closed: hit-reg parallax, head-zone height, re-entry collider orphan, muzzle side, reload snap, barrel→crosshair aim, reticle/classifier split, v0.2.111 regression batch.

---

### Rapier / Physics

Seams extracted: bodies, raycast, RaycastService facade (ARS-3, consumed by bot LOS + weapons/player bullet+aim rays as of v0.2.132 + the reticle preview as of v0.2.133; injected-fake-world tests added v0.2.133; no direct `castRay` consumers remain) | ARS-3 raycast migration COMPLETE

```
[##############################################....] 5 / ~5 SDK seams
```

---

### SDK / API

SDK boundaries started: 6 (physics raycast, physics bodies, combat classifier, combat damage, combat aim, reload pose) [baseline] + `src/sdk/index.js` public entrypoint (ARS-5, v0.2.131) + component contract (`engine/components/contract.js`, CMP-1/2, v0.2.132) + reference components (gateway CMP-8 v0.2.133; read-only product display CMP-13 v0.2.134) + gateway-protocol URL-handoff helpers (`engine/gateway/travelIntent.js`, GWPROTO-1, v0.2.134) + Nostr leaderboard score-event helpers (`engine/nostr/leaderboard.js`, LB-1, v0.2.134) + component loader/registry (`engine/components/registry.js`, CMP-7, v0.2.135) + gateway portal/handoff shell (`engine/gateway/gatewayHandoff.js`, CMP-8, v0.2.135) + product panel view-model (`engine/components/productPanel.js`, v0.2.135) + leaderboard publisher adapter (`engine/nostr/leaderboardPublisher.js`, LB-1, v0.2.135) + gateway portal VIEW shell (`engine/gateway/gatewayPortal.js`, CMP-8, v0.2.136) + product panel RENDER shell (`engine/components/productPanelShell.js`, CMP-13, v0.2.136) + read-only leaderboard display/preview shell (`engine/nostr/leaderboardView.js`, LB-1, v0.2.136)
Remaining before Layer 1 complete: player boundary full lift, BotAgent runtime, grow the SDK surface as boundaries stabilise

```
[###########################################.......] 18 / ~21 Layer 1 boundaries
```

---

### Nostr / Plebeian / Open-World

Skeletons present: NAP zone module, world handoff, presence | Protocol drafted: 1 (Gateway Protocol) | Formalised: 0

```
[########..................................................] 0 / 5+ formalised (Gateway Protocol drafted v0.2.134; skeletons only)
```

v0.2.134 added the open **Gateway Protocol** draft (`GATEWAY_PROTOCOL.md`) + pure URL-handoff helpers (`travelIntent.js`) and pure unsigned Nostr leaderboard score-event helpers (`leaderboard.js`) — wire-format + helpers, not yet wired to relays/handoff. v0.2.135 added the component loader/registry (`registry.js`, CMP-7), the gateway portal/handoff shell (`gatewayHandoff.js`, CMP-8 — turns a gateway component into a validated travel intent/URL, pure return values, no navigation), the product panel view-model (`productPanel.js`), and the leaderboard publisher adapter shape (`leaderboardPublisher.js`, LB-1 — injected signer/publisher, build-only by default, no relay/secrets). v0.2.136 turned that infrastructure into pure, render-ready VIEW shells with no side effects: the gateway portal view shell (`gatewayPortal.js` — destination labels/prompt/armed debug state + URL preview, never navigates), the product panel render shell (`productPanelShell.js` — read-only panel layout, `actionable:false` footer + empty `actions[]`, no checkout surface), and the read-only leaderboard display/preview shell (`leaderboardView.js` — ranks scores + build-only `leaderboardPreview` with no signer/relay).
Blocked on: SDK Layer 1 close-out, identity boundary, kind:0 profile sync, the real signer/publisher + relay read, and wiring the gateway handoff into `world/handoff.js` + a portal mesh to actually move the player.

**Security gates (v0.2.135 review):** SEC-1 — require explicit user consent before any live NIP-07 signing or relay publish (leaderboardPublisher is currently pure/injected, not wired). SEC-2 — add cryptographic verification / signing-layer checks to `world/handoff.js` before it acts on live relay data. SEC-3 — tighten product URL validation from regex-only to `URL`-object parsing before productDisplay/productPanel URLs become clickable or fetched. **v0.2.136 preserves all three:** `gatewayPortal.urlPreview` is display-only (never assigned to `window.location`), `productPanelShell.footer` is `actionable:false` with empty `actions[]`, and `leaderboardView.leaderboardPreview` runs build-only through a no-signer/no-publisher adapter.

---

### Deployment / VPS / Update System

Source reconciliation: done | Source is build truth: yes | Deploy is a manual maintainer step
The live instance can lag the source HEAD; the exact live version is not pinned here (it is whatever the maintainer last published) — this file tracks the SOURCE version only to avoid stale-version drift.

```
[#########################.........................] source clean, live behind
```

Next: manual smoke test v0.2.133 → publish source-built artifact to `torii-quest.pplx.app`.

---

## 15-Hour Proof-of-Concept Route (ACTIVE)

The active MVP — demonstrate the end-to-end freedom-tech loop (thin vertical slices, not polish). **Shooter is maintenance-only unless demo-breaking; retrospective polish after PoC validation.** See `strategy.md`/`todo.md` → "15-Hour Proof-of-Concept Route".

| # | Slice | Status |
|---|-------|--------|
| LEAN-1 | Torii.quest live (publish current green source — manual maintainer deploy) | pending (smoke first) |
| LEAN-2 | Gateway / NAP-to-NAP preview via Torii Gateway component (relay-mediated) | foundation in (v0.2.134 protocol + `travelIntent`; v0.2.135: CMP-7 `registry` loader + CMP-8 `gatewayHandoff` shell turns a gate into a validated travel intent/URL; v0.2.136: `gatewayPortal` VIEW shell — render-ready portal view-model with label/prompt/armed/urlPreview, never navigates; v0.2.139: `gatewayPreview` VISIBLE — inert title-screen card via `gatewayPreviewBlock` (destination/status/relay/intent/URL rows + "PREVIEW · SAFE · INERT" badge), rendered with `textContent` only, read-only at `ToriiDebug.shells.gatewayPreview()`); needs the portal mesh + `world/handoff.js` to ACT on the intent |
| LEAN-3 | Plebeian / Nostr product panel proof (Plebeian.Market product display) | skeleton in (v0.2.134: read-only `productDisplay`; v0.2.135: `productPanel` view-model shell; v0.2.136: `productPanelShell` render shell — read-only panel layout, `actionable:false` footer, no checkout; v0.2.140: `productPreview` VISIBLE — inert title-screen card via `productPreviewBlock` (Product/Price/Seller-npub/reward/Marketplace/Link rows + "PREVIEW · READ ONLY · NO CHECKOUT" badge), rendered with `textContent` only, read-only at `ToriiDebug.shells.productPreview()`); needs the in-world panel mesh over the shell + a real listing |
| LEAN-4 | Leaderboard preview (Nostr signed events) | skeleton in (v0.2.134: pure unsigned `leaderboard`; v0.2.135: `leaderboardPublisher` adapter shape — injected signer/publisher; v0.2.136: `leaderboardView` read-only display + build-only `leaderboardPreview`); needs the real signer + relay read |
| LEAN-5 | torii.quest GitHub update-check | helper + view-model + docs in (v0.2.138: pure `engine/update/updateCheck.js` — `compareVersions`/`parseRelease`/`evaluateUpdate`/`updateCheckView`, inert `actionable:false` view-model, NO network/auto-update; `UPDATE_CHECK.md`; `tests/update-check.test.js`); v0.2.142: `updatePreview` VISIBLE — inert title-screen card via `updatePreviewBlock` (Version/Latest/Status/Source/Notes rows + "PREVIEW · MANUAL · NO AUTO-UPDATE" badge), rendered with `textContent` only from a deterministic LOCAL sample release, read-only at `ToriiDebug.shells.updatePreview()`; needs the read-only releases fetch + in-world prompt mesh |

---

## Active Goals

1. Close out **ARS-1 through ARS-7** — agent-readable structure layer. Enables safe cross-session, cross-agent handoff.
2. **Lift source-built artifact to live** — publish v0.2.129 (or latest passing smoke test) to `torii-quest.pplx.app`.
3. **Player boundary full extraction** — movement tick, combat, lifecycle, body-state behind the seam.
4. **BotAgent runtime migration** — wire `decideActions`, migrate stateful tick/shoot/blowback.
5. **Formalise NAP zone + handoff** — promote skeletons to working boundaries before Nostr/world features scale.

---

## Current Sprint

| # | Track | Task | Status |
|---|-------|------|--------|
| ARS-1 | Foundation | ToriiDebug.snapshot() / combat.report() / physics.report() | done (v0.2.130) |
| ARS-2 | Rapier | Physics interaction API (pure interactions.js + mock tests) | done (v0.2.130) |
| ARS-3 | Rapier | RaycastService injectable facade (+ bot-LOS call-site migrated v0.2.131) | done (v0.2.130) |
| ARS-4 | Foundation | Fold reloading/pointerLocked into guarded FSM + GAMEOVER edge | partial — canShoot/canReload + isEngaged/needsPointerLock + isReloading/tickReload (v0.2.132) predicates extracted; real GAMEOVER edge (`END`/`endRun()`) wired v0.2.133 |
| ARS-3+ | Rapier | Weapons/player bullet+aim ray migration to RaycastService | done (v0.2.132); reticle preview migrated + injected-world tests v0.2.133 → ARS-3 cleanup done |
| CMP-1/2 | SDK/Nostr | Component contract + manifest spec (`COMPONENTS.md`, `contract.js`, SDK `component`) | done (v0.2.132) |
| CMP-8 | SDK | First reference component — Torii gateway skeleton (`toriiGateway.js`, SDK `toriiGateway`) | done (v0.2.133) |
| GWPROTO-1 | SDK/Nostr | Gateway Protocol draft (`GATEWAY_PROTOCOL.md`) + pure URL-handoff helpers (`engine/gateway/travelIntent.js`, SDK `travelIntent`) | done (v0.2.134) |
| CMP-13 | SDK | Read-only product display reference component (`productDisplay.js`, SDK `productDisplay`) | done (v0.2.134); panel view-model `productPanel.js` added v0.2.135 |
| LB-1 | Nostr | Leaderboard score-event helpers — pure unsigned template (`engine/nostr/leaderboard.js`, SDK `leaderboard`) | done (v0.2.134); publisher adapter shape `leaderboardPublisher.js` added v0.2.135 |
| CMP-7 | SDK | Component loader/registry — local built-in lookup, validates before load (`engine/components/registry.js`, SDK `registry`) | done (v0.2.135) |
| CMP-8+ | SDK | Gateway portal/handoff shell — gateway component → validated travel intent/URL (`engine/gateway/gatewayHandoff.js`, SDK `gatewayHandoff`) | done (v0.2.135); needs portal mesh + `world/handoff.js` to act on the intent |
| CMP-8++ | SDK | Gateway portal VIEW shell — render-ready portal view-model (`engine/gateway/gatewayPortal.js`, SDK `gatewayPortal`) | done (v0.2.136); display-only, never navigates; needs portal mesh to render it |
| CMP-13+ | SDK | Product panel RENDER shell — read-only panel layout (`engine/components/productPanelShell.js`, SDK `productPanelShell`) | done (v0.2.136); `actionable:false` + empty `actions[]`; needs panel mesh |
| LB-1+ | Nostr | Read-only leaderboard display + build-only preview shell (`engine/nostr/leaderboardView.js`, SDK `leaderboardView`) | done (v0.2.136); no signing/relay; needs real signer + relay read |
| HARD-1..4 | Tooling/UI/Security/Debug | Safe-hardening batch — version-drift guard, mock-chat marked non-live, CSP gstatic documented, `ToriiDebug.shells.*` read-only reports (`engine/debug/shellReport.js`) | done (v0.2.137) |
| MVP-pivot | Docs | Refocus living docs onto the 15-hour proof-of-concept route — shooter maintenance-only unless demo-breaking; active MVP = the freedom-tech loop | done (v0.2.138) |
| LEAN-5 | Infra/SDK | torii.quest GitHub update-check architecture — pure `engine/update/updateCheck.js` + inert view-model + `UPDATE_CHECK.md` (no network/auto-update) | done (v0.2.138); needs releases fetch + prompt mesh |
| LEAN-2+ | SDK/UI | Gateway/NAP-to-NAP VISIBLE preview — inert title-screen card (`engine/gateway/gatewayPreview.js`, SDK `gatewayPreview`); rendered by `main.js` via `textContent` only, read-only at `ToriiDebug.shells.gatewayPreview()` | done (v0.2.139); display-only, never navigates; needs portal mesh + `world/handoff.js` to act |
| LEAN-3+ | SDK/UI | Plebeian/Nostr product/market VISIBLE preview — inert title-screen card (`engine/components/productPreview.js`, SDK `productPreview`); rendered by `main.js` via `textContent` only, read-only at `ToriiDebug.shells.productPreview()` | done (v0.2.140); read-only, no checkout/navigation; needs in-world panel mesh + a real listing |
| LEAN-4+ | SDK/UI | Local/mock leaderboard VISIBLE preview — inert title-screen card (`engine/nostr/leaderboardPreview.js`, SDK `leaderboardPreview`); rendered by `main.js` via `textContent` only, read-only at `ToriiDebug.shells.leaderboardPreview()` | done (v0.2.141); local/mock rank rows + kind-30000/#torii-quest proof shape + npub flavour; signed:false/published:false; needs real signer (NIP-07, SEC-1) + relay read |
| LEAN-5+ | SDK/UI | torii.quest update-check VISIBLE preview — inert title-screen card (`engine/update/updatePreview.js`, SDK `updatePreview`); rendered by `main.js` via `textContent` only from a deterministic LOCAL sample release, read-only at `ToriiDebug.shells.updatePreview()` | done (v0.2.142); running version + sampled latest + update-available/up-to-date/unknown status + GitHub releases path as text; actionable:false; NO fetch/install/auto-update/navigation; needs the read-only releases fetch (CSP) + in-world prompt mesh |
| LEAN-loop | SDK/UI | MVP loop clarity pass — title-screen header framing the four preview cards as Travel→Market→Score→Update (`engine/mvpLoop.js`, SDK `mvpLoop`); rendered by `main.js` via `textContent` only, read-only at `ToriiDebug.shells.mvpLoop()`; each card title carries its step number | done (v0.2.143); content/CSS/labelling only; actionable:false; no network/links/actions |
| PROOF-MESH-SPEC | World/SDK | Pure in-world proof-mesh LAYOUT/SPEC layer — `engine/world/proofSurfaceSpecs.js` (`PROOF_SURFACE_SPECS` for gateway portal panel / product stall panel / leaderboard board / update prompt board: id, step+LEAN, feeding SDK preview + shells report, NAP-zone anchor, plain `{x,y,z}`/`{w,h,d}`/`yawRad`, inert invariants) + `getProofSurfaceSpec()` + `proofSurfaceLayout()` (`allInert` gate, NAP-zone bounds); SDK `proofSurfaceSpecs` + read-only `ToriiDebug.shells.surfaceSpecs()` | done (v0.2.147); spec layer only — no Three/render/gameplay; plain data, no hot-path alloc; +9 tests. Next: the isolated mesh pass that reads these specs |
| PROOF-SPEC-CHECK | Debug/SDK | Pure spec↔registry cross-check — `engine/debug/proofSurfaceCheck.js` (`checkProofSurfaceSpecs()`, read-only at `ToriiDebug.shells.surfaceSpecCheck()`): verifies each proof-surface spec's `previewSdk` is a real SDK experimental namespace + `shell` is a real `ToriiDebug.shells` report, re-asserts inert invariants, scans for leaked live-action keys → `{ok,errors,warnings,surfaces}` | done (v0.2.148); reviewability only — no render/network/DOM; deterministic; +14 tests. The guard to run before the future mesh pass |
| PROOF-ANCHOR-XF | World/SDK | Pure anchor→transform contract — `engine/world/anchorTransforms.js` (`PROOF_SURFACE_ANCHORS` registry + `getAnchor()`/`resolveAnchorTransform()`/`resolveAllAnchors()`, SDK `anchorTransforms` + read-only at `ToriiDebug.shells.anchorTransforms()`): maps each proof-surface `anchor` id to a ground origin + parent/zone hint, binds a spec to a plain transform descriptor (origin/position/`offset`/size/yawRad, `origin+offset===position`), reports unresolved anchors → `{ok,count,resolved,unresolved}` | done (v0.2.149); placement contract only — no Three/render/gameplay; plain data, no hot-path alloc; +14 tests. The isolated mesh pass that reads these transforms is next |
| PROOF-MESH-PASS | World/Debug | FIRST display-only in-world proof-surface MESH pass — pure `engine/world/proofSurfaceRenderPlan.js` (`buildProofSurfaceRenderPlan()` gates on `resolveAllAnchors().ok` + `checkProofSurfaceSpecs().ok`, → plain-data RENDER PLAN of inert panels) + browser-only adapter `engine/world/proofSurfaceMeshes.js` (`buildProofSurfaceMeshes(scene)` builds a coloured board + `CanvasTexture` label per panel EXACTLY ONCE when `plan.ok`); wired into `arena.js` `_buildNapZone()`; read-only at `ToriiDebug.shells.surfaceRender()` → `{rendered,count,ok,badge,reasons}` | done (v0.2.150); display-only/inert — no click/raycast/navigation/payments/Nostr/live-data/fetch; setup-only, no hot-path alloc; +14 tests. Next: anchor↔scene-graph parent binding + fold `surfaceRender().ok` into promotion review/regression check |
| DOC-GUARD | Docs/Infra | Docs/status consistency guard — pure node-safe `tools/docConsistency.mjs` (`versionInText`/`findVersionMarkers`/`staleLiveVersionLines`/`checkDocConsistency` + `CONTINUITY_DOCS`/`ADVISORY_DOCS`) + regression-check `[14]`: HARD FAIL on current-version drift in the core continuity docs (todo/progress/HANDOFF) or a missing core doc; ADVISORY warn for advisory-doc lag (SDK_DEBUG_INDEX/CODE_INDEX) + stale "live/published version: vX" lines | done (v0.2.154); reporting-first, low-maintenance — no runtime/gameplay change; removed the stale `Live published version: v0.2.113-alpha` contradiction; +18 tests. Next: extend opportunistically only if drift recurs |
| BUNDLE-TRACK | Perf/Infra | Bundle-size tracking baseline — pure node-safe `tools/bundleSizes.mjs` (`formatBytes`/`classifyAsset`/`isJsCategory`/`severityFor`/`summarizeBundle` + `KIB`/`MIB`/`DEFAULT_WARN_LIMIT=700·KiB` mirroring vite `chunkSizeWarningLimit`) + CLI `tools/bundle-report.mjs` (reads `dist/assets/*.js`+`dist/index.html`, raw+gzip via `node:zlib`, prints per-asset/per-category baseline + advisory flag); `npm run bundle:report`; non-failing advisory `[13]` in `tools/regression-check.mjs` | done (v0.2.153); ADVISORY/reporting only — no runtime/gameplay/bundle-splitting change; pure helpers unit-tested (+16 tests); reads built artifacts only, no network/install. Next: structured code-splitting work off this baseline |
| PROOF-GATE | Debug/Infra | Proof-surface promotion/regression GATE — pure `engine/debug/proofSurfaceGate.js` (`proofSurfaceGate(opts?)`) folds the spec↔registry cross-check + render plan + scene-graph parent binding into one fail-fast `{ok,gates:{specCheck,renderPlan,parentBinding},counts,reasons}` (each input injectable for tests); wired into `tools/regression-check.mjs` as RUNTIME check [12] (fails with the gate's `reasons` if any layer is broken/unsafe) and read-only at `ToriiDebug.shells.surfaceGate()` | done (v0.2.152); review/CI guardrail only — no Three/DOM/network/gameplay; pure/deterministic; +7 tests. Next: first live proof-surface read once promotion is sanctioned |
| PROOF-MESH-BIND | World/Debug | Anchor↔scene-graph PARENT BINDING for the proof-surface boards — pure `engine/world/proofSurfaceParentBinding.js` (`PARENT_NODE_NAMES`, `parentNodeName()`, `parentGroupName()`, `resolveParentBindings(plan)` → `{badge,group,count,ok,groups,unbound,rendered:false,actionable:false}`) maps each panel's `parent` hint to a live node name + a NAMED subgroup under the `proof-surfaces` root; render plan now carries `parent` per panel; `proofSurfaceMeshes.js` builds one subgroup per parent and mounts each board there (boards keep WORLD positions — structural/discoverability only, NO visual change); `arena.js` `.name`s the live `nap-zone-floor` + `torii-gate` (fallback + GLB) nodes; read-only at `ToriiDebug.shells.surfaceBindings()` | done (v0.2.151); display-only/inert — no re-parenting onto rotated/async live nodes, no click/raycast/navigation/payments/Nostr/live-data; setup-only, no hot-path alloc; +10 tests. Next: fold `surfaceRender().ok`/`surfaceBindings().ok` into promotion review/regression check |
| SHELLS-DIFF | Debug/SDK | Proof-surface review symmetry + diff — gateway preview gains `readOnly:true` (all four surfaces now expose `readOnly`+`actionable`) + read-only `ToriiDebug.shells.diff(a,b)` (pure `shellsDiff()`) comparing two `summary()` outputs and flagging inertness-loosening invariant flips (`loosened[]` checklist for preview→live promotions) | done (v0.2.146); debug only — no network/actions/DOM/THREE; +6 tests |
| SDK-DEBUG-INDEX | Docs/SDK | SDK/debug discoverability — `SDK_DEBUG_INDEX.md` (SDK namespaces by tier, the four MVP proof surfaces + loop, `ToriiDebug.shells` reports + invariants, test map, how to add a proof card / promote preview→live) + read-only `ToriiDebug.shells.summary()` (pure `shellsSummary()`, `allInert` gate) | done (v0.2.145); docs/debug only — actionable:false; no network/actions; +4 tests |
| VPS-docs | Docs | Host-side self-hosting guide — `VPS_INSTALL.md` (Ubuntu VPS, Caddy/Nginx, HTTPS, DNS, min specs, manual GitHub update sequence, symlink rollback, security notes, deferred guarded update-button sketch); links `UPDATE_CHECK.md` | done (v0.2.144); docs only — no server/install/auto-update; aligns with HANDOFF §7 |
| ARS-5 | SDK | src/sdk/index.js skeleton with stability tiers | done (v0.2.131) |
| ARS-6 | Foundation | CODE_INDEX.md upkeep pass after each ARS task | ongoing |
| ARS-7 | Foundation | HANDOFF.md template | done (v0.2.130) |
| TQ-MANUAL-113 | Combat | Manual smoke test on real hardware | pending |
| PROGRESS-1 | Docs | Formalise / maintain progress.md | in progress |

---

## Completed Last 24h

Items stay here (crossed out) for ~24 hours, then move to Archive below.

- v0.2.154-alpha docs/status consistency guard (safe — docs/CI tooling only; no runtime/gameplay/visual change) — reduced drift between the runtime VERSION and the cross-model (Claude/GPT/Deepseek) handoff docs so continuity stays reliable. New PURE node-safe `tools/docConsistency.mjs` (no fs/network/THREE/DOM) exports `CONTINUITY_DOCS` (`todo.md`/`progress.md`/`HANDOFF.md`), `ADVISORY_DOCS` (`SDK_DEBUG_INDEX.md`/`CODE_INDEX.md`), `versionInText(version,text)`, `findVersionMarkers(text)`, `staleLiveVersionLines(text,version)` (narrowly matches a `live/published/deployed … version <marker>` status assertion so prose/task/changelog lines that merely mention "live"+a version are NOT false-flagged), and `checkDocConsistency({version,files,present})` → `{ok,version,errors,warnings,checked}`. New regression-check `[14]` reads the docs and runs the pure checker: HARD FAILS (touches `fails`) on clear current-version drift in a core continuity doc or a missing core doc; emits ADVISORY warnings (never fails) for advisory-doc version lag and stale "live/published version: vX" lines. Also fixed the genuine contradiction the guard targets — the stale `Live published version: v0.2.113-alpha` / "source ahead by 20 versions" lines in progress.md §Deployment now state that deploy is a manual maintainer step and the file tracks the SOURCE version only (no pinned stale live version). Reporting-first + low-maintenance by design — no brittle archaeology, no gameplay/visual/runtime/network/navigation/payments/signing/relay/fetch/auto-update/click/raycast change; tooling lives in `tools/` and never ships in the bundle. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +18 tests (486 total / 43 files); check now 14/14.
- v0.2.153-alpha bundle-size tracking baseline (safe — ADVISORY/reporting only; no runtime/gameplay/visual/bundle-splitting change) — turned the recurring Vite "large chunk" build warning into a measurable, AI-handoff-friendly local baseline WITHOUT touching the runtime or the existing `manualChunks` splitting. New PURE node-safe `tools/bundleSizes.mjs` (no fs/zlib/THREE/DOM) exports `KIB`/`MIB`/`DEFAULT_WARN_LIMIT` (700·KiB, mirroring vite.config.js `chunkSizeWarningLimit`), `formatBytes(bytes,digits)` (B/KB/MB, `n/a` on invalid/negative), `classifyAsset(name)` (hash-agnostic stem match → `app`/`three`/`rapier`/`runtime`/`html`/`other`), `isJsCategory`, `severityFor(bytes,limit)` (`warn` strictly over limit), and `summarizeBundle(entries,opts)` → JSON-serialisable `{warnLimit,assets[sorted bytes desc],totals{count,jsBytes,jsGzip,htmlBytes,allBytes},categories,warnings}` (gzip optional, html excluded from JS totals). New CLI `tools/bundle-report.mjs` does the fs+`node:zlib` gzip I/O over `dist/assets/*.js`+`dist/index.html` and prints a per-asset + per-category table with an advisory over-limit flag (`npm run bundle:report`; exits 0 always — visibility, not a gate; prints a hint + exits 0 when no `dist/`). Added a NON-FAILING advisory `[13]` to `tools/regression-check.mjs` (never touches `fails`; one-line total-JS + per-category summary + over-limit advisory; skipped when no `dist/`) so `npm run check` stays green (13/13). Baseline measured: total JS 2.8 MB raw / 1006.8 KB gzip (app 116.3 KB, three 609.1 KB, rapier 2.1 MB); 1 chunk over 700 KB (rapier, expected — lazy-loaded on Enter Arena). NO network/navigation/payments/signing/relay/fetch/WebSocket/auto-update/click/raycast/external side effects; tooling lives in `tools/` so it never bloats the runtime bundle. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +16 tests (468 total / 42 files); check now 13/13.
- v0.2.152-alpha proof-surface promotion/regression GATE (safe — review/CI guardrail only; no Three/DOM/network/gameplay/visual change) — closed the v0.2.151 "Next" by folding the proof-board safety gates into the regression path so a broken board or binding fails fast instead of surfacing in the browser. New PURE node-safe `engine/debug/proofSurfaceGate.js` exports `PROOF_GATE_BADGE` + `proofSurfaceGate(opts?)`, which composes the three already-pure layers that must ALL hold before the display-only proof boards may be built (and, in future, before any preview→live promotion): (1) the spec↔registry cross-check `checkProofSurfaceSpecs().ok` (v0.2.148), (2) the render plan `buildProofSurfaceRenderPlan().ok` (v0.2.150), and (3) the scene-graph parent binding `resolveParentBindings(plan).ok` (v0.2.151) → a single JSON-serialisable `{badge,ok,gates:{specCheck,renderPlan,parentBinding},counts:{panels,groups,bound,unbound},reasons,rendered:false,actionable:false}`. `ok` is true iff all three pass; `reasons` lists concrete failures (e.g. `render-plan: anchors-unresolved`, `parent-binding: unbound <id>`). Each input (`check`/`anchors`/`plan`/`binding`) is INJECTABLE so a test can drive a deliberately-broken layer and prove the gate catches it. Wired into `tools/regression-check.mjs` as RUNTIME check [12] (`await import` of the pure gate, fails with the gate's own `reasons`) — the gate chain is THREE/DOM-free so it imports cleanly under `npm run check`; also surfaced read-only at `ToriiDebug.shells.surfaceGate()`. NO render/network/navigation/signing/gameplay — composes plain data only. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +7 tests (452 total / 41 files); check now 12/12.
- v0.2.151-alpha anchor↔scene-graph PARENT BINDING for the proof-surface boards (safe — structural/discoverability only; no visual/gameplay/network change) — made the v0.2.150 board mounting explicit, discoverable, and testable. New PURE node-safe `engine/world/proofSurfaceParentBinding.js` exports `PARENT_BINDING_BADGE`, `PROOF_SURFACE_GROUP` (`'proof-surfaces'`), `PARENT_NODE_NAMES` (frozen `torii-gate`/`nap-zone-floor` map), `parentNodeName(parent)` (→ live node name or null), `parentGroupName(parent)` (→ `proof-surfaces::<parent>`), and `resolveParentBindings(plan)` — which reads each panel's `parent` hint (falling back to the anchor registry via `getAnchor`), groups panel ids per parent into a named subgroup, lists any `unbound` panels, and returns `{badge,group,count,ok,groups,unbound,rendered:false,actionable:false}` (deterministic, JSON-serialisable, no THREE/DOM). The render plan now carries `parent` on every panel (self-describing); `proofSurfaceMeshes.js` builds ONE named `THREE.Group` subgroup per parent under the `proof-surfaces` root and mounts each board into its subgroup (boards keep their WORLD positions, subgroups sit at identity — a STRUCTURAL/discoverability change ONLY, no visual change), and `surfaceRender()` now reports a `parents[]` breakdown. CRITICAL safety choice: boards are NOT physically re-parented onto the live `nap-zone-floor` (rotated −π/2 on X) or the async-loaded `torii-gate` GLB — doing so would rotate/break them; instead the live nodes are `.name`d in `arena.js` (`nap-zone-floor`; `torii-gate` on both the fallback and the GLB) purely so `scene.getObjectByName` can find them. Read-only at `ToriiDebug.shells.surfaceBindings()` (pure `resolveParentBindings(buildProofSurfaceRenderPlan())`). DISPLAY-ONLY/INERT: no click handlers, raycast, navigation, payments, Nostr actions, live data, or fetch; setup-only, no hot-path allocation. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +10 tests (445 total / 40 files).
- v0.2.150-alpha FIRST display-only in-world proof-surface MESH pass (safe — first actual visual in-world step; inert markers only; no gameplay/network change) — turned the v0.2.147–v0.2.149 specs/checks/anchors into real (but inert) panels in the NAP zone, kept minimal + rollback-friendly. Split in two: the PURE node-safe `engine/world/proofSurfaceRenderPlan.js` (`buildProofSurfaceRenderPlan(opts?)` runs both gates — `resolveAllAnchors().ok` + `checkProofSurfaceSpecs().ok`, either injectable — and turns the four specs into a plain-data RENDER PLAN `{badge,ok,gates,count,panels,reasons,rendered:false,actionable:false}`, each panel `{id,label,sublabel,kind,anchor,position,size,yawRad,color,readOnly:true,actionable:false}`; NO THREE/DOM, fully deterministic/testable), and the browser-only adapter `engine/world/proofSurfaceMeshes.js` (`buildProofSurfaceMeshes(scene,opts?)` consumes the plan and, ONLY when `plan.ok`, builds an inert coloured `BoxGeometry` board + a `CanvasTexture` label plate per panel — the same canvas-text pattern as the bitcoin sun, no new dependency — EXACTLY ONCE during scene setup; idempotent `rendered` guard; `proofSurfaceRenderState()` mirrors the result). Wired into `arena.js` `_buildNapZone()` after `_buildNapTree`; read-only render state at `ToriiDebug.shells.surfaceRender()` → `{rendered,count,ok,badge,reasons}`. DISPLAY-ONLY/INERT: no click handlers, raycast/interaction, navigation, payments, Nostr actions, live data, or external fetch; no per-frame/hot-path allocation (setup-only). The plan imports `proofSurfaceCheck.js` so it stays debug-only (not in the SDK barrel — avoids a barrel↔leaf cycle). SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +14 tests (435 total / 39 files).
- v0.2.149-alpha proof-surface anchor→transform contract (safe — pure spec/validation only; no Three/render/gameplay/network change) — gave the future proof meshes one source of truth for resolving anchors. New `engine/world/anchorTransforms.js` exports `PROOF_SURFACE_ANCHORS` — a frozen registry mapping each of the four anchor ids (`torii-gate-threshold`, `nap-zone-north-stall`, `nap-zone-far-centre`, `nap-zone-south-board`) to plain placement metadata: a ground `origin` (`{x,y:0,z}`), a `parent` hint (`torii-gate` / `nap-zone-floor`), the NAP `zone`, and a note. Plus `ANCHOR_IDS`, `getAnchor(id)`, `resolveAnchorTransform(spec)` (→ a plain transform descriptor: anchor id/parent/zone, `origin`, surface `position`, `offset`=position−origin, `size`, `yawRad`, `rendered:false`/`actionable:false`; the invariant `origin+offset===position` lets a mesh pass parent to the anchor + apply a local offset), and `resolveAllAnchors(specs=PROOF_SURFACE_SPECS)` (→ `{badge,count,ok,resolved,unresolved,rendered:false,actionable:false}`, listing any spec with an unknown anchor instead of throwing). PLAIN data only — NO THREE classes/Vector3/Matrix4, NO DOM/renderer, NO gameplay. Imports only pure leaves (`config.js` + `proofSurfaceSpecs.js`) so it re-exports cleanly from the SDK barrel (no barrel↔leaf cycle): exposed via SDK experimental `anchorTransforms` AND read-only `ToriiDebug.shells.anchorTransforms()`. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +14 tests (421 total / 37 files).
- v0.2.148-alpha proof-surface spec↔registry cross-check (safe — pure validation/reviewability only; no Three/render/gameplay/network change) — added a guard that keeps the in-world proof-surface specs ALIGNED with the live registries before any future mesh pass. New `engine/debug/proofSurfaceCheck.js` exports `checkProofSurfaceSpecs(surfaceMap?, specs?)` — for each spec it verifies `previewSdk` names a real SDK experimental namespace (`SDK_SURFACE`; unknown → error, known-but-non-experimental → warning), `shell` names a real `ToriiDebug.shells` report (a `buildShellReport()` key), the inert invariants hold (`readOnly:true`/`actionable:false`, never `signed`/`published` true), and no live-action key has crept onto the spec. Returns `{ok,badge,checked,errors,warnings,surfaces}`; deterministic; reads only static `SDK_SURFACE` metadata + the demo-fixture output of `buildShellReport()` — NO render/network/DOM/THREE. Lives in `engine/debug` (not the SDK barrel) so the SDK's pure-leaf re-export rule holds and no barrel↔leaf import cycle forms; exposed read-only at `ToriiDebug.shells.surfaceSpecCheck()` (callers can inject their own `{sdk,shells}` map). SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +14 tests (407 total / 36 files).
- v0.2.147-alpha in-world proof-mesh spec layer (safe — pure specs/layout helpers; no Three/render/gameplay/network change) — prepared the future in-world proof meshes by defining a pure, node-safe LAYOUT/SPEC layer. New `engine/world/proofSurfaceSpecs.js` exports `PROOF_SURFACE_SPECS` — frozen, MVP-loop-ordered specs for the four proof surfaces (gateway portal panel · product stall panel · leaderboard board · update prompt board): a stable `id`, the loop `step` + `lean` slice, the feeding SDK preview namespace + `ToriiDebug.shells` report, an in-world `anchor` in the NAP zone (east of the torii gate), approximate `{x,y,z}` `position` + `{width,height,depth}` `size` + `yawRad` (PLAIN data — no THREE classes, so no Vector3/Matrix4 alloc), and inert `invariants` (readOnly:true/actionable:false; leaderboard also signed:false/published:false). Plus `getProofSurfaceSpec(id)`, `PROOF_SURFACE_IDS`, and `proofSurfaceLayout()` (a read-only summary with an `allInert` gate read from the specs' own invariants, NAP-zone `bounds`, `rendered:false`/`actionable:false`). Exposed via SDK experimental `proofSurfaceSpecs` and read-only `ToriiDebug.shells.surfaceSpecs()`. NO Three.js/DOM/renderer mutation, NO gameplay integration — this is the spec/contract layer the future (isolated, non-hot-path) mesh pass will read. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +9 tests (393 total / 35 files).
- ~~v0.2.146-alpha proof-surface review symmetry + `shells.diff()` (safe — debug only; no network/actions/gameplay change) — made future preview→live promotions safer to review for AI/dev handoffs. (1) The gateway preview now reports `readOnly:true` (added to `gatewayPreview.js`, surfaced through `gatewayPreviewReport` + `shellsSummary`), so all four MVP proof surfaces expose the same `readOnly`+`actionable` invariant pair — one consistent shape a reviewer can assert across the loop. (2) Added a pure read-only `ToriiDebug.shells.diff(a,b)` (pure `shellsDiff()` in `engine/debug/shellReport.js`) that compares two `shells.summary()` outputs (before/after a promotion), classifies every invariant flip, and flags the ones that *loosen* inertness (`actionable→true`, `readOnly→false`, `signed/published→true`, `allInert→false`, `network/autoUpdate→true`) into a `loosened[]` review checklist with a single `safe` boolean. No network/actions/DOM/THREE/Rapier — only compares two already-computed summaries. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +6 tests (383 total / 34 files).~~
- ~~v0.2.145-alpha SDK/debug discoverability index + shells.summary() (safe — docs/debug only; no network/actions/gameplay change) — made the SDK + debug surfaces easy for AI handoffs and FOSS devs to inspect. New `SDK_DEBUG_INDEX.md` maps: the SDK entrypoint + STABLE/EXPERIMENTAL/INTERNAL namespaces (source of truth `SDK_SURFACE`), the four MVP proof surfaces (gateway/product/leaderboard/update previews) framed by the MVP loop with each one's SDK namespace + `ToriiDebug.shells` report + inert invariants (readOnly/actionable/signed/published), the full `ToriiDebug.shells.*` report shapes, where each test lives, and a step-by-step for adding a new proof card or promoting preview→live. Added a read-only `ToriiDebug.shells.summary()` (pure `shellsSummary()` in `engine/debug/shellReport.js`) that aggregates the four surfaces + loop header into a compact JSON-serialisable map — each invariant READ from the live `buildShellReport` output (so it can't over-claim inertness) — with an `allInert` gate plus `network:false`/`autoUpdate:false` flags; surfaced at `ToriiDebug.shells.summary()`. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged; debug ships unconditionally. +4 tests (377 total / 34 files).~~
- ~~v0.2.144-alpha VPS install / manual-update documentation (safe — docs only; no server access, no install, no DNS change, no network/gameplay change) — added `VPS_INSTALL.md`, a host-side guide for self-hosting the static `dist/` build at `torii.quest` on a shared Ubuntu 22.04/24.04 VPS: MVP recommendation (static build served by Caddy or Nginx, no production Node), minimum specs, a DNS checklist for `torii.quest`, optional Node 20+ build-on-box install, the `git clone` → `npm ci` → `npm run build` → publish-to-`releases/<version>/` → flip-`current`-symlink flow, Caddy (automatic HTTPS) and Nginx (+Certbot) configs with WASM/cache headers, the **manual** GitHub update sequence (main or release tag), symlink-based rollback (keep 2–3 release folders + optional tarball), security notes (no auto-update, no shell endpoint, least-privilege deploy user, UFW/SSH hardening, unattended-upgrades, backups), and a deferred guarded "update button" architecture (check → human confirm → locked server-side update pinned to a verified tag → auto-rollback). Cross-linked from `UPDATE_CHECK.md` §3/§4. NO code touches a server, performs an install, fetches the network, or auto-updates; honours `UPDATE_CHECK.md` §4 + `HANDOFF.md` §7. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged. No test count change (373 total / 34 files; build + 11 checks green).~~
- v0.2.143-alpha MVP loop clarity pass (safe — no deploy/network/gameplay change; content/CSS + pure helper) — made the four title-screen preview cards read as ONE proof-of-concept loop. New pure node-safe `engine/mvpLoop.js` (`mvpLoopSummary` → render-ready header block: title "TORII QUEST · MVP LOOP", flow "Travel → Market → Score → Update", a "these are inert previews" note, the four ordered steps mapped to their cards, "PREVIEW · READ ONLY · MANUAL" badge; `actionable:false`/`readOnly:true`; `MVP_LOOP_STEPS`/`MVP_LOOP_FLOW`/`MVP_LOOP_BADGE`). `index.html` gains an inert `#mvp-loop` header card (violet accent) above the four previews, and each card title now carries its step (`1 · TRAVEL` … `4 · UPDATE`); `main.js` `renderMvpLoop()` writes the flow + note via `textContent` only — NO links, NO actions, NO network. Surfaced read-only at `ToriiDebug.shells.mvpLoop()` (via `engine/debug/shellReport.js`) and in the SDK as `mvpLoop` (experimental). SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged. +7 tests (373 total / 34 files)
- ~~v0.2.142-alpha LEAN-5 visible torii.quest update-check preview (safe — no deploy/network/gameplay change; pure helper + inert title card) — made the torii.quest GitHub update-check proof VISIBLE on the title screen. New pure node-safe `engine/update/updatePreview.js` (`updatePreviewBlock` flattens the `updateCheckView` view-model into a render-ready block of label/value rows — Version (running), Latest (sampled release), Status (UPDATE AVAILABLE / UP TO DATE / UNKNOWN), Source (GitHub releases page URL as TEXT), Notes (capped single-line peek) — plus a `statusLabel` helper + frozen `STATUS_TEXT` and a "PREVIEW · MANUAL · NO AUTO-UPDATE" badge; every block is `actionable:false`/`readOnly:true`; draft/unparseable releases degrade to UNKNOWN, no throw). `main.js` renders the inert card into `#update-preview` using `textContent` only from a DETERMINISTIC LOCAL SAMPLE release — NO network fetch, NO install, NO shell execution, NO auto-update, NO navigation, NO clickable link. Surfaced read-only at `ToriiDebug.shells.updatePreview()` (via `engine/debug/shellReport.js`, with a `DEMO_RELEASE` fixture) and in the SDK as `updatePreview` (experimental). SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged. +11 tests (366 total / 33 files)~~
- ~~v0.2.141-alpha LEAN-4 visible local/mock leaderboard preview (safe — no deploy/network/gameplay change; pure helper + inert title card) — made the Nostr leaderboard proof VISIBLE on the title screen. New pure node-safe `engine/nostr/leaderboardPreview.js` (`leaderboardPreviewBlock` flattens the `leaderboardView` ranked DISPLAY shell into a render-ready block of label/value rows — Mode, Signer (npub identity flavour, shortened), Status (UNSIGNED · NOT PUBLISHED), Event (kind-30000 / #torii-quest score-event proof shape), then ranked `#n` rows — plus `modeLabel`/`formatRankRow`/`shortNpub` helpers and a "PREVIEW · LOCAL MOCK · NO PUBLISH" badge; every block is `signed:false`/`published:false`/`actionable:false`/`readOnly:true`; invalid scores degrade into `skipped`, empty → "NO LOCAL SCORES", no throw). `main.js` renders the inert card into `#leaderboard-preview` using `textContent` only — NO NIP-07 signing, NO relay publish, NO live submission, NO fetch/WebSocket. Surfaced read-only at `ToriiDebug.shells.leaderboardPreview()` (via `engine/debug/shellReport.js`) and in the SDK as `leaderboardPreview` (experimental). SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged. +14 tests (355 total / 32 files)~~
- ~~v0.2.140-alpha LEAN-3 visible Plebeian/Nostr product/market preview (safe — no deploy/network/gameplay change; pure helper + inert title card) — made the Plebeian/Nostr product/market proof VISIBLE on the title screen. New pure node-safe `engine/components/productPreview.js` (`productPreviewBlock` flattens the `productPanelShell` RENDER shell into a render-ready block of label/value rows — Product, Price, Nostr seller npub (shortened ownership proof), optional in-game reward, Marketplace label, capped Plebeian.Market link — plus `shortNpub`/`previewUrl` helpers and a "PREVIEW · READ ONLY · NO CHECKOUT" badge; every block is `actionable:false`/`readOnly:true`; invalid products degrade to `ok:false` with errors, no throw). `main.js` renders the inert card into `#product-preview` using `textContent` only — NO link, NO checkout/pay/zap, NO navigation, NO fetch, NO signing. Surfaced read-only at `ToriiDebug.shells.productPreview()` (via `engine/debug/shellReport.js`) and in the SDK as `productPreview` (experimental). SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged. +11 tests (341 total / 31 files)~~
- ~~v0.2.139-alpha LEAN-2 visible gateway/NAP-to-NAP preview (safe — no deploy/network/gameplay change; pure helper + inert title card) — made the gateway/NAP-to-NAP hop VISIBLE on the title screen. New pure node-safe `engine/gateway/gatewayPreview.js` (`gatewayPreviewBlock` flattens the `gatewayPortal` VIEW shell into a render-ready block of label/value rows — destination, status, relay hint, travel intent, capped URL preview — plus `statusText`/`previewUrl` helpers and a "PREVIEW · SAFE · INERT" badge; every block is `actionable:false`). `main.js` renders the inert card into `#gateway-preview` using `textContent` only — NO link, NO navigation, NO fetch, NO signing. Surfaced read-only at `ToriiDebug.shells.gatewayPreview()` (via `engine/debug/shellReport.js`) and in the SDK as `gatewayPreview` (experimental). SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc; firing rule unchanged. +12 tests (330 total / 30 files)~~
- ~~v0.2.138-alpha MVP-pivot + torii.quest update-check batch~~ (safe — no DNS/VPS/deploy; docs + pure helper only) — **refocused the project onto the 15-hour proof-of-concept route**: pivoted the living docs (todo.md, strategy.md, progress.md, HANDOFF.md, CODE_INDEX.md) so shooter polish is MAINTENANCE-ONLY unless demo-breaking and the active MVP is the freedom-tech loop (gateway/NAP-to-NAP preview, Plebeian/Nostr product panel proof, leaderboard preview, torii.quest GitHub update-check), with retrospective polish after PoC validation. LEAN-5: scaffolded the torii.quest GitHub update-check architecture — pure node-safe `engine/update/updateCheck.js` (`compareVersions` tolerant semver compare, `parseRelease` GitHub-release normaliser, `evaluateUpdate`, `updateCheckView` + `RELEASE_SOURCE`/`UPDATE_STATUS`) that compares a release manifest's tag against runtime `VERSION` and returns an INERT "update available" view-model (`actionable:false`) — NO network fetch, NO auto-update, NO install; the releases fetch + in-world prompt mesh are documented deferred host steps. Surfaced via SDK `updateCheck` (experimental); `UPDATE_CHECK.md`; `tests/update-check.test.js`. SEC-1/2/3 gates intact; godMode false; no new setTimeout/hot-path alloc. +13 tests (318 total / 29 files)
- ~~v0.2.137-alpha safe-hardening batch (safe — no DNS/VPS/deploy; no gameplay-risk change) — addressed security/handoff-review warnings without touching gameplay. HARD-1: fixed package/runtime version drift (`package.json` 0.2.1 → semver `0.2.137-alpha`) + regression-check [5] now ties `package.json version` to `EXPECTED_VERSION` (v-stripped) so they can't drift again. HARD-2: marked the mock chat non-live — `#chat-input`/`#chat-send` disabled + greyed (`:disabled` CSS), placeholder "chat preview — not live", header "LIVE CHAT (preview)", comment added; still non-transmitting, no networking. HARD-3: reviewed + documented the CSP `connect-src https://www.gstatic.com` entry as REQUIRED (DRACO decoder fetch in arena.js/weapons.js); kept, not broadened. HARD-4: `engine/debug/shellReport.js` read-only debug reports over the v0.2.136 shells (`gatewayReport`/`productReport`/`leaderboardReport`/`buildShellReport` + demo fixtures) on `ToriiDebug.shells.*` — no signer/relay/publish/navigation; `tests/shell-report.test.js`. SEC-1/2/3 gates intact. +8 tests (305 total / 28 files)~~
- ~~v0.2.136-alpha visible-shells batch (safe — no DNS/VPS/deploy; no side effects) — turned the v0.2.135 pure infrastructure into render-ready VIEW shells, all node-pure (no THREE/Rapier/DOM/scene imports), all security gates intact. CMP-8 `engine/gateway/gatewayPortal.js` gateway portal VIEW shell (`gatewayPortalView`/`destinationLabel`/`shortKey` + `PORTAL_PROMPT`; emits status/isGateway/armed/destination/relay/prompt/plan/urlPreview — `armed = plan.valid`, prompt+urlPreview blank unless armed; DISPLAY-ONLY, never assigns window.location/contacts relay/signs; `tests/gateway-portal.test.js`); CMP-13 `engine/components/productPanelShell.js` product panel RENDER shell (`productPanelShell` → ordered `{title,imageUrl,lines[Price/Seller/(reward)],footer{kind:'link',actionable:false},actions:[],readOnly:true}`; invalid product degrades to `panel:null`; NO checkout/pay/zap/buy surface; `tests/product-panel-shell.test.js`); LB-1 `engine/nostr/leaderboardView.js` read-only leaderboard display + build-only preview (`rankScores` deterministic desc sort + 1-based rank, `leaderboardView({mode})` rejects any non-mock/build mode i.e. no 'live'/relay, `accuracyLabel`, `leaderboardPreview` runs through a no-signer/no-publisher adapter → `signed:false/published:false`; `tests/leaderboard-view.test.js`); all three surfaced via the SDK at the experimental tier. +23 tests (297 total / 27 files)~~
- ~~v0.2.135-alpha component-loader + handoff batch (safe — no DNS/VPS/deploy) — CMP-7 pure `engine/components/registry.js` component loader/registry (`createRegistry`/`createBuiltinRegistry`/`builtinRegistry`; register-by-factory probes + validates manifest/contract, `load(id, config)` returns a FRESH contract-valid instance, unknown/incompatible loads degrade safely; LOCAL built-ins only — no eval/dynamic-import/remote code; `tests/registry.test.js`); CMP-8 pure `engine/gateway/gatewayHandoff.js` portal/handoff shell (`gatewayDestination`/`planGatewayTravel`/`gatewayTravelUrl` map a gateway component's destination onto a validated travel intent / URL string; pure return values, NO window.location/relay/signing; `tests/gateway-handoff.test.js`); `engine/components/productPanel.js` read-only product panel view-model (`productPanelViewModel`/`priceLabel`; flat render-ready bag, no checkout surface; `tests/product-panel.test.js`); LB-1 `engine/nostr/leaderboardPublisher.js` publisher adapter shape (`createLeaderboardPublisher({sign,publish})`; INJECTED deps, build-only by default, captures sign/publish failures without throwing, no relay/secrets; `tests/leaderboard-publisher.test.js`); all four surfaced via the SDK at the experimental tier. +33 tests (274 total / 24 files)~~
- ~~v0.2.134-alpha lean-MVP foundation batch (safe — no DNS/VPS/deploy) — GWPROTO-1 `GATEWAY_PROTOCOL.md` n2n spatial-hop protocol DRAFT (relay-first hybrid discovery, URL handoff MVP, world/zone/gateway identity, travel intent, return path, signed-event future, security tiers, NIP path; "component is code, protocol is agreement") + pure `engine/gateway/travelIntent.js` URL-handoff helpers (`buildTravelIntent`/`validateTravelIntent`/`buildTravelUrl`/`parseTravelUrl`; no navigation/relay/signing; `tests/travel-intent.test.js`); CMP-13 read-only `engine/components/productDisplay.js` reference component (`createProductDisplay`/`productDisplay`/`validateProduct`, manifest kind:'product'/mountTarget:'panel', links OUT to Plebeian.Market, NO checkout/pay/zap/publish, safe https-only validation; `tests/product-display.test.js`); LB-1 pure `engine/nostr/leaderboard.js` unsigned Nostr score-event helpers (`buildScore`/`validateScore`/`buildScoreEventTemplate`, kind 30000, indexable tags, headshots≤kills; no signing/relay/publish; `tests/leaderboard.test.js`); all three surfaced via the SDK at the experimental tier. +41 tests (241 total / 20 files)~~
- ~~v0.2.133-alpha gateway batch (reconciled onto published v0.2.132 — no v0.2.132 work dropped) — ARS-4 real `GAMEOVER` edge (`GAME_EVENT.END` + `endRun()` in state.js, PLAYING/DEAD → terminal GAMEOVER; behaviour-preserving, no live caller yet; +state tests); ARS-3 final raycast cleanup (reticle preview `targetReticle.js` → `raycastService.ray`, no direct `castRay` consumers remain; +injected-fake-world ray/LOS tests); CMP-8 first reference component `engine/components/toriiGateway.js` (`createToriiGateway`/`toriiGateway`, skeleton no-op lifecycle, manifest kind:'gateway'/mountTarget:'scene', SDK `toriiGateway` experimental namespace; `tests/torii-gateway.test.js`). +15 tests (200 total / 17 files)~~
- ~~v0.2.132-alpha infrastructure batch — ARS-4 reload sub-state fold (`isReloading`/`tickReload` pure predicates in state.js, adopted in player.js/weapons.js/main.js; +5 state tests); ARS-3 weapons/player bullet+aim ray migration to `raycastService.ray`/`.rayStatic` (behaviour-identical, barrel→crosshair preserved; +3 service-wiring tests); CMP-1 `COMPONENTS.md` manifest spec (identity/provenance/npub, bundle hash, capabilities, deps, assets, config→mount options, pricing/zap split, Nostr listing events, security rules); CMP-2 `src/engine/components/contract.js` pure lifecycle contract (`validateManifest`/`isComponent`/`defineComponent`, idempotent mount/unmount) surfaced via SDK `component` namespace (experimental tier); `tests/component.test.js` (+14 tests). +22 tests (185 total / 16 files)~~
- ~~v0.2.131 foundation batch — ARS-5 `src/sdk/index.js` public SDK entrypoint (curated node-safe re-exports + `SDK_VERSION`/`STABILITY`/frozen `SDK_SURFACE` tier map; `tests/sdk.test.js`); ARS-3 follow-up: bot-LOS call-site migrated to `raycastService.lineOfSight()`; ARS-4 pointer-lock fold (`isEngaged`/`needsPointerLock` predicates in state.js, adopted at the main.js canvas re-lock guard; +4 state tests); CMP-1..16 component-marketplace tasks added to todo.md (Later track); esbuild dev-server advisory assessed + deferred (audit fix too broad). +11 tests (163 total / 15 files)~~
- ~~v0.2.130 no-blocker foundation batch — ARS-1 `engine/debug/snapshot.js` (`ToriiDebug.snapshot()`/`combat.report()`/`physics.report()`); ARS-2 `engine/physics/interactions.js` (pure `nudgeImpulse`/`applyNudge`, crate nudge tuning moved off weapons.js); ARS-3 `engine/physics/raycastService.js` (injectable facade on `ToriiDebug.physics.service`); FSM slice `canShoot`/`canReload` predicates in state.js (dead `state.paused` removed); ARS-7 `HANDOFF.md`; +26 tests (152 total / 14 files)~~
- ~~v0.2.129 muzzle origin side fix — `engine/weapons/muzzle.js`; `camera.getWorldQuaternion()` so barrel tracks yaw; +11 muzzle tests (126 total / 11 files)~~
- ~~v0.2.128 head-zone lowered (centre 1.65→1.55, radius 0.22→0.20); `_arenaBootstrapped` guard fixes re-entry collider orphan; +7 classifier tests~~
- ~~v0.2.127 reload snap viewmodel — pure `engine/weapons/reloadPose.js`; +8 reload-pose tests~~
- ~~v0.2.126 barrel→crosshair aim — pure `engine/combat/aim.js`; `tests/aim.test.js` (100 tests / 9 files)~~
- ~~v0.2.125 headshot damage extracted — `engine/combat/damage.js`; one-shot headshot / two-shot body contract locked~~
- ~~v0.2.124 shot diagnostics — `engine/combat/shotDiagnostics.js`; `ToriiDebug.combat.lastShot/lastMiss`~~
- ~~v0.2.123 player movement heading basis extracted to `engine/entities/player.js`; import narrowed to pure `bodies.js` leaf~~
- ~~v0.2.122 BotAgent SDK boundary first slice — pure `engine/entities/bot-agent.js`; BotAgent scalar helpers wired into bots.js~~
- ~~v0.2.121 first PHASE_CHANGE subscriber — screen visibility centralised in `engine/ui/phaseScreens.js`~~
- ~~v0.2.120 Vitest added; state machine, event bus, headshot classifier suites~~
- ~~v0.2.119 mirror handle accessor — last functional window.* global decoupled~~
- ~~v0.2.118 foliage shader materials moved off window into module-scope registry~~
- ~~v0.2.117 bot-hit bridge migrated onto event bus (EV.BOT_HIT_BY_PLAYER)~~
- ~~v0.2.116 event-bus seam formalised; EV.PHASE_CHANGE wired~~
- ~~v0.2.115 state-machine first slice — explicit FSM in state.js~~
- ~~v0.2.114 player boundary first slice — pure geometry/spawn/look-down POV math~~
- ~~progress.md created and added to todo.md, strategy.md, CODE_INDEX.md~~

---

## Archive

Completed items older than ~24h live here. Newest first.

### v0.2.100 – v0.2.113 — Foundation and Source Reconciliation (2026-06-23)

- Source reconciliation (v0.2.100–v0.2.108) reverse-ported into clean source by concern.
- v0.2.109 source reconciliation build.
- v0.2.110 foundation sprint: physics SDK seams, ToriiDebug namespace, hardening, NAP/handoff/presence skeletons, regression tooling.
- v0.2.111 regression repair: FP neck clipping, footstep drumroll, reflected gun roll, headshot classification, NAP NPC, reload viewmodel.
- v0.2.112 collision/POV tuning: widened bot head/body colliders, look-down camera arc, ToriiDebug.combat.lastHit.
- v0.2.113 foundation tuning: shared classifier for bullets + HUD, reticle restored, crate bullet nudges, faster reload.
- Safety hardening batch: CSP header, Nostr avatar URL validation, kill-feed innerHTML → safe DOM.
- All functional window.* globals decoupled across v0.2.117–v0.2.119.

---

## Update Rules

1. **Completed todo items** stay crossed out in `todo.md` for roughly 24 hours so context is preserved in active sessions.
2. After ~24 hours, move them from the "Completed Last 24h" section here into Archive, grouped by date or sprint.
3. `todo.md` should remain focused on active and near-term work only — no graveyard of old completions.
4. `progress.md` (this file) is the visual execution layer. `strategy.md` owns vision and decision rules. `todo.md` owns the active task queue.
5. Update track bars when a seam is extracted, a sprint block closes, or a major fix lands. Exact counts are less important than directional accuracy.
6. Version the Archive entries by sprint or date cluster — avoid per-version archaeology.
7. Do not list Google, Cloudflare, Microsoft, or Babylon.js anywhere in this file.
