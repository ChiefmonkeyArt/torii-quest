# Torii Quest — Contributor / Agent Handoff

Current version: v0.2.763-alpha - THREE-OPTION LOGIN + GUEST HEADLESS-BODY FIX: (1) The title screen becomes a three-option login — NEW PLAYER (enter as guest + pick a character via two torso-thumbnail cards: nostrich / poo-poo-head), CREATE WITH AI (kept on login for now, opens the settings Character Forge where the Step-B mock lives), and RETURNING? (LOGIN WITH NOSTR auto-seats the npub's kind-35100 character so the avatar follows you). The guest picker stores the choice in main.js (_pendingGuestChar) and seats it via _arena.setCharacter() before boot; new torso assets public/ui/char-{nostrich,guest}-torso.png were rendered from the GLBs via tools/char-torso-render.html + tools/char-torso-screenshot.mjs. (2) Fixed the guest headless-body bug — firstPersonBody.js hardcoded chiefmonkey-headless.glb for every character, so guests/nostrich saw chiefmonkey's teal body (and its layer-2 mesh ghosted); it now loads only for chiefmonkey. (3) Nostr .glb-on-npub research: no NIP exists for 3D avatars — the correct pattern is a Blossom sha256 blob + NIP-94 kind:1063 (m: model/gltf-binary) referenced by the kind-35100 manifest. +5 tests (guest-character-picker); 3838 tests / 316 files.

Prior: v0.2.762-alpha - CREATE WITH AI MOCK (character-creation Step B): the Character Forge's disabled "Create with AI" placeholder is now a living LOCAL mock proving the full prompt → thinking → validate → verdict loop with zero network/payment/signing. New src/engine/character/meshGenerationMock.js composes the REAL seams — planGeneration() builds/validates the request, a canned generic-humanoid (Meshy-style, 24-bone) fixture result is gated by validateGeneratedMesh() — so only the external generator's output is mocked (it passes assessRig 'riggable'). characterForgePanel.js swaps the "Coming soon" card for a prompt textarea + "Generate (demo)" action and a dedicated ai-flow view (thinking / accepted / rejected), driven by a new _forgeAIState in main.js with the thinking→done transition riding the shell rAF tick (no window timers). Real Meshy/Tripo + routstr/Cashu wiring is Step C (the injectable executor seam is already reserved for it). +14 tests (character-mesh-generation-mock + character-forge-panel ai-flow locks); 3833 tests / 315 files.

Prior: v0.2.761-alpha - GUEST-MASTER ANIMATION RETARGET: the master animation-library's full 18 clips are baked onto the guest avatar via offline world-delta retargeting (tools/glb_retarget.py) — both rigs share the Meshy 24-bone names, so idle / stylish-walk / walk-back / run / run-forward firing / run-back / jump / reload / hit-react / knock-down / dance / victory-cheer / melee / two falls / walk-back-with-gun all landed as a straight bake. The guest asset becomes public/models/guest-master.glb (WebP texture + Draco mesh, 1.2 MB — 82% smaller than the 6.6 MB uncompressed bake), replacing the sparse 3-clip guest-head4.glb (5.9 MB, deleted). playerModel.js's 'guest' CHARACTER gains the full 15-slot master anim table and joins the GAME_STATE_TO_CLIP branch alongside chiefmonkey/nostrich, so the anonymous default now has identical locomotion + combat coverage. Infra (no client-version bump, PR #131): corrected the ADR-0101 deploy-hook request path (stale /opt/torii-quest → /apps/quest = the runner's APPS_ROOT) and added ops/install-deploy-ssh.sh so GitHub-triggered deploys stop silently no-op'ing. No new tests; 3819 tests / 314 files.

Prior: v0.2.760-alpha - GUEST-FIRST ENTRY + GUEST AVATAR: the title screen no longer makes visitors pick a character — everyone enters anonymously as a GUEST with the new guest avatar (guest-head4.glb, the Meshy head4 re-export). Step 1 is now ENTER AS GUEST (the #btn-enter-nap id is kept so the existing entry handler needs no logic change), Step 2 is the optional LOGIN WITH NOSTR; the SELECT YOUR CHARACTER heading and the chiefmonkey/nostrich .char-btn buttons are removed (character selection/forging now lives in Settings). playerModel gains a 'guest' CHARACTER and defaults to it: Meshy clips Walking/Running/baselayer map through the generic anim branch — full locomotion/combat coverage is deferred until the master animation-library is retargeted onto guest-head4.glb (the nostrich-master path). The server AUTH character whitelist is extracted to server/auth/characterKeys.js and now accepts 'guest' alongside the legacy presets and a 64-hex Character-Forge mesh hash. +5 tests (tests/character-keys.test.js); 3819 tests / 314 files.

Prior: v0.2.759-alpha - GENERAL HUMANOID BONE CLASSIFIER (ADR-0107): the Character Forge validator now maps any humanoid rig, not just Mixamo/Biped. Added TRIPO_BONE_MAP (Hip / L_Thigh / L_Calf / L_Upperarm / L_Forearm / L_Hand / Waist / L_Clavicle) and GENERIC_HUMANOID_BONE_MAP (Hips / Spine / LeftArm / LeftForeArm / LeftUpLeg / LeftLeg / lowercase neck — Meshy's prefix-stripped Mixamo style, also the Unreal Mannequin), plus a conservative classifyBone() keyword+side heuristic as a per-bone fallback for arbitrary future generators (word prefixes LeftArm, short prefixes L_Thigh, Unreal suffixes thigh_l). detectConvention now scores over all tables instead of prefix-sniffing; mapBonesToRoles resolves per-bone across every table then the heuristic; rigAssessment verdict is mapping-based (riggable when every required role maps, regardless of convention label). Unblocks the anonymous-entry guest avatars (raw Tripo head + Meshy head4 re-export) and the "Create with AI" backend wiring with no further bone-name work. +10 test locks (Tripo/Meshy fixtures use the real guest-avatar GLB bone names); 3814 tests / 313 files.

Prior: v0.2.758-alpha - KAMI DEV MENU SYNC + POINTER-LOCK FLAP DIAGNOSTIC: (1) The Kami dev panel (switches) was delayed on show/hide — the old 1Hz refresh throttle left it appearing/vanishing up to a second after the emagake + ema chat, breaking the "all at once" expectation. Removed the throttle; the driver now re-renders every frame but is still gated by the snapshot-key change detector (zero DOM churn on unchanged frames), so the panel syncs within the same frame as Kami enter/exit. (2) Added a bounded pointer-lock flap diagnostic for the playtest report "cursor switches to mouse and back when firing in the NAP zone" — logs a concise console.warn on rapid lock↔unlock cycles while playing (max 4) so the next report names the trigger. No new tests; 3804 tests / 313 files.

Prior: v0.2.757-alpha - STICKER BAKED-DECAL LOCAL-SPACE PARENTING FIX: the baked (DecalGeometry) stickers STILL wouldn't stick to static meshes (trees, the torii gate, the sats symbol) even after the DoubleSide fix — they stuck to the NPC (plane path) but not to objects. Root cause: the decal's WORLD-space vertices were parented to the hit mesh with a quaternion-inverse + local-position counteraction that only cancelled rotation/translation (not scale), double-transforming the decal out of view. Fix: bake the DecalGeometry's vertices/ normals into the hit mesh's LOCAL space (applyMatrix4 of inverse matrixWorld) and parent with an identity transform, so it inherits position/rotation/scale exactly and conforms to the surface. Baked mode (switch OFF) now sticks to objects and the NPC alike, so the A/B is finally testable. +2 test locks (local-space parenting asserts); 3804 tests / 313 files.

Prior: v0.2.756-alpha - STICKER BAKED-DECAL VISIBILITY + KAMI LEAK/ESC FIXES: four fixes from the playtest report. (1) Baked (DecalGeometry) stickers were INVISIBLE with the Force-Plane toggle OFF — the baked material used the default FrontSide culling, but DecalGeometry clips in WORLD space and is parented to the hit mesh with a pre-inverted quaternion, so its winding doesn't reliably face the camera; back-facing triangles were culled and nothing stuck. The material is now built by createBakedDecalMaterial() with side:DoubleSide (matching the plane path), so baked and plane render visibly and are finally comparable. (2) Esc wouldn't exit Kami while the ema note was open — it discarded only the note and left the rack up, reading as "Esc broken". Esc now discards the note AND exits Kami in ONE press (matching the ✕ button + the "ESC EXIT" badge). (3) The Kami "N EMA ON THE RACK" tray (#kami-tray) leaked as an empty amber-bordered pill at bottom-center (the "little orange box") because its cssText set display:flex, which overrode the [hidden] attribute — the same ADR-0027 gotcha as #kami-overlay; it now toggles style.display directly. (4) The owner-side dev menu (#torii-dev-menu) is now top-aligned with the vertically-centered emagake (top:27vh) instead of bottom-docked. New tests/sticker-baked-decal-material.test.js (4 locks) + 1 lock in pause-input.test.js; 3802 tests / 313 files.

Prior: v0.2.755-alpha - UPDATE-BUTTON DIAGNOSTIC + KAMI DEV-MENU CUTOFF FIX: (1) The update button stayed hidden despite "Latest v0.2.752" showing. The button gate is `admin && updateAvailable`; version-compare is correct, so the failure is runtime (likely capability probe adminPubkey null OR a timing miss). Added a one-shot console.warn when updateAvailable && !show that dumps {admin, adminPubkey, nostrPubkey, selfUpdate, latest, current} so the next report names the exact cause. (2) Kami dev menu (#torii-dev-menu) was cut off at the bottom: it was docked at top:calc(8vh + 3*(27vh + 10px)) ≈ 89vh, pushing its bottom off-screen on narrow windows. Now bottom-anchored (bottom:14px) with max-height + overflow-y so it never leaves the viewport. No new tests (diagnostic + CSS positioning); 3797 tests / 312 files unchanged.

Prior: v0.2.754-alpha - STICKER FIRING POLISH (muzzle origin + preload): (1) Stickers visibly LEFT THE PLAYER\u0027S FACE instead of the gun. fireStickerAtNpc(aim, ad) passed the AIM (camera) origin as BOTH the ray origin and the sprite spawn point, so the sticker popped out of the camera. Now fireStickerAtNpc(origin, dir, spawnOrigin) targets along the aim ray but spawns the sprite at the gun muzzle (arenaRuntime passes the gun `origin`), matching bullets (spawnBullet(origin, dir)). (2) The first few shots showed the PINK 0xff00ff fallback because _preloadTexture() only ran inside fireStickerAtNpc — the FTFF PNG was still streaming. Moved preload to tickStickerNpc() so it streams on arena entry (guarded, idempotent). Also confirmed via console the raycast crash culprit was Mesh:grass-instanced (procedural InstancedBufferGeometry, no position attribute — now correctly skipped). New tests/sticker-muzzle-origin.test.js (5 locks). 3797 tests / 312 files.

Prior: v0.2.753-alpha - STICKER RAYCAST CRASH FIX ("stickers not working" + arena firing): firing in the NAP zone threw, on EVERY shot, "Uncaught TypeError: Cannot read properties of undefined (reading \u0027getX\u0027)" from Three.js r184 Mesh.raycast → getVertexPosition() → fromBufferAttribute(geometry.attributes.position). A scene Mesh whose geometry has NO `position` attribute made _raycastScene\u0027s intersectObjects() throw mid-fire, aborting fireStickerAtNpc before any sticker could spawn (hence "nothing at all"). Fix (two layers): (1) _getMeshes() now SKIPS any mesh lacking geometry.attributes.position, warning once per offender so the culprit is named for root-cause follow-up; (2) _raycastScene() wraps both intersectObjects calls (static + instanced) in try/catch so a mesh disposed/rebuilt between the 2s cache refills can never sink a fire. New tests/sticker-raycast-position-guard.test.js (4 locks). 3792 tests / 311 files, 21/21 gates.

Prior: v0.2.752-alpha - SELF-VIEW STICKER ORBIT FIX (P-key "beneath ground"): pressing P dropped the orbit camera beneath the terrain/grass. Root cause: stickerSelfView._characterCenter computed the centre as mesh.getWorldPosition().y + (geometry.boundingBox.max.y - min.y)*0.5 — the LOCAL bind-pose Y span. For a Z-up GLB the local Y span is the character DEPTH not height, and for an Armature-scaled rig it is unscaled, so the centre landed at (or under) ground level. Fix: the centre now comes from the SKELETON torso bones (hips→spine, both mixamorig colon and no-colon forms), whose WORLD positions track the animated pose AND the player world transform exactly. Falls back to mesh world origin + 0.9 only for an unrigged/bone-less mesh. New source-level lock tests/sticker-self-view-center.test.js (4 tests). 3788 tests / 310 files, 21/21 gates.

Prior: v0.2.751-alpha - HEARTBEAT SETTINGS FIX + SETTINGS CLOSE-UX + PROFILE FORM REDESIGN. (1) Heartbeat: the Settings > Heartbeat tab showed OFF/red and couldn't be switched on even while the world was live — it rendered the raw client heartbeat status ('idle' → OFF) and its toggle (onPublishNode) ran a client-only publish that never mirrored to the ADR-0094 server beacon, unlike the in-game menu. Fix: extracted a shared _applyHeartbeatToggle(next) (the server-beacon-mirroring path) used by BOTH the menu's onToggleHeartbeat and the settings tab's onPublishNode, and _homepageStubState now applies the server-beacon override (effectiveHeartbeat = _beacon.state.enabled === true ? 'live' : hb) so the tab's status + toggle agree with the menu. (2) Close UX: removed every ✕ close button from the settings panel (top-right ts-close-x) and the Access tab (is-close) — the panel now closes only on backdrop click ('click anywhere outside') and ESC; the dead data-action='close' handler and ts-close-x/is-close CSS were removed. (3) Profile form: each .pf-row is now a two-column grid (label left-aligned in a 160px cell, input/textarea in the adjacent right cell), and inputs use a warm desaturated 'pulp' terracotta tone (complementary to the green success accent) with 12px rounding + roomier padding — replacing the previous UNSTYLED native-white boxes (the .pf-* classes referenced no CSS after the v0.2.739 design-system consolidation orphaned them). New source-level lock tests/main-settings-heartbeat-beacon.test.js (5 tests); the instance-settings close-button assertion now pins no-close-button. 3784 tests / 309 files, 21/21 gates.

Prior: v0.2.750-alpha - NEVER FORCE-UPDATE (ADR-0106): deploys are manual-only. The ADR-0101/0105 auto-deploy-on-tag pipeline is switched off — `.github/workflows/deploy-on-tag.yml` is renamed to `deploy-manual.yml` and re-triggered via `workflow_dispatch` only (a human runs it, optionally naming a tag); `tag-release.yml` (commit → tag) stays automated because tagging is inert and ships nothing. The runtime capability flag `autoUpdate` is renamed `selfUpdate` everywhere (server + client + tests) to drop the misleading 'auto-' — it means 'this host can self-update when the admin opts in', never 'updates by itself'. User-facing copy now reads 'update available', not 'auto-update' (button 'UPDATE AVAILABLE · MANUAL INSTALL', fallback label 'update available — manual install on the host'). The release-metadata safety field `update.autoUpdate` is deliberately KEPT (it asserts a release does NOT auto-update, and validateReleaseMeta rejects autoUpdate:true). Principle documented in torii-quest-strategy.md Core Principles. New source-level lock tests/deploy-manual-trigger.test.js (4 tests). 3779 tests / 308 files, 21/21 gates.

Prior: v0.2.749-alpha - DUPLICATE PLAYER CHARACTER FIX (layer-1 leak): the full player character (render layer 1, normally visible only to the NAP mirror's reflection camera and the P-key self-view) was leaking into first-person view, leaving a full-body duplicate of the player clipped in front of the FPS camera. Fix is a defensive guard in the arena render loop (`src/arenaRuntime.js`): whenever sticker self-view is NOT active, `camera.layers.disable(1)` plus `camera.layers.enable(2)` re-assert the headless first-person body state every frame, so a leaked layer-1 enable (a self-view entry never cleanly exited) can no longer persist. New source-level regression lock `tests/self-view-layer-guard.test.js` (3 tests). 3775 tests / 307 files, 21/21 gates.

Prior: v0.2.746-alpha — Beacon relay coverage refresh (ADR-0104). `DEFAULT_NODE_RELAYS` is now the writable-verified set only. Live WRITE probe (`tools/relay-probe.mjs`, committed) tests each relay with a real kind:30078 event + REQ round-trip using a throwaway keypair. Two entries removed after explicit rejects: `main.relay.gamestr.io` ("blocked: kind 30078 not allowed") and `relay.vertexlab.io` ("unsupported kind: 5312–5315"). Added `relay.damus.io` and `relay.primal.net`. Coverage 5/5. gamestr leaderboard reads (kind-30762) still use `main.relay.gamestr.io` via a separate hardcoded target in `gamestrScore.js` — untouched. No security-model change; the removed relays never accepted our writes in the first place, so no publish surface is lost.

Prior: v0.2.745-alpha — Character Forge accepts the `mixamorig:` colon form of Mixamo bone names (ADR-0103). Fix is a pure `normalizeBoneName(name)` in `src/engine/character/skeleton.js` (fast-path no-op for the no-colon form, maps `mixamorig:X` and `Armature:mixamorig:X` to `mixamorigX`, strips namespaces for non-Mixamo colon forms). `detectConvention` and `mapBonesToRoles` normalize before lookup; the original bone name is preserved in `mapped[role]` so downstream code addresses the real scene node. Sticker-zone raycast inherits the fix (calls `mapBonesToRoles`). Verified against a real 1.1 MB Mixamo GLB — verdict flips from `unknown-convention` to `riggable`. 12 new tests. 3772 tests / 306 files.

Prior: v0.2.744-alpha — Recording-ring toggle (ADR-0100) + auto-deploy pipeline landed (ADR-0101 Accepted). Every future `v*-alpha` tag push auto-deploys via `.github/workflows/deploy-on-tag.yml` — no manual VPS command. Ship reports drop the "VPS deploy pending" line starting v0.2.746. VPS side is a deploy-only ed25519 key locked by authorized_keys forced-command to `/usr/local/bin/torii-deploy-hook`; sudoers narrows ubuntu to 4 verbs; private key lives only in the `TORII_DEPLOY_SSH_KEY` repo secret. Recording-ring toggle: owner-controlled pause for the ADR-0055 1Hz auto-capture, registered as the second row on the ADR-0099 Kami dev-menu shell. 11 new tests. 3763 tests / 306 files.

Prior: v0.2.743-alpha — Kami-mode dev menu (ADR-0099). Owner-only in-world runtime toggles surface on the left edge; first toggle is the sticker RENDER-MODE A/B (was ToriiDebug console-only). Gate enforced in CODE, not just UI. 18 new tests. 3752 tests / 305 files.

Prior: v0.2.742-alpha — Home button client-suspend (ADR-0098).

Single-page onboarding for the next contributor — human or AI agent. Keep it current as the codebase moves. Pre-1.0 alpha; no API/behaviour compatibility promise across versions.

**Runtime direction (2026-09-01):** keep the custom in-quest napplet runtime (`src/engine/napplets/`). The napplet protocol creator confirms there is intentionally no single 'the runtime' — Hyprgate is unreleased, `@kehto/*` are kernel packages, and kehto/paja is only a development runtime — so the guidance is build your own runtime, or use the open runtimes (Myco, Amethyst, Stlstr, Grimoire) as reference. Torii keeps its own engine, aligned to the NAP spec registry (`napplet/naps`). Adopting `@kehto/*` kernel packages is **not recommended now**: kehto/web is TypeScript (Torii is vanilla JS) and ships **no license** (all-rights-reserved by default) — a hard blocker unless they add one.

**Current version:** v0.2.742-alpha - HOME CLIENT-SUSPEND (ADR-0098): pause modal Home button now silences + parks the local client without disposing the world. World is perpetual + server-authoritative, so Home never tears down peers' game. New `src/engine/state/clientSuspended.js` flag gates `playBotShoot` (audio.js) + `applyBotShot` (bots.js); new `stopLoop` export in loop.js halts rAF; new `suspendAudioContext`/`resumeAudioContext` in audio.js park the WebAudio graph; `arenaRuntime.js` gains `leaveToTitle` (wired into `#btn-home` BEFORE the HOME transition) and `resumeFromTitle` (called first from `enter()`). MP socket deliberately stays open (idle `BOT_STATE` only) — dropping it would leave `enter()` with no bots + no peers because `_mp` creation lives inside once-per-page `boot()`. 21 new tests lock loop, flag, guard positions, wiring order, export shape. 3734 tests / 304 files, 21/21 gates.
**Prior:** v0.2.741-alpha - UGC STICKER SYSTEM SLICE 2 (ADR-0090): any-surface stickers + baked/plane A/B renderer. Removes curated EXCLUDE_NAMES (only gun viewmodel excluded), adds InstancedMesh raycast for grass, hybrid renderer picks true DecalGeometry for static faced hits + plane decals for skinned/instanced/fallback. Pure new module src/engine/character/stickerRenderMode.js (15 tests). ToriiDebug.stickers.forcePlaneMode(true|false) flips global A/B at playtest. Slice 3 (MP broadcast) next. 3713 tests / 303 files, 21/21 gates.
**Prior:** v0.2.740-alpha - RECONCILIATION: brings PRs #96 (ADR README rebuild), #97 (eight ADRs Accepted), #98 (handoff ADR baseline), #99 (UGC sticker library slice 1, ADR-0090), and #95 (NIP-60 retirement, cashu.me is the wallet rail) onto the v0.2.739 base. Fixes the orphan-tag drift where v0.2.734–v0.2.739 shipped to VPS as tags but never merged to main. main HEAD now == VPS after this merges + tags + deploys. 3698 tests / 302 files, 21/21 regression gates green.
**Prior:** v0.2.739-alpha - SETTINGS POLISH (ADR-0097): backdrop dim + blur, settings-scoped UI sans font, Character tab renders pre-login preview with disabled actions, new toast module + button micro-interactions wired to profile/relay/heartbeat actions. 3684 tests / 301 files. Scoped to `#torii-settings-backdrop` / `#torii-settings-panel` / `#torii-toast-layer`; game HUD unchanged.
**Prior:** v0.2.726-alpha - CHARACTER FORGE — IN-WORLD SELF-VIEW STICKER PLACEMENT (ADR-0088 follow-up): the input binding now lands. Press `P` in-world to enter a self-view orbit camera around your own character — the direct, usable form of the NAP-zone mirror. Pointer-lock mouse orbits + aims, left-click confirms a live raycast placement, `Esc` cancels. New `src/stickerSelfView.js` (detaches the shared camera, orbits the character centre, swaps the layer mask to show layer 1 + hide the headless layer-2 FP body, drives the pure `stickerPlacementMode` + `stickerRaycast` + `stickerStudio` raycast). Confirmation flows through a new `confirmStickerPlacement` runtime hook → `main.js`, which folds `{hash,zoneId,u,v,rot}` through `addSticker` + the kind-35100 republish. `player.js` hands the camera to the self-view while active (`state.stickerPlacementActive`); the gun viewmodel is hidden; `Esc` cancels instead of pausing. Browser-playtested (enter/aim/cancel) headless-webgl. Visual decal baking on the skin remains a later rendering step.
**Prior:** v0.2.725-alpha - CHARACTER FORGE — IN-WORLD RAYCAST STICKER PLACEMENT (ADR-0088) + LIVE GENERATOR CLIENTS/EXECUTOR (ADR-0089). (1) In-world placement: pure `src/engine/character/stickerRaycast.js` (hit→placement — `boneNames`→zone via `resolveZoneFromBoneNames`, geometry u/v clamped, `rot` from surface-normal azimuth) + `src/engine/character/stickerPlacementMode.js` (inert enter→aim→confirm/cancel state machine; `aim` combines the resolved hit with the selected `STICKER_LIBRARY` hash — the raycast supplies position, the library selection supplies identity — and `placementToManifest` folds a confirmed draft through `addSticker`). The live THREE raycast of the player's OWN character is isolated in `src/stickerStudio.js` (reuses the proven stickerNpc.js SkinnedMesh path — recompute bounding sphere + skeleton.update → skinIndex/skinWeight bones + geometry UV — against `playerModel.getPlayerModelRoot()`, layer 1, mirror view), deliberately NOT SDK-exported. (2) Live generation: pure `src/engine/character/meshGenerationClient.js` (`buildBackendRequest`/`normalizeBackendResponse` + `createGeneratorClient({fetcher})` — INERT, never fetches without an injected fetcher) + `src/engine/character/meshGenerationExecutor.js` (client → `validateGeneratedMesh` gate → injected routstr/Cashu `charge` → plan; never signs/publishes/seats). No live network or payment is possible by default.
**Prior:** v0.2.724-alpha - CHARACTER FORGE — STICKER PLACEMENT UI + VALIDATOR-GATED EXTERNAL MESH GENERATION (ADR-0086 + ADR-0087): the two remaining Character Forge slices land together. (1) Sticker placement: pure `src/engine/character/stickerPlacement.js` (body-zone registry `STICKER_ZONES` + bone→zone resolution + immutable `addSticker`/`removeSticker`/`updateSticker` manifest ops with a `MAX_STICKERS` cap) plus a sticker editor in the Character tab — list/remove stickers, add one from a curated `STICKER_LIBRARY` (its decal reuses the shipped ftff-sticker.png so the hash is real + Blossom-resolvable), each edit re-signing + republishing the kind-35100 event. (2) External mesh generation: pure `src/engine/character/meshGeneration.js` — a swappable `GENERATION_BACKENDS` registry (Meshy/Tripo/Hunyuan3D), a prompt-bounded request + INERT plan (`performed:false`), and the `validateGeneratedMesh()` GATE that accepts a generated mesh ONLY when the manifest validates AND the rig is auto-riggable (composes `validateCharacterManifest` + `assessRig`). The live generator clients + routstr/Cashu executor are a later host slice.
**Prior:** v0.2.722-alpha - CHARACTER FORGE — PEER-AVATAR MESH LOADING (ADR-0082): peers now see each other's custom mesh. The player's mesh hash (from the kind-35100 event) is broadcast through the MP `character` field (now accepts a 64-hex sha256 alongside the skin keys), the server relays it, and the peer loader resolves the hash to a Blossom URL and loads that GLB instead of the built-in default (`_peerMeshUrl()` in arenaRuntime.js). The player path (self) + peer path now both load custom meshes — the full automatic mesh-loading loop is closed. Sticker placement UI + external mesh generation remain.
**Prior:** v0.2.721-alpha - CHARACTER FORGE — AUTOMATIC MESH LOADING (player path) + CUSTOM MESH UPLOAD (ADR-0082): the player's own character mesh now actually LOADS. At login the game fetches the npub's kind-35100 event, resolves its mesh hash to a Blossom URL (`src/engine/character/characterMesh.js`), and seats it before arena boot so `loadPlayerModel()` fetches the custom mesh instead of the built-in default (`setCustomMeshUrl()` in playerModel.js). The Character tab also gained an "Upload custom mesh" button — upload a .glb to Blossom (NIP-98) and publish the character event in one flow. The peer-avatar path (broadcasting each peer's mesh hash through the MP protocol) is the remaining slice.
**Prior:** v0.2.720-alpha - CHARACTER FORGE — CREATE ROUND-TRIP (ADR-0082): the Character tab now WRITES a character, not just reads one. Curated presets (`src/engine/character/characterPresets.js` — Chiefmonkey + Nostrich, content-addressed mesh hashes) feed a preset picker; picking one builds a `torii.character` manifest, signs the kind-35100 event via NIP-07, and publishes to the unified relay list (`publishCharacter()` in nostr.js, mirroring publishAccessSettings). Also added the Blossom upload path (`uploadBlossom()` + `buildBlossomAuthEvent()` — NIP-96/NIP-98) so a custom mesh GLB can be uploaded and its content-addressed sha256 slotted into the manifest. Sticker placement UI + external mesh generation remain follow-up slices.
**Prior:** v0.2.719-alpha - CHARACTER FORGE — LIVE RELAY READ (ADR-0082 follow-up): the Character settings tab now checks the logged-in npub's kind-35100 character event over a live relay read and seats an existing character automatically (the "smooth experience" seam — no prompt, no re-upload). New pure `src/engine/character/characterRelayRead.js` (filter builder + read/reduce + newest-per-author) + `fetchOwnCharacter()` in nostr.js (reuses fanoutReq). The create round-trip (writing a new character event) is the next slice.
**Prior:** v0.2.718-alpha - CHARACTER FORGE GROUNDWORK (ADR-0082): validator-first character pipeline — no in-house generation model. New pure `src/engine/character/` modules: `skeleton.js` (canonical role-based skeleton contract + Mixamo/Biped bone maps + axis detection), `rigAssessment.js` (auto-rig verdict ladder), `characterManifest.js` (`torii.character` manifest validator), `characterEvent.js` (kind-35100 character-event parser — the "smooth experience" seam). New "Character" settings tab (`src/engine/settings/characterForgePanel.js`, registered in main.js + settingsPanel.js). SDK exposure at the experimental tier. Live relay read/create round-trip is a follow-up slice.
**Post-v0.2.717 (main, untagged, PR #87):** ADR-0084 + ADR-0085 arena + sticker studio napplet registrations (wiring-only). Factories in `src/engine/napplets/arenaNappletRegistration.js` (dTag `torii-arena`, over `createGameHandlers`) and `src/engine/napplets/stickerStudioNappletRegistration.js` (dTag `sticker-studio`, over `createAvatarHandlers`, declares `requires: ['torii-avatar-write']` gated in code). Napplets never sign, never touch relays - the shell brokers publish. `NAPPLET_IDENTITY` uses the `@v0-wiring` placeholder that ADR-0092 will replace with real bundle hashes at release. 21 new tests / +2 test files; curated count 279 -> 281 across `toriiQuestDashboardData.js`, `mvpReadiness.js`, `NEXT_ACTION_STATE.json`. Full suite 3508 green.
**Post-v0.2.717 (main, untagged, PR #84):** ADR-0082 + ADR-0083 napplet game host + avatar shell scaffolds. Added `GAME_NAMESPACE` + `AVATAR_NAMESPACE` to `nappletEnvelope.js`; new files under `src/engine/napplets/`: `gameNappletSrcdoc.js`, `gameNappletHandlers.js`, `NappletGameHost.js`, `avatarNappletSrcdoc.js`, `avatarNappletHandlers.js`. Async handler protocol (`{ __async, promise }`) so relay-mediated actions stay shell-owned. Trust boundary unchanged across all three shells (product / game / avatar): `sandbox="allow-scripts"` only, `MessageEvent.source` validated (never `event.origin`), per-mount channelId nonce, CSP `default-src 'none'; connect-src 'none'; img-src 'none'`. 35 new tests. Curated count 276 -> 279.
**Prior:** v0.2.717-alpha - SETTINGS PANEL SMOKED GLASS + PROFILE PREFILL: removed the full-screen dark backdrop behind the settings panel (it now sits on a transparent click-catcher) and gave the panel a smoked-glass look (semi-transparent + backdrop blur + shadow). The Profile tab now pre-fills from the logged-in user's published kind:0 Nostr profile (display name, bio, avatar, website, NIP-05, lightning address) when the local draft is empty — via a new fetchOwnProfile() in nostr.js.
**Prior:** v0.2.716-alpha - RELAY TAB POLISH: removed the 'STARTER' badge from the Relay settings tab header (it overlapped the panel's top-right close button) and gave every relay row a Remove button — previously the curated starter relays rendered as read-only rows with a 'starter' tag and no delete. Removing a starter relay now materialises the remaining defaults as the operator's explicit config (remove-relay reads the effective list, not the raw stored string).
**Prior:** v0.2.715-alpha - SINGLE UNIFIED RELAY LIST (ADR-0081): one relay list for the whole game. The separate hardcoded read-path RELAYS (nos.lol + vertexlab.io) in nostr.js is removed; profile/login/leaderboard/discovery reads AND presence/heartbeat publish now all use the single owner-editable list (localStorage torii.node.relays + <meta name=torii-relays>) with a curated 5-relay default (gamestr.io, plebeian.market, routstr.com, nos.lol, vertexlab.io). Per-action publish consent (heartbeat signer prompt, gamestr toggle, access-settings save) is unchanged — the list decides WHERE, never WHETHER.
**Prior:** v0.2.712-alpha - RESTORE ACCESS SETTINGS TAB (ADR-0078): re-surfaced the instanceSettings admin surface (arrival authority + write authority, backed by the signed kind:30078) as a fifth "Access" tab in the settings panel — it was hidden since v0.2.676 but the underlying enforcement (handoffArrival.js / writeAuthority.js) stayed live the whole time. Restored the v0.2.400 read/save relay round-trips in main.js (readLatestAccessSettings / publishAccessSettings) + the _instanceSettingsState / _refreshInstanceSettingsAccessState / _saveInstanceSettingsAccess functions recovered verbatim from the v0.2.676-predecessor commit. The Access-tab renderer filters the model's sections to only 'access' so the inert placeholder sections ('multiplayer' / 'more' "More coming soon") are NOT surfaced — only live, useful controls. Wired delegated change (radio → draft) + submit (data-form=access-settings → _saveInstanceSettingsAccess) listeners on #torii-settings-content. settingsPanel TABS is now [profile, gateway, heartbeat, relay, access] (getSettingsTabIds() exposed for testability). No new sign/publish path (reuses nostr.js signEvent / fanoutPublish — same as heartbeat + profile tabs); enforcement unchanged + cannot be weakened by the edit surface (verify-on-read gate intact). Deferred (need core refactors, not fake tabs): Audio (no master gain API), Graphics (no manual override), Controls (no keybind layer). 3449 tests / 275 files, 21/21 gates.
**Prior:** v0.2.711-alpha - HEARTBEAT AUTO-ON + RELAY CLEANUP (ADR-0076 update + ADR-0077): heartbeat is now ON by default and auto-fires the first presence publish when the owner logs in (login click authorises the NIP-07 signer prompt — approving it IS the consent; a rejection pauses so it never nags). Removed the manual-first-publish gate from _heartbeatTick (isHeartbeatDue contract unchanged). Added relay.routstr.com to DEFAULT_NODE_RELAYS (now gamestr + plebeian + routstr, all Torii-ecosystem, WS-verified live). Dropped the 503ing relay.damus.io from the read path (nostr.js RELAYS now nos.lol + relay.vertexlab.io) — fixes the connection-failed console noise. CSP updated (dropped damus, added routstr + vertexlab). Fixed bump-ver.sh (was missing toriiQuestDashboardData.js + torii-quest-dashboard tests — VERSION constant stuck at 709). Fixed regression-check.mjs EXPECTED_VERSION to read dynamically from package.json (was hardcoded, drifted since 710). 3445 tests / 274 files, 21/21 gates.
**Prior:** v0.2.709-alpha - VERSION FIX: arena multiplayer server SERVER_VERSION constant was hardcoded and never bumped (ADR-0075). Root cause found when Bekka's arena HELLO reported v0.2.602-alpha though her release was v0.2.706-alpha. Initially diagnosed as a stale build; on checking the source, server/arena-ws.js line 69 has `const SERVER_VERSION = 'v0.2.602-alpha'` as a hardcoded literal — bump-ver.sh never touched server/arena-ws.js, so the constant was stuck at 602 across tags 706/707/708 (chiefmonkey's own instance had the same bug). Fix: (1) added server/arena-ws.js to bump-ver.sh's sed file list so the constant is bumped on every future release; (2) bumped SERVER_VERSION to v0.2.709-alpha to match the release. Bekka (and any operator) can now pull v0.2.709-alpha, rebuild, and the arena HELLO will report the correct version. 3445 tests / 274 files, 21/21 gates.
**Prior:** v0.2.706-alpha - ADMIN GREETING COLOUR SPLIT (ADR-0072, supersedes ADR-0071 colour claim): only the owner's name is now orange (#f7931a); 'Welcome ', ',', and 'you are ' are the neutral caption tone (#d8c3ac); 'logged in' is green (#6DAA45) to balance the orange name; the green dot stays green. Line 1 is rebuilt via the DOM API (createElement + textContent per span, no innerHTML) so a Nostr display name can never inject markup; badge text split into a neutral 'you are ' span + a green 'logged in' span. .toc-greet no longer sets orange (only .toc-name does). Same isAdminOperator() gate, display:none fail-closed, no pubkey leak. 3445 tests / 274 files, 21/21 gates.
**Prior:** v0.2.705-alpha - ADMIN LOGGED-IN CAPTION REWORDED TO A GREETING (ADR-0071): when the admin is logged in, the top-left caption now reads "Welcome <owner name>," on line 1 (prominent 13px orange, sentence-case via a new .toc-line1.toc-greet class that overrides the 9px uppercase label style) and a green dot + "you are logged in" on line 2 (badge text changed from "logged in"; .toc-loggedin text-transform set to none so it renders in the quoted sentence case, not uppercase). The name span (#torii-owner-name) is hidden on line 2 for the admin (the name moved into the greeting); non-admin/anonymous visitors see the unchanged "This torii belongs to / <name>" caption. Same isAdminOperator() gate as ADR-0070, no new auth, no pubkey leak; greeting + badge swap atomically in one _refreshOwnerLabel call (no flicker). 3445 tests green / 274 files, 21/21 regression gates.
**Prior:** v0.2.704-alpha - ADMIN-ONLY "LOGGED IN" BADGE ON THE OWNER CAPTION (ADR-0070): a small green pulsing dot + the text "logged in" now appears to the right of the owner's name on the top-left homescreen caption ("This torii belongs to"), shown ONLY when the logged-in viewer IS the configured instance owner. Reuses the existing isAdminOperator(viewer, adminPubkey) check (no new auth/fetch); the name was split into its own truncating span (#torii-owner-name) so the badge sits to its right without being clipped by the ellipsis; the badge is display:none by default and only .show when login is confirmed, so it never flashes before login and never appears for non-owners/anonymous; never surfaces the pubkey (toggles a CSS class only). The owner's name remains visible to all visitors, unchanged. New test file main-owner-loggedin-badge.test.js (6 source-level wiring assertions). 3445 tests green / 274 files, 21/21 regression gates.
**Prior:** v0.2.703-alpha - PAUSE MODAL RECOLORED TO TORII SUNSET THEME (ADR-0069): the pause modal shown after ESC releases pointer lock and transitions to PAUSED (#pause-overlay / #pause-box / #pause-title in index.html) was still on the legacy dark-neon-purple palette (rgba(20,20,35,0.97) panel + rgba(139,92,246,0.4) purple border + rgba(0,0,0,0.6) backdrop). Recolored to the torii-gate sunset theme: warm backdrop rgba(20,13,9,0.7), warm panel rgba(30,18,12,0.97) (warm twin of the old purple at the same opacity so the paused game stays blocked out) with var(--panel-glass-border) bronze border + a subtle depth box-shadow, and the PAUSED title in var(--accent) #e8842c with a warm glow. Layout, sizing, buttons (.btn / .btn-secondary already themed), the .show toggle, focus/pointer-lock behavior, and the resume flow are unchanged — color-only migration, no DOM/JS/asset change. 3439 tests green / 273 files, 21/21 regression gates.
**Prior:** v0.2.702-alpha - BOOT OVERLAY RECOLORED TO TORII SUNSET THEME (ADR-0068): the full-screen loading overlay shown after Nostr login + ENTER TORII (#boot-overlay in index.html) was the last player-facing surface still on the legacy dark-neon-purple palette (#140a28 / #b794f4 / #8b5cf6). Recolored to the canonical torii-gate sunset theme via the existing :root tokens (--accent #e8842c, --accent-soft #c9793a, --orange #f7931a, --panel-text, --panel-text-dim, --muted): warm radial background (#2a1810→#140d09→#0a0604), accent-orange title with warm glow, bronze→orange progress bar with a soft orange box-shadow, and warm-dim status/sub text. Layout, sizing, spacing, and the eased progress animation are unchanged — color-only migration, no DOM/JS/asset change. 3439 tests green / 273 files, 21/21 regression gates.
**Prior:** v0.2.701-alpha - HOMEPAGE OWNER CAPTION NOW SHOWS THE ADMIN'S REAL NOSTR NAME TO ALL VISITORS: the top-right "This torii belongs to" caption previously fell back to a shortened npub for every visitor except the owner viewing their own signed-in browser (which could resolve the name locally from their own profile state). Now any visitor — logged in or not — sees the admin's real published Nostr display name. New `fetchOwnerProfileName(pubkey)` in `src/nostr.js` — a pure, read-only relay fetch (kind:0, via `fanoutReq` + `readProfiles`) with a 5-minute in-memory TTL cache, validated against a strict 64-char lowercase-hex pubkey regex, and never mutates the viewer's own `state.nostrName`/`nostrAvatar`. `resolveToriiOwnerLabel()` (`src/engine/identity/toriiOwnerLabel.js`) gains a new tier-2 `ownerProfileName` param, giving a 4-tier priority: owner's own in-progress draft → owner's published Nostr name (NEW, visible to all) → shortened npub → "not yet claimed". Wired into `main.js`'s `_refreshOwnerLabel()` via a guarded, once-per-pubkey `_fetchOwnerProfileNameOnce()` that repaints the caption when the fetch resolves. Update button's functional deploy-trigger wiring confirmed untouched (regression-locked by a new source-level test). New tests: 6 added to `torii-owner-label.test.js` (12 total), new `nostr-fetch-owner-profile-name.test.js` (9 tests), new `main-owner-profile-name-wiring.test.js` (6 tests). 3439 tests green / 273 files, 21/21 regression gates.

**Prior:** v0.2.700-alpha - HOMEPAGE OWNER CAPTION + VERSION PANEL RESTYLE: moved the top-left Torii icon/menu button to the bottom-left, added a new top-left "This torii belongs to: <name>" caption (owner's displayName/name when the viewer is the admin, else a shortened npub, or "not yet claimed" pre-configuration) via the new pure `resolveToriiOwnerLabel()` helper (`src/engine/identity/toriiOwnerLabel.js`, 7 unit tests), wired to refresh on capability probes, NOSTR_LOGIN, and profile-save. Moved the version/update-check panel to the bottom-right and re-themed it from blue/navy to the bronze/orange smoked-glass palette (border, background, backdrop-filter, text tones all matched to brand; the on-brand orange "new version" highlight left untouched). Found and fixed a real CSS bug during verification: `#title-centre.glass-panel`'s `backdrop-filter` was silently creating a new containing block for the panel's `position: fixed`, anchoring it to the console instead of the viewport corner — fixed by lifting `#update-preview` out of that subtree to sit as a direct child of `#screen-title` (documented inline for future maintainers). Also fixed a long-displayName 2-line-wrap collision with the TORII wordmark by switching the caption to single-line ellipsis truncation with a `title` attribute for the full name on hover/long-press. Confirmed via screenshots at 1440×900 and 375×812 (iPhone SE) with both short and long names. Update button's functional deploy-trigger wiring untouched — visual/positional CSS only. Also merged in the previously-tagged-but-unmerged v0.2.699-alpha (ADR-0067 relay trim) commit onto this branch. 3419 tests green / 271 files, 21/21 regression gates.

**Prior:** v0.2.699-alpha - RELAY TRIM (ADR-0067): fixed repeated browser-console WebSocket errors on relay.damus.io/relay.nostr.band by testing every relay in GAMESTR_RELAYS (leaderboard, src/engine/gamestr/gamestrScore.js) and nostr.js RELAYS (profile reads) with the EXACT queries the game sends, from both the sandbox and the live VPS. relay.nostr.band timed out (~8s, down); relay.damus.io 503'd the leaderboard REQ; relay.primal.net rejected the #game tag filter with a NOTICE. Trimmed GAMESTR_RELAYS from 5 relays to 2 (main.relay.gamestr.io + nos.lol — the only ones that actually work for this query); trimmed nostr.js RELAYS from 4 to 2 (relay.damus.io + nos.lol, both verified against a real kind:0 query). Fixed a latent CSP bug in tools/csp.mjs: main.relay.gamestr.io — the leaderboard's own authoritative relay — was never in connect-src; now included alongside both plebeian relays (marketStall.js uses staging + prod). PLEBEIAN_RELAYS (config.js, still staging) intentionally untouched — marketplace concern, out of scope, would risk the current test auction. torii.quest confirmed NOT a relay and not the user's domain (unrelated site, SSL fails) — no relay exists there. gamestr.io confirmed as a NIP-133-descended Nostr leaderboard protocol; main.relay.gamestr.io is its authoritative relay and was already in use. Updated the GAMESTR_RELAYS source-contract test to assert the exact trimmed list instead of a loose ≥ 4 lower bound. Repo-only change — no VPS deploy. 3412 tests green / 270 files, 21/21 regression gates.
**Prior:** v0.2.698-alpha - FIRST-LOAD HEARTBEAT CONSENT FIX (default-ON now actually reaches NIP-07 consent): a fresh install ships the heartbeat toggle default-ON (`adminPrefs.getHeartbeatIntent()` defaults to `'on'`), but three call sites derived "is the switch ON" from that raw stored *intent* string instead of from whether a publish had actually happened — so a brand-new owner's very first click read intent as already `'on'`, flipped it straight to `'off'`, and showed "Heartbeat OFF" with no NIP-07 signer prompt ever reachable via the UI. Root cause: intent (`'on'`/`'off'`, what the owner *wants*) was conflated with broadcast state (`'off'|'idle'|'publishing'|'live'|'stale'|'blocked:*'|'failed:*'|'paused:wallet-requires-approval'`, what is *actually happening*) — idle was being read as ON. Fix: new shared pure helper `isHeartbeatBroadcasting(status)` in `src/engine/presence/heartbeat.js` (true only for `'live'`/`'stale'`/`'publishing'`/`'paused:wallet-requires-approval'`; false for `'off'`/`'idle'`/any `'blocked:*'`/`'failed:*'`/non-string) now drives all three call sites: `heartbeatPanel.js`'s `_isOnState()` (idle-state publish label reworded to "Publish my node presence (click to give signer consent + go live)"), `toriiMenu.js`'s `hbOn` derivation, and `main.js`'s `onPublishNode` toggle-direction logic (was inverting off raw intent — `getHeartbeatIntent() === 'on' ? 'off' : 'on'` — now correctly checks `isHeartbeatBroadcasting()` so the first click on a fresh install publishes and requests NIP-07 consent instead of silently flipping to off). `homepageStub.js`'s own Publish card confirmed dead code (unreachable via any live click path) and left untouched. New tests: `isHeartbeatBroadcasting` coverage in `heartbeat.test.js` (all statuses + non-string fail-closed + cross-checks against `heartbeatStatus()`); new `heartbeatPanel.test.js` (hb-switch class/aria-checked per status, idle label wording, owner-gating); new source-level regression locks `torii-menu-heartbeat-toggle.test.js` + `main-heartbeat-consent.test.js` (toriiMenu.js/main.js have no DOM test harness in this repo, so these assert via readFileSync + regex that the shared helper is imported and used at each call site). 3412 full suite green / 270 files, 21/21 regression gates green.
**Prior:** v0.2.697-alpha - ONE-COMMAND DOCKER INSTALLER: new self-contained `install.sh` (+ `install/lib/ui.sh`, `install/lib/run.sh`) gives a fresh `git clone` of this repo a single `sudo ./install.sh` path to a live instance — Docker install if missing, prompts for domain/email/admin npub, builds + launches the compose stack, verifies over loopback, colourful boxed summary. Multiplayer (`arena-ws`) ships ON by default alongside the game and Nostr relay (`strfry`), matching the standing MP-is-integral decision; owners can reconfigure/duplicate/remove any compose service afterwards. `Caddyfile`+`Dockerfile` reworked so the CSP header is extracted from the real per-build `dist/_headers` at image-build time instead of a hand-copied static value (kills a class of drift — `VPS_INSTALL.md`'s three copy-pasted CSP blocks, found stale with a dead `'strict-dynamic'` directive during this pass, now point at the same live-extraction command instead of a hardcoded string). Fixed a real bash pitfall in `ui_ask`/`ui_confirm`: reopening `/dev/stdin` via redirect for each prompt could read-ahead the whole pipe buffer on some bash builds and flush the unconsumed remainder as literal NUL bytes on exit — now reads the inherited stdin fd directly when no controlling TTY is present, with a real write-probe (not just permission bits) to detect TTY availability; also fixed `run.sh`'s log-dir bootstrap to fall back to a per-user tmp dir instead of crashing with a raw `mkdir: Permission denied` before the friendly non-root message can print. VPS_INSTALL.md gains a new quick-start section pointing at the installer; the manual bare-metal/systemd path stays as documented fallback. 3380 tests green / 267 files, 21/21 regression gates.
**Prior:** v0.2.695-alpha - MINIMAP REMOVED + SINGLE-K KAMI ENTRY (ADR-0064): (a) removed the in-game minimap entirely — the `#minimap` canvas was a sibling of the `#minimap-wrap` positioning div (not a child), so it fell to the top-left of the HUD instead of the intended bottom-right; since a minimap wasn't wanted, it (canvas + wrapper + `drawMinimap` + its frame-tick) was deleted outright rather than repositioned; (b) a single bare K now enters Kami Mode AND opens the ema note input in one press (was a 2-step "K to enter, K again to note" flow per ADR-0029) — `openNote()` already entered Kami Mode internally, so the K handler now calls it directly; Enter/Esc on the note no longer re-acquire pointer lock, leaving the mouse free to click the emagake rack. 3380 tests green / 267 files, 21/21 regression gates.
Prior: v0.2.692-alpha - HOMEPAGE PANEL: REAL SMOKED GLASS RESTORED WITH TRUE EDGE FADE: brings back genuine backdrop-blur "smoked glass" behind the main homepage panel's text (v0.2.684-alpha had removed all blur to kill a hard rectangular edge, leaving only a flat tint — visually correct fade but no actual glass effect). Root cause confirmed via isolated Chromium tests: a single backdrop-filter blur on a masked box always shows a hard blur-sampling edge, because the browser rasterizes the blurred backdrop over the element's full box BEFORE the mask is composited — no amount of mask feathering hides the blurred/unblurred pixel boundary. Fix: stack four concentric ring layers (gp-ring1..4) with decreasing blur radius (16px → 10px → 5px → 2px) and radial masks staggered further outward on each ring, so the transition from fully-blurred to unblurred happens gradually across several soft bands instead of one cliff; a flat-tint top layer (gp-tint, no blur) then carries the visible fill the rest of the way to true 0% opacity at 92% radius. Verified against the real homepage background (not just synthetic test patterns): the torii-gate silhouette is visibly softened/blurred behind the panel text, and the panel edge fades out with no visible boundary line. Buttons and interactive content remain on top and fully clickable (gp-content at z-index 1 above the ring/tint layers, which are pointer-events:none). Zero gameplay-file overlap. 3363 full suite green / 264 files (5 skipped), 21/21 regression gates green.
Prior: v0.2.685-alpha - ADR-0059 AUCTION PANEL HEADER HARDENING: eliminate every innerHTML write in auctionPanel.js — the header (chips + high-bid) and the legacy fallback bid-list body now render via createElement + textContent, so no relay/profile-derived string (auction_type, currency, bidder name, picture URL) ever reaches innerHTML. renderChips → buildChips (returns [{cls,text}] data), renderBidRow → buildBidRow (returns a plain descriptor); the renderer builds DOM nodes. New malicious-input test proves auction_type/currency/profile-name render as inert text (no element injection). 3368 full suite green / 265 files.
Prior: v0.2.684-alpha - SETTINGS PANEL: PROFILE + RELAY TABS, HOMEPAGE FADE, TOP-RIGHT CLOSE (non-gameplay UI/UX pass, six-item polish request): (1) the homepage centre panel's bounding box corners are now fully rounded and the smoked-glass background fades to true 100% transparency at the edges with no hard edge (radial mask-image tuned to a soft feathered stop). (2) new Relay tab (relayPanel.js, pure renderer) lets the owner view/add/remove the wss:// relays their node publishes heartbeat presence to — list rows with a live-dot + URL + Remove button, a comma/newline-separated add/edit textarea, Save Relays button, and an explicit "never falls back to public relays" note; backed by existing nodeRelays getNodeRelays()/setNodeRelays() storage. (3) Heartbeat toggle now defaults to on (adminPrefs.getHeartbeatIntent() default 'on') with an intuitive red/green switch (hb-switch, red track+OFF label when off, green track+ON label + knob slid right when on) instead of a plain checkbox. (4) removed the stale "To visit another world, use the Torii Gateway inside the NAP zone." copy from the Gateway Setup tab (node travel has gone through the in-world NAP-zone Gateway since v0.2.669-alpha; the tab no longer needed to say so). (5) the admin settings panel's bottom-left Close button is replaced by a standard top-right ✕ button (.ts-close-x, circular, top:10px/right:10px) on every tab. (6) new Profile tab (profilePanel.js, first/top nav item) exposes the standard Nostr kind:0 profile fields an owner needs to customise their Quest installation — Display name, Bio, Avatar URL, Website, NIP-05, Lightning address — with a login gate ("Log in with Nostr to customise your profile.") when signed out, a localStorage draft (profileDraft.js, key torii.profile.draft) so in-progress edits survive a reload, and Save Profile building an unsigned kind:0 event (profileMetadata.js buildProfileMetadataEvent, validates pubkey + sanitizes every field) that is signed via the existing NIP-07 signer and published through the existing fanoutPublish/RELAYS path (states: saved-local → publishing → published/failed). New modules: relayPanel.js, profilePanel.js, profileMetadata.js, profileDraft.js. Settings panel TABS reordered to [Profile, Gateway Setup, Heartbeat, Relay] with Profile as the new default active tab. Zero gameplay-file overlap (bots.js/botModel.js/bot-tactics.js/botNetState.js/botSim.js/physics.js/player.js/weapons.js/scene.js/hud.js/input.js/lod.js/multiplayerHost.js/wireProtocol.js/arenaRuntime.js/server/arena-ws.js/server/bots/botStateGate.js all untouched). 3361 full suite green / 264 files (5 skipped), 21/21 regression gates green. Prior: v0.2.683-alpha - ADR-0058 PRODUCT PANEL NAPPLET CONVERSION: first LIVE nap-torii-world napplet. Shell (marketStall.js) keeps the Plebeian relay subscription + brokers serialized auction snapshots INTO a sandboxed iframe; the napplet renders the bid-list body (DOM nodes + textContent, never innerHTML of untrusted data) while the shell's legacy renderer keeps the panel header. New modules: productNappletSnapshot.js (pure serializeBidList → plain JSON, caps 64, strips Maps/images/functions), productPanelNappletSrcdoc.js (self-contained renderer), productNappletHost.js (browser DI mount/push/destroy/fallback). Updated nappletSrcdoc.js (parent source check, per-mount channelId nonce validated+stamped, world.on/off shell→napplet channel, CSP connect-src 'none' img-src 'none'), NappletSurface.js (nonce + post()), marketStall.js (mount+push, legacy skipBody fallback), worldNappletSurfaceConfig.js (product-stall-panel enabled:true, allowedEmitKinds:[] read-only), worldNappletHandlers.js (world.emit returns accepted:false 'no-handler'), auctionPanel.js (skipBody). Built-in LOCAL identity (NIP-5A deferred). New+updated tests across 4 files; 3366 full suite green / 265 files. Prior: v0.2.682-alpha - ADR-0057 NAPPLET WORLD-SURFACE SHELL SCAFFOLD. Six modules under src/engine/napplets/: nappletEnvelope (pure NIP-5D envelope validate/result/error + splitType; unknown types silently ignored), nappletIdentity (pure (dTag, aggregateHash)), nappletSrcdoc (pure buildWorldSrcdoc() bootstraps window.napplet.world with the six v0 methods, never reads parent.document), worldNappletHandlers (dispatcher: world.attach.get + world.zone.list + allow-listed world.emit; 'unsupported' for pose.*/visit), worldNappletSurfaceConfig (frozen per-surface config keyed by existing proof surface ids; enabled:false; does NOT mutate proofSurfaceSpecs.js), NappletSurface (browser-only DI: iframe sandbox EXACTLY allow-scripts no allow-same-origin, binds contentWindow to identity, validates every inbound message by MessageEvent.source NOT event.origin, destroy() cleanup). DOM-overlay iframe (NOT iframe-to-texture/WebXR). Game host DEFERRED to ADR-0058+. No live relay/Blossom/signing/wallet/travel — test-only/dormant. 39 new tests + 3338 full suite green / 263 files. Prior: v0.2.681-alpha - ADR-0056 HOTFIX (time base + ring count + error honesty): four correctness bugs caught on advisor review of v0.2.680. (1) the indicator used performance.now() for its age math but the autocap report stores Date.now() wall-clock — the 'last Xs' age + recentOk window were wrong; fixed to Date.now(). (2) the capture tick still ran AFTER renderFrame so the JPEG drained a frame late vs the snapshot — moved BEFORE renderFrame. (3) the ring count used capture ATTEMPTS (captured) instead of stored uploads (uploaded) — fixed to uploaded so the count reflects the real VPS ring. (4) the error state had a 60s grace window that hid a current failure — since markUploaded clears lastError, any lastError present is a current unresolved failure; flattened to isError = !!lastError. markUploaded/markFailed now stamp completion-time Date.now(), not the captured request time. 3299 full suite green. Prior: v0.2.680-alpha - ADR-0056 RECORDING INDICATOR + DASHBOARD SURFACING: a live amber glowing 'RECORDING' HUD overlay (recIndicator.js) renders top-right in the arena for the owner while the ADR-0055 1Hz auto-capture ring is live. Owner-gated (not debug-gated) so the owner sees the ring is recording; non-owners never see it. Pure CSS glow animation (rAF only throttles text/state, ~250ms — near-zero cost); prefers-reduced-motion disables the pulse. Honest states: RECORDING (amber) when active/recent/inflight, REC ERROR (red) when lastError set + no upload since, with ring n/120 + last-capture age as a secondary line. Zero cost when off — no DOM until the owner plays + capability resolved. The static oversight dashboard gains a 'Live diagnostics' section documenting the autocap ring (cap 120, path, separation, gate, retrieval CLI) — static/read-only, NO network calls added. 11 new tests + 3298 full suite green. Prior: v0.2.679-alpha - ADR-0055 HOTFIX (owner gate + URL + bounded ring + timing): the first pass had four real bugs caught on advisor review. (1) POST URL was /mp/kami/autocap but resolveMpHttpBase() already returns the /mp base — fixed to /kami/autocap (matches the manual ema route). (2) the client owner gate was just 'has a hex pubkey' so any logged-in visitor would fire 1Hz captures that 403'd — now uses the real admin capability check (fetchCapability + isAdminOperator), resolved once + cached, so non-admins never capture. (3) the autocap index was append-only jsonl (unbounded at 1Hz) — redesigned to ONE {ema,shot} JSON file per frame, ring-culled to 120 (a TRUE ring: disk strictly bounded). (4) the capture tick ran AFTER renderFrame so the queued JPEG drained a frame late vs the snapshot — moved BEFORE renderFrame so they align in the same render tick; timestamps now use Date.now() (wall-clock) for server-log correlation. Dump CLI updated to read the one-file-per-frame ring. 3287 full suite green. Prior: v0.2.678-alpha - ADR-0055 EMA AUTO-CAPTURE DIAGNOSTIC (1Hz rolling ring): a pure 1Hz auto-capture state machine (kamiAutoCapture.js) grabs a canvas frame + a full debug snapshot every second while the owner plays, seals them to the owner+Kami pubkey (same split-seal as the manual ema), and POSTs to a SEPARATE autocap ring (/var/lib/torii-quest/kami/autocap, cap 120) so it never evicts a real manual ema screenshot (shots/ cap 420) and never appends to ema.jsonl (forever). Hooks the in-arena rAF loop (no new timers). Owner-only + playing-only + inflight backpressure. The manual ema snapshot now reports the ring status so a hung ema points at nearby frames. New retrieval CLI (tools/kami-autocap-dump.mjs) decrypts the ring with KAMI_PRIV into JPEG frames + timeline.jsonl, optionally stitching an MP4 via ffmpeg. Catches the transient phantoms + desync the manual K-press always missed. 29 new tests + 3287 full suite green, build clean, check ALL GREEN. No gameplay change - diagnostic capture only. Prior: v0.2.677-alpha - ADR-0053 + ADR-0054 GATEWAY FIXES (live directory + in-place smoked-glass panel): the gateway directory was polluted with 48 stale records (38-56 days old) plus travel handshake (req/res) noise, so no live instance was visible. ADR-0053: readGateways now drops handshake records (state tag) + expired (NIP-40) + stale no-expiration records (1h grace). ADR-0054: the gateway screen opens IN PLACE (no blacked-out backdrop) on a smoked-glass card with three columns — Friends (mutual follows), Follows (you follow), Games (instances that published a game) — reusing classifySections. 5 new tests + 3258 full suite green. Prior: v0.2.676-alpha - SETTINGS PANEL RESTRUCTURE + HOMEPAGE POLISH (non-gameplay UI/UX pass): removed the Instance Settings tab and all its arrival-mode/write-policy admin wiring from the title-screen settings panel (nothing useful there for admins or guests; the underlying signed kind:30078 access-control enforcement in handoffArrival.js/writeAuthority.js is untouched). Split "Publish my node's presence" out of the Gateway Setup tab into its own Heartbeat tab (new heartbeatPanel.js renderer; same underlying state/callback). Settings panel is now exactly 2 tabs: Gateway Setup, Heartbeat. On the homepage: vertically centred the numbered steps (1/2/3) with their row content, moved the "CHARACTER" label above the step rows and reworded it to "Select your character", increased the centre panel's translucency by ~20% and added a radial mask-image so its background fades to full transparency at the edges, removed its light golden border, and removed the title header's table/cell border + dark-brown background (text + torii icon unchanged) while lightening the SOVEREIGNTY · OPEN WORLD · FREE MARKET · ABUNDANCE subtitle text. 3253 full suite green (no new tests needed — no existing test imported the removed modules), 21/21 regression gates green. No gameplay change. Prior: v0.2.675-alpha - ADR-0052 KAMI MODE STATE DIAGNOSTIC (pinpoint the 'stuck in Kami Mode — Esc does nothing' bug): a playtest ema reported being locked in Kami Mode with Esc doing nothing, and the server confirmed KAMI_STATE active=true never cleared. The ema snapshot couldn't tell WHY (active stuck? noteOpen? entering? pointer-lock?). Fix: add a kami sub-report to the debug snapshot (buildKamiReport) capturing active/noteOpen/entering/pointerLocked, wired via installToriiDebug + a new kamiEntering() export. 3 new tests + 3253 full suite green. No gameplay change — read-only diagnostic; the next stuck ema disambiguates the root cause. Prior: v0.2.674-alpha - ADR-0051 WRAP THE POINTER-LOCK YAW TO [-π, π] (fix for BAD_FIELD MOVE rejection): the pointer-lock yaw accumulated without bound (input.js `_yaw -= movementX * SENS`), so after a full 360° turn it exceeded 2π and the server's isRot2 (ROT_ABS = 2π) rejected EVERY MOVE with BAD_FIELD — 1656 rejections in ~10 min, the server silently stopped tracking the player's position. Fix: wrapAngle() normalises to [-π, π] on read in getYaw() — the accumulator stays continuous while every consumer (MOVE rot, ema yaw, fly camera) sees a wrapped value. 6 new tests + 3250 full suite green. No protocol change. Prior: v0.2.673-alpha - HOMEPAGE POLISH (non-gameplay UI/UX pass, rebased onto v0.2.672-alpha): removed the FLY MODE toggle from the homepage (button, pill-switch CSS, and the wiring IIFE in main.js) — in-game F-key free-fly is unaffected, arenaRuntime's #btn-fly-toggle DOM sync is null-guarded. Removed the large standalone #version-label; the version is now shown slightly larger (9px → 11px, .up-row-value) inside the update-preview box below, with a static fallback row added for pre-JS paint + the regression-check version-marker gate. Made the centre panel background highly translucent (background: rgba(30,18,12,0.18), scoped to #title-centre.glass-panel only — the header glass-panel is unaffected). Added large numbered steps (1 2 3) to the left of the character selector, LOGIN WITH NOSTR, and ENTER TORII via a new .step-row/.step-num wrapper. Full suite green, regression check ALL GREEN (21/21). Diff scoped to index.html + src/main.js only — zero gameplay-file overlap with the concurrent ADR-0048/0049/0050 diagnostic work merged just ahead of this commit. Prior: v0.2.672-alpha - ADR-0050 DECOUPLE BOT_STATE BROADCAST FROM THE KAMI MODE ROSTER GATE (root-cause fix for the ~12m desync): ADR-0049 exonerated the client (socket closeCount=0, heartbeat maxGap 2.8s one-time) — the BOT_STATE stall is server-side. The server broadcast BOT_STATE only when players.length > 0, and players EXCLUDED Kami Mode sessions (ADR-0032 bots-ignore-admin), so a sole player in Kami Mode silenced the stream and froze every bot on the client. Fix: extract buildBotTickRoster + shouldBroadcastBotState into server/bots/botStateGate.js — authedCount counts every authed session (Kami or not) while the bot-brain roster still excludes Kami, and the broadcast gate keys off authedCount. Bots still ignore the admin; BOT_STATE keeps flowing. 10 new tests + 3244 full suite green. Confirm live: lastIngestAge should now stay ~66ms across Kami Mode. Prior: v0.2.671-alpha - ADR-0049 WEBSOCKET OPEN/CLOSE + MAIN-THREAD HEARTBEAT DIAGNOSTIC (split the BOT_STATE stall cause): ADR-0048 proved the stream stalls (lastIngestAge 4-7s at shot time in 3 of 4 ema). This deploy adds a pure createConnectionDiagnostics(now) factory (src/engine/diagnostics/connectionDiagnostics.js) tracking (1) the WebSocket transport+protocol lifecycle (connect/open/close/reconnect/state with ages) and (2) a main-thread heartbeat (rAF gap: lastGap/maxGap/stallCount). wsClient emits socket_connect/open/close; multiplayerHost records them; loop.js heartbeats each rAF tick; arenaRuntime captures combat.lastSentShot.conn at shot time. Read: ws.lastCloseAge small ⇒ socket dropped; heartbeat.maxGap large ⇒ main thread froze. 9 new tests + 3234 full suite green, build clean. NO gameplay change — diagnostic only. Prior: v0.2.670-alpha - ADR-0048 BOT_STATE INGEST-RATE DIAGNOSTIC (find why client bots are ~12m stale): ADR-0047 quantified the desync — the client's bots are ~12m stale vs the server (Happy: client (15.2,2.94) vs server (3.62,-1.82), ~5-10s of movement), and the server broadcasts BOT_STATE @15Hz so the stall is client-side. This deploy adds a pure diagnose(nowMs) to botNetState (tracks _ingestCount + _lastIngestAt) exposed via bots.js getBotNetDiagnostic(), captured at SHOT time into combat.lastSentShot.botNet ({ingestCount,lastIngestAge,bots:[{id,sampleCount,newestAge,oldestAge}]}). lastIngestAge ~66ms ⇒ stream flowing (desync is in the client apply/interp path); >>1s ⇒ stream stalled. 3 new tests + 3225 full suite green, build clean, check ALL GREEN. NO gameplay change — diagnostic only. Prior: v0.2.669-alpha - HOMEPAGE SIMPLIFICATION + SETTINGS PANEL MODULE + ORANGE/BRONZE RECOLOR (non-gameplay UI/UX pass, rebased onto v0.2.668-alpha): removed the chat panel, leaderboard panel, gateway-preview panel, and player-stats card from the homepage; removed the instance-settings link, project-dashboard link, and gateway-setup button; merged ENTER ARENA + ENTER NAP into a single ENTER TORII button (btn-enter-nap / elNapBtn); deleted a duplicated second title block; moved the torii icon button from top-right to top-left. New settings panel module (src/engine/settings/settingsPanel.js, nav-left/content-right) replaces the old toriiMenu popup on icon click, hosting Instance Settings + Gateway Setup tabs. Gateway Setup tab (src/engine/settings/gatewaySetupPanel.js) has exactly 3 owner-facing cards (Choose Blank, Use My World as Template, Publish My Node) — no 'Visit a Node' card, node travel goes through the Torii Gateway inside the NAP zone instead; Node Directory tab dropped entirely. Orange/bronze recolor over a torii-gate background image with a frosted-glass content panel. Removed a stray unstyled GATEWAY SETUP button IIFE and a dead #btn-enter inline-script fallback listener. Rebound tests/entry-flow-smoke.test.js to btn-enter-nap/elNapBtn/'ENTER TORII'. Updated tools/csp.mjs INLINE_SCRIPT_SHA256 to match the edited inline script. Zero overlap with combat/bot/physics/net files (bots.js, botModel.js, bot-tactics.js, botNetState.js, botSim.js, physics.js, player.js, weapons.js, scene.js, hud.js, input.js, lod.js, multiplayerHost.js, wireProtocol.js, arenaRuntime.js untouched). 3217 full suite green, check ALL GREEN. Prior: v0.2.668-alpha - ADR-0047 RENDERED BOT-POSITION DIAGNOSTIC (exposes client/server desync): ADR-0046 confirmed the camera ray is sent unaltered (usedAimRay=true + sentOrigin==aimOrigin in all 8 ema) — so the miss is NOT camera-vs-muzzle. The server SHOT-RESOLVE log shows the real cause: the SAME camera ray hits a bot on the CLIENT at 6.36m but misses on the SERVER by bodyGap=1.68m — the client's bot collider sits ~2.7m from where the server's bot actually is (a client/server bot-position desync, not a collider-radius shortfall). This deploy captures the client's RENDERED bot positions at shot time into combat.lastSentShot.bots ({id,x,z,alive,hp} via new pure snapshotBotPositions in lastShotStore.js) so a miss ema can be diffed against the server's cur=/rew= per bot id. Also added x/z to getBotRenderStates (ADR-0045). 2 new tests + 3222 full suite green, build clean, check ALL GREEN. NO gameplay change — diagnostic only. Prior: v0.2.667-alpha - ADR-0046 SENT-RAY DIAGNOSTIC (proves camera-vs-muzzle on miss): on-target shots at STATIONARY bots miss by bodyGap=0.46-0.53m (ray passes 0.81-0.88m from the capsule axis; radius 0.35m) — NOT a rewind/lag-comp issue (dxz=0.00). buildShotPayload already prefers the camera aim ray (peer-combat test proves it). The open question: at RUNTIME, is aimOrigin/aimDir actually present when SHOOT fires, or does it fall back to the muzzle ray? Only player.js emits SHOOT (always with aimOrigin/aimDir). This deploy adds `combat.lastSentShot` to the ema: {ts,viewLag,usedAimRay,sentOrigin,sentDir,muzzleOrigin,muzzleDir,aimOrigin,aimDir} — the ACTUAL buildShotPayload output that went to the server, with full muzzle/aim vectors (not just Y). Written to its OWN _lastSentShot slot (independent of _lastShot, which only exists when aimOrigin/aimDir are present) so it is NEVER stale in the usedAimRay=false failure case. usedAimRay=true + sentOrigin≈aimOrigin → camera ray (then bot-only collider assist); usedAimRay=false → muzzle ray (fix aimOrigin passing). Server SHOT-RESOLVE now also logs rounded origin/dir + clientTs so the ema's sentOrigin/sentDir/ts can be matched numerically against what the server received. Extracted lastShot store into pure module engine/combat/lastShotStore.js so setLastShotSent/setLastSentShot are unit-testable. 4 new tests + 3219 full suite green, build clean, check ALL GREEN. NO gameplay change — diagnostic only. Prior:  - ADR-0045 BOT RENDER-STATE EMA DIAGNOSTIC (per-bot render state added to the ema snapshot: hasModel/modelLoaded/hasRoot/rootVisible/nameplateVisible/hasCapsule/capsuleVisible/scale/dist — narrows which branch is broken when bots show as cubes / floating nameplates with no body, instead of guessing). NO runtime combat/model change. 3215 tests / 251 files, build clean, check ALL GREEN. Prior: v0.2.665-alpha - ADR-0042 INSTANT EMA POST + BOT HIT VISUALS (round-2 playtest): (1) Enter in an ema note now seals + POSTs to /kami/ema INSTANTLY (pending→sent/failed) — no more Shift+K to hang; Shift+K is now the retry path for failed posts only, and a failed POST keeps the note on the rack. Root cause of "hang failed" was EROFS: torii-arena-ws.service runs ProtectSystem=strict + ReadWritePaths=/opt/torii-quest/mp only, so /var/lib/torii-quest/kami was read-only to the service; fixed via systemd drop-in /etc/systemd/system/torii-arena-ws.service.d/kami-ema.conf adding ReadWritePaths=/var/lib/torii-quest/kami (verified write from service namespace; follow-up to bake into torii-suite installer). (2) Bot hit visual feedback: bots had NO reaction to server-confirmed hits. applyBotHit now calls model.flashHit() (red 0xff3030 emissive flash on every bot material for 0.18s, decayed in tick()) + model.updateNameplate(name, hp/maxHp) (HP bar green→red on the nameplate canvas); render loop passes !st.alive so the death anim plays. Stickers do NOT stick to bots by design (isBotMesh flag excludes them from the sticker raycaster) — flash+HP+death ARE the feedback; sticker-on-SkinnedMesh deferred. Also: sealTo payload guard relaxed from instanceof Uint8Array to a byteLength check (cross-realm Uint8Array from iframes/workers/jsdom failed instanceof). 15 new tests (ema send-state + emagake postState tag + botModel flash + kamiMode instant-post integration) + __resetKamiForTests export for module-state isolation under isolate:false. 3205 full suite green, build clean, check ALL GREEN. Prior: v0.2.660-alpha - ADR-0041 PLAYTEST UX (reliable kami exit + visible hit feedback): round-1 bot playtest exposed two usability bugs (NOT hit-reg — the server damage chain is correct). (1) Stuck in Kami Mode: ESC is browser-reserved while pointer-locked, so the 2nd ESC never reached exitKamiMode (KAMI_STATE stayed active=true); added a pointer-lock-independent ✕ exit button to the rack box that calls exitKamiMode directly. (2) "many hits and nothing": flashCross was a 0.12s crosshair blip + the stronger screen-edge flashHit was NOT wired to bot hits; BOT_HIT_BY_PLAYER now also calls flashHit (body=25→~0.55 opacity, head=50→0.8) + flashCross duration 0.12→0.25s. Ema store ENOENT was a missing VPS dir (/var/lib/torii-quest/kami); ensure() already uses recursive mkdir so it self-heals — dir created on the VPS. 3190 full suite green, build clean, check ALL GREEN. Prior: v0.2.659-alpha - ADR-0040 POLISH (declare undici dep + kind:14 doc/test accuracy): v0.2.658-alpha's `import { WebSocket } from 'undici'` resolved only TRANSITIVELY (node_modules/undici pulled in by another package — `require('module').builtinModules` has NO 'undici', so it is NOT a Node core module). Declared undici (^7.29.0) as an explicit top-level dependency so future `npm install`s are robust against transitive-dep removal. Also corrected ADR-0040's inner-rumor description (kind:14 NIP-17 private direct message, not kind:1) + added test assertions that the unwrapped rumor is kind:14 + sender pubkey === Kami's pubkey. Tool comment clarified + usage example fixed (sudo -u scrubs env → set KAMI_PRIV inside the torii-quest bash -c). Zero production behavior change (deployed v0.2.658-alpha already worked via transitive undici; VPS dry-run re-confirmed after this bump). 3190 full suite green, build clean, check ALL GREEN. Prior: v0.2.658-alpha - ADR-0040 HOTFIX (undici WebSocket for Node relay publish): v0.2.657-alpha's tools/kami-nostr-reply.mjs fell back to a non-existent global WebSocket in Node 20 (no undici import), so every relay publish returned 'no-websocket' and the NIP-17 DM never left the VPS. Fix: import WebSocket from Node's built-in undici (browser-API compatible) + pass as WebSocketCtor; robustify onmessage data-coercion for Buffer(ws) vs string(undici). Smoke-tested live: published a real kind:1059 to the owner's npub, accepted by 3/4 relays (plebeian staging + nos.lol + primal); subscribed + confirmed the gift-wrap outer signers are all ephemeral (not Kami's real key ea3ff08e…), #p tag = owner. In-game rack path unaffected. 3190 full suite green, build clean, check ALL GREEN. Prior: v0.2.657-alpha - NOSTR-NATIVE KAMI CHAT BRIDGE (ADR-0040 Stage 1: Kami replies now dual-write — appended to replies.jsonl for the in-game emagake rack (ADR-0039 path unchanged) AND published as a native NIP-17 gift-wrapped DM (kind:1059) to the owner's npub, so Kami replies show up as real Nostr direct messages in Buzz / Amethyst / Nostir. Corrected premise before coding: the browser CANNOT decrypt NIP-44/NIP-17 without window.nostr.nip44 (most extensions don't expose it), so the in-game rack keeps the HTTP replies.jsonl path; Kami (VPS, nostr-tools) only needs the owner's PUBLIC npub to publish. New server module server/kami/kamiNostr.js (buildGiftWrap + publishEventToRelay) + tools/kami-nostr-reply.mjs (dual-write CLI, runs as torii-quest user) + src/engine/kami/kamiNostrCap.js (browser NIP-07 feature detection, foundation for future in-game decrypt, not wired into the rack). Added nostr-tools as a server-only dep (NOT in the browser bundle — confirmed via build). 15 new tests + 3189 full suite green, build clean.) Prior: v0.2.656-alpha - KAMI HANG HOTKEY COPY FIX (ADR-0039 hotfix: empty-rack + tray-counter said CTRL+E / CTRL+SHIFT+E to hang, but the real hotkey is Shift+K since ADR-0031. Both corrected to "SHIFT+K TO HANG". 3175 full suite green, build clean.) Prior: v0.2.655-alpha - 2-WAY EMA COMMS / AI REPLIES IN THE EMAGAKE RACK (ADR-0039: AI replies render in the in-game emagake rack. Direction 1 user→AI unchanged from ADR-0038. Direction 2 AI→user is plaintext because the browser can't decrypt kamiSeal (NIP-07 has no ECDH). Separate `replies.jsonl` feed: `server/kami/kamiReplyStore.js` + `server/kami/kamiReplyRoute.js` + admin-gated `GET /mp/kami/replies?since=<ts>` route in arena-ws.js. AI posts via `tools/kami-reply.mjs` on the VPS (reads text from file/stdin, never shell-quoted). Client `kamiMode.js` polls every ~5s in Kami Mode + owner token; `emagakePanel.js` renders distinct `ema-row kami-reply` rows, textContent only, deduped by id. Also deployed rotated KAMI_PUBKEY `ea3ff08e…`. 24 new tests + 3175 full suite green, build clean.) Prior: v0.2.654-alpha - REOPEN ALL BOARDS ON EVERY TRIGGER PRESS (ADR-0037: setBoardsActive(true) now always force-shows all three boards instead of short-circuiting when already active — fixes the bug where pressing Q a second time after closing the boards only reopened the auction panel, leaving the three boards hidden, because hideOwnerBoard() never reset `_active`. 11 close-button tests + 3151 full suite green.) Prior: v0.2.653-alpha - PRODUCT SIGN PROXIMITY+Q TRIGGER (ADR-0036: the auction-panel + three ADR-0035 owner boards no longer auto-show on NAP-zone entry and have zero relationship to Kami Mode — they open only via a proximity+Q trigger on the in-world PRODUCT sign mesh (`product-stall-panel`), each panel with its own explicit close/X button. 25 new tests (15 product-panel-trigger + 10 product-panel-close-buttons) + 3150 full suite green.) Prior: v0.2.652-alpha - KAMI VISUAL UNIFICATION + OWNER PRODUCT/AUCTION BOARDS (ADR-0034 + ADR-0035, one deploy: ADR-0034 replaces the full-screen darkened `#kami-overlay` ema editor with an inline bottom bar in the same translucent panel language as the emagake rack — world stays fully visible at all times, no backdrop dimming; a 3rd K press while the editor is already open highlights it instead of opening a second one; `setMarketActive` is now gated off `kamiActive()` so no panel auto-pops over Kami Mode's own components. ADR-0035 replaces the single hardcoded-auction `#auction-panel` with three separate NAP-zone DOM board overlays — Live Products, Live Auctions, Past Auctions — sourced from the owner's own npub via generalized `plebeianRelay.js` (NIP-99 kind:30402 products, kind:30408/1023 auctions/bids), built as `boardModel.js` + `boardPanel.js` + `ownerBoards.js`; visible to any guest, not gated to Kami Mode or the owner; DOM overlays only in this slice, an in-world 3D board render is deferred to the future napplet-architecture work. Also folded in: historical ADRs 0025/0026/0028/0029/0030/0031 fully rewritten in place to read "emagake" throughout (footnote-only approach from ADR-0033 reversed per explicit owner instruction — see ADR-0033's Amendment); `setTimeout` allowlist gate extended to cover `kamiMode.js`/`marketStall.js`/`plebeianRelay.js`'s existing revert/reconnect timers. 28 new tests (9 board-model + 9 board-panel + 8 reduce-author-events + 2 note-bar tests folded into kami-state-machine.test.js) + 3125 full suite green, build clean, regression check all green.) Prior: v0.2.651-alpha - EMAGAKE SPELLING FIX (ADR-0033: user researched the romanization and confirmed "emakake" was a spelling mistake — 絵馬掛け correctly romanizes as `emagake` (rendaku k→g). Renamed every reference visible to a Japanese-literate reader: DOM IDs (`#emakake`→`#emagake`, `#emakake-body`→`#emagake-body`, `#emakake-count`→`#emagake-count`, `#emakake-empty`→`#emagake-empty`, `#emakake-header`→`#emagake-header`), the `src/engine/kami/emakakePanel.js`→`emagakePanel.js` module (git mv, preserving history) and its exported `renderEmakake`/`showEmakake`/`hideEmakake`→`renderEmagake`/`showEmagake`/`hideEmagake`, every CSS selector, test file, and cross-reference in `kamiMode.js`/`arenaRuntime.js`/`auctionPanel.js`. Historical ADRs that used the old spelling (0025, 0026, 0028, 0029, 0030, 0031) were left with their original text intact for the record — each got a short "Spelling correction (ADR-0033)" footnote instead of being rewritten, per the [Amended 2026-08-23: footnote approach reversed per explicit owner instruction — see the v0.2.652-alpha entry above and ADR-0033's Amendment section; ADRs 0025/0026/0028/0029/0030/0031 are now fully rewritten to say "emagake" throughout, footnotes removed.] original no-silent-history-edits rule. Zero behavior change; this is a rename only. 3097 full suite green both before and after.). Prior: v0.2.650-alpha - KAMI SERVER-AUTHORITATIVE INVINCIBILITY (ADR-0032: user reported that after the K-hotkey fix, they entered Kami Mode but bots still shot and killed them — client-side `kamiInvincible()` only guarded `player.js`'s local `takeDamage()`, which nothing calls once a multiplayer session is live; the real game is server-authoritative (`server/arena-ws.js` runs its own bot AI + resolves ALL combat with zero knowledge Kami Mode exists). Approved design: "Full version: ignore + invincible." Fix: new additive `KAMI_STATE` wire message (client→server, no PROTOCOL_VERSION bump); `isKamiActive(sess)` on the server re-verifies the SESSION'S OWN `sess.pubkey` against `ADMIN_PUBKEY_HEX` before ever honouring the client's claim (never trusts a bare flag); while active the admin's session is excluded from the ~20Hz bot-tick's `players` roster (bots literally can't see/target them — primary "ignore" mechanism); two backstop guards at `onBotShot`'s ray-cast loop and `resolveAndBroadcast`'s damage application cover in-flight shots and peer-vs-peer fire that bypass the roster exclusion; a reconnect resync (`mp_state`→CONNECTED handler) re-sends `active:true` if Kami was already on when the socket reconnects. Client wiring: `multiplayerHost.js` gained `sendKamiState(active)` (same `_send` pattern as `sendChat`/`sendKill`); `kamiMode.js`'s `enterKamiMode()`/`exitKamiMode()` (both exit paths) call it. Caught in testing: `installKamiMode()` explicitly whitelists its `deps` object into `_deps` — the new `sendKamiState` dep was silently dropped until added to that whitelist too (would have made the whole client-side send permanently inert). 8 new tests (KAMI_STATE validator + round-trip + sanitize in `wire-protocol.test.js`; `sendKamiState` drop/frame behaviour in `multiplayer-host.test.js`; enter/exit call-site coverage in `kami-state-machine.test.js`) + 3097 full suite green.). Prior: v0.2.649-alpha - KAMI HOTKEY FIX (ADR-0031: user reported "still no kami" after v0.2.648 — console log showed ZERO [kami] output, not even the unconditional hotkey-pressed diagnostic. Code-level audit ruled out: stale deploy (verified live bundle matches source), a swallowing listener (no other keydown handler touches KeyE), and an install-order bug (installKamiMode runs unconditionally in arenaRuntime boot). User confirmed the actual cause live: Ctrl+E (and Cmd+E) is a Brave/Chromium browser shortcut that focuses the address bar — the keydown never reaches the page at all. Fix: switched the hotkey to a bare `K` press (no modifier) — browsers don't own unmodified single-key presses on a focused page. Ctrl+K/Cmd+K explicitly excluded (real browser-reserved combo) so it still passes through to browser chrome; a focused text field also still owns bare K (mirrors the ADR-0027 typing guard). Badge + rack empty-state copy updated to "K". 2 new regression tests (Ctrl/Cmd+K does NOT enter Kami; bare K in a focused input does NOT enter Kami) + 3093 full suite green.). Prior: v0.2.648-alpha - KAMI MODE VISIBILITY (ADR-0030: the emagake rack WAS rendering but was camouflaged — empty, translucent body with no smoked-glass backing, sitting over the weapon. Owner saw 'no kami' despite [kami] logs confirming enterKamiMode ran. Diagnosed by live cloud-browser inspection: `elementFromPoint` at the rack center returned the rack's own child (`isRackOnTop: true`), display:flex, position:fixed, z-index:40, rect 300x123; the 3D canvas is z-index:auto so the rack stacks above it correctly. Root cause: (1) rack overlapped the gun view-model at right:14px, (2) empty so just 'RACK IS EMPTY' text, (3) #emagake-body had no background so text floated bare over the gun. Fix: tall min-height:46vh rack, smoked-glass #emagake-body backing, larger centered empty state, persistent #kami-mode-badge pill (pointer-events:none, top-center, z-index:45) toggled in enterKamiMode/exitKamiMode, Mac-aware CTRL/+E copy. Honors owner's 'smokier + taller' request. NOTE: v0.2.647 had a badge CSS bug (base rule `display:none` meant removeAttribute('hidden') couldn't show it — badge stayed 0x0); v0.2.648 fixes it by hiding via [hidden] attr only, mirroring #emagake. Verified in-browser: rack now 300x460, badge 373x28 display:block, both visible over the live arena.). Prior: v0.2.643-alpha - FLOATING PANELS DOM FIX (ADR-0028: the ema rack (#emagake) + Plebeian auction panel (#auction-panel) were invisible in the arena. Root cause found by live browser inspection: both were nested inside #screen-title > #title-columns. phaseScreens.js applyPhaseScreens() adds .hidden (→ display:none) to #screen-title the moment the phase leaves TITLE (PLAYING/PAUSED/DEAD); display:none removes the ENTIRE subtree from the render tree — including position:fixed descendants — so a panel nested under #screen-title is invisible in-game no matter its .floating / z-index. Fix: moved both panels to top-level body scope (siblings of #hud, after #screen-title closes). 5 nesting regression tests + 3084 full suite green.). Prior: v0.2.642-alpha - KAMI EMA INPUT OWNED (ADR-0027: the ema note input now owns its keystrokes while open — Space no longer jumps the player + Escape closes the modal instead of opening the pause menu; see Recent shipped work for the root cause + fix. 9 input-boundary tests + 3078 full suite green.). Prior: v0.2.636-alpha - BIDDER IDENTITY + REVERSE-ORDER BIDS (ADR-0026 slice 3: the auction panel's bid list is now highest-first (descending by amount); each row shows a circular bidder avatar (profile picture where a kind-0 Nostr profile exists on the staging/prod Plebeian relays, else a deterministic colored-circle with the bidder's initial) + the bidder's display name (or short pubkey fallback). marketStall best-effort fetches kind-0 profiles for the current bidders from both Plebeian relays after bids arrive, then re-renders; a missing profile falls back gracefully. The 'high bid' flag now marks only the single top bid (was: every ascending bid). 35 plebeian tests + 3072 full suite green.). Prior: v0.2.635-alpha - SPATIAL MARKETPLACE (ADR-0026: watch-only Plebeian auction reader in the NAP zone. Smoked-glass DOM overlay (#auction-panel) subscribes to Plebeian's own relay wss://relay.staging.plebeian.market, renders kind-30408 auctions + kind-1023 bid history with running high bid + end-time countdown; watch-only — footer links out to auctions.plebeian.market, no bidding in-panel. Toggled by NAP-zone entry (setMarketActive, mirroring setNapMode); lazy relay subscription. Modules: src/engine/plebeian/{auctionModel,plebeianRelay,auctionPanel,marketStall}.js.). Prior: v0.2.634-alpha - KAMI MODE (ADR-0025: owner's sealed in-world ema note-taking surface. Ctrl+E or pause-modal "⛩ HANG AN EMA" button captures a note pinned to the cursor's world position / UI control + a same-tick screenshot, sealed in the browser with ECDH+HKDF+AES-256-GCM [zero new deps] before POST. Server holds only ciphertext: ema text forever in ema.jsonl, screenshots ring-buffered to 420 in shots/<id>.bin. POST /mp/kami/ema is admin-gated (fail-closed bearer check). Browser can seal but not decrypt the backlog (NIP-07 hides the owner privkey; Kami privkey is off-box), so the in-browser rack is session-scoped and tools/kami-read.mjs decrypts the sealed backlog on a trusted machine holding the Kami key. Storage dir /var/lib/torii-quest/kami [chown torii-quest, mode 700] created on VPS. 47 kami tests + 3037 full suite green.). Prior: v0.2.633-alpha - LAG-COMP ROUND TRIP (ADR-0024: 53 instrumented shots = 43 hits (10 head, 33 body; hat/headshot zone CONFIRMED WORKING) + 10 misses. 4 misses were 11-28m off (not aimed at a bot); 6 genuine, with 3 just outside the 0.35m head radius. Cause found by reading the clocks: botNetState stamps samples with CLIENT RECEIVE time so the downlink is already baked in, while the server applies viewLag to shot ARRIVAL time - so the offset is TWO one-way trips, not one, and the server rewound one one-way too recent (0.10-0.35m lateral error). One line: +ow -> +2*ow.). Prior: v0.2.632-alpha - MISS-GEOMETRY DIAGNOSTICS (ADR-0023). Prior: v0.2.631-alpha - MP MODEL ATTACH (ADR-0022). Prior: v0.2.630-alpha - MP AUTHORITATIVE ROSTER (ADR-0021). Prior: v0.2.629-alpha - COMPLETE STALE-BOT CLEANUP (ADR-0020). Prior: v0.2.628-alpha - STALE-BOT CLEANUP + TEST_MODE (ADR-0019). Prior: v0.2.627-alpha - CONTROLLED TEST ENVIRONMENT (ADR-0018: server-only env vars `BOT_COUNT_OVERRIDE` / `BOSS_COUNT_OVERRIDE`). Prior: v0.2.626-alpha - CLIENT BOT STATE SYNC + ZONE PLUMB (ADR-0016 nameplate lifecycle + dead-bot LOD after death arc; ADR-0017 zone plumbed into client applyBotHit). Prior: v0.2.625-alpha - MP HIT-REG ALIVE-WINDOW FIX (ADR-0015). Prior: v0.2.624-alpha - TRIGGER-FIRE DIAGNOSTICS (ADR-0014). Prior: v0.2.623-alpha - BOT IDENTITY + DIAGNOSTICS LAYER (ADR-0013). See §0 for the current snapshot.

**Recent shipped work (newest first):**
- **v0.2.699-alpha - ONE-COMMAND DOCKER INSTALLER**: new self-contained `install.sh` gives a fresh clone a single `sudo ./install.sh` path to a live instance (Docker install-if-missing, domain/email/npub prompts, build+launch+verify, colourful boxed summary); multiplayer (`arena-ws`) ships on by default alongside the game and relay. `Caddyfile`/`Dockerfile` now extract the CSP header from the real per-build `dist/_headers` instead of a hand-copied static value, and `VPS_INSTALL.md`'s three stale/insecure CSP blocks (dead `'strict-dynamic'`, wrong sha) were fixed the same way. Found and fixed a real bash bug: reopening `/dev/stdin` by redirect per prompt could flush an unconsumed pipe read-ahead as literal NUL bytes on some bash builds — `ui_ask`/`ui_confirm` now read the inherited stdin fd directly when there's no controlling TTY. 3380 tests green / 267 files, 21/21 regression gates.
- **v0.2.695-alpha - MINIMAP REMOVED + SINGLE-K KAMI ENTRY (ADR-0064)**: (a) removed the in-game minimap entirely — was rendering top-left instead of the intended bottom-right (canvas was a sibling, not a child, of its positioning wrapper); deleted rather than repositioned since a minimap wasn't wanted; (b) single bare K now enters Kami Mode + opens the ema note input in one press (was 2-step per ADR-0029); Enter/Esc no longer re-lock the pointer, leaving it free to click the emagake. 3380 tests green / 267 files, 21/21 regression gates.
- **v0.2.693-alpha - FOUR CONSOLE/UX CLEANUPS (ADR-0063)**: (1) removed the Gateway Setup settings-panel auto-open that fired the moment Nostr login resolved — panel now reachable only via explicit settings icon / KeyM menu; (2) KeyQ is now a toggle for the in-world product HUD — first press opens auction-panel + owner boards, second press closes them all (works even out of prompt range, via new `isMarketActive()` getter); (3) migrated render loop `THREE.Clock`→`THREE.Timer` (Clock deprecated since three r183) — delta semantics unchanged, same 0.05s cap; (4) `requestLock` now catches the `requestPointerLock()` Promise rejection (NotAllowedError when the user-gesture expired). Items 3 (rapier deprecation, library-internal) and 5 (relay WebSocket failures, environmental) deliberately left — not code-fixable. 3375 tests green / 266 files, 21/21 regression gates.
- **v0.2.692-alpha - HOMEPAGE PANEL: REAL SMOKED GLASS RESTORED WITH TRUE EDGE FADE** (non-gameplay UI/UX pass): v0.2.684-alpha's edge-fade fix had removed `backdrop-filter` blur entirely (flat no-blur tint) to kill a hard-edge artifact, losing the actual smoked-glass look; root cause is that Chromium/WebKit rasterize backdrop blur over the element's full box before mask compositing, so any single blur+mask always shows a hard sampling boundary against a detailed background. Fix: a 4-ring concentric technique — `.gp-ring1`-`.gp-ring4` taper blur radius (16→10→5→2px) across staggered radial-gradient mask bands, plus a `.gp-tint` flat-color layer carrying the fade the rest of the way to true 0% opacity — so the blur itself softens gradually instead of cutting off at one edge. Verified against the real homepage background (not synthetic test patterns); buttons stay clickable (`.gp-content` wrapper at z-index 1). Merged cleanly on top of a concurrent session's v0.2.685-alpha (ADR-0059 auction panel header hardening) with zero file overlap. 3363/3368 full suite green (5 skipped), 21/21 regression gates green. See Current version line above for full detail.
- **v0.2.684-alpha - SETTINGS PANEL: PROFILE + RELAY TABS, HOMEPAGE FADE, TOP-RIGHT CLOSE** (six-item polish request): homepage panel corners fully rounded + smoked-glass fades to true 100% transparency at the edges (no hard edge); new Relay tab (view/add/remove wss:// heartbeat relays); Heartbeat toggle now defaults on with a red/green switch; removed stale "visit another world" copy from Gateway Setup; admin panel Close button moved from bottom-left to a standard top-right ✕; new Profile tab (Display name, Bio, Avatar URL, Website, NIP-05, Lightning address) with a login gate, localStorage draft, and kind:0 publish via the existing signer/relay path. Zero gameplay-file overlap. 3361 full suite green / 264 files, 21/21 regression gates green. See Current version line above for full detail.
- **v0.2.652-alpha - KAMI VISUAL UNIFICATION + PRODUCT/AUCTION BOARDS** (ADR-0034 + ADR-0035): inline bottom-bar ema editor (no darkened modal, world always visible), `setMarketActive` gated off `kamiActive()`; three new DOM-overlay boards (Live Products / Live Auctions / Past Auctions) sourced from the owner's own npub via generalized `plebeianRelay.js`. Also: historical ADRs 0025/0026/0028–0031 fully rewritten to "emagake" (footnotes removed, reversing the v0.2.651-alpha approach — see ADR-0033's Amendment). See Current version line above for full detail.
- **v0.2.651-alpha - EMAGAKE SPELLING FIX** (ADR-0033): "emakake" corrected to "emagake" everywhere visible in code/DOM/docs; historical ADRs originally got a footnote instead of a rewrite — later reversed in v0.2.652-alpha. See Current version line above for full detail.
- **v0.2.650-alpha - KAMI SERVER-AUTHORITATIVE INVINCIBILITY** (ADR-0032): server-verified KAMI_STATE message, bot-roster exclusion + damage backstops. See prior handoff entry / ADR-0032 for full detail.
- **v0.2.649-alpha - KAMI HOTKEY FIX** (ADR-0031): Ctrl/Cmd+E silently intercepted by Brave's address-bar shortcut. Switched to bare `K`. See Current version line above for full detail.
- **v0.2.648-alpha - KAMI MODE VISIBILITY (ADR-0030).** The emagake rack WAS rendering but was camouflaged: empty, `#emagake-body` had no smoked-glass backing (text floated bare over the gun), + it overlapped the weapon view-model at `right:14px`. The owner saw "no kami" despite `[kami]` console logs confirming `enterKamiMode` ran + shooting was suppressed. Diagnosed by live cloud-browser inspection, not guessing: `document.elementFromPoint()` at the rack's center returned the rack's own child (`isRackOnTop: true`); computed style `display:flex; position:fixed; right:14px; z-index:40`, rect 300x123; the 3D canvas is `z-index:auto` (appended to `document.body` by `scene.js:34`) so the rack stacks above it correctly. Root cause = camouflage, not absence. Fix (CSS + one DOM toggle, no state-machine change): (1) `#emagake.floating` gains `min-height:46vh` so an empty rack is a tall smoked panel, not ~120px of floating text; (2) `#emagake-body` gains `background:rgba(8,10,20,0.38)` + `backdrop-filter:blur(6px)` + faint border (honors owner's "increase the smokiness"); (3) `#emagake-empty` centered + tinted; (4) new `#kami-mode-badge` — a `pointer-events:none` pill pinned top-center (`z-index:45`, above HUD 10, below pause 60/death 50), copy `⛩ KAMI MODE · CTRL/+E EMA · ESC EXIT`, toggled via `setKamiBadge(true/false)` in `enterKamiMode` + every `exitKamiMode` path; (5) Mac-aware `CTRL/+E` copy. NOTE: v0.2.647 had a badge CSS bug (base rule `display:none` meant `removeAttribute('hidden')` could not show it — badge stayed 0x0); v0.2.648 fixes it by hiding via the `[hidden]` attribute only, mirroring `#emagake[hidden]`. Verified in-browser: rack 300x460, badge 373x28 `display:block`, both visible over the live 3D arena. 3091/3091 tests, build clean. The NAP-zone auction auto-show (covers the emagake) is deferred to a separate auction-button slice.
- **v0.2.647-alpha - KAMI MODE VISIBILITY (ADR-0030, first attempt).** Same fix as 648 but the `#kami-mode-badge` base CSS rule carried `display:none`, so the badge never showed. Superseded by 648.
- **v0.2.646-alpha - MAC ⌘E HOTKEY + OWNER-CHECK LOG.** The Ctrl+E handler checked `ev.ctrlKey` only, so macOS ⌘E was silently ignored — the root cause of the prior "no kami" reports. Now accepts `ev.ctrlKey || ev.metaKey`. Also: `checkOwner()` pubkey-keyed (no longer caches a false/empty result if the first Ctrl+E fires before Nostr login resolves) + emits `[kami] owner-check` / `[kami] hotkey pressed` diagnostic logs.
- **v0.2.643-alpha - FLOATING PANELS DOM FIX (ADR-0028).** The ema rack (#emagake) + Plebeian auction panel (#auction-panel) were invisible in the arena. Root cause found by live browser inspection, not guessing: both panels were nested inside `#screen-title` > `#title-columns`. `phaseScreens.js` `applyPhaseScreens()` adds `.hidden` (→ `display:none`) to `#screen-title` the moment the phase leaves TITLE (PLAYING/PAUSED/DEAD). `display:none` removes the ENTIRE subtree from the render tree — including position:fixed descendants — so a panel nested under `#screen-title` is invisible in-game no matter its `.floating` / `z-index`. Verified in-browser: with `#screen-title` hidden, nested `#emagake` had rect w:0,h:0; after moving to `document.body` it rendered (300×123) and with `.floating` pinned to `position:fixed;right:14px` (x:1606, top:50%). Fix: moved both `#emagake` + `#auction-panel` to top-level body scope (siblings of `#hud`, after `#screen-title` closes). Comments updated to forbid re-nesting under `#screen-title`. 5 source-contract nesting tests (`tests/floating-panels-nesting.test.js`) + 3084 full suite green.
- **v0.2.642-alpha - KAMI EMA INPUT OWNED (ADR-0027).** Live-testing the v0.2.636 auction panel surfaced a Kami Mode regression: pressing Ctrl+E opened the ema note, but Space jumped the player and Escape couldn't close the modal. Root cause by reading the code, not guessing. `player.js:89` jumps on `Space`/`KeyE` via an `onKeyDown` callback wired through `input.js`'s `downCbs`; `input.js:20-33`'s document keydown set `keys[e.code]=true` + fired the callbacks **without checking whether a text field had focus** — so a Space typed into the ema textarea also reached the jump callback. Separately, `arenaRuntime.js:1248` registers a **capture-phase** document Escape listener (`true`) that, when `isPlaying()` (which `openNote` does not unset), calls `_openPause()` + `stopImmediatePropagation()`; capture fires before the textarea's own bubble-phase `onKey`, so the ema's `finish(false)` → close never ran and the owner was trapped. Fix (3 surgical changes + a suppression flag): (1) `input.js` early-returns when `e.target` is a focused form field (Space/E no longer leak for any text input); (2) `input.js` exposes `setGameInputSuppressed(bool)` — while set, `keys` is never latched, `downCbs` never fire, AND the mousedown shoot handler early-returns, so a click on the ema modal cannot fire a shot even if focus has slipped off the textarea; (3) `arenaRuntime.js`'s capture Escape handler yields when `kamiNoteOpen()` is true (`_escapeHandledOnKeyDown = true; return;`) so the textarea's `onKey` receives Escape + discards, and the pointer-lock keyup fallback sees `handled=true` so it doesn't open pause on the same gesture; plus the ema `onKey` now `stopPropagation`s its Escape/Enter. `kamiMode.openNote` toggles suppression on, `finish` toggles it off; `clearKeys()` runs on enable so no held movement key carries into the modal. 9 input-boundary tests (source-contract) + 3078 full suite green.
- **v0.2.636-alpha - BIDDER IDENTITY + REVERSE-ORDER BIDS (ADR-0026 slice 3).** Panel feedback from the owner: (1) bids now render highest-first (descending by amount); (2) the second column's raw 8-hex pubkey is replaced by the bidder's Nostr display name where a kind-0 profile exists, else a short pubkey fallback; (3) a small circular avatar sits left of the time column — a profile picture where the bidder set one on either Plebeian relay, else a deterministic colored circle with the initial. `marketStall` best-effort fetches kind-0 profiles for the current bidders from both `wss://relay.staging.plebeian.market` + `wss://relay.plebeian.market` after bids arrive (400ms debounce), then re-renders; a missing/broken profile falls back to the colored-circle avatar (the `<img>` sits over the fallback and hides on load error). The 'high bid' flag now marks only the single top bid (`isTopBid`) rather than every ascending bid, so the flag reads cleanly in a highest-first list. 35 plebeian tests + 3072 full suite green.
- **v0.2.635-alpha - SPATIAL MARKETPLACE (ADR-0026).** Watch-only Plebeian auction reader wired into the NAP market zone. Pure auction view-model (`auctionModel.js`) parses kind-30408 auctions + kind-1023 bids; browser relay client (`plebeianRelay.js`) subscribes to `wss://relay.staging.plebeian.market`; smoked-glass DOM renderer (`auctionPanel.js`) mirrors emagakePanel; controller (`marketStall.js`) lazy-starts on NAP entry. Read-only — footer links to `auctions.plebeian.market/auctions/<id>`, no bidding in-panel.
- **v0.2.633-alpha - LAG-COMP ROUND TRIP (ADR-0024).** The ADR-0023 instrumentation did its job. A high-ground session logged 53 shots: **43 hits (10 head, 33 body) and 10 misses (18.9%)**. Two things fell straight out. (1) **HEADSHOTS REGISTER CORRECTLY** - 10 head zones resolved and logged, so the hat-as-headshot zone from ADR-0023 works as designed and that hypothesis is CLOSED. (2) The misses split into two clearly separate populations. Four missed by **11-28 metres** (`bodyGap` 11.1/14.7/17.3/27.9, one with `rng=0.0` meaning the bot was BEHIND the shooter) - those are shots not aimed at a bot at all, not a registration fault, and were excluded from the analysis. That left six genuine near-misses, three of them within 15cm of the body capsule with `headHorz` of 0.350, 0.383 and 0.531 against a **0.35m head radius** - passing JUST outside the head collider. In those six, `headHorz` tracked `dxz` (bot travel during the rewind window) closely: 0.53/0.58, 0.72/0.78, 1.54/0.90. That correlation was treated as a POINTER TO READ THE CODE, not a conclusion - n=6, and missing a moving target naturally skews along its travel, so some correlation is expected even under perfect compensation. **The defect, from the code:** `botNetState.ingest(states, nowMs)` stamps every sample with `nowMs`, the CLIENT RECEIVE clock, and `sample()` renders at `nowMs - DEFAULT_INTERP_DELAY_MS`. So a snapshot generated on the server at `s` is stamped `s + ow` - the downlink trip is ALREADY BAKED INTO THE STAMPS - and selecting a sample 100ms back in RECEIVE time actually selects server content from `100 + ow` ago. Meanwhile the server does `rewindTs = now - viewLag` where `now` is the shot's ARRIVAL time, one uplink trip after the click, and `viewLag = interpDelay + oneWayMs`. In one aligned frame with `fire` = the click instant: `seen = fire - interpDelay - ow` (downlink) vs `tested = (fire + ow) - viewLag` (uplink). Those are equal ONLY when `viewLag = interpDelay + 2*ow`; with a single `ow` the residual is exactly `ow`, so **the server rewound to a state one one-way trip TOO RECENT** and a moving bot was tested `ow` of travel ahead of where the player saw it. The comment on `arena-ws.js:535` ('the client ts is already inside viewLag, do NOT subtract it again here') is where the double-count was reasoned away - it accounts for the uplink correctly but misses the downlink carried in the receive stamps. At the measured `viewLag=164` (`ow~64ms`) and observed bot speeds of 1.5-5.5 m/s that is **0.10-0.35m of lateral misregistration - the width of the entire head collider**, scaling with bot speed, which is why fast lateral movement felt worst and why the small head zone suffered while the wider body capsule mostly absorbed it. It also explains the consistently negative `headVert` with no vertical bug: the residual is applied along the bot's travel on long, steeply downward shots. **FIX: one line** - `viewLagMs()` in `multiplayerHost.js`, `DEFAULT_INTERP_DELAY_MS + ow` -> `+ 2 * ow`. The 250ms clamp is unchanged so it stays inside the server's 300ms `LAG_COMP_MS`; `oneWayMs` is already capped at 250 by `wsClient` so the worst case saturates the clamp rather than overflowing the window. At `ow=64` a shot now reports `viewLag=228` instead of `164`. No server, wire, protocol, collider or damage change; **peer-vs-peer hit-reg reads the same `viewLag` and is fixed by the same line**. REJECTED: using the on-wire `clientTs` (needs `serverTsOffset`, adds a second estimated quantity); stamping `botNetState` samples with server send time (more principled but changes the meaning of every existing timestamp for the same arithmetic result); widening the head collider (papers over a clock bug and would corrupt the deliberate ADR-0023 hat sizing). RISK ACCEPTED: shots rewind ~64ms further into the past, slightly increasing 'shot behind cover' favouring the shooter - the standard lag-comp trade - and on a poor link the clamp now binds at `ow>75` rather than `ow>150`. Tests: `lag-comp-alignment.test.js` (11) in three groups - the `viewLagMs` contract with latency driven through the REAL PONG measurement path (not by poking a field), the PREMISE PROVEN EMPIRICALLY against the real `botNetState` (a 1 m/s bot with samples stamped `s + ow` renders at `clientNow - interpDelay - ow`, explicitly NOT `clientNow - interpDelay`), and the alignment identity (exact under `+2*ow`, residual exactly `ow` under the old form, 0.192m of lateral error for a 3 m/s bot at `ow=64`, no accumulated drift as latency varies). Gates: build OK, regression ALL GREEN, vitest 2989/2989 across 229 files, three consecutive full runs green. NEXT: replay from high ground - the three tight misses should now resolve as hits, and any remaining `headGap` should be clearly positive rather than clustered just outside 0.35m.
- **v0.2.632-alpha - MISS-GEOMETRY DIAGNOSTICS (ADR-0023).** Play test after ADR-0022: body hits good, head/hat shots intermittently not registering. A 25-min live capture of 51 shots showed a **23.5% miss rate**. Six hypotheses were ruled out BY READING THE CODE, not guessing: (1) fixed aim-origin height offset - dead, there are miss->hit pairs at identical geometry (miss `dy=2.45`, hit `dy=2.45`); (2) hat protruding above the head sphere - dead, the sphere spans foot+1.20->1.90 while the banker GLB is only 1.70 tall; (3) rewind snapping to the nearest snapshot - dead, `sampleBotsAt` interpolates between flanking snaps; (4) `footY` not interpolated - dead, `interpRows` blends `footY` alongside `x`/`z`; (5) client/server time-frame mismatch - dead, client renders at `now-100` and the server rewinds to `now-(100+one-way)`, and the observed `viewLag=164` matches; (6) bot movement during the rewind window - no correlation, miss rate by `dxz` bucket is non-monotonic (42.9%, 16.7%, 0%, 44.4%). The ONE real correlation is vertical offset: **5.0% miss when level with the bot (n=20) vs 31.8% when 2.0-2.5m above its feet (n=22)**; the outer `dy` buckets (n=5, n=4) are too small to lean on. Further progress was blocked by a DIAGNOSTIC GAP: `[SHOT-RESOLVE]` never logged the hit zone (though `resolvePlayerShot` already returned it), so a missed headshot was indistinguishable from a body hit; and a miss logged no margin, so a 4cm graze and a metre-wide shot looked identical. Fix: log `botHit=<id>@t=<t>@<zone>`, and on a miss add new `missGeomDiag` output - `headGap`/`bodyGap` (closest approach MINUS radius, so >0 is the miss margin in metres), `headVert`/`headHorz` (head miss split into signed vertical + horizontal so 'over the hat' is distinguishable from 'beside the head'), and `rng`. Uses a clamped ray/segment closest-point solve (`_raySegDist`). Diagnostics only - never consulted for resolution; no collider, damage, authority or wire change; no client change. **DESIGN INTENT NOW RECORDED:** the head sphere deliberately reaches foot+1.90 on a 1.70m model because these bots wear hats and the hat is INTENTIONALLY part of the headshot zone - they are small characters and their size would otherwise be an unfair advantage in a shooter. A positive `headVert` near the sphere top is EXPECTED; two tests guard this so nobody later shrinks the collider to fit the mesh. Verified a hat-height shot (foot+1.62) resolves as `zone='head'`. **TEST FLAKE FIXED (test-only, no production change):** `player-bot-combat.test.js` failed ~1 run in 3 (two different cases) while passing in isolation, blocking clean gates. Cause: unseeded `Math.random()` spawn positions (`botSim.js:145-146`) - the tests pick the highest-X REGULAR bot and fire along -X from 3-4m, and the BOSS is the only bot allowed a higher X, so it can randomly land in the firing corridor and intercept the ray, making `res.botId` the boss. Fix: an `isolate(sim, keep)` helper parks every non-target bot 1000m away; the target keeps its real random position so the terrain-frame geometry is still exercised. Seeding the production spawn RNG was considered and DELIBERATELY NOT DONE - it would change shipped behaviour to fix a test. Verified 8 consecutive isolated runs + 3 consecutive full-suite runs green. Tests: `miss-geometry.test.js` (11). Gates: build OK, regression ALL GREEN, vitest 2978/2978 across 228 files. NEXT: capture a session shooting from HIGH GROUND (the 32% bucket) - `headGap`/`headVert` will say whether those rays pass just outside the sphere (lag-comp/precision) or well outside it (framing/occlusion).
- **v0.2.631-alpha - MP MODEL ATTACH (ADR-0022).** Exposed by ADR-0021. After the local roster was correctly removed, the single MP bot rendered as the PURPLE FALLBACK CAPSULE with no head/limb colliders. Root cause: MP never had a model-attach path at all — `_modelsReady` was assigned (`bots.js:248`) and **never read anywhere**, and `_attachModelBot` was called **only** from the SP `initBots()` path. MP worked BY ACCIDENT: the SP init eagerly spawned a local roster WITH models, and `_tickNet`'s `_botById(p.id)` lookup reused those modelled wrappers whenever a server id happened to match. Removing the local roster removed that accidental model supply, leaving only the capsule → no `SkinnedMesh` → `createBotBoneColliders` never ran → no per-bone limb colliders (body/head colliders were still built by `_ensureBotColliders`, which is why it stayed shootable). Second gap: `preloadBossModel()` was only kicked off inside the now net-gated `initBots` continuation, so MP never fetched the boss template. Fix: `_tickNet` upgrades each row in place once its template is ready (`!bot.model` guard makes it idempotent), regulars immediately, boss deferred behind a new `_ensureBossPreload()` with a regular-model fallback on GLB failure; `BotModel.init` is synchronous from the cached template and sets `skinnedMesh`, so limb colliders are built in the same tick. `src/bots.js` only; server, wire, damage and collider geometry untouched; SP unaffected. Test: `tests/multiplayer/mp-model-attach.test.js` (8). Gates: build OK, regression ALL GREEN, vitest 2967/2967 across 227 files. NOTE: `player-bot-combat.test.js` "torso-height shot" failed 1 of 3 full runs, passes in isolation, imports only server modules — pre-existing flake from unseeded `Math.random()` spawn positions (`botSim.js:145-146`); logged in ADR-0022, fix deliberately NOT applied without approval.
- **v0.2.630-alpha - MP AUTHORITATIVE ROSTER (ADR-0021).** THE root cause of the frozen bots / water-bound Augustink / orphan nameplates that ADR-0016→0020 all failed to fix. Live server logs showed every `[SHOT-RESOLVE]` line carrying `bot=0` — the server had exactly ONE bot all session — while the client rendered Augustink plus others, so the extra bots were never server entities. `initBots()` runs unconditionally at world start (before any MP connection) and calls `sim.spawnAll(BOT_COUNT)` from CLIENT config, pushing 5 regulars + the Augustink boss (models AND nameplate sprites) into `bots[]`. MP connects later, `setBotNetMode(true)` flips `_netMode`, and `tickBots` switches permanently to the render-only `_tickNet` branch — so the local sim never ticks again and every local bot the server lacks stays frozen at its spawn point with its label drawn each frame (Augustink's client spawn is in the water). ADR-0019/0020 both cleared on `setBotNetMode(false)` — the DISCONNECT branch, which never runs in normal play. Fix: clear the roster on the OFF→ON transition edge (guarded so a repeated `setBotNetMode(true)` cannot wipe live server rows), plus three `if (_netMode) return;` spawn guards closing the async race (the `preloadBotModel().then` spawn, the late ~7.6MB `bossReady.then` boss attach, and the capsule `.catch` fallback) — without them the roster is re-created after the clear and freezes again. `src/bots.js` only; server, wire protocol, colliders and damage untouched; SP unaffected (`_netMode` stays false). Test: `tests/multiplayer/local-roster-mp-authority.test.js` (6). Gates: build OK, regression ALL GREEN, vitest 2959/2959 across 226 files.
- **v0.2.629-alpha - COMPLETE STALE-BOT CLEANUP (ADR-0020).** ADR-0019 cleared `_botNet` (interpolation buffer) but NOT the `bots[]` wrapper array that owns the THREE.js models + nameplate sprites — so the boss's model + "Augustink" label stayed frozen in the scene after a roster change. This adds `_clearAllBots()` (called in `setBotNetMode(false)`): disposes each `BotModel` (root + nameplate sprite), removes the capsule mesh, and `removeBotColliders(bot)` (new export in `bodies.js` — removes body/head/bone colliders + clears collider→bot/part lookups), then empties the array. Server unchanged, wire unchanged, SP unaffected. Test: `tests/multiplayer/bot-collider-removal.test.js` (3). Gates: build OK, regression ALL GREEN, vitest 2953/2953 across 225 files.
- **v0.2.628-alpha - STALE-BOT CLEANUP + TEST_MODE (ADR-0019).** Two changes. (A) Ghost-nameplate ROOT CAUSE finally found: `botNetState.js` exposes `clear()`/`remove()` but neither is called anywhere, so the client bot map grows monotonically and bots the server no longer has (e.g. the boss after a `BOSS_COUNT_OVERRIDE=0` restart) linger as frozen nameplates. Fix: `_botNet.clear()` in `setBotNetMode(false)` (WS close). `_botNet` is MP-only so SP is unaffected. (B) `TEST_MODE=1` env var — one-stop test flag: defaults roster to 1 regular / 0 bosses (granular overrides still win) AND makes `killBot` skip the death arc + set `respawnTimer=0` so the bot revives next tick (no 8s wait). Wire protocol unchanged; client unchanged. Tests: `tests/multiplayer/bot-test-mode-respawn.test.js` (4), +2 cases in `arena-bot-sim-env-override.test.js`. Gates: build OK, regression ALL GREEN, vitest 2950/2950 across 224 files.
- **v0.2.627-alpha - CONTROLLED TEST ENVIRONMENT (ADR-0018).** Repeated ghost-nameplate + hit-reg fixes (v0.2.623…626) shipped without producing a clean repro; play tests were noisy with 5 bots + boss. This ships a server-only knob: `BOT_COUNT_OVERRIDE` and `BOSS_COUNT_OVERRIDE` env vars read at `createArenaBotSim` module init (`server/bots/arenaBotSim.js`). Operator sets `BOT_COUNT_OVERRIDE=1 BOSS_COUNT_OVERRIDE=0` via `systemctl edit torii-quest` and restarts — spawns a lone Doc (id=0, per deterministic `id % 7` name allocator in `botIdentity.js`) with no boss. Defaults unchanged when env is absent or invalid. `arenaBotSim.spawn()` now defaults to the env-driven count (call sites like `arena-ws.js:182` no longer pass `BOT_COUNT`). Client untouched (MP roster comes from `BOT_STATE` snapshots; SP still reads config `BOT_COUNT`). Wire protocol unchanged. Tests: `tests/multiplayer/arena-bot-sim-env-override.test.js` — 6 real cases against actual `arenaBotSim` module with `vi.resetModules()` per case (defaults, 1/0, 0/0, 3/1, invalid falls back, explicit spawn arg still overrides). Gates: build OK, regression OK, vitest 2944/2944.
- **v0.2.626-alpha - CLIENT BOT STATE SYNC + ZONE PLUMB (ADR-0016 + ADR-0017).** Fixes two v0.2.625 play-test bugs. (A) Ghost nameplates: screenshot showed dwarf-name labels floating above ground with no body. Root cause: nameplate is `scene.add(sprite)` (scene-level sibling of `root`, not a child), so `applyLod` setting `root.visible=false` never touched it; the dead branch of `_syncNetBot` also never called `applyLod`, so a dead bot stayed with the last LOD's root visibility while the nameplate updated position every frame. (B) `[SHOT]` diagnostics all read `zone=unknown` despite server BOT_HIT carrying zone: client dispatch dropped it. ADR-0016 adds `BotModel.setNameplateVisible(v)` and an invariant enforced each frame in both `_syncNetBot` and SP `_syncBot` (nameplate visible iff `root.visible && alive`); dead bots now run LOD after `DEATH_ARC_DURATION` (1.3 s) so the ragdoll flight is never popped mid-air but old corpses cull consistently with live bots. ADR-0017 is a one-line client fix: `applyBotHit(p.botId, p.hp, p.zone)`. Zero server change, zero wire-protocol change. Tests: `tests/multiplayer/bot-nameplate-lifecycle.test.js` (8), `tests/multiplayer/bot-hit-zone.test.js` (4). Gates: build OK, regression OK.
- **v0.2.625-alpha - MP HIT-REG ALIVE-WINDOW FIX (ADR-0015).** Fixes the "shot bot many times to no effect" bug captured in the v0.2.624 [FIRE] diagnostics. Root cause: `server/bots/arenaBotSim.js::resolvePlayerShot` rewound bot POSITION to shot-ts but checked the CURRENT `live.alive` bit. For the ~viewLag ms between server-death and BOT_KILL reaching the client, every client-confirmed hit was silently dropped even though the shooter saw a live target. Fix: `wasAlive || isAlive` gate accepts a hit when the bot was alive at the rewound instant OR is alive now. Closes the death-side window without reopening the v0.2.383 respawn-side fix (freshly respawned bot must be hittable). Damage against a bot that has since died is safely absorbed by `applyBotDamage`'s `!st.alive` short-circuit (no double-kill). Server-only change, ~5 lines in resolvePlayerShot. Zero client change, zero protocol change, zero damage/collider/authority change. Test: `tests/multiplayer/bot-alive-window.test.js` (5/5). Gates: build OK, regression 21/21, vitest all green.
- **v0.2.624-alpha - TRIGGER-FIRE DIAGNOSTICS (ADR-0014).** One `[FIRE]` log line per trigger pull, wired into `src/weapons.js::recordPlayerShot` right after `resolveLocalHitscan`. Shape: `[FIRE] mode=<sp|mp> hit=<none|bot|terrain|dead-bot|other> botId=<N|-> name=<Name|-> zone=<head|body|limb|-> toi=<m|-> resolved=<yes|no|mp> reason=<clean-hit|miss|dead|net|other>`. Closes the ADR-0013 blind spot: shots that never resolved to a bot hit (aim ray missed / hit terrain / hit a dead bot's parked collider / MP deferred) now leave a log fingerprint. Gate: `window.__toriiFireDiag` (default ON, independent from `__toriiBotDiag`). Zero gameplay change — no extra rays, no damage/authority/state mutation. New `src/engine/entities/fireDiagnostics.js` (pure composer + gated logger). Test: `tests/fire-diagnostics.test.js` (14/14). Gates: build OK, regression 21/21, vitest 2921/2921. Next: play a round, share the `[FIRE]` stream from the moment shots start missing.
- **v0.2.623-alpha - BOT IDENTITY + DIAGNOSTICS LAYER (ADR-0013).** Diagnostic + identity layer, zero gameplay change. Regulars now spawn with deterministic dwarf names (Doc, Grumpy, Happy, Sleepy, Bashful, Sneezy, Dopey) via `nameForBotId(id % 7)`, shared between SP (`src/engine/entities/botSim.js`) and the MP server (which imports the same sim). Every live bot renders a world-space name sprite above its head, built on the existing boss nameplate lifecycle in `src/botModel.js`; `raycast=()=>{}` so labels never intercept shots. Every shot/kill/respawn logs a single greppable `[SHOT|KILL|RESPAWN] botId=N name=Sleepy hp=5→3 zone=body alive=true isDying=false lod=full dist=14.2` line, gated behind `window.__toriiBotDiag` (default ON while chasing the stuck-bot bug). No HP arithmetic, damage-table, HIT authority, collider, death-FSM, or AI changes — pure identity + observability. Follow-up: reproduce the stuck-bot bug with logs, then a targeted fix under its own ADR. Test: `tests/bot-identity.test.js` (7/7). All gates green.
- **v0.2.622-alpha - INPUT + RENDER STABILITY (cherry-pick from v0.2.608 + v0.2.612).** Two independent fixes on the v0.2.605 baseline: (1) combat/bots/LOD improvements from v0.2.608 — hit-classifier tightening, bot-tactics tuning, LOD hysteresis, updated bot-sim + classifier tests; (2) input & render stability from v0.2.612 — stuck-key guard clears held W/A/S/D on window blur, tab-hide, and pointer-lock exit (fixes NIP-07-signer-caused stuck-run); quality-tier no longer toggles renderer.shadowMap.enabled at runtime (removes mid-game shader recompile stall) — LOW now uses a cheap 256px shadow map instead. ADRs 0011 (combat/LOD) and 0012 (input + quality-tier) lock these in. Skipped v0.2.616 (homepage sea/grass) — depends on v0.2.607+ homepage rebuild which is ADR-0010 risky. All tests + regression check green.
- **v0.2.621-alpha - HARD RESET TO v0.2.605 BASELINE.** Literal `git reset --hard v0.2.605-alpha` + version-string bump only. Restored working crosshair + ESC after v0.2.606–v0.2.620 accumulated regressions broke them. Ten load-bearing ADRs installed in `docs/adr/` to gate future changes to sensitive areas.
- **v0.2.528-alpha - NAP ZONE LAYOUT FIX.** Fixed the NAP zone object layout to be left-to-right as the maintainer specified: leaderboard → torii gate → product panel → mirror, all flush with the curved NAP edge. The NAP zone edge is a CURVE (not a straight line) — each object's z-position must match the edge at its x-position. Previous attempts (v0.2.525–v0.2.527) failed because objects were placed at wrong z-values (not flush), the mirror was too wide (8.12m) and overlapped the torii gate, and the ordering was wrong. Final positions: Leaderboard (-6.5, 32.5) yaw=PI, Torii gate (0, 32) unchanged yaw=PI-PI/4-PI/18, Product panel (5, 31) yaw=PI+0.15, Mirror (9, 28.5) width shrunk to 5m yaw=PI+PI/4 (45° to follow edge curve). Mirror was shrunk from 8.12m to 5m to keep both edges on solid NAP land. Both mirror edges verified on land (h=0.92). NPC (napNpc.js) confirmed working in v0.2.526 console output: walks, loads 18 gesture clips. 2668 tests pass, 21 checks green.
- **v0.2.527-alpha - NAP LAYOUT ATTEMPT (FAILED).** Incorrectly placed mirror at (12.26, 25) — too far around the curve, appeared to cross the NAP/arena boundary. Product panel at wrong z. Leaderboard at wrong z. Angles not updated for new positions. User reported "even worse." Superseded by v0.2.528.
- **v0.2.526-alpha - MIRROR REPOSITION ATTEMPT.** Moved mirror to (0.26, 31) — but this was on top of the torii gate at (0, 32). User reported "nothing has changed" (the mirror was invisible behind/inside the gate). NPC fix confirmed working in this version's console output.
- **v0.2.525-alpha - NAP OBJECT ROTATIONS.** Applied: torii gate +10° CW (YAW_DELTA = -PI/4 - PI/18 = 55° total), leaderboard +5° CCW, product +10° CW. Mirror moved to x=-1. These were correct angle changes but at wrong positions.
- **v0.2.520-alpha - NAP OBJECT REPOSITION + NPC REWRITE.** Moved all NAP objects to z=31 edge. Rewrote napNpc.js with _minY fix, separate GLTFLoader for gesture GLB, fade transitions. NPC starts at (-4, 22), wanders NAP zone using isNapLand() + NAP_BBOX. 2668 tests pass.
- **v0.2.601-alpha - PER-BONE STICKER COLLIDERS ON BOTS.** Extended the per-bone sticker system to bots. Added createBotBoneColliders() in bodies.js that creates ball sensor colliders on all bot skeleton bones, reusing the colliderToBone map with { kind:'bot', bot, bone } entries. BotModel now exposes skinnedMesh ref. bots.js creates bone colliders in _ensureBotColliders (after body+head), syncs them each frame after model.tick()+syncTo() in both _syncBot (SP) and _syncNetBot (MP), parks them at y=-100 on death (_applyKillRender + _syncNetBot dead path), and re-creates on revive. Capsule-to-model upgrade in _attachModelBot also creates bone colliders. castRay() now excludes bone colliders by default when no filterPredicate is passed — prevents combat raycasts from hitting per-bone sensors and breaking damage resolution. stickerNpc.js handles bot bone hits (bone.attach) and bot broad capsule hits (parent to bot.model.root). Bot meshes excluded from Three.js raycaster via userData.isBotMesh flag. 2668 tests pass, 21 checks green.
- **v0.2.466-alpha - SUNRISE SUN FIX.** Retired the entire `_buildBitcoinSun` IIFE (additive canvas-corona sprite scale 38 + NormalBlending PNG ₿ overlay scale 55) - it stacked on the shader disc at (0.85, 0.18, -0.45) and bloomed into the right-side white glare. Sky shader now carries the ONLY sun: disc pow 90->40 rebuilt as `mix(base, vec3(1.0,0.50,0.18), disc*0.8)` (deep orange, no white clamp), corona pow 14->8 * 0.4, wide glow 0.35->0.12, and the dome-wide pow(sunAngle,2.0)*0.18 horizon flush DELETED (left-side glare). Added subtle Japanese rising-sun rays (14 beams, cross-product basis around sunDir, smoothstep-masked near the sun/horizon, *0.12). Zenith gold band 0.45->0.20, shimmer 0.08->0.04. Ambient hue 0xffd9a0->0xffd090, fog hue 0xe6c4a4->0xe6bc94 (density/intensity unchanged). Combat / score publishing untouched.
- **v0.2.465-alpha - SUNRISE ATMOSPHERE POLISH.** Rebalance after the v0.2.464 tone-down went too far on the ring and the sun still read as glare. Ring: coastline neon emissiveIntensity 0.95->1.4 (visibly glowing again, no sky bloom) and ground-wash opacity 0.22->0.28 to keep the ring/ground balance. Sun: sprite + sky-shader sunDir lowered to y=0.18 with depthTest ON so the mountain ridge occludes the lower half of the disc; sprite scale 48->38 (btc 72->55); corona/disc pulled toward amber; shader amber-disc multiplier 2.2->1.4; horizon mix deepened to peach-amber; ambient 0.72->0.85; fog density 0.007->0.008. Combat / score publishing untouched.
- **v0.2.464-alpha - SUNRISE-OF-HOPE ATMOSPHERICS.** Tone down sun glare and redirect arena-ring glow onto the ground. Sky: exposure 1.8->1.2, directional sun 1.8->1.15 amber, fog 0xc8dde8->peach 0xe6c4a4, sky disc 6.0->2.2 warm gold, Bitcoin sprite moved onto the same low-east sunDir and scaled 90->48. Ring: neon emissive 2.2->0.95 plus `coastline-ground-glow` additive ribbon inset 0.7m. Combat / score publishing untouched.
- **v0.2.463-alpha - HANDOFF DOC REFRESH.** Doc-only rewrite of §0/§1/§9 for a clean agent hand-over.
- **v0.2.462-alpha - PEER JOIN APPEARANCE SPEEDUP (warm avatar pool).** Slow peer appearance was NOT the GLB fetch (SW cache-first) - it was the per-avatar BUILD running synchronously on the join path after the download: skeletonClone, a full traverse re-materialising every mesh, computeBoundingBox, mixer.clipAction() for all 18 clips, and the gun bone-attach. Fix: `_prewarmPeerTemplates()` now fully BUILDS one avatar per character into a new `_mpWarmPool` at MP connect; `_createPeerAvatar` hands the pooled instance to the roster (stamping the real peerId) so a peer pops in THIS frame. Cold-build fallback when the pool is empty. `_createPeerAvatar` split into `_buildPeerAvatarObject(character, peer?)` + a thin pool-aware wrapper.
- **v0.2.461-alpha - MIRROR SELF-SHOOT FIX.** Firing while standing still showed plain IDLE in your own mirror (local `tickPlayerModel` gated RUN_SHOOT behind forward/run movement). A PEER standing still already reads as firing via its Run_Forward_Firing shoot one-shot (in-place since the root-motion strip). Fix: `playerModel.js` plays the SAME RUN_SHOOT clip for `isShooting && !moving`, so your mirror-self fires in place exactly like a peer.
- **v0.2.460-alpha - MP IDLE-DROP FIX (Option A: idle players stay connected) + JOIN-DELAY PREWARM.** Peers vanished after a few idle minutes because the server idle-swept any session silent for 60s (`IDLE_DISCONNECT_MS`) and broadcast LEFT, while the client keepalive PINGed only every 25s - a single tab-throttled setTimeout could slip past the reap. Fixed both ends: client `KEEPALIVE_MS` 25s -> 15s (4x margin) and server `IDLE_DISCONNECT_MS` 60s -> 90s (6 missed pings), so an idle player is never dropped. Reconnect `BACKOFF_MS_CAP` 30s -> 2s so a real blip rejoins in ~2s. Plus the first template-only `_prewarmPeerTemplates()` on MP connect. Tests reference the constants by name, so they track the values.
- **v0.2.459-alpha - RUN+SHOOT ANIM FIX + NOSTRICH MASTER RE-BAKE.** RUN_SHOOT never played (single-frame `_isShooting` flag); fixed with a sticky 400ms `_shootUntil` window driving both `tickPlayerModel` and the MP anim hint. nostrich-master.glb re-baked with three `tools/glb_retarget.py` fixes (frame-change matrix was the inverse of intended; added per-bone shortest-arc bind-axis alignment; exact animated-parent local conversion; quaternion hemisphere continuity). Bone-axis deviation vs library now 0-4deg every clip (was 50-180deg), zero flips, root motion 0.000m, Draco 3.66MB. New `tools/glb_pose_compare.py`.

**Deployed live:** the maintainer deploys manually over SSH with `sudo torii-deploy <tag>` (the update-runner resolves the git tag, builds with `--base=/quest/`, and installs to chiefmonkey.art). **Latest tag pushed: v0.2.528-alpha** (not yet deployed — user runs `sudo torii-deploy v0.2.528-alpha`) - confirm the live version with `curl -s https://chiefmonkey.art/quest/ | grep -o 'v0\.2\.[0-9]*-alpha'`. MVP APPROVED 2026-07-27. Suite `install-quest.sh` tolerates generated-but-tracked files (`public/dashboard.html` + `public/torii-quest-data.json` are rewritten by `npm run build` on every deploy).

---

## 0. Current snapshot

> This section was last fully updated 2026-08-17 and drifted out of sync with the top-of-file "Current version" line as later releases shipped. Version/live/test-count below are corrected as of 2026-08-26; everything else in this section (combat values, MP timing model, known issues) has not been re-verified since 2026-08-17 and may itself be stale — re-check against source before relying on it.

- **Source version:** main @ post-v0.2.717 (untagged); `package.json` still `0.2.717-alpha`. PR #84 (ADR-0082/0083 napplet game host + avatar shell scaffolds) and PR #87 (ADR-0084/0085 arena + sticker studio napplet registrations, wiring-only) merged 2026-08-31 - alpha marker will bump on next release cut. See the "Current head" block at the top of this file.
- **Live:** https://chiefmonkey.art/quest/ is running **v0.2.717-alpha** (frontend + arena-ws, checked 2026-08-31) — current source version. The maintainer deploys manually; the update-runner resolves the git tag and builds with `--base=/quest/`. Source version routinely runs ahead of what's actually deployed — always verify live version rather than assuming it matches source.
- **Multiplayer:** LIVE and working (`MP_ENABLED=true` in `src/config.js`); two-npub in-world play confirmed 2026-08-15: idle players stay connected (v0.2.460), peers appear promptly on join (v0.2.462 warm pool), and mirror self-shoot reads correctly (v0.2.461).
- **Tests (source, current):** 3508 across 281 files at main HEAD (post-v0.2.717); curated file count lives in `src/engine/dashboard/toriiQuestDashboardData.js`, `src/engine/status/mvpReadiness.js`, `NEXT_ACTION_STATE.json` (must all agree). `node tools/regression-check.mjs` 21/21 GREEN, verified 2026-08-31.
- **Nostr score publishing:** DISABLED (`SCORE_PUBLISH_ENABLED=false` in `src/config.js`) as of the 2026-08-17 snapshot. Do NOT re-enable until the maintainer explicitly says so - it is for the very end of alpha.
- **Combat values (DO NOT CHANGE):** BOT_HP=5, BODY_DAMAGE=3, HEADSHOT_DAMAGE=9, BOSS_HP=60, BOSS_TARGET_HEIGHT=3.0, BOSS_RADIUS=0.8, BOT_BODY_RADIUS=0.30, BOT_HEAD_RADIUS=0.30, LAG_COMP_MS=300.
- **MP timing model (verified 2026-08-15):** client PING `KEEPALIVE_MS=15_000`, server idle reap `IDLE_DISCONNECT_MS=90_000` (sweep interval still 60s), reconnect backoff `BACKOFF_MS_INITIAL=500` doubling to cap `BACKOFF_MS_CAP=2_000`. On reconnect the WELCOME carries the full roster; `finishAuth` re-broadcasts JOIN to others.

### Known remaining issues (not blockers)
- **Animation clip-choice polish** - minor glitchiness in clip selection/transitions; maintainer deferred this (it is about picking better clips, not a correctness bug).
- **Bot hit inconsistency** - occasional reported inconsistency in bot hit registration.
- **Flaky test** - `tests/` 'a regular bot takes damage and DIES' occasionally fails on first run and passes on re-run (test-isolation flake, not a product bug).

### For the next agent
- The active MVP focus is the 15-hour proof-of-concept route (`torii-quest-strategy.md`); the shooter is maintenance-only unless demo-breaking.
- Read `torii-quest-strategy.md` + `torii-quest-todo.md` + `torii-quest-progress.md` first; use the §10 next-job format.
- NIP-60 is retired (PR #95, `retire-nip60-cashu-me`); cashu.me is the wallet rail, and the `meshGenerationExecutor` `charge` seam is its integration placeholder. Release-time napplet identity is NOT "ADR-0092" — that is arena-full-sandboxing (parked); the real-bundle-hash work needs a new ADR defining what "the bundle" is (see todo item 3).
- **ADR baseline is clean (2026-09-01):** numbering deduplicated (the 0082 and 0090 collisions resolved), `docs/adr/README.md` index rebuilt across all 97 ADRs, and shipped ADRs flipped from Proposed to Accepted. Adding a new ADR means taking the next number from `main` per rule 7, not from a local branch.
- Always read code before editing. Root causes in this codebase are found by reading (e.g. parsing GLB binary, tracing the MP wire), not guessing.

### NAP zone object layout (CRITICAL — read before touching NAP objects)

The NAP zone edge is a CURVE (mitsudomoe shape), not a straight line. Each object's z-position MUST match the edge at its x-position. Use `isNapLand(x, z)` + `sampleNapHeight(x, z)` from `src/terrain/tomoeShape.js` / `src/terrain/heightmap.js` to verify positions are on solid land (h > 0.7).

**Current layout (v0.2.528-alpha), left to right, flush with curved edge:**

| Object | Position (x, z) | Yaw | File |
|---|---|---|---|
| Leaderboard | (-6.5, 32.5) | `Math.PI` (face south) | `src/engine/world/proofSurfaceSpecs.js` |
| Torii gate (travel gateway) | (0, 32) | `Math.PI - PI/2` (90° CW, gate plane north-south) | `src/config.js` (TRAVEL_GATE_*) + `src/arena.js` |
| Product panel | (5, 31) | `Math.PI + 0.15` | `src/engine/world/proofSurfaceSpecs.js` |
| Mirror (with frame) | (9, 28.5) | `Math.PI + PI/4` (45°, follows curve) | `src/mirror.js` (MX, MZ, MW, mirror.rotation.y) |

**Key values:**
- `TRAVEL_GATE_X = 0`, `TRAVEL_GATE_Z = 32`, `TRAVEL_GATE_YAW_DELTA = -Math.PI/2` (90° CW, gate plane north-south) (in `src/config.js`)
- Mirror: `MW = 5.0` (shrunk from 8.12m in v0.2.528), `MX = 9`, `MZ = 28.5`, `mirror.rotation.y = Math.PI + Math.PI / 4`
- Anchor positions in `src/engine/world/anchorTransforms.js` must match proofSurfaceSpecs positions
- `ARENA_HALF = 20` (in `src/config.js`)

**Two gates (CRITICAL DISTINCTION):**
1. **Travel gateway** (`torii-gateway-experience.glb`): At (0, 32) in NAP zone with portal ring. This is what the user means by "torii gate" or "Torii Gateway". GLB uses `gate.rotation.y = Math.PI + TRAVEL_GATE_YAW_DELTA`. Procedural fallback uses `fallback.rotation.y = Math.PI / 2 + TRAVEL_GATE_YAW_DELTA` — DIFFERENT base rotation (90° mismatch).
2. **Entrance/gamma markets gate** (`torii-gate.glb`): At bridge position (-24, 3), hardcoded `gate.rotation.y = Math.PI` — NEVER touched by TRAVEL_GATE_YAW_DELTA.

**NAP edge reference (z at each x where height > 0.7):**
x=-12→z=30.5, x=-10→z=31.5, x=-8→z=32.5, x=-6→z=32.5, x=-4→z=32.5, x=-2→z=32.5, x=0→z=32, x=2→z=32, x=4→z=31.5, x=6→z=31, x=8→z=30, x=10→z=28.5, x=12→z=25.5

**NPC (napNpc.js):** Starts at (-4, 22), wanders NAP zone using `isNapLand()` + `NAP_BBOX`. Plays random gesture clips from `chiefmonkey-npc-animations.glb` every 5-10s. Confirmed working in v0.2.526 console output. NPC animations GLB is for NPC ONLY — do NOT add to playable characters.

**User's layout instructions (verbatim):**
- "from left to right the correct layout is, leaderboard, torii gate, product panel, mirror"
- "flushed with the edge of the nap zone"
- Mirror and frame treated as one grouped object
- Torii gate and glow ring treated as one grouped object
- "to the right" = right-hand side when facing north toward the NAP zone edge
- The NAP zone edge curves — "flush with the edge" means each object sits at the correct z for its x

---

## 1. What this is

A browser arena shooter — Three.js (WebGL) render, Rapier3D (WASM) physics, Nostr identity, Bitcoin/ecash (fake sats in alpha). Vite build. Pure ES modules. GPL-3.0.

- **Live:** https://chiefmonkey.art/quest/ (SHC VPS, Torii Suite install — the ONLY live host). Multiplayer runs on the VPS at `wss://chiefmonkey.art/mp` (root-level same-origin, nginx → `torii-arena-ws.service` on `127.0.0.1:8788`). The old pplx.app publish (`quest-torii.pplx.app`) is a stale fallback whose backend sandbox is down — do NOT treat it as live.
- **Active focus:** 15-hour proof-of-concept route (`torii-quest-strategy.md` → "15-Hour Proof-of-Concept Route"; `torii-quest-todo.md` → "ACTIVE FOCUS"). Shooter is maintenance-only; the active MVP is the freedom-tech loop — gateway/NAP-to-NAP preview, Plebeian/Nostr product panel proof, leaderboard preview, torii.quest update-check (LEAN-1..LEAN-5).
- **Multiplayer architecture (current):** server-authoritative hit resolution on a single wire (`PROTOCOL_VERSION=1`). Pure server modules under `server/combat/` (snapshotRing, capsuleModel, rayVsCapsule, damageTable, hpLedger, hitResolver); arena-ws keeps a per-peer snapshot ring, rewinds to the shot timestamp (clamped to `LAG_COMP_MS=300`), damages via a parity-locked table (head=9, body=3), broadcasts `HIT`/`KILL` to ALL, and issues `RESPAWN` to the victim after `RESPAWN_MS=3000`. Client `sendHit` is a no-op; `wsClient` handles `RESPAWN` and `arenaRuntime` warps the player + resets HP. Auth is a per-session NIP-42 (kind:22242) challenge, OR a server-issued session token from a single NIP-98 (kind:27235) login sign (v0.2.375) so players sign once at login and never in-game. Peer avatars load via a DRACOLoader-equipped GLTFLoader and are pre-built into a warm pool at MP connect (v0.2.462).
- **Nostr score/leaderboard publishing (MP-3):** the code LANDED (v0.2.366) but is intentionally DISABLED — `SCORE_PUBLISH_ENABLED=false` in `src/config.js`. Do NOT re-enable until the maintainer explicitly says so (it is for the very end of alpha). Server broadcasts an authoritative `SCORE` frame on peer disconnect; each peer signs its OWN row via nip07 and publishes kind:30078 (`d=torii-quest`) + kind:1 (`t=torii-quest-score`). Modules: `server/combat/scoreLedger.js`, `src/engine/multiplayer/scoreReporter.js`, `src/engine/multiplayer/leaderboardAgg.js`, `src/ui/leaderboardPanel.js`.

## 2. Standing operating rules (project-wide, across all Torii repos)

1. Each Torii app lives in a fully separate GitHub repo (`torii-quest`, `torii-continuum`, `torii-de`, `torii-base`, `torii-suite`). Files carry ONLY that repo's project name — Quest files say "quest", never "continuum" or "de". Never cross-name.
2. Bump the version on EVERY change — including doc-only changes, comment tweaks, filename renames, typo fixes.
3. Push everything to GitHub immediately via a PR that lands on `main`. No local-only work.
4. Never publish device names, hostnames, or local machine identifiers to GitHub. Use generic terms like "your local machine".

## 3. Hard constraints (do NOT break these)

Enforced by `npm run check` (`tools/regression-check.mjs`).

1. **Version bump on every deploy.** Every marker in §4 matches `EXPECTED_VERSION`.
2. **`godMode` stays `false`** in `src/config.js`. Never commit `true`.
3. **No new `setTimeout`** except the two allowed sites (`nostr.js` WS-close, `hud.js` kill-feed).
4. **No new `Vector3`/`Matrix4` in hot paths.** Reuse module-scope scratch vars. Modules with no `three` import are exempt.
5. **Spelling:** comments say "nostrich" (never "ostrich"); character is "Chiefmonkey" (exact case).
6. **Debug tools ship unconditionally** — `window.ToriiDebug` is intentional in public alpha.
7. **ESC = instant pause**; a click that only re-locks a panel-locked cursor must never fire the weapon.
8. **Firing:** bullets originate at the gun barrel and aim through the crosshair (camera ray finds aim point; barrel→point is the bullet line).
9. **`state.phase` is written ONLY in `state.js`** (via `transition()`). Others read predicates (`isPlaying()` etc.).
10. **No internal use** of deprecated globals `window._onBotHit`, `window._grassMat`, `window._flowerMat`, `window._mirrorMesh` — kept as documented debug taps only.
11. **Split modules by concern, not line count.**
12. **Do not name** Google, Cloudflare, Microsoft, or Babylon.js in docs.

## 4. Version markers (bump together)

| File | Location |
|---|---|
| `src/config.js` | `export const VERSION` |
| `index.html` | `#version-label`, `#ver` |
| `package.json` | `"version"` — semver form, no leading `v` |
| `tools/regression-check.mjs` | header comment, `EXPECTED_VERSION`, stale-version guard regex (flag the PREVIOUS version) |
| `src/engine/dashboard/toriiQuestData.js` | `TORII_QUEST_VERSION` (pinned to `config.js`) + `metrics` "Source version" + "Tests" test-count row |
| `public/sw.js` | `CACHE_VERSION` literal (`tq-<version>`) — copied verbatim by Vite; check [5] fails if stale |
| `MVP_APPROVAL_STATE.json` | `version` — generated by `npm run approval:state -- --write`; test asserts it tracks `config.js` |
| `torii-quest-{strategy,todo,progress,handoff}.md` | "Current version" lines |

**NOT version markers — do NOT bump:**
- `MVP_PLAYTEST_RESULTS.md` — the human tester fills the build cells with what they actually tested; committed baseline is `not-run`.
- `LIVE_SMOKE_STATE.json` — records the DEPLOYED build a live smoke observed; legitimately lags `config.js`.
- `DASHBOARD_SMOKE_STATE.json` — same rule for the dashboard smoke.

## 5. Source of truth

**Code:**
- `src/config.js` — all constants and tuning. No scattered magic numbers.
- `src/state.js` — the ONLY place game phase changes; FSM table + weapon predicates.
- `src/main.js` — wiring only; no game logic.
- `src/sdk/index.js` — public SDK entrypoint. Curated node-safe re-exports, `SDK_VERSION`, `STABILITY` tiers, frozen `SDK_SURFACE` tier map. Only re-export modules that never transitively import `scene.js`.
- `engine/` — extracted mostly-pure SDK seams (debug, physics, combat, entities, ui, weapons). Prefer adding pure logic here so it stays node-testable.
- `CODE_INDEX.md` — file-by-file map. Update when adding/moving a module.

**Docs (project-scoped, this repo only):**
- `torii-quest-strategy.md` — vision + decision rules.
- `torii-quest-todo.md` — active task queue.
- `torii-quest-progress.md` — execution dashboard.
- `torii-quest-handoff.md` — this file.
- `docs/adr/` — architecture decision records; `docs/adr/README.md` is the index + numbering convention (rule 7: numbers are claimed by merge on `main`, not booked in advance).
- `VPS_INSTALL.md` — self-hosting the static build at torii.quest.
- `UPDATE_CHECK.md` — manual update-check safety boundary.
- `NOSTR_ARENA_MASTER_TODO.md` — archival history only; not an active queue.

**Safe edits to the four continuity docs** go through `npm run md:patch` (`tools/mdPatch.mjs`): whitelist-confined, per-file capability map (handoff is append-only; `replace` is rejected), `.bak` backup before every edit, no network, no arbitrary writes. The `note` action appends a timestamped bullet under the file's default heading — e.g. `npm run md:patch -- note torii-quest-progress.md "shipped X"`.

## 6. Build / test / check commands

```bash
npm install
npm run dev                 # local dev server (vite)
npm run build               # production build → dist/
npm run check               # static regression guardrails (tools/regression-check.mjs)
npm test                    # vitest — FULL unit suite (node env)

# Test profiles (v0.2.173) — deterministic curated file lists in tools/testProfiles.mjs
npm run test:fast           # ~5 core files, innermost edit→test loop
npm run test:foundation     # ~16 files, broader confidence (fast ⊆ foundation)
npm run test:release        # build + FULL vitest + check + bundle:report + handoff:status
                            # THE release gate — profiles never replace it

# Visibility / readiness tools (read-only, network-free, exit 0; NOT gates)
npm run handoff:status      # one-glance snapshot: version/pkg sync, git commit, live URL, checks, docs, reports, bundle
npm run release:status      # single READY/NOT-READY/INCOMPLETE verdict aggregating ship signals
npm run release:status:json # same verdict as JSON (or: node tools/release-readiness.mjs --json)
npm run handoff:summary     # concise brief for the next agent/model
npm run docs:stale          # advisory stale-doc detector (drift catcher; NOT in `check`)
npm run bundle:report       # advisory built-bundle size baseline
npm run zones:check         # verifies /zone/* SPA-fallback docs + dist layout (also regression-check [15])
npm run vps:dry-run         # local pre-deploy readiness checklist; exits non-zero on blocking FAIL only
npm run release:meta        # release/update metadata for the future torii.quest update-checker
npm run release:dry-run     # local GitHub MVP release dry-run; runs NO git tag/push/gh release
```

**Green** = build + check + test all pass. Docs/status drift is guarded by check `[14]`; the continuity docs must carry the current version or `npm run check` fails.

Tests run in node (`vite.config.js` → `environment: 'node'`). `WebGLRenderer` is created at module load in `scene.js`, so any module that transitively imports `scene.js` (`player.js`, `weapons.js`) CANNOT be imported in a node test. Write new logic as a pure module (no `three`/Rapier/DOM import) to keep it testable — see `engine/debug/snapshot.js`, `engine/physics/interactions.js`, `engine/physics/raycastService.js` for the pattern.

Optional headless smoke (not in CI): `npm i -D puppeteer-core`, drive Chrome with swiftshader flags against `npm run preview`, click `#btn-enter`, inspect `window.ToriiDebug.snapshot()`.

## 7. Deploy / publish (task agents may publish when explicitly instructed)

Deploy target is the pplx.app subdomain `quest-torii.pplx.app`. Build artifact is `dist/` (`npm run build`). **Maintainer shortcut: "bump and push" = bump version, update the live site quest-torii.pplx.app, and push everything to GitHub.**

**Task agents SHOULD ship end-to-end when the maintainer says "go", "publish it", "bump and push", or similar.** The pipeline is: green gate (build + check + vitest + release:status) → commit → PR → squash-merge to main → `npm run build` → deploy_website → publish_website (with site_id `93507979-679f-4aac-949d-20a4a33d7352`) → smoke test the live URL. Old handoff rule that maintainer publishes is superseded as of v0.2.365.

**publish_website shape (locked):** `project_path=/home/user/workspace/torii-quest/dist`, `dist_path=/home/user/workspace/torii-quest/dist`, `app_name='Torii Quest'`, `port=5000`, `run_command='node server/arena-ws.cjs'`, `install_command='npm install --omit=dev --no-audit --no-fund'`. Requires `dist/package.json` to exist with `ws` runtime dep — created if missing.

**Self-hosted VPS at torii.quest** is the eventual target but Sovereign Hybrid Compute VPS is currently paused — do NOT reference Namecheap or any other provider.

**Pre-publish security review is REQUIRED** — run subagent with `/home/user/workspace/skills/website-building/website-publishing/security_subagent_prompt.md` against `dist/`. Address BLOCK findings automatically; surface WARN findings to maintainer.

**Self-hosting the static build at `torii.quest`** (shared Ubuntu VPS — Caddy/Nginx + HTTPS, DNS checklist, manual GitHub update sequence, symlink rollback, security posture): see `VPS_INSTALL.md`. No server is touched from this repo.

**SPA `/zone/<slug>` deep-link rewrite (REQUIRED for hard-refresh).** The `zoneRoute` parser gives `/zone/<slug>` a safe client-side interpretation, but it only runs after `index.html` + JS have loaded. A static host will 404 on a cold hard-refresh to `/zone/<slug>` unless configured to fall back to `index.html`:
- **Nginx:** `location / { try_files $uri $uri/ /index.html; }`
- **Caddy:** `try_files {path} /index.html`
- **Static CDN / object storage:** set SPA/404 fallback document to `index.html`

Keep CSP unchanged. Same-origin in-app navigation (`history.pushState`) is unaffected. `npm run zones:check` verifies the docs describe the fallback and that `dist/` has no file shadowing `/zone/*`. Full checklist: `ZONE_FALLBACK_READINESS.md`; concrete server blocks in `VPS_INSTALL.md` §6a/§6b/§11.

## 8. Debug surface

`window.ToriiDebug` (ships in alpha):
- `.snapshot()` — one JSON-serialisable object: version, phase, run state, player pos, combat last shot/hit/miss, physics+crate summary, tuning. Safe anytime.
- `.combat.report()` / `.physics.report()` — focused JSON sub-reports.
- `.shells.*()` — read-only reports over the SDK view shells + preview blocks (gateway, product, leaderboard, updatePreview, mvpLoop, hostTransport, gatewayActivation, gatewayPortalActivation, summary, diff, surfaceSpecs, surfaceGate, …). No signer, relay, publish, navigation, checkout, or fetch. See `SDK_DEBUG_INDEX.md`.
- `.physics.service` — injectable RaycastService facade (`ray`/`rayStatic`/`lineOfSight`).
- `.bots`, `.player`, `.physics`, `.world`, `.fx`, `.combat`, `.identity`.

## 9. Active issues / open edges

> **READ FIRST (2026-08-15):** the entries below are HISTORICAL, from the July pplx.app→VPS migration era (v0.2.366–v0.2.375). The bugs they describe (backend sandbox 503, peer avatars invisible/T-posed, idle-drop reconnect loops, ENTER-ARENA freeze, "LIVE vX until Suite redeploy") are all RESOLVED and superseded. Multiplayer is live and working on the VPS — see §0 for the current snapshot and the current known-issues list. Keep these entries only as a record of how the wire protocol, auth, and peer-avatar pipeline were built; do not act on their "pending redeploy" framing.

- **QA-MP-BLOCKER-1 — peer discovery verified at the protocol level on chiefmonkey.art/quest/ (2026-07-13; full in-world ENTER path NOT exercised in headless).** A simulated two-npub live test against the production install confirmed the wire-level handshake and cross-client peer visibility: two isolated browser contexts, each performing a real NIP-07 nostr-login with a distinct burner npub, both opened `wss://chiefmonkey.art/mp` → received `HELLO` (`serverVersion: v0.2.366-alpha`, `protocolVersion: 1`) → sent `AUTH` (NIP-01 kind-22242, BIP-340 schnorr, challenge-bound via `window.nostr.signEvent`) → server accepted (WELCOME returned). **B's `WELCOME.roster` contained A**, and **A received a `JOIN` frame for B** (`{id, npub, pos, rot, character}`). So the auth gate, roster fan-out, and JOIN broadcast are confirmed working on the live install.
- **v0.2.375-alpha — session-token arena auth ("1 sign at login, 0 signs in-game"); LIVE v0.2.370-alpha until a Suite redeploy picks up v0.2.375 (bump `TORII_QUEST_REF`).** The arena authed with a per-session NIP-42 (kind:22242) challenge. Because that challenge is anti-replay it CANNOT be cached, so every arena entry and every reconnect re-prompted the NIP-07 signer; combined with the score-publish sign and the login presence-publish sign, players hit ~5 signer prompts. Fix: the player signs ONE NIP-98 (kind:27235) HTTP-auth event at login and receives a server-issued opaque bearer token the arena WS reuses. **SERVER** — new PURE `server/auth/sessionTokens.js` (node-testable; injectable clock/RNG/stores): one-time-use challenges (TTL 60s, delete-on-consume), tokens (TTL 8h) stored as `sha256(token)` ONLY — the raw token is returned once and never stored or logged. Two plain-HTTP endpoints added to `server/arena-ws.js`: `GET …/mp/auth-challenge` → `{challenge,ttl}` and `POST …/mp/session` → `{token,npub}` after `verifyLoginEvent` (kind 27235, matching `challenge` tag, `u`+`method:POST` tags, hex64 pubkey, real schnorr via the shared `verifyNostrEventSig`). A new `finishAuth(sess,{npub,pubkey})` unifies the WELCOME + JOIN broadcast for BOTH the new `AUTH_TOKEN` branch (`verifyToken` → hex pubkey) and the unchanged NIP-42 `AUTH` branch. `sessionTokens.cleanup()` runs on the existing idle-sweep interval. **CLIENT** — new `src/engine/multiplayer/sessionAuth.js`: `resolveMpHttpBase()` derives the HTTP base from the SAME `MP_WS_PATH` mount the WS URL uses (mirrors `multiplayerHost.resolveUrl`'s PORT_SENTINEL logic — NEVER a hard-coded `/mp`); sessionStorage token store (`tq.mp.sessionToken`); `loginForSessionToken()` fetches a challenge, signs ONE kind:27235 event scoped to `POST /session`, exchanges it for a token, stores it, returns `{token,npub}` or null. `src/nostr.js` `nostrLogin()` tries the token flow (only when `window.nostr.signEvent` exists), then falls back to `getPublicKey()`. `wsClient.js` sends `{t:AUTH_TOKEN, token}` on HELLO when `getSessionToken()` returns one and NEVER calls `signAuth` in that case; on `AUTH_FAIL` for a token it calls `clearSessionToken()`, closes, and the reconnect (now tokenless) drives the NIP-42 `signAuth` path — one fallback prompt, then normal play. `multiplayerHost.js`/`arenaRuntime.js` thread `getSessionToken`/`clearSessionToken` (backed by the sessionStorage helpers). **PRESENCE** — the auto presence-publish on `EV.NOSTR_LOGIN` in `src/main.js` was removed (presence is WS-roster only now). In-game score signs were already zero: `createScoreReporter` is never invoked; the manual "PUBLISH MY SCORE" leaderboard button is user-initiated and left intact. `AUTH_TOKEN` is additive on `PROTOCOL_VERSION=1` (old clients drop it via the UNKNOWN_TYPE guard). Verified: `npm run build`, `npm run check`, `npm test` all green; new `tests/multiplayer/session-tokens.test.js` (challenge/token lifecycle + real-schnorr verify + raw-token-not-stored), `session-auth.test.js` (HTTP-base derivation, storage, one-sign login), AUTH_TOKEN cases in `wire-protocol.test.js`, and token-auth + AUTH_FAIL-fallback cases in `ws-client-state.test.js`. **NGINX (torii-suite v0.7.6-alpha):** `installers/install-quest.sh` `/mp` fragment split into exact-match plain-HTTP locations for `/mp/auth-challenge` + `/mp/session` (the login handshake) plus the existing Upgrade proxy for the arena socket — a `map`-based `$connection_upgrade` is illegal inside the server-scoped fragment, so the path split carries both transports. **Source-only — do NOT touch the VPS while the Continuum session is active.** Not closed until a real-browser retest shows exactly one signer prompt at login and zero on arena entry/reconnect/death.
- **v0.2.374-alpha — peer combat wired client-side (server-authoritative); LIVE v0.2.370-alpha until a Suite redeploy picks up v0.2.374.** The server already ran authoritative combat (`MP_MODE='authoritative'` in `server/arena-ws.js`: `SHOT` → `resolveAndBroadcast` → `HIT`/`KILL` via lag-compensated peer snapshots), but the client had two dead wires: the `EV.SHOOT` handler never called `_mp.sendShot`, and `_mpEmit` only handled `mp_respawn`, so inbound `HIT`/`KILL` were dropped. Players therefore could not damage each other. Fix: two seams in `src/arenaRuntime.js`, both delegating to a new PURE `src/engine/multiplayer/peerCombat.js` (no three/DOM — unit-testable, keeps the runtime a thin wiring layer). **OUTBOUND** — the `EV.SHOOT` handler sends `_mp.sendShot({origin,dir,ts})` for every arena shot when `_mp.selfId` is set and the player is inside the arena (`shouldSendShot({playerX,napX,selfId})`); it prefers the AIM ray (`aimOrigin`/`aimDir`, camera-through-crosshair) so server hit-detection matches what the shooter saw, falling back to the muzzle `origin`/`dir` (`buildShotPayload`), serialising each `Vector3` to a `[x,y,z]` array. The server no-ops when the shot resolves no peer. **INBOUND** — `_mpEmit` calls `createPeerCombat(...)` first; it returns `true` for `mp_shot`/`mp_hit`/`mp_kill` (and `false` so `mp_respawn` still falls through intact): `mp_shot` `{id,origin,dir,ts}` is VISUAL ONLY — a muzzle spark + short tracer at the peer origin via `fx.js` (`spawnSpark`/`spawnRicochet`, auto-ticked by `tickFx` inside `tickWeapons`), skips our own echoed shot, and runs NO local hit detection (server is authoritative); `mp_hit` `{id,targetId,dmg,zone}` applies `takeDamage(dmg)` only when `targetId === selfId` and `flashCross()` when we are the shooter (wire shooter field is `id`, per `wireProtocol` MSG.HIT map — NOT `shooterId`); `mp_kill` `{shooterId,victimId}` runs the transition-guarded `killPlayer()` when `victimId === selfId` (it increments `state.deaths` exactly once — a no-op if `takeDamage` already killed us this frame, so no double-count) and scores `state.kills++` + an `addKill('Fragged a rival')` killfeed line when `shooterId === selfId`. Local bot hits remain a SEPARATE client-side path (`EV.BOT_HIT_BY_PLAYER` → `hitBot`): a single shot may both hit a bot locally AND resolve a peer hit server-side — expected, not deduped; every arena shot sends `SHOT` (the server ignores bot-only shots). NO wire-protocol change. Verified: check green, build green, tests green; new `tests/multiplayer/peer-combat.test.js` covers the outbound gate/payload (aim-ray arrays, NAP + falsy-selfId suppression, muzzle fallback) and the inbound gating (`mp_shot` visual-only + self-echo skip, `mp_hit` damage only on `targetId===selfId`, `mp_kill` death only on `victimId===selfId` + `kills++` on shooter side). Bots stay client-side (a later MP milestone; NOT bundled here). **Source-only — pending a Suite redeploy that includes v0.2.374 (bundles v0.2.371 DRACOLoader + v0.2.372 keepalive + v0.2.373 avatar transform + this); do NOT touch the VPS while the Continuum session is active.** Not closed until a real-browser two-npub retest shows one player damaging + killing the other with correct HUD/killfeed/respawn.
- **v0.2.373-alpha — peer avatars rendered high in the sky, backwards, and T-posed fixed (peer avatarLoader returned the raw `gltf.scene`); LIVE v0.2.370-alpha until a Suite redeploy picks up v0.2.373.** After the v0.2.371 DRACOLoader fix peers became visible, but appeared ~4-5 body-heights UP, FACING 180° away, and in a static T-POSE (bind pose, no animation). Root cause: the peer `avatarLoader` in `src/arenaRuntime.js` resolved the RAW `gltf.scene` with none of the setup `playerModel.js`/`botModel.js` apply — (1) chiefmonkey6.glb geometry sits well above its origin (large `gMinY`), so with no feet offset the model floated at its authored height (and `sendMove` broadcasts eye-height Y ≈ 1.7, compounding it); (2) the GLB is authored facing +Z while game-forward is −Z, so with no `rotation.y = π` the avatar faced backwards; (3) no `THREE.AnimationMixer`, so the skeleton stayed in bind-pose (T-pose). Fix (mirror `botModel.js` + the `playerModel.js` transform): load chiefmonkey6.glb ONCE into a module-level template (scene + clips + geometry `gMinY`), then per peer `SkeletonUtils.clone` it into a wrapper `THREE.Group` (remoteAvatars sets the wrapper transform) with `model.position.y = -gMinY - 1.7`, `model.rotation.y = Math.PI`, opaque materials + `frustumCulled = false`, and an `AnimationMixer` playing `Idle_03` (looped; falls back to `animations[0]` with a warn) ticked once to leave bind-pose. The wrapper exposes `obj.update(dt)` → `mixer.update(dt)`; `remoteAvatars.tick(renderTime)` now derives a clamped wall-clock `dt` (skipping the first tick) and calls `entry.obj.update(dt)` each frame. NO movement-based clip switching (IDLE only; WALK-on-move noted as MP-1.5); NO `sendMove`/wire change. Verified: check green, build green, tests green; new `remote-avatars.test.js` cases assert `tick` drives `obj.update(dt)`, skips the first tick, clamps a large gap, and is safe without an `update()`. **Source-only — pending a Suite redeploy that includes v0.2.373 (bundles the v0.2.371 DRACOLoader + v0.2.372 keepalive + this transform/animation fix); do NOT touch the VPS while the Continuum session is active.** Not closed until a real-browser two-npub retest shows both avatars standing on the ground, facing correctly, and idling.
- **v0.2.372-alpha — repeating NIP-07 signer-prompt loop on the pause/idle screen fixed (client keepalive PING); LIVE v0.2.370-alpha until a Suite redeploy picks up v0.2.372.** On the pause/idle screen a paused client sends no MOVE frames, so the connection goes silent. The server idle-drops silent sessions after 60s (`IDLE_DISCONNECT_MS` in `server/arena-ws.js`) and never initiates its own PING, while `wsClient.js` only sent a PONG in reply to a server PING (which never came). So a paused session was dropped at ~60s → `onclose` → `_scheduleReconnect` → reconnect → fresh `HELLO`/NIP-42 challenge → `signAuth` → **another NIP-07 signer prompt**, looping every ~60s. Fix (client-side, minimal): `wsClient.js` now sends a keepalive `{ t: MSG.PING, ts: now() }` every 25s (`KEEPALIVE_MS`) while CONNECTED, started in the WELCOME handler and cleared in `disconnect()` and `onclose` (recursive `setTimeoutFn`, single-instance guard). NO server change and NO session-token/auth-caching scheme (NIP-42 challenges are per-session + anti-replay); a *genuine* network drop still reconnects + re-auths — future hardening would be a server-issued session token. Verified: `npm run check`, `npm run build`, `npm test` all green; new keepalive unit tests in `tests/multiplayer/ws-client-state.test.js`. **Source-only — pending a Suite redeploy that includes v0.2.372 (bundles the v0.2.371 DRACOLoader peer-visibility fix + this keepalive fix); do NOT touch the VPS while the Continuum session is active.**
- **v0.2.371-alpha — H4 peer-visibility fixed (peer-avatar GLTFLoader was missing a DRACOLoader); LIVE v0.2.370-alpha (CSP freeze fixed) until Suite redeploy picks up v0.2.371.** With the v0.2.370 fix deployed, anonymous ENTER ARENA now BOOTS on chiefmonkey.art/quest/, but **two players still cannot see each other** in the arena. Root cause: `src/arenaRuntime.js` built the peer-avatar `GLTFLoader` (`_mpGltf`) WITHOUT a `DRACOLoader`, unlike every other loader that touches `chiefmonkey6.glb` (`playerModel.js`, `botModel.js`, `arena.js`, `firstPersonBody.js`). `chiefmonkey6.glb` is Draco-compressed, so `_mpGltf.load(assetUrl('/chiefmonkey6.glb'))` REJECTED (Draco mesh cannot decode) for every peer → `remoteAvatars.upsert()` caught the reject and DELETED the roster entry (`remoteAvatars.js`) → peer avatars never rendered. Wire-level peer discovery already works (`peerJoin`→`roster.upsert` in `multiplayerHost.js`; verified in QA-MP-BLOCKER-1), so this was purely the loader. Fix (minimal, targeted): give `_mpGltf` a `DRACOLoader` mirroring `playerModel.js` (`new DRACOLoader()` + `setDecoderPath(assetUrl('/draco/'))` + `setDRACOLoader`), and add a `console.warn('[mp] avatar_load_error', …)` on load failure so future GLB errors are visible. NO capsule fallback added. Verified: build green, checks green, tests green. **Source-only fix — pending a Suite redeploy that includes v0.2.371 (the maintainer must bump the Suite Quest ref to v0.2.371-alpha or the merge commit, then redeploy with `--base=/quest/`); do NOT touch the VPS while the Continuum session is active.** H4 NOT closed until a real-browser two-npub retest on the redeployed build shows both avatars in-world.
- **v0.2.370-alpha — base-aware pinned-entry import fixed (ENTER ARENA no longer freezes under the `/quest/` mount); LIVE still v0.2.366-alpha until Suite redeploy.** A real-browser test on chiefmonkey.art/quest/ (v0.2.369-alpha, Brave + Firefox) found the pre-auth menu responsive but clicking **ENTER ARENA froze the session** before any canvas/frame — the arena never booted. Root cause: the vite CSP plugin (`vite.config.js`) emitted the pinned-entry URL **root-relative** (`/assets/torii-entry.js?v=<stamp>`) for BOTH the inline bootstrap `import()` and every chunk's back-reference import of the entry. Under the Suite's `--base=/quest/` build those URLs 404 at host root; because `arenaRuntime.js` statically imports the entry, ENTER ARENA's `import('./arenaRuntime.js')` graph load REJECTED and the arena never started (live symptom: freeze). Fix: the plugin now captures the resolved deploy base (`configResolved`) and emits `${base}assets/torii-entry.js?v=<stamp>` (`/quest/…` on the Suite, `/…` at root) via a single `entryUrl(base)` helper, applied to the inline import, the removal regexes, and the chunk rewrite. `tools/regression-check.mjs` [16] base-agnostic; new deterministic regression `tests/quest-base-entry.test.js` does a real `--base=/quest/` build and asserts every `torii-entry.js?v=` URL carries the `/quest/` base, is byte-identical (single module instance), and that no static entry `<script>` survives. Verified: `--base=/quest/` build emits `/quest/assets/torii-entry.js?v=<stamp>` everywhere; puppeteer ENTER-ARENA repro on a clean `/quest/` server boots the arena (frames advance, no "Failed to fetch dynamically imported module"). 2269/2269 tests, checks green. **Source-only fix — the Suite installer is pinned at `TORII_QUEST_REF=v0.2.367-alpha`, so the maintainer must first bump the Suite Quest ref to v0.2.370-alpha (or the merge commit), then a Suite redeploy (which builds with `--base=/quest/`) will include it; do NOT touch the VPS while the Continuum session is active on the shared host.** No gameplay/physics/CSP-policy/Nostr change; the v0.2.369 assetUrl fix is untouched.
- **v0.2.369-alpha — root-relative asset paths fixed (graphics + peer-avatar visibility); LIVE still v0.2.366-alpha until Suite redeploy.** A real-browser two-npub test (Firefox + Brave) on chiefmonkey.art/quest/ confirmed the arena loads and peers auth/join, but **no `.glb` models load** (bots fall back to capsule "pills") and **peer avatars never appear** (players can't see one another) — the peer-avatar `avatarLoader` has no fallback on GLB failure. Root cause: all `GLTFLoader.load('/foo.glb')`, `setDecoderPath('/draco/')`, and `TextureLoader.load('/bitcoin-b.png')` in `src/` were **root-relative**; under the `/quest/` mount they 404 at host root (confirmed: root 404, `/quest/` 200). Fix: new `src/assetUrl.js` helper (`${import.meta.env.BASE_URL}${name}`, mirroring `audio.js`) applied across 8 source files (8 GLB loads, 6 Draco paths, 1 texture, `char.file`); `tools/regression-check.mjs` [16] now requires `assetUrl('/draco/')` and FAILS on any bare root-relative `.load('/...glb|png|…')`. Verified: a `--base=/quest/` build inlines `/quest/` + passes all GLB args to the helper; 2264/2264 tests, 20/20 checks. **Source-only fix — the Suite installer is pinned at `TORII_QUEST_REF=v0.2.367-alpha`, so the maintainer must first bump the Suite Quest ref to v0.2.369-alpha (or the merge commit), then a Suite redeploy (which builds with `--base=/quest/`) will include it; do NOT touch the VPS while the Continuum session is active on the shared host.** H4 NOT closed until a real-browser retest on the redeployed build shows both avatars in-world. SW follow-up (non-blocking): `public/sw.js` registration + precache are still root-relative and don't register under `/quest/`; the game loads via network regardless.
  - **NOT confirmed in this test:** the full in-world arena render + roster visualisation via the app's normal `ENTER ARENA` → `boot()` path. Under headless software-WebGL (swiftshader) the arena's render loop starves the async NIP-07 callback, so `signEvent` does not resolve before the 10s `AUTH_TIMEOUT_MS` when driven through the full 3D flow; the test therefore drove the AUTH handshake directly in-page (same `wss://chiefmonkey.art/mp`, same `window.nostr` shim) to isolate the protocol path. This is a **headless test-environment limitation, not proven to be free of a product-side rendering/roster-visualisation issue (H4)** — re-verify the full ENTER→in-world path on real GPU hardware before closing H4.
  - H2 (MP not eagerly loaded) and H3 (silent AUTH rejection) are not implicated by this test (both peers authed and received WELCOME/JOIN), but were not directly exercised through the ENTER button either.
  - The original pplx.app symptom was solely the broken pplx.app backend sandbox (still 503), now bypassed by the SHC VPS install. Full evidence in `torii-quest-todo.md` §QA-MP-BLOCKER-1.
- **Deployment conventions — chiefmonkey.art (Torii Suite install; source of truth: `torii-quest-todo.md` → “Deployment conventions — chiefmonkey.art”).** Quest is live via the **Torii Suite** installer (`torii-suite` v0.7.0-alpha), NOT the ad-hoc Caddy bundle. Frontend mounts at the path prefix **`/quest/`** (root `/` 404s — no launcher mounted yet). Multiplayer WS is the root-level same-origin **`wss://chiefmonkey.art/mp`** (no `/port/5000/mp` sentinel on the VPS path — that sentinel is pplx.app-sandbox-only; `multiplayerHost.resolveUrl()` resolves to `wss://chiefmonkey.art/mp`). Backend is the hardened systemd unit **`torii-arena-ws.service`** (`User=torii-quest`, `127.0.0.1:8788`, `NoNewPrivileges`/`ProtectSystem=strict`); nginx `location /mp` → `127.0.0.1:8788` (WS upgrade) via `/opt/torii/nginx-fragments/quest-mp.conf`. Apps mount as path prefixes via fragments in `/opt/torii/nginx-fragments/<app>.conf` under one domain; the shared parent `/opt/torii` stays `root:root 0755` (world-traversable) — NEVER re-own/re-mode it (v0.2.30 permission-regression fix). Siblings: Continuum `/continuum/` (agent `127.0.0.1:8787`, API `/api/`), Plebeian `/plebeian/`. A Continuum session is actively managing onboarding on this shared host — do NOT touch shared nginx/parent-dir config while that work is in flight. Suite installer: `curl -fsSL https://raw.githubusercontent.com/ChiefmonkeyArt/torii-suite/v0.7.0-alpha/bootstrap.sh | sudo bash`; pinned `TORII_QUEST_REF=v0.2.367-alpha` (live `arena-ws` advertises v0.2.366-alpha in HELLO — the live build predates the v0.2.369-alpha source bump; cosmetic, no functional impact).
- **Travel-time lead on fast-moving targets** — bullets are hitscan-aimed but projectile-flown; long shots on strafing bots can trail. Tracked in `torii-quest-todo.md`.
- **pplx.app backend sandbox BROKEN (2026-07-11; SECONDARY host, now bypassed by chiefmonkey.art — kept as fallback).** v0.2.366-alpha published to quest-torii.pplx.app via `publish_website` (status:published, `visibility_setting: Public`). Static frontend serves (HTTP 200) but the **backend sandbox is not running** — `/port/5000/healthz` returns empty 503 in ~0.2s (fast rejection, not a cold start; a 90s-timeout probe still 503s in 0.198s). WS upgrade OPENs at the proxy but no HELLO arrives. Server code is verified healthy: clean-room replication of the exact production boot (`npm install --omit=dev` → `node server/arena-ws.cjs` PORT=5000) gives `/healthz` 200 `{"ok":true,"version":"v0.2.366-alpha"}` + WS `HELLO` in 7ms. Two republishes did not change the 503. Residual platform-side backend-hosting degradation from the 32h bridge outage (diagnostic `9a174646`). **Decision (2026-07-11): move the live backend to the SHC VPS at chiefmonkey.art** (single-origin, VPS_INSTALL.md §16) — bypasses the broken pplx.app backend entirely. Ready-to-run deploy bundle built: `torii-quest-vps-deploy-v0.2.366-alpha.tar.gz` (dist + Caddy site block + systemd unit + idempotent `deploy.sh`; CSP baked from the build with inline-script sha `8RxbohhIbgMGQaBj0CcykJ4wbu0FIyUvCrGVRHXu8xE=`). VPS was paused — unpause first; DNS (apex + www A records → VPS) confirmed by maintainer. Once up: `sudo bash deploy.sh` → smoke `wss://chiefmonkey.art/mp` (expect HELLO v0.2.366-alpha) → two-npub test.
- **ESBUILD-1** (deferred) — low-severity dev-server-only esbuild advisory. `npm audit fix` pulls a broad rolldown/vite chain, deemed too risky for alpha. Tracked WARN in `torii-quest-todo.md`.
- ~~**SEC-1 (mandatory gate on `leaderboardPublisher`)**~~ — **LANDED v0.2.355-alpha.** The `createLeaderboardPublisher({ sign, publish, gate })` adapter no longer treats `gate` as optional: `gate` DEFAULTS to `verifyPublishGate` (the crypto-verified SEC-1 gate), so any live publisher inherits real BIP-340 verification + the consent check by default. An explicit `gate: null` combined with a wired `publish` is a SEC-1 CONSTRUCTION ERROR — `publishScore` fails closed on every call, never signs, never publishes, and returns `ok:false` with a `SEC-1: publish is wired without a gate` error. The build-only path (no publisher) still needs no gate. This closes the earlier bypass where a caller could wire `{ sign, publish }` without a gate and quietly ship stub-signed or unverified events to a relay. Tests: 5 new cases across `tests/leaderboard-publisher.test.js` (mandatory-gate fail-closed describe block) and `tests/leaderboard-publish-gate.test.js` (the old "backward compatible" bypass test flipped to two fail-closed assertions). Consent gating for the real signer/relay wiring landed earlier (v0.2.257 publishGate, v0.2.277 real BIP-340, v0.2.285 live NIP-07); v0.2.355 removes the last opt-out path.
- ~~**SEC-2 (handoff verification on `world/handoff.js`)**~~ — **LANDED v0.2.356-alpha.** The traveller-side handoff skeleton (`src/world/handoff.js`) now runs real BIP-340 schnorr verification before it hands a caller a spawn descriptor. New `verifyHandoffCrypto(h, { expectedPlayerPubkey, now, requireFresh })` composes the pure structural pre-flight (schema/namespace/freshness) with a re-derived NIP-01 event id + `schnorr.verify(h.sig, h.id, h.player)` under the traveller's hex64 pubkey, mirroring the SEC-2 gateway gate in `engine/gateway/handoffVerify.js` and the SEC-1 leaderboard gate. `resolveHandoffSpawn(h, destZoneMeta, { expectedPlayerPubkey })` is the choke-point: the `expectedPlayerPubkey` opt is REQUIRED (must be hex64) and the crypto verdict must be trusted, so an unsigned envelope, a tampered body, a wrong-key signature, or an envelope naming a different traveller returns null. New helpers `deriveHandoffId(h)` (pure, so signer + verifier agree on what the sig commits to) and `signHandoffEvent(h, sk)` (test/demo convenience, injected sk only) round out the module. Tests: 32 new cases in `tests/world-handoff.test.js` covering constants, factory shape, structural verify parity, id derivation, sign+verify round-trip, malformed opts, identity mismatch, tampered body, stub sig, wrong-key sig, freshness gate, resolveHandoffSpawn fail-closed matrix, and serialize/deserialize sig preservation. Historical crypto SEC-2 in the gateway path (`handoffVerify.js`, live-signature verify) landed earlier at v0.2.252 (structural) and v0.2.263 (real BIP-340); v0.2.356 completes SEC-2 on the traveller/arrival side. **Note:** live relay ingest still requires the maintainer to wire `resolveHandoffSpawn` into whatever transport lands `h` — the module has no relay layer yet.
- ~~**SEC-3 (product URL validation)**~~ — **LANDED v0.2.354-alpha.** `productDisplay.isSafeHttpUrl` (the shared validator both `productDisplay` and the `productPanel` view-model use) is now a WHATWG `URL`-object parser: it trims + rejects any embedded whitespace, tries `new URL(s)`, and only accepts a result whose `protocol === 'https:'` and whose `hostname` is non-empty. The old regex `^https:\/\/[^\s]+$` accepted malformed inputs like `https://` and `https:javascript:…`; the parser refuses them and normalises the permissive-but-safe cases (`https:host`, `https:///host`, `HTTPS://`) to a real https host, so a listing can no longer smuggle a non-https scheme through us. Tests: 6 new cases in `tests/product-display.test.js` locking scheme/host enforcement, malformed rejection, WHATWG normalisation behaviour, and non-string safety.

## 9.5. Active task / next steps (2026-08-31)

**QUEUED (design done 2026-08-31; no implementation yet):**
- **ADR-0090 arena full sandboxing (proposed).** Follow-up to ADR-0084 wiring. Move Three + Rapier inside the `sandbox="allow-scripts"` iframe; define the DOM<->WebGL bridge and the per-frame snapshot protocol. Design doc authored + reviewed; renumbered 0088 → 0090.
- **ADR-0091 sticker studio full sandboxing (DEFERRED).** Follow-up to ADR-0085 wiring. SkinnedMesh raycast inside the iframe with a snapshot-based avatar API back to the shell. Renumbered 0089 → 0091; deferred — Character Forge's ADR-0088 (in-world raycast placement, PR #89) is the shipping self-placement path, and the sandboxed studio stays a future replacement once third-party character napplets are a real goal.
- **ADR-0092 release-time napplet identity.** Replace `torii-arena@v0-wiring` / `sticker-studio@v0-wiring` placeholders with real bundle hashes as part of the release pipeline. Renumbered 0086 → 0092 (0086 is now Character Forge's sticker-placement-model ADR).

**CURRENT (priority):** N2N TRAVEL TEST WITH BEKKA — chiefmonkey.art/quest/ ↔ torii.plebeian.build. Verify the open-visit travel path (direct navigate + `?torii-traveller=`) and presence discovery across the unified relay list (ADR-0081). Both instances live again after the HashIT outage (resolved 2026-08-31). Bekka must be on v0.2.713+ (one-command bootstrap) for the heartbeat/relay/access-tab changes.

> Historical entry below, dated 2026-08-17. Resolved: live is now confirmed running v0.2.717-alpha (checked 2026-08-31). Left as-is for the historical record; do not action this instruction.

**PENDING DEPLOY (historical, resolved — see note above):** v0.2.603-alpha is pushed to GitHub (tag `v0.2.603-alpha`) but NOT yet deployed to the VPS. The user needs to run:
```
sudo truncate -s 0 /var/log/torii-quest-update.log && sudo torii-deploy v0.2.603-alpha
```

**WHAT WAS DONE THIS SESSION:**
1. v0.2.520: Repositioned all NAP objects to z=31 edge. Rewrote napNpc.js (NPC walking + gestures). 2668 tests pass.
2. v0.2.521–v0.2.525: Iterative rotation adjustments (torii gate, leaderboard, product panel, mirror). Multiple failed attempts due to wrong positions and not following the curved edge.
3. v0.2.526: Mirror at (0.26, 31) — on top of torii gate. User reported "nothing has changed."
4. v0.2.527: Mirror at (12.26, 25) — too far around curve, crossed NAP/arena boundary. User reported "even worse."
5. v0.2.528: Correct layout — all objects flush with curved edge, left to right: leaderboard → torii gate → product panel → mirror. Mirror shrunk to 5m, rotated 45° to follow curve. Both edges verified on land.

**WHAT TO VERIFY AFTER DEPLOY:**
- All four objects visible in correct left-to-right order along the NAP edge
- Mirror not overlapping any other object
- Mirror angled to follow the curve (45° from south)
- NPC (chiefmonkey) walking and doing gestures in NAP zone
- Torii gate at (0, 32) with its glow ring, rotated 55° CW

**IF THE LAYOUT STILL LOOKS WRONG:**
1. Check browser console for `[arena] travel-gateway GLB unavailable` — if present, the fallback gate shows at a different angle (Math.PI/2 base vs Math.PI for GLB)
2. Hard refresh (Ctrl+Shift+R) to bypass service worker cache
3. Verify the deployed version: `curl -s https://chiefmonkey.art/quest/ | grep -o 'v0\.2\.[0-9]*-alpha'`
4. Check `src/mirror.js` for MW, MX, MZ values
5. Check `src/engine/world/proofSurfaceSpecs.js` for panel/leaderboard positions
6. Check `src/config.js` for TRAVEL_GATE_* values
7. Use `isNapLand(x, z)` + `sampleNapHeight(x, z)` to verify any new positions are on solid land

**DEPLOY PIPELINE:**
- Push: `git push origin main --tags` with `api_credentials=["github"]`
- Deploy on VPS: `sudo truncate -s 0 /var/log/torii-quest-update.log && sudo torii-deploy <version>`
- The `torii-deploy` script writes a JSON request to `/opt/torii-quest/mp/update-requests/`, an update-runner service picks it up, does git fetch + checkout by TAG (not branch), builds, and logs result
- MUST create git tag for each version: `git tag v0.2.XXX-alpha && git push origin v0.2.XXX-alpha`
- Repo: `https://github.com/ChiefmonkeyArt/torii-quest.git`
- Live URL: https://chiefmonkey.art/quest/
- VPS: ubuntu@chiefmonkey
- SSH key NOT available from sandbox — print deploy command for user to run manually
- dist/ is in .gitignore — VPS builds from source via `npm run build`
- Service worker: network-first for JS/CSS/HTML, cache-first for GLBs/images/fonts

**VERSION MARKER FILES (bump ALL on each version):**
MVP_APPROVAL_STATE.json, NEXT_ACTION_STATE.json, index.html, public/dashboard.html, public/sw.js, public/torii-quest-data.json, server/arena-ws.js, src/config.js, src/engine/dashboard/toriiQuestDashboardData.js, tests/torii-quest-dashboard.helpers.test.js, tests/torii-quest-dashboard.model.test.js, tests/torii-quest-dashboard.render.test.js, tests/torii-quest-dashboard.sdk.test.js, tools/regression-check.mjs, torii-quest-handoff.md, torii-quest-progress.md, torii-quest-todo.md, package.json, dist/index.html

---

## 10. Next-job format

When picking up work, state it as:

```
TASK:        <one line>
VERSION:     bump v0.2.<n> → v0.2.<n+1>-alpha
CONSTRAINTS: (default = all of §3; note any the task explicitly relaxes)
SCOPE:       files expected to change; split by concern
DONE WHEN:   build + check + test green; docs (§5) updated; version markers (§4) bumped
DEPLOY:      NO (maintainer publishes) unless explicitly instructed
```

Keep changes incremental and reversible. If scope balloons, stop at a green checkpoint and report what remains rather than half-landing a broad rewrite.

## 11. Deferred — Auction-button slice (ADR-0030, not yet started)

Shipped v0.2.648-alpha (ADR-0029 Kami state machine) leaves the auction panel
still auto-showing on NAP-zone entry for guests. The deferred slice:
- Stop `setMarketActive(true)` auto-show on NAP entry (src/engine/plebeian/marketStall.js).
- Add a button to the existing product panel (src/engine/components/productPanel*.js)
  that opens `#auction-panel`.
- Gate logged-in admin only.
- Full admin-stall rework (display fixed-price products + auctions from the
  owner's Plebeian stall) is further deferred — this slice is just the button trigger.
Separate commit + ADR-0030. Owner said: "we will focus on this in a bit… please
make a note to do that in a bit."
