# Torii Quest ToDo

Current version: v0.2.722-alpha - CHARACTER FORGE — PEER-AVATAR MESH LOADING (ADR-0082): peers now see each other's custom mesh. The player's mesh hash (from the kind-35100 event) is broadcast through the MP `character` field (now accepts a 64-hex sha256 alongside the skin keys), the server relays it, and the peer loader resolves the hash to a Blossom URL and loads that GLB instead of the built-in default (`_peerMeshUrl()` in arenaRuntime.js). The player path (self) + peer path now both load custom meshes — the full automatic mesh-loading loop is closed. Sticker placement UI + external mesh generation remain.
Prior: v0.2.721-alpha - CHARACTER FORGE — AUTOMATIC MESH LOADING (player path) + CUSTOM MESH UPLOAD (ADR-0082): the player's own character mesh now actually LOADS. At login the game fetches the npub's kind-35100 event, resolves its mesh hash to a Blossom URL (`src/engine/character/characterMesh.js`), and seats it before arena boot so `loadPlayerModel()` fetches the custom mesh instead of the built-in default (`setCustomMeshUrl()` in playerModel.js). The Character tab also gained an "Upload custom mesh" button — upload a .glb to Blossom (NIP-98) and publish the character event in one flow. The peer-avatar path (broadcasting each peer's mesh hash through the MP protocol) is the remaining slice.
Prior: v0.2.720-alpha - CHARACTER FORGE — CREATE ROUND-TRIP (ADR-0082): the Character tab now WRITES a character, not just reads one. Curated presets (`src/engine/character/characterPresets.js` — Chiefmonkey + Nostrich, content-addressed mesh hashes) feed a preset picker; picking one builds a `torii.character` manifest, signs the kind-35100 event via NIP-07, and publishes to the unified relay list (`publishCharacter()` in nostr.js, mirroring publishAccessSettings). Also added the Blossom upload path (`uploadBlossom()` + `buildBlossomAuthEvent()` — NIP-96/NIP-98) so a custom mesh GLB can be uploaded and its content-addressed sha256 slotted into the manifest. Sticker placement UI + external mesh generation remain follow-up slices.
Prior: v0.2.719-alpha - CHARACTER FORGE — LIVE RELAY READ (ADR-0082 follow-up): the Character settings tab now checks the logged-in npub's kind-35100 character event over a live relay read and seats an existing character automatically (the "smooth experience" seam — no prompt, no re-upload). New pure `src/engine/character/characterRelayRead.js` (filter builder + read/reduce + newest-per-author) + `fetchOwnCharacter()` in nostr.js (reuses fanoutReq). The create round-trip (writing a new character event) is the next slice.
Prior: v0.2.718-alpha - CHARACTER FORGE GROUNDWORK (ADR-0082): validator-first character pipeline — no in-house generation model. New pure `src/engine/character/` modules: `skeleton.js` (canonical role-based skeleton contract + Mixamo/Biped bone maps + axis detection), `rigAssessment.js` (auto-rig verdict ladder), `characterManifest.js` (`torii.character` manifest validator), `characterEvent.js` (kind-35100 character-event parser — the "smooth experience" seam). New "Character" settings tab (`src/engine/settings/characterForgePanel.js`, registered in main.js + settingsPanel.js). SDK exposure at the experimental tier. Live relay read/create round-trip is a follow-up slice.
Prior: v0.2.717-alpha - SETTINGS PANEL SMOKED GLASS + PROFILE PREFILL: removed the full-screen dark backdrop behind the settings panel (it now sits on a transparent click-catcher) and gave the panel a smoked-glass look (semi-transparent + backdrop blur + shadow). The Profile tab now pre-fills from the logged-in user's published kind:0 Nostr profile (display name, bio, avatar, website, NIP-05, lightning address) when the local draft is empty — via a new fetchOwnProfile() in nostr.js.
Prior: v0.2.716-alpha - RELAY TAB POLISH: removed the 'STARTER' badge from the Relay settings tab header (it overlapped the panel's top-right close button) and gave every relay row a Remove button — previously the curated starter relays rendered as read-only rows with a 'starter' tag and no delete. Removing a starter relay now materialises the remaining defaults as the operator's explicit config (remove-relay reads the effective list, not the raw stored string).
Prior: v0.2.715-alpha - SINGLE UNIFIED RELAY LIST (ADR-0081): one relay list for the whole game. The separate hardcoded read-path RELAYS (nos.lol + vertexlab.io) in nostr.js is removed; profile/login/leaderboard/discovery reads AND presence/heartbeat publish now all use the single owner-editable list (localStorage torii.node.relays + <meta name=torii-relays>) with a curated 5-relay default (gamestr.io, plebeian.market, routstr.com, nos.lol, vertexlab.io). Per-action publish consent (heartbeat signer prompt, gamestr toggle, access-settings save) is unchanged — the list decides WHERE, never WHETHER.
Prior: v0.2.712-alpha - RESTORE ACCESS SETTINGS TAB (ADR-0078): re-surfaced the instanceSettings admin surface (arrival authority + write authority, backed by the signed kind:30078) as a fifth "Access" tab in the settings panel — hidden since v0.2.676 but the underlying enforcement (handoffArrival.js / writeAuthority.js) stayed live the whole time. Restored the v0.2.400 read/save relay round-trips in main.js (readLatestAccessSettings / publishAccessSettings) + the _instanceSettingsState / _refreshInstanceSettingsAccessState / _saveInstanceSettingsAccess functions recovered verbatim from the v0.2.676-predecessor commit. The Access-tab renderer filters the model's sections to only 'access' so the inert placeholder sections ('multiplayer' / 'more' "More coming soon") are NOT surfaced — only live, useful controls. Wired delegated change (radio → draft) + submit (data-form=access-settings → _saveInstanceSettingsAccess) listeners on #torii-settings-content. settingsPanel TABS is now [profile, gateway, heartbeat, relay, access] (getSettingsTabIds() exposed for testability). No new sign/publish path (reuses nostr.js signEvent / fanoutPublish); enforcement unchanged + cannot be weakened by the edit surface. Deferred (need core refactors, not fake tabs): Audio (no master gain API), Graphics (no manual override), Controls (no keybind layer). 3449 tests / 275 files, 21/21 gates.
Prior: v0.2.711-alpha - HEARTBEAT AUTO-ON + RELAY CLEANUP (ADR-0076 update + ADR-0077): heartbeat is now ON by default and auto-fires the first presence publish when the owner logs in — the login click authorises the NIP-07 signer prompt, and approving it IS the consent; a signer rejection sets republishPaused so it never nags (the operator can re-toggle to retry). Removed the manual-first-publish gate from _heartbeatTick (isHeartbeatDue's contract is unchanged — first-publish is driven by a local `firstPublishDue = lastPublishedAt === null` check). Added relay.routstr.com to DEFAULT_NODE_RELAYS (now gamestr + plebeian + routstr, all Torii-ecosystem, WS-verified live 2026-08-27). Dropped the 503ing relay.damus.io from the read path (nostr.js RELAYS now nos.lol + relay.vertexlab.io) — fixes the connection-failed console noise Bekka saw. CSP updated (dropped damus, added routstr + vertexlab). Fixed bump-ver.sh (was missing toriiQuestDashboardData.js + the torii-quest-dashboard tests — the VERSION constant was stuck at 709). Fixed regression-check.mjs EXPECTED_VERSION to read dynamically from package.json (was hardcoded, had drifted since 710). 3445 tests / 274 files, 21/21 gates.
Prior: v0.2.709-alpha - VERSION FIX: arena multiplayer server SERVER_VERSION constant was hardcoded and never bumped (ADR-0075). Root cause found when Bekka's arena HELLO reported v0.2.602-alpha though her release was v0.2.706-alpha. Initially diagnosed as a stale build; on checking the source, server/arena-ws.js line 69 has `const SERVER_VERSION = 'v0.2.602-alpha'` as a hardcoded literal — bump-ver.sh never touched server/arena-ws.js, so the constant was stuck at 602 across tags 706/707/708 (chiefmonkey's own instance had the same bug). Fix: (1) added server/arena-ws.js to bump-ver.sh's sed file list so the constant is bumped on every future release; (2) bumped SERVER_VERSION to v0.2.709-alpha to match the release. Bekka (and any operator) can now pull v0.2.709-alpha, rebuild, and the arena HELLO will report the correct version. 3445 tests / 274 files, 21/21 gates.
Prior: v0.2.706-alpha - ADMIN GREETING COLOUR SPLIT (ADR-0072, supersedes ADR-0071 colour claim): only the owner's name is now orange (#f7931a); 'Welcome ', ',', and 'you are ' are the neutral caption tone (#d8c3ac); 'logged in' is green (#6DAA45) to balance the orange name; the green dot stays green. Line 1 is rebuilt via the DOM API (createElement + textContent per span, no innerHTML) so a Nostr display name can never inject markup; badge text split into a neutral 'you are ' span + a green 'logged in' span. .toc-greet no longer sets orange (only .toc-name does). Same isAdminOperator() gate, display:none fail-closed, no pubkey leak. 3445 tests / 274 files, 21/21 gates.
Prior: v0.2.705-alpha - ADMIN LOGGED-IN CAPTION REWORDED TO A GREETING (ADR-0071): when the admin is logged in, the top-left caption now reads "Welcome <owner name>," on line 1 (prominent 13px orange, sentence-case via a new .toc-line1.toc-greet class that overrides the 9px uppercase label style) and a green dot + "you are logged in" on line 2 (badge text changed from "logged in"; .toc-loggedin text-transform set to none so it renders in the quoted sentence case, not uppercase). The name span (#torii-owner-name) is hidden on line 2 for the admin (the name moved into the greeting); non-admin/anonymous visitors see the unchanged "This torii belongs to / <name>" caption. Same isAdminOperator() gate as ADR-0070, no new auth, no pubkey leak; greeting + badge swap atomically in one _refreshOwnerLabel call (no flicker). 3445 tests green / 274 files, 21/21 regression gates.
Prior: v0.2.704-alpha - ADMIN-ONLY "LOGGED IN" BADGE ON THE OWNER CAPTION (ADR-0070): a small green pulsing dot + the text "logged in" now appears to the right of the owner's name on the top-left homescreen caption ("This torii belongs to"), shown ONLY when the logged-in viewer IS the configured instance owner. Reuses the existing isAdminOperator(viewer, adminPubkey) check (no new auth/fetch); the name was split into its own truncating span (#torii-owner-name) so the badge sits to its right without being clipped by the ellipsis; the badge is display:none by default and only .show when login is confirmed, so it never flashes before login and never appears for non-owners/anonymous; never surfaces the pubkey (toggles a CSS class only). The owner's name remains visible to all visitors, unchanged. New test file main-owner-loggedin-badge.test.js (6 source-level wiring assertions). 3445 tests green / 274 files, 21/21 regression gates.
Prior: v0.2.703-alpha - PAUSE MODAL RECOLORED TO TORII SUNSET THEME (ADR-0069): the pause modal shown after ESC releases pointer lock and transitions to PAUSED (#pause-overlay / #pause-box / #pause-title in index.html) was still on the legacy dark-neon-purple palette (rgba(20,20,35,0.97) panel + rgba(139,92,246,0.4) purple border + rgba(0,0,0,0.6) backdrop). Recolored to the torii-gate sunset theme: warm backdrop rgba(20,13,9,0.7), warm panel rgba(30,18,12,0.97) (warm twin of the old purple at the same opacity so the paused game stays blocked out) with var(--panel-glass-border) bronze border + a subtle depth box-shadow, and the PAUSED title in var(--accent) #e8842c with a warm glow. Layout, sizing, buttons (.btn / .btn-secondary already themed), the .show toggle, focus/pointer-lock behavior, and the resume flow are unchanged — color-only migration, no DOM/JS/asset change. 3439 tests green / 273 files, 21/21 regression gates.
Prior: v0.2.702-alpha - BOOT OVERLAY RECOLORED TO TORII SUNSET THEME (ADR-0068): the full-screen loading overlay shown after Nostr login + ENTER TORII (#boot-overlay in index.html) was the last player-facing surface still on the legacy dark-neon-purple palette (#140a28 / #b794f4 / #8b5cf6). Recolored to the canonical torii-gate sunset theme via the existing :root tokens (--accent #e8842c, --accent-soft #c9793a, --orange #f7931a, --panel-text, --panel-text-dim, --muted): warm radial background (#2a1810→#140d09→#0a0604), accent-orange title with warm glow, bronze→orange progress bar with a soft orange box-shadow, and warm-dim status/sub text. Layout, sizing, spacing, and the eased progress animation are unchanged — color-only migration, no DOM/JS/asset change. 3439 tests green / 273 files, 21/21 regression gates.
Prior: v0.2.701-alpha - HOMEPAGE OWNER CAPTION NOW SHOWS THE ADMIN'S REAL NOSTR NAME TO ALL VISITORS: the top-right "This torii belongs to" caption previously fell back to a shortened npub for every visitor except the owner viewing their own signed-in browser (which could resolve the name locally). Now any visitor sees the admin's real published Nostr display name. New `fetchOwnerProfileName(pubkey)` in `src/nostr.js` — a pure, read-only relay fetch (kind:0, via `fanoutReq` + `readProfiles`) with a 5-minute in-memory TTL cache, validated against a strict 64-char lowercase-hex pubkey regex, and never mutates the viewer's own `state.nostrName`/`nostrAvatar`. `resolveToriiOwnerLabel()` (`src/engine/identity/toriiOwnerLabel.js`) gains a new tier-2 `ownerProfileName` param, giving a 4-tier priority: owner's own in-progress draft → owner's published Nostr name (NEW, visible to all) → shortened npub → "not yet claimed". Wired into `main.js`'s `_refreshOwnerLabel()` via a guarded, once-per-pubkey `_fetchOwnerProfileNameOnce()` that repaints the caption when the fetch resolves. Update button's functional deploy-trigger wiring confirmed untouched (regression-locked by a new source-level test). New tests: 6 added to `torii-owner-label.test.js` (12 total), new `nostr-fetch-owner-profile-name.test.js` (9 tests), new `main-owner-profile-name-wiring.test.js` (6 tests). 3439 tests green / 273 files, 21/21 regression gates.
Prior: v0.2.700-alpha - HOMEPAGE OWNER CAPTION + VERSION PANEL RESTYLE: moved the top-left Torii icon/menu button to the bottom-left, added a new top-left "This torii belongs to: <name>" caption (owner's displayName/name when the viewer is the admin, else a shortened npub, or "not yet claimed" pre-configuration) via the new pure `resolveToriiOwnerLabel()` helper (`src/engine/identity/toriiOwnerLabel.js`, 7 unit tests), wired to refresh on capability probes, NOSTR_LOGIN, and profile-save. Moved the version/update-check panel to the bottom-right and re-themed it from blue/navy to the bronze/orange smoked-glass palette (border, background, backdrop-filter, text tones all matched to brand; the on-brand orange "new version" highlight left untouched). Found and fixed a real CSS bug during verification: `#title-centre.glass-panel`'s `backdrop-filter` was silently creating a new containing block for the panel's `position: fixed`, anchoring it to the console instead of the viewport corner — fixed by lifting `#update-preview` out of that subtree to sit as a direct child of `#screen-title` (documented inline for future maintainers). Also fixed a long-displayName 2-line-wrap collision with the TORII wordmark by switching the caption to single-line ellipsis truncation with a `title` attribute for the full name on hover/long-press. Confirmed via screenshots at 1440×900 and 375×812 (iPhone SE) with both short and long names. Update button's functional deploy-trigger wiring untouched — visual/positional CSS only. Also merged in the previously-tagged-but-unmerged v0.2.699-alpha (ADR-0067 relay trim) commit onto this branch. 3419 tests green / 271 files, 21/21 regression gates.
Prior: v0.2.699-alpha - RELAY TRIM (ADR-0067): fixed repeated browser-console WebSocket errors on relay.damus.io/relay.nostr.band by testing every relay in GAMESTR_RELAYS (leaderboard, src/engine/gamestr/gamestrScore.js) and nostr.js RELAYS (profile reads) with the EXACT queries the game sends, from both the sandbox and the live VPS. relay.nostr.band timed out (~8s, down); relay.damus.io 503'd the leaderboard REQ; relay.primal.net rejected the #game tag filter with a NOTICE. Trimmed GAMESTR_RELAYS from 5 relays to 2 (main.relay.gamestr.io + nos.lol — the only ones that actually work for this query); trimmed nostr.js RELAYS from 4 to 2 (relay.damus.io + nos.lol, both verified against a real kind:0 query). Fixed a latent CSP bug in tools/csp.mjs: main.relay.gamestr.io — the leaderboard's own authoritative relay — was never in connect-src; now included alongside both plebeian relays (marketStall.js uses staging + prod). PLEBEIAN_RELAYS (config.js, still staging) intentionally untouched — marketplace concern, out of scope, would risk the current test auction. torii.quest confirmed NOT a relay and not the user's domain (unrelated site, SSL fails) — no relay exists there. gamestr.io confirmed as a NIP-133-descended Nostr leaderboard protocol; main.relay.gamestr.io is its authoritative relay and was already in use. Updated the GAMESTR_RELAYS source-contract test to assert the exact trimmed list instead of a loose ≥ 4 lower bound. Repo-only change — no VPS deploy. 3412 tests green / 270 files, 21/21 regression gates.
Prior: v0.2.698-alpha - FIRST-LOAD HEARTBEAT CONSENT FIX (default-ON now actually reaches NIP-07 consent): a fresh install ships the heartbeat toggle default-ON (`adminPrefs.getHeartbeatIntent()` defaults to `'on'`), but three call sites derived "is the switch ON" from that raw stored *intent* string instead of from whether a publish had actually happened — so a brand-new owner's very first click read intent as already `'on'`, flipped it straight to `'off'`, and showed "Heartbeat OFF" with no NIP-07 signer prompt ever reachable via the UI. Root cause: intent (`'on'`/`'off'`, what the owner *wants*) was conflated with broadcast state (`'off'|'idle'|'publishing'|'live'|'stale'|'blocked:*'|'failed:*'|'paused:wallet-requires-approval'`, what is *actually happening*) — idle was being read as ON. Fix: new shared pure helper `isHeartbeatBroadcasting(status)` in `src/engine/presence/heartbeat.js` (true only for `'live'`/`'stale'`/`'publishing'`/`'paused:wallet-requires-approval'`; false for `'off'`/`'idle'`/any `'blocked:*'`/`'failed:*'`/non-string) now drives all three call sites: `heartbeatPanel.js`'s `_isOnState()` (idle-state publish label reworded to "Publish my node presence (click to give signer consent + go live)"), `toriiMenu.js`'s `hbOn` derivation, and `main.js`'s `onPublishNode` toggle-direction logic (was inverting off raw intent — `getHeartbeatIntent() === 'on' ? 'off' : 'on'` — now correctly checks `isHeartbeatBroadcasting()` so the first click on a fresh install publishes and requests NIP-07 consent instead of silently flipping to off). `homepageStub.js`'s own Publish card confirmed dead code (unreachable via any live click path) and left untouched. New tests: `isHeartbeatBroadcasting` coverage in `heartbeat.test.js` (all statuses + non-string fail-closed + cross-checks against `heartbeatStatus()`); new `heartbeatPanel.test.js` (hb-switch class/aria-checked per status, idle label wording, owner-gating); new source-level regression locks `torii-menu-heartbeat-toggle.test.js` + `main-heartbeat-consent.test.js` (toriiMenu.js/main.js have no DOM test harness in this repo, so these assert via readFileSync + regex that the shared helper is imported and used at each call site). 3412 full suite green / 270 files, 21/21 regression gates green.
Prior: v0.2.697-alpha - ONE-COMMAND DOCKER INSTALLER: new self-contained `install.sh` (+ `install/lib/ui.sh`, `install/lib/run.sh`) gives a fresh `git clone` of this repo a single `sudo ./install.sh` path to a live instance — Docker install if missing, prompts for domain/email/admin npub, builds + launches the compose stack, verifies over loopback, colourful boxed summary. Multiplayer (`arena-ws`) ships ON by default alongside the game and Nostr relay (`strfry`), matching the standing MP-is-integral decision; owners can reconfigure/duplicate/remove any compose service afterwards. `Caddyfile`+`Dockerfile` reworked so the CSP header is extracted from the real per-build `dist/_headers` at image-build time instead of a hand-copied static value (kills a class of drift — `VPS_INSTALL.md`'s three copy-pasted CSP blocks, found stale with a dead `'strict-dynamic'` directive during this pass, now point at the same live-extraction command instead of a hardcoded string). Fixed a real bash pitfall in `ui_ask`/`ui_confirm`: reopening `/dev/stdin` via redirect for each prompt could read-ahead the whole pipe buffer on some bash builds and flush the unconsumed remainder as literal NUL bytes on exit — now reads the inherited stdin fd directly when no controlling TTY is present, with a real write-probe (not just permission bits) to detect TTY availability; also fixed `run.sh`'s log-dir bootstrap to fall back to a per-user tmp dir instead of crashing with a raw `mkdir: Permission denied` before the friendly non-root message can print. VPS_INSTALL.md gains a new quick-start section pointing at the installer; the manual bare-metal/systemd path stays as documented fallback. 3380 tests green / 267 files, 21/21 regression gates.
Prior: v0.2.695-alpha - MINIMAP REMOVED + SINGLE-K KAMI ENTRY (ADR-0064): (a) removed the in-game minimap entirely — canvas was a sibling (not a child) of its positioning wrapper, so it rendered top-left instead of the intended bottom-right; since a minimap wasn't wanted it was deleted rather than repositioned; (b) a single bare K now enters Kami Mode AND opens the ema note input in one press (was a 2-step flow per ADR-0029); Enter/Esc no longer re-acquire pointer lock, leaving the mouse free to click the emagake rack. 3380 tests green / 267 files, 21/21 regression gates.
Prior: v0.2.685-alpha - ADR-0059 AUCTION PANEL HEADER HARDENING: eliminate every innerHTML write in auctionPanel.js — the header (chips + high-bid) and the legacy fallback bid-list body now render via createElement + textContent, so no relay/profile-derived string (auction_type, currency, bidder name, picture URL) ever reaches innerHTML. renderChips → buildChips (returns [{cls,text}] data), renderBidRow → buildBidRow (returns a plain descriptor); the renderer builds DOM nodes. New malicious-input test proves auction_type/currency/profile-name render as inert text (no element injection). 3368 full suite green / 265 files. | v0.2.684-alpha - SETTINGS PANEL: PROFILE + RELAY TABS, HOMEPAGE FADE, TOP-RIGHT CLOSE (non-gameplay UI/UX pass, six-item polish request): (1) the homepage centre panel's bounding box corners are now fully rounded and the smoked-glass background fades to true 100% transparency at the edges with no hard edge (radial mask-image tuned to a soft feathered stop). (2) new Relay tab (relayPanel.js, pure renderer) lets the owner view/add/remove the wss:// relays their node publishes heartbeat presence to — list rows with a live-dot + URL + Remove button, a comma/newline-separated add/edit textarea, Save Relays button, and an explicit "never falls back to public relays" note; backed by existing nodeRelays getNodeRelays()/setNodeRelays() storage. (3) Heartbeat toggle now defaults to on (adminPrefs.getHeartbeatIntent() default 'on') with an intuitive red/green switch (hb-switch, red track+OFF label when off, green track+ON label + knob slid right when on) instead of a plain checkbox. (4) removed the stale "To visit another world, use the Torii Gateway inside the NAP zone." copy from the Gateway Setup tab (node travel has gone through the in-world NAP-zone Gateway since v0.2.669-alpha; the tab no longer needed to say so). (5) the admin settings panel's bottom-left Close button is replaced by a standard top-right ✕ button (.ts-close-x, circular, top:10px/right:10px) on every tab. (6) new Profile tab (profilePanel.js, first/top nav item) exposes the standard Nostr kind:0 profile fields an owner needs to customise their Quest installation — Display name, Bio, Avatar URL, Website, NIP-05, Lightning address — with a login gate ("Log in with Nostr to customise your profile.") when signed out, a localStorage draft (profileDraft.js, key torii.profile.draft) so in-progress edits survive a reload, and Save Profile building an unsigned kind:0 event (profileMetadata.js buildProfileMetadataEvent, validates pubkey + sanitizes every field) that is signed via the existing NIP-07 signer and published through the existing fanoutPublish/RELAYS path (states: saved-local → publishing → published/failed). New modules: relayPanel.js, profilePanel.js, profileMetadata.js, profileDraft.js. Settings panel TABS reordered to [Profile, Gateway Setup, Heartbeat, Relay] with Profile as the new default active tab. Zero gameplay-file overlap (bots.js/botModel.js/bot-tactics.js/botNetState.js/botSim.js/physics.js/player.js/weapons.js/scene.js/hud.js/input.js/lod.js/multiplayerHost.js/wireProtocol.js/arenaRuntime.js/server/arena-ws.js/server/bots/botStateGate.js all untouched). 3361 full suite green / 264 files (5 skipped), 21/21 regression gates green. Prior: v0.2.683-alpha - ADR-0058 PRODUCT PANEL NAPPLET CONVERSION: the product-stall-panel becomes the first LIVE nap-torii-world napplet. The shell (marketStall.js) keeps the Plebeian relay subscription (network stays in the trusted shell, per NIP-5D) and brokers serialized auction snapshots INTO a sandboxed iframe; the napplet becomes the renderer. New modules: productNappletSnapshot.js (pure serializeBidList → plain JSON, caps bids at 64, strips Maps/functions/remote picture URLs — no innerHTML-of-untrusted risk), productPanelNappletSrcdoc.js (self-contained bid-list renderer using DOM nodes + textContent only, never innerHTML of untrusted data), productNappletHost.js (browser-only DI: mounts NappletSurface into #auction-panel-body, pushes world.surface.update envelopes, destroys on close, falls back to legacy renderAuctionPanel on failure). Updated: nappletSrcdoc.js (parent source check `ev.source !== parent`, per-mount channelId nonce stamped on every message + validated inbound, world.on/off shell→napplet event channel, CSP `connect-src 'none'; img-src 'none'` so the iframe cannot fetch/WebSocket/load images even if a bug tried), NappletSurface.js (generates + validates the channelId nonce, returns post() for shell→napplet pushes), marketStall.js (mounts host when enabled + pushes snapshots; legacy renderAuctionPanel with skipBody fallback when disabled/failed), worldNappletSurfaceConfig.js (product-stall-panel enabled:true, allowedEmitKinds:[] read-only), worldNappletHandlers.js (world.emit returns accepted:false 'no-handler' — no noop for live surfaces), auctionPanel.js (skipBody option so the shell keeps the header while the napplet owns the bid-list body). Built-in LOCAL napplet identity (not external NIP-5A manifest — deferred). New + updated tests across 4 files (srcdoc, surface, snapshot, host); 3366 full suite green / 265 files. Prior: v0.2.682-alpha - ADR-0057 NAPPLET WORLD-SURFACE SHELL SCAFFOLD: the first implementation of the nap-torii-world trusted-shell boundary. Six new modules under src/engine/napplets/ — nappletEnvelope (pure NIP-5D-style postMessage envelope validate/result/error + splitType; unknown types silently ignored for forward-compat), nappletIdentity (pure (dTag, aggregateHash) normalize + key), nappletSrcdoc (pure buildWorldSrcdoc() returns the in-iframe HTML that bootstraps window.napplet.world with the six v0 methods; never reads parent.document), worldNappletHandlers (mostly pure dispatcher implementing world.attach.get + world.zone.list + allow-listed world.emit; returns 'unsupported' for pose.*/visit), worldNappletSurfaceConfig (frozen per-surface config keyed by the EXISTING proof surface ids; every surface stays enabled:false — does NOT mutate proofSurfaceSpecs.js, keeping its inert contract), NappletSurface (browser-only, DI of window/document: mounts an iframe with sandbox EXACTLY 'allow-scripts' no allow-same-origin, binds contentWindow to identity, validates every inbound message by MessageEvent.source NOT event.origin, routes through handlers, destroy() cleanup). DOM-overlay iframe (NOT iframe-to-texture/WebXR — research confirms WebXR immersive renders only the WebGL canvas not the DOM, so embedding a live iframe in a 3D scene is unsolved; v0 proof is a flat surface panel so this gap doesn't block). Game host (NappletGameHost, relay pool, consent gate, game.event.publish/subscribe, fullscreen/zone lifecycle) DEFERRED to ADR-0058+; the shared envelope/identity/srcdoc core is shaped so it can reuse it. No live relay/Blossom/signing/wallet/travel/product-panel conversion — scaffold is test-only/dormant. 39 new tests under tests/napplets/ (envelope 14, srcdoc 7, world handlers 12, NappletSurface boundary 6); 3338 full suite green / 263 files. Prior: v0.2.681-alpha - ADR-0056 HOTFIX (time base + ring count + error honesty): four correctness bugs caught on advisor review of v0.2.680. (1) the indicator used performance.now() for its age math but the autocap report stores Date.now() wall-clock — the 'last Xs' age + recentOk window were wrong; fixed to Date.now(). (2) the capture tick still ran AFTER renderFrame so the JPEG drained a frame late vs the snapshot — moved BEFORE renderFrame. (3) the ring count used capture ATTEMPTS (captured) instead of stored uploads (uploaded) — fixed to uploaded so the count reflects the real VPS ring. (4) the error state had a 60s grace window that hid a current failure — since markUploaded clears lastError, any lastError present is a current unresolved failure; flattened to isError = !!lastError. markUploaded/markFailed now stamp completion-time Date.now(), not the captured request time. 3299 full suite green. Prior: v0.2.680-alpha - ADR-0056 RECORDING INDICATOR + DASHBOARD SURFACING: a live amber glowing 'RECORDING' HUD overlay (recIndicator.js) renders top-right in the arena for the owner while the ADR-0055 1Hz auto-capture ring is live. Owner-gated (not debug-gated) so the owner sees the ring is recording; non-owners never see it. Pure CSS glow animation (rAF only throttles text/state, ~250ms — near-zero cost); prefers-reduced-motion disables the pulse. Honest states: RECORDING (amber) when active/recent/inflight, REC ERROR (red) when lastError set + no upload since, with ring n/120 + last-capture age as a secondary line. Zero cost when off — no DOM until the owner plays + capability resolved. The static oversight dashboard gains a 'Live diagnostics' section documenting the autocap ring (cap 120, path, separation, gate, retrieval CLI) — static/read-only, NO network calls added. 11 new tests + 3298 full suite green. Prior: v0.2.679-alpha - ADR-0055 HOTFIX (owner gate + URL + bounded ring + timing): the first pass had four real bugs caught on advisor review. (1) POST URL was /mp/kami/autocap but resolveMpHttpBase() already returns the /mp base — fixed to /kami/autocap (matches the manual ema route). (2) the client owner gate was just 'has a hex pubkey' so any logged-in visitor would fire 1Hz captures that 403'd — now uses the real admin capability check (fetchCapability + isAdminOperator), resolved once + cached, so non-admins never capture. (3) the autocap index was append-only jsonl (unbounded at 1Hz) — redesigned to ONE {ema,shot} JSON file per frame, ring-culled to 120 (a TRUE ring: disk strictly bounded). (4) the capture tick ran AFTER renderFrame so the queued JPEG drained a frame late vs the snapshot — moved BEFORE renderFrame so they align in the same render tick; timestamps now use Date.now() (wall-clock) for server-log correlation. Dump CLI updated to read the one-file-per-frame ring. 3287 full suite green. Prior: v0.2.678-alpha - ADR-0055 EMA AUTO-CAPTURE DIAGNOSTIC (1Hz rolling ring): a pure 1Hz auto-capture state machine (kamiAutoCapture.js) grabs a canvas frame + a full debug snapshot every second while the owner plays, seals them to the owner+Kami pubkey (same split-seal as the manual ema), and POSTs to a SEPARATE autocap ring (/var/lib/torii-quest/kami/autocap, cap 120) so it never evicts a real manual ema screenshot (shots/ cap 420) and never appends to ema.jsonl (forever). Hooks the in-arena rAF loop (no new timers). Owner-only + playing-only + inflight backpressure. The manual ema snapshot now reports the ring status so a hung ema points at nearby frames. New retrieval CLI (tools/kami-autocap-dump.mjs) decrypts the ring with KAMI_PRIV into JPEG frames + timeline.jsonl, optionally stitching an MP4 via ffmpeg. Catches the transient phantoms + desync the manual K-press always missed. 29 new tests + 3287 full suite green, build clean, check ALL GREEN. No gameplay change - diagnostic capture only. Prior: v0.2.677-alpha - ADR-0053 + ADR-0054 GATEWAY FIXES (live directory + in-place smoked-glass panel): the gateway directory was polluted with 48 stale records (38-56 days old) plus travel handshake (req/res) noise, so no live instance was visible. ADR-0053: readGateways now drops handshake records (state tag) + expired (NIP-40) + stale no-expiration records (1h grace). ADR-0054: the gateway screen opens IN PLACE (no blacked-out backdrop) on a smoked-glass card with three columns — Friends (mutual follows), Follows (you follow), Games (instances that published a game) — reusing classifySections. 5 new tests + 3258 full suite green. Prior: v0.2.676-alpha - SETTINGS PANEL RESTRUCTURE + HOMEPAGE POLISH (non-gameplay UI/UX pass): removed the Instance Settings tab and all its arrival-mode/write-policy admin wiring from the title-screen settings panel (nothing useful there for admins or guests; the underlying signed kind:30078 access-control enforcement in handoffArrival.js/writeAuthority.js is untouched). Split "Publish my node's presence" out of the Gateway Setup tab into its own Heartbeat tab (new heartbeatPanel.js renderer; same underlying state/callback). Settings panel is now exactly 2 tabs: Gateway Setup, Heartbeat. On the homepage: vertically centred the numbered steps (1/2/3) with their row content, moved the "CHARACTER" label above the step rows and reworded it to "Select your character", increased the centre panel's translucency by ~20% and added a radial mask-image so its background fades to full transparency at the edges, removed its light golden border, and removed the title header's table/cell border + dark-brown background (text + torii icon unchanged) while lightening the SOVEREIGNTY · OPEN WORLD · FREE MARKET · ABUNDANCE subtitle text. 3253 full suite green (no new tests needed — no existing test imported the removed modules), 21/21 regression gates green. No gameplay change. Prior: v0.2.675-alpha - ADR-0052 KAMI MODE STATE DIAGNOSTIC (pinpoint the 'stuck in Kami Mode — Esc does nothing' bug): a playtest ema reported being locked in Kami Mode with Esc doing nothing, and the server confirmed KAMI_STATE active=true never cleared. The ema snapshot couldn't tell WHY (active stuck? noteOpen? entering? pointer-lock?). Fix: add a kami sub-report to the debug snapshot (buildKamiReport) capturing active/noteOpen/entering/pointerLocked, wired via installToriiDebug + a new kamiEntering() export. 3 new tests + 3253 full suite green. No gameplay change — read-only diagnostic; the next stuck ema disambiguates the root cause. Prior: v0.2.674-alpha - ADR-0051 WRAP THE POINTER-LOCK YAW TO [-π, π] (fix for BAD_FIELD MOVE rejection): the pointer-lock yaw accumulated without bound (input.js `_yaw -= movementX * SENS`), so after a full 360° turn it exceeded 2π and the server's isRot2 (ROT_ABS = 2π) rejected EVERY MOVE with BAD_FIELD — 1656 rejections in ~10 min, the server silently stopped tracking the player's position. Fix: wrapAngle() normalises to [-π, π] on read in getYaw() — the accumulator stays continuous while every consumer (MOVE rot, ema yaw, fly camera) sees a wrapped value. 6 new tests + 3250 full suite green. No protocol change. Prior: v0.2.673-alpha - HOMEPAGE POLISH (non-gameplay UI/UX pass, rebased onto v0.2.672-alpha): removed the FLY MODE toggle from the homepage (button, pill-switch CSS, and the wiring IIFE in main.js) — in-game F-key free-fly is unaffected, arenaRuntime's #btn-fly-toggle DOM sync is null-guarded. Removed the large standalone #version-label; the version is now shown slightly larger (9px → 11px, .up-row-value) inside the update-preview box below, with a static fallback row added for pre-JS paint + the regression-check version-marker gate. Made the centre panel background highly translucent (background: rgba(30,18,12,0.18), scoped to #title-centre.glass-panel only — the header glass-panel is unaffected). Added large numbered steps (1 2 3) to the left of the character selector, LOGIN WITH NOSTR, and ENTER TORII via a new .step-row/.step-num wrapper. Full suite green, regression check ALL GREEN (21/21). Diff scoped to index.html + src/main.js only — zero gameplay-file overlap with the concurrent ADR-0048/0049/0050 diagnostic work merged just ahead of this commit. Prior: v0.2.672-alpha - ADR-0050 DECOUPLE BOT_STATE BROADCAST FROM THE KAMI MODE ROSTER GATE (root-cause fix for the ~12m desync): ADR-0049 exonerated the client (socket closeCount=0, heartbeat maxGap 2.8s one-time) — the BOT_STATE stall is server-side. The server broadcast BOT_STATE only when players.length > 0, and players EXCLUDED Kami Mode sessions (ADR-0032 bots-ignore-admin), so a sole player in Kami Mode silenced the stream and froze every bot on the client. Fix: extract buildBotTickRoster + shouldBroadcastBotState into server/bots/botStateGate.js — authedCount counts every authed session (Kami or not) while the bot-brain roster still excludes Kami, and the broadcast gate keys off authedCount. Bots still ignore the admin; BOT_STATE keeps flowing. 10 new tests + 3244 full suite green. Confirm live: lastIngestAge should now stay ~66ms across Kami Mode. Prior: v0.2.671-alpha - ADR-0049 WEBSOCKET OPEN/CLOSE + MAIN-THREAD HEARTBEAT DIAGNOSTIC (split the BOT_STATE stall cause): ADR-0048 proved the stream stalls (lastIngestAge 4-7s at shot time in 3 of 4 ema). This deploy adds a pure createConnectionDiagnostics(now) factory (src/engine/diagnostics/connectionDiagnostics.js) tracking (1) the WebSocket transport+protocol lifecycle (connect/open/close/reconnect/state with ages) and (2) a main-thread heartbeat (rAF gap: lastGap/maxGap/stallCount). wsClient emits socket_connect/open/close; multiplayerHost records them; loop.js heartbeats each rAF tick; arenaRuntime captures combat.lastSentShot.conn at shot time. Read: ws.lastCloseAge small ⇒ socket dropped; heartbeat.maxGap large ⇒ main thread froze. 9 new tests + 3234 full suite green, build clean. NO gameplay change — diagnostic only. Prior: v0.2.670-alpha - ADR-0048 BOT_STATE INGEST-RATE DIAGNOSTIC (find why client bots are ~12m stale): ADR-0047 quantified the desync — the client's bots are ~12m stale vs the server (Happy: client (15.2,2.94) vs server (3.62,-1.82), ~5-10s of movement), and the server broadcasts BOT_STATE @15Hz so the stall is client-side. This deploy adds a pure diagnose(nowMs) to botNetState (tracks _ingestCount + _lastIngestAt) exposed via bots.js getBotNetDiagnostic(), captured at SHOT time into combat.lastSentShot.botNet ({ingestCount,lastIngestAge,bots:[{id,sampleCount,newestAge,oldestAge}]}). lastIngestAge ~66ms ⇒ stream flowing (desync is in the client apply/interp path); >>1s ⇒ stream stalled. 3 new tests + 3225 full suite green, build clean, check ALL GREEN. NO gameplay change — diagnostic only. Prior: v0.2.669-alpha - HOMEPAGE SIMPLIFICATION + SETTINGS PANEL MODULE + ORANGE/BRONZE RECOLOR (non-gameplay UI/UX pass, rebased onto v0.2.668-alpha): removed the chat panel, leaderboard panel, gateway-preview panel, and player-stats card from the homepage; removed the instance-settings link, project-dashboard link, and gateway-setup button; merged ENTER ARENA + ENTER NAP into a single ENTER TORII button (btn-enter-nap / elNapBtn); deleted a duplicated second title block; moved the torii icon button from top-right to top-left. New settings panel module (src/engine/settings/settingsPanel.js, nav-left/content-right) replaces the old toriiMenu popup on icon click, hosting Instance Settings + Gateway Setup tabs. Gateway Setup tab (src/engine/settings/gatewaySetupPanel.js) has exactly 3 owner-facing cards (Choose Blank, Use My World as Template, Publish My Node) — no 'Visit a Node' card, node travel goes through the Torii Gateway inside the NAP zone instead; Node Directory tab dropped entirely. Orange/bronze recolor over a torii-gate background image with a frosted-glass content panel. Removed a stray unstyled GATEWAY SETUP button IIFE and a dead #btn-enter inline-script fallback listener. Rebound tests/entry-flow-smoke.test.js to btn-enter-nap/elNapBtn/'ENTER TORII'. Updated tools/csp.mjs INLINE_SCRIPT_SHA256 to match the edited inline script. Zero overlap with combat/bot/physics/net files (bots.js, botModel.js, bot-tactics.js, botNetState.js, botSim.js, physics.js, player.js, weapons.js, scene.js, hud.js, input.js, lod.js, multiplayerHost.js, wireProtocol.js, arenaRuntime.js untouched). 3217 full suite green, check ALL GREEN. Prior: v0.2.668-alpha - ADR-0047 RENDERED BOT-POSITION DIAGNOSTIC (exposes client/server desync): ADR-0046 confirmed the camera ray is sent unaltered (usedAimRay=true + sentOrigin==aimOrigin in all 8 ema) — so the miss is NOT camera-vs-muzzle. The server SHOT-RESOLVE log shows the real cause: the SAME camera ray hits a bot on the CLIENT at 6.36m but misses on the SERVER by bodyGap=1.68m — the client's bot collider sits ~2.7m from where the server's bot actually is (a client/server bot-position desync, not a collider-radius shortfall). This deploy captures the client's RENDERED bot positions at shot time into combat.lastSentShot.bots ({id,x,z,alive,hp} via new pure snapshotBotPositions in lastShotStore.js) so a miss ema can be diffed against the server's cur=/rew= per bot id. Also added x/z to getBotRenderStates (ADR-0045). 2 new tests + 3222 full suite green, build clean, check ALL GREEN. NO gameplay change — diagnostic only. Prior: v0.2.667-alpha - ADR-0046 SENT-RAY DIAGNOSTIC (proves camera-vs-muzzle on miss): on-target shots at STATIONARY bots miss by bodyGap=0.46-0.53m (ray passes 0.81-0.88m from the capsule axis; radius 0.35m) — confirmed NOT a rewind/lag-comp issue (dxz=0.00). buildShotPayload already prefers the camera aim ray (peer-combat test proves it). The open question: at RUNTIME, is aimOrigin/aimDir actually present when SHOOT fires, or does it fall back to the muzzle ray? Only player.js emits SHOOT (always with aimOrigin=_camPos, aimDir=_camFwd). This deploy adds `combat.lastSentShot` to the ema snapshot: {ts,viewLag,usedAimRay,sentOrigin,sentDir,muzzleOrigin,muzzleDir,aimOrigin,aimDir} — the ACTUAL buildShotPayload output that went to the server, with full muzzle/aim vectors (not just Y, since Y alone couldn't distinguish camera vs barrel). Written to its OWN _lastSentShot slot (independent of _lastShot, which only exists when aimOrigin/aimDir are present) so it is NEVER stale in the usedAimRay=false failure case. usedAimRay=true + sentOrigin≈aimOrigin → camera ray sent (then bot-only collider assist); usedAimRay=false → muzzle ray (fix aimOrigin passing). Server SHOT-RESOLVE log now also records rounded origin/dir vectors + clientTs so the ema's sentOrigin/sentDir/ts can be matched numerically against what the server received. Extracted lastShot store into pure module engine/combat/lastShotStore.js (no three/Rapier) so setLastShotSent/setLastSentShot are unit-testable. 4 new tests + 3219 full suite green, build clean, check ALL GREEN. NO gameplay change — diagnostic only. Prior:  - ADR-0042 INSTANT EMA POST + BOT HIT VISUALS (round-2 playtest): (1) Enter in an ema note now seals + POSTs to /kami/ema INSTANTLY (pending→sent/failed); Shift+K is now the retry path for failed posts only; a failed POST keeps the note on the rack. Root cause of "hang failed" was EROFS (torii-arena-ws.service ProtectSystem=strict, ReadWritePaths=/opt/torii-quest/mp only, so /var/lib/torii-quest/kami read-only) — fixed via systemd drop-in adding ReadWritePaths=/var/lib/torii-quest/kami (verified; follow-up to bake into torii-suite installer). (2) Bot hit feedback: bots had NO visible reaction to server-confirmed hits. applyBotHit now calls model.flashHit (red 0xff3030 emissive flash 0.18s, decayed in tick) + model.updateNameplate(name, hp/maxHp) (HP bar green→red); render loop passes !st.alive so death anim plays. Stickers do NOT stick to bots by design (isBotMesh excludes them from the sticker raycaster) — flash+HP+death ARE the feedback. Also: sealTo guard relaxed from instanceof Uint8Array to byteLength (cross-realm). 15 new tests + __resetKamiForTests for isolate:false module-state isolation. 3205 full suite green, build clean, check ALL GREEN. Prior: v0.2.660-alpha - ADR-0041 PLAYTEST UX (reliable kami exit + visible hit feedback): round-1 bot playtest exposed two usability bugs (NOT hit-reg — the server damage chain is correct). (1) Stuck in Kami Mode: ESC is browser-reserved while pointer-locked, so the 2nd ESC never reached exitKamiMode (KAMI_STATE stayed active=true); added a pointer-lock-independent ✕ exit button to the rack box that calls exitKamiMode directly. (2) "many hits and nothing": flashCross was a 0.12s crosshair blip + the stronger screen-edge flashHit was NOT wired to bot hits; BOT_HIT_BY_PLAYER now also calls flashHit (body=25→~0.55 opacity, head=50→0.8) + flashCross duration 0.12→0.25s. Ema store ENOENT was a missing VPS dir (/var/lib/torii-quest/kami); ensure() already uses recursive mkdir so it self-heals — dir created on the VPS. 3190 full suite green, build clean, check ALL GREEN. Prior: v0.2.659-alpha - ADR-0040 POLISH (declare undici dep + kind:14 doc/test accuracy): v0.2.658-alpha's `import { WebSocket } from 'undici'` resolved only TRANSITIVELY (node_modules/undici pulled in by another package — `require('module').builtinModules` has NO 'undici', so it is NOT a Node core module). Declared undici (^7.29.0) as an explicit top-level dependency so future `npm install`s are robust against transitive-dep removal. Also corrected ADR-0040's inner-rumor description (kind:14 NIP-17 private direct message, not kind:1) + added test assertions that the unwrapped rumor is kind:14 + sender pubkey === Kami's pubkey. Tool comment clarified + usage example fixed (sudo -u scrubs env → set KAMI_PRIV inside the torii-quest bash -c). Zero production behavior change (deployed v0.2.658-alpha already worked via transitive undici; VPS dry-run re-confirmed after this bump). 3190 full suite green, build clean, check ALL GREEN. Prior: v0.2.658-alpha - ADR-0040 HOTFIX (undici WebSocket for Node relay publish): v0.2.657-alpha's tools/kami-nostr-reply.mjs fell back to a non-existent global WebSocket in Node 20 (no undici import), so every relay publish returned 'no-websocket' and the NIP-17 DM never left the VPS. Fix: import WebSocket from Node's built-in undici (browser-API compatible) + pass as WebSocketCtor; robustify onmessage data-coercion for Buffer(ws) vs string(undici). Smoke-tested live: published a real kind:1059 to the owner's npub, accepted by 3/4 relays (plebeian staging + nos.lol + primal); subscribed + confirmed the gift-wrap outer signers are all ephemeral (not Kami's real key ea3ff08e…), #p tag = owner. In-game rack path unaffected. 3190 full suite green, build clean, check ALL GREEN. Prior: v0.2.657-alpha - NOSTR-NATIVE KAMI CHAT BRIDGE (ADR-0040 Stage 1: Kami replies now dual-write — appended to replies.jsonl for the in-game emagake rack (ADR-0039 path unchanged) AND published as a native NIP-17 gift-wrapped DM (kind:1059) to the owner's npub, so Kami replies show up as real Nostr direct messages in Buzz / Amethyst / Nostir. Corrected premise before coding: the browser CANNOT decrypt NIP-44/NIP-17 without window.nostr.nip44 (most extensions don't expose it), so the in-game rack keeps the HTTP replies.jsonl path; Kami (VPS, nostr-tools) only needs the owner's PUBLIC npub to publish. New server module server/kami/kamiNostr.js (buildGiftWrap + publishEventToRelay) + tools/kami-nostr-reply.mjs (dual-write CLI, runs as torii-quest user) + src/engine/kami/kamiNostrCap.js (browser NIP-07 feature detection, foundation for future in-game decrypt, not wired into the rack). Added nostr-tools as a server-only dep (NOT in the browser bundle — confirmed via build). 15 new tests + 3189 full suite green, build clean.) Prior: v0.2.656-alpha - KAMI HANG HOTKEY COPY FIX (ADR-0039 hotfix: empty-rack + tray-counter said CTRL+E / CTRL+SHIFT+E to hang, but the real hotkey is Shift+K since ADR-0031. Both corrected to "SHIFT+K TO HANG". 3175 full suite green, build clean.) Prior: v0.2.655-alpha - 2-WAY EMA COMMS / AI REPLIES IN THE EMAGAKE RACK (ADR-0039: AI replies render in the in-game emagake rack. Direction 1 user→AI unchanged from ADR-0038. Direction 2 AI→user is plaintext because the browser can't decrypt kamiSeal (NIP-07 has no ECDH). Separate `replies.jsonl` feed: `server/kami/kamiReplyStore.js` + `server/kami/kamiReplyRoute.js` + admin-gated `GET /mp/kami/replies?since=<ts>` route in arena-ws.js. AI posts via `tools/kami-reply.mjs` on the VPS (reads text from file/stdin, never shell-quoted). Client `kamiMode.js` polls every ~5s in Kami Mode + owner token; `emagakePanel.js` renders distinct `ema-row kami-reply` rows, textContent only, deduped by id. 24 new tests + 3175 full suite green, build clean.) Prior: v0.2.654-alpha - REOPEN ALL BOARDS ON EVERY TRIGGER PRESS (ADR-0037: setBoardsActive(true) now always force-shows all three boards instead of short-circuiting when already active — fixes the bug where pressing Q a second time after closing the boards only reopened the auction panel, leaving the three boards hidden, because hideOwnerBoard() never reset `_active`. 11 close-button tests + 3151 full suite green.) Prior: v0.2.653-alpha - PRODUCT SIGN PROXIMITY+Q TRIGGER (ADR-0036: the auction-panel + three ADR-0035 owner boards no longer auto-show on NAP-zone entry and have zero relationship to Kami Mode — they open only via a proximity+Q trigger on the in-world PRODUCT sign mesh (`product-stall-panel`), each panel with its own explicit close/X button. 25 new tests (15 product-panel-trigger + 10 product-panel-close-buttons) + 3150 full suite green.) Prior: **v0.2.652-alpha** - KAMI VISUAL UNIFICATION + OWNER PRODUCT/AUCTION BOARDS (ADR-0034 + ADR-0035, one deploy: ADR-0034 replaces the full-screen darkened `#kami-overlay` ema editor with an inline bottom bar in the same translucent panel language as the emagake rack — world stays fully visible at all times, no backdrop dimming; a 3rd K press while the editor is already open highlights it instead of opening a second one; `setMarketActive` is now gated off `kamiActive()` so no panel auto-pops over Kami Mode's own components. ADR-0035 replaces the single hardcoded-auction `#auction-panel` with three separate NAP-zone DOM board overlays — Live Products, Live Auctions, Past Auctions — sourced from the owner's own npub via generalized `plebeianRelay.js` (NIP-99 kind:30402 products, kind:30408/1023 auctions/bids), built as `boardModel.js` + `boardPanel.js` + `ownerBoards.js`; visible to any guest, not gated to Kami Mode or the owner; DOM overlays only in this slice, an in-world 3D board render is deferred to the future napplet-architecture work. Also folded in: historical ADRs 0025/0026/0028/0029/0030/0031 fully rewritten in place to read "emagake" throughout (footnote-only approach from ADR-0033 reversed per explicit owner instruction — see ADR-0033's Amendment); `setTimeout` allowlist gate extended to cover `kamiMode.js`/`marketStall.js`/`plebeianRelay.js`'s existing revert/reconnect timers. 28 new tests (9 board-model + 9 board-panel + 8 reduce-author-events + 2 note-bar tests folded into kami-state-machine.test.js) + 3125 full suite green, build clean, regression check all green.) Prior: `v0.2.651-alpha` - EMAGAKE SPELLING FIX (ADR-0033: "emakake" was a spelling mistake — the correct romanization of 絵馬掛け is `emagake` (rendaku k→g), confirmed against Japanese-language sources. Renamed everywhere a Japanese-literate person could see it: DOM IDs (`#emakake`→`#emagake` etc.), the `emakakePanel.js`→`emagakePanel.js` module, all exported function/variable names, CSS, tests, and tracking docs. Historical ADRs (0025, 0026, 0028–0031) keep their original text for the record with a short correction footnote appended rather than being silently rewritten. 3097 full suite green, zero behavior change. [Amended 2026-08-23: footnote approach reversed per explicit owner instruction — see the v0.2.652-alpha entry above and ADR-0033's Amendment section; ADRs 0025/0026/0028/0029/0030/0031 are now fully rewritten to say "emagake" throughout, footnotes removed.]). Prior: `v0.2.650-alpha` - KAMI SERVER-AUTHORITATIVE INVINCIBILITY (ADR-0032: bots/peers could still shoot + kill the admin in Kami Mode — the existing invincibility flag was client-side only and never reached the server, which independently resolves all MP combat. Added a verified `KAMI_STATE` wire message; server re-checks the sender's own pubkey against the admin pubkey before honouring it, excludes the admin from the bot AI's per-tick target roster (bots literally can't see them), and backstops the in-flight bot ray-cast + peer/bot damage-resolution paths. 8 new tests + 3097 full suite green.). Prior: `v0.2.649-alpha` - KAMI HOTKEY FIX (ADR-0031: Ctrl/Cmd+E was silently swallowed by Brave's address-bar shortcut — zero [kami] console output, no keydown ever reached the page. Switched to a bare `K` press — no browser owns unmodified single-key presses. Ctrl/Cmd+K explicitly excluded so it still reaches browser chrome. 2 new regression tests + 3093 full suite green.). Prior: `v0.2.648-alpha` - FLOATING PANELS DOM FIX (ADR-0028: the ema rack `#emagake` + Plebeian auction `#auction-panel` were nested inside `#screen-title`, which `phaseScreens.js` hides (`display:none`) during PLAYING — removing even `position:fixed` descendants from the render tree, so both panels were invisible in the arena. Moved both to top-level body scope (siblings of `#hud`). 5 nesting regression tests + 3084 full suite green.). Prior: `v0.2.642-alpha` - KAMI EMA INPUT OWNED (ADR-0027: the ema note input now owns its keystrokes while open — Space no longer jumps the player, Escape closes the modal instead of opening the pause menu. 9 input-boundary tests + 3078 full suite green.). Prior: `v0.2.636-alpha` - BIDDER IDENTITY + REVERSE-ORDER BIDS (ADR-0026 slice 3: bid list now highest-first; each row shows a circular bidder avatar (profile picture where a kind-0 Nostr profile exists on the staging/prod Plebeian relays, else a deterministic colored-circle with the initial) + display name (or short pubkey fallback); marketStall best-effort fetches kind-0 profiles from both relays after bids arrive, then re-renders; the 'high bid' flag now marks only the single top bid. 35 plebeian tests + 3072 full suite green.). Prior: `v0.2.635-alpha` - SPATIAL MARKETPLACE (ADR-0026: watch-only Plebeian auction reader in the NAP zone. Smoked-glass DOM overlay (#auction-panel) subscribes to Plebeian's own relay, renders kind-30408 auctions + kind-1023 bid history with running high bid + end-time countdown; watch-only, links out to auctions.plebeian.market, no in-panel bidding. Toggled by NAP-zone entry.). Prior: `v0.2.634-alpha` - KAMI MODE (ADR-0025: owner's sealed in-world ema note-taking surface; Ctrl+E / pause-modal captures note+screenshot, sealed ECDH+HKDF+AES-256-GCM in-browser before admin-gated POST; server holds ciphertext only; 47 kami tests). Prior: `v0.2.633-alpha` - LAG-COMP ROUND TRIP (ADR-0024: one-line viewLag fix +ow -> +2*ow; peer-vs-peer hit-reg fixed by the same line). Prior: `v0.2.632-alpha` - MISS-GEOMETRY DIAGNOSTICS (ADR-0023). Prior: `v0.2.631-alpha` - MP MODEL ATTACH (ADR-0022). Prior: `v0.2.630-alpha` - MP AUTHORITATIVE ROSTER (ADR-0021). Prior: `v0.2.629-alpha` - COMPLETE STALE-BOT CLEANUP (ADR-0020). Prior: `v0.2.626-alpha` - CLIENT BOT STATE SYNC + ZONE PLUMB (ADR-0016 nameplate lifecycle + dead-bot LOD; ADR-0017 zone in BOT_HIT). Prior: `v0.2.625-alpha` - MP HIT-REG ALIVE-WINDOW FIX (ADR-0015). Prior: `v0.2.624-alpha` - COMPONENT SEAM (0l.1 expand path + crates-as-components, 0l.2 runtime host + beacon, 0l.3 portal/NAP data-shell). Prior: `v0.2.602-alpha` - PER-BONE STICKER COLLIDERS ON BOTS (extended to bots, castRay default excludes bone sensors). Prior: `v0.2.574-alpha` - PER-BONE NPC STICKER COLLIDERS (per-bone Rapier ball sensors + Object3D.attach).

## 🚨 TOP OF QUEUE

### SHIPPED v0.2.699-alpha — ONE-COMMAND DOCKER INSTALLER (ADR-0065, scoped + shipped 2026-08-26). Self-contained `install.sh` (+ `install/lib/ui.sh`, `install/lib/run.sh`) gives a fresh `git clone` of `torii-quest` a single `sudo ./install.sh` path to a live instance — Docker install-if-missing, domain/email/admin-npub prompts, builds + launches the Compose stack, verifies over loopback, colourful boxed summary. Multiplayer (`arena-ws`) ships ON by default alongside the game and relay (`strfry`), per the standing MP-is-integral decision; owners can reconfigure/duplicate/remove any Compose service afterwards. Entirely self-contained in this repo — no dependency on `torii-suite`/`bootstrap.sh`. `Caddyfile`/`Dockerfile` now extract the CSP header from the real per-build `dist/_headers` instead of a hand-copied static value, closing a drift class also found stale (dead `'strict-dynamic'`) in three spots in `VPS_INSTALL.md`, which now point at the same live-extraction command. Two real bugs found + fixed: a `/dev/stdin` redirect in `ui_ask`/`ui_confirm` could flush an unconsumed pipe read-ahead as literal NUL bytes on exit (fixed via a TTY-presence check reading the inherited stdin fd directly), and `run.sh`'s log-dir bootstrap could crash non-root invocations with a raw `mkdir: Permission denied` before the friendly error could print (fixed with a per-user tmp fallback). 3380 tests green / 267 files, 21/21 regression gates. See ADR-0065 for full decision + consequences.

### SHIPPED v0.2.652-alpha — KAMI MODE VISUAL UNIFICATION (ADR-0034, scoped + shipped 2026-08-23). Replaced the darkened full-screen `#kami-overlay` ema editor with an inline bottom bar styled like the emagake rack — world stays fully visible in every Kami state, matching the single-K-press aesthetic. 2nd K opens/focuses the bottom editor instead of popping a centered modal; a 3rd K while already open highlights it. Also gated `setMarketActive` off `kamiActive()` so the auction/board panels no longer auto-pop on NAP-zone entry while in Kami Mode. See ADR-0034 for full decision + consequences.

### SHIPPED v0.2.652-alpha — PRODUCT/AUCTION BOARDS (ADR-0035, scoped + shipped 2026-08-23). Reimagined the single hardcoded auction panel into THREE separate guest-visible NAP-zone DOM boards sourced from the owner's real Plebeian Market listings (dev: auctionsdev.plebeian.market, confirmed seller pubkey `ec79b568...906f25`): Live Products (kind:30402), Live Auctions (kind:30408, future end-time), Past Auctions (kind:30408, past end-time). DOM overlays only in this slice — an in-world 3D board render is deferred to the future napplet architecture. Single-seller (owner npub) only for now; architected so multi-seller/other-shop boards and the future napplet contract aren't blocked. See ADR-0035 for full decision + consequences.

### NEXT (PRIORITY) — N2N TRAVEL TEST WITH BEKKA (2026-08-31). Both instances are live again (HashIT outage resolved 2026-08-31 — both sites now ~0.17s TTFB). Priority is a real node-to-node test: chiefmonkey.art/quest/ ↔ torii.plebeian.build. Verify the open-visit travel path (direct navigate + ?torii-traveller=) and presence discovery across the unified relay list (ADR-0081). Bekka must be on v0.2.713+ (one-command bootstrap) for the heartbeat/relay changes.

### RECENTLY SHIPPED (v0.2.713 → v0.2.717, 2026-08-28/29):
- v0.2.717-alpha — settings panel smoked glass + profile prefill from Nostr kind:0
- v0.2.716-alpha — relay tab polish (STARTER badge removed, per-row Remove button)
- v0.2.715-alpha — single unified relay list (ADR-0081)
- v0.2.714-alpha — login status pubkey fragment removed (ADR-0080)
- v0.2.713-alpha — one-command VPS bootstrap (ADR-0079)

### DEFERRED (need core refactors, not fake tabs):
- Audio tab (no master gain API)
- Graphics tab (no manual override)
- Controls tab (no keybind layer)
- Phase 3 truly-silent beacon (server-side delegated signer, ADR-0077 §3)

### FUTURE — CLEAN-ROOM PASS ON COMBAT/COLLISION MODULE. Once the sent-ray diagnostic (ADR-0046) closes the camera-vs-muzzle question and the resulting fix (bot-only collider assist or aimOrigin passing fix) is deployed and confirmed stable live, do a narrow clean-room refactor isolating `rayVsBot`/capsule math (currently in `server/bots/botColliders.js` + `rayVsCapsule.js`) into a more testable, self-contained module — NOT a full engine rebuild (discussed with owner 2026-08-24: full rebuild rejected as costlier with no guarantee of avoiding this class of bug; scope stays limited to combat/collision only, keep `rayVsPeer`/shared capsule code untouched unless a test proves it needs to change).


### SP BODY-SHOT FIX — hitscan-on-aim-ray (v0.2.397-alpha) — GitHub-only (no live deploy). Single-player body shots landed at close range but on-target (green-reticle) shots dropped at long range OR when a bot was near an object. ROOT CAUSE: SP resolved the local shot via a TRAVELLING PROJECTILE (per-tick Rapier raycast along barrel→aimPoint in `src/weapons.js` `tickWeapons`), which (a) let a moving bot dodge during flight at range and (b) fired from the OFFSET gun barrel so its line clipped nearby geometry the camera/crosshair ray cleared. MP already did it right (`buildShotPayload` → server hitscan). FIX: SP now resolves damage via HITSCAN on the camera/aim ray AT FIRE TIME. New pure module `src/engine/combat/localShot.js` `resolveLocalHitscan(hit, netMode)` → `{bot,dmg,isHead,toi}` for a live-bot hit in SP else `null`; called in `recordPlayerShot` right after the aim cast (capturing the impact point before the pred cast clobbers the shared `hit.point` scratch), reusing the existing `EV.BOT_HIT_BY_PLAYER`→`hitBot` path so HP/score/kill are byte-identical, with `classifyHeadshot` on the aim-ray impact preserving head(9)/body(3). The tracer is now COSMETIC: player bullets are flagged `b.noDamage` so `tickWeapons` still sparks but does NOT apply bot damage (no double-damage). Gated SP-only via injected `isBotNetMode` (`arenaRuntime`→`initWeapons` 4th arg); MP payload + server hitscan path untouched. `ToriiDebug.combat` diagnostics preserved (aim==outcome). Combat values unchanged (BOT_HP=5/BODY=3/HEADSHOT=9, radii 0.30, LAG_COMP_MS=300). New `tests/localShot.test.js` (8). `PROTOCOL_VERSION=1`.

### UPDATE-UX STUCK-DEPLOYING FIX (v0.2.393-alpha) — GitHub-only (no live deploy). The in-game "Update Now" progress UI stuck at ~80-90%/DEPLOYING forever even though the deploy actually succeeded (~9s); only the CLIENT poller never reached DONE. ROOT CAUSE: mid-deploy arena-ws RESTARTS and drops its in-memory admin session token, so post-restart `GET /mp/admin/update-status` polls 403'd → the poller treated non-`succeeded` as "still deploying" → stuck, never auto-refreshing. FIX (defense in depth): (1) UNGATE the status read — `GET /mp/admin/update-status` is now PUBLIC (removed the `adminFromRequest` 403 gate); it exposes only read-only progress (state/targetRef/startedAt/finishedAt/message), no secrets; `POST /mp/admin/update` stays admin-gated + fresh-signed-intent. (2) `adminUpdateClient.fetchStatus` makes the bearer token OPTIONAL and returns `{state:'unavailable', code}` (code=HTTP status, 0 for network errors) so the poller can tell a post-restart 403 from a network blip. (3) `src/main.js` `_pollUpdateStatus` tracks `deployingSince` and flips to DONE + `location.reload()` on a post-progress 403 OR when DEPLOYING exceeds the new `DEPLOY_STALL_MS=30000` hard ceiling. (4) Keeps polling through transient network/403/502 errors. `PROTOCOL_VERSION=1`.

### HIT-REG CLOCK-SYNC ROOT-CAUSE FIX (v0.2.392-alpha) — GitHub-only (no live deploy). Code-confirmed root cause of the persistent hit-reg failures: the lag-comp rewind indexed the server's bot/peer snapshot rings (stamped in SERVER time) with a value derived from the CLIENT clock. v0.2.391 stamped SHOT ts = `client Date.now() − viewLag`, but the client clock is NOT synchronised to the server (RTT/2 is a latency estimate, not clock sync), so `shotTimeRows`/`resolveShot` sampled the WRONG snapshot row (or clamped) → the bot was rewound to the wrong position → the ray missed. FIX: eliminate the client clock from the rewind. The client now sends its measured `viewLag` (render-interp delay + network one-way, clamped ≤250ms) on the SHOT (additive wire field, `PROTOCOL_VERSION` stays 1) and RAW `Date.now()` as `ts` (logging only). The SERVER computes `rewindTs = server_now − viewLag` entirely in its own clock frame and passes that to BOTH the peer (`resolveShot`) and bot (`resolvePlayerShot`) resolvers. one-way is counted exactly once (inside viewLag) — the server does NOT subtract it again. Damage/collider values unchanged (BOT_HP=5/BODY=3/HEADSHOT=9, radii 0.30, LAG_COMP_MS). Added a per-shot `[SHOT-RESOLVE]` diagnostic (viewLag, rewindAge, rwClamp, nearest bot CUR vs REWOUND XZ + dxz) as a live safety net.

### HIT-REG + SPAWN + BOSS — combat/UX fixes (v0.2.391-alpha) — GitHub-only (no live deploy). THREE fixes. (1) HIT REGISTRATION deeply broken (intermittent "shoot a bot 10-30× and nothing"): the server lag-comp rewinds its bot snapshot ring (stamped with the SERVER clock) using the client's raw `Date.now()` shot ts, and NEVER accounted for the 100ms client interpolation delay (`botNetState` renders bots `DEFAULT_INTERP_DELAY_MS=100` in the PAST). Net effect: the server rewinds ~100ms + one-way-latency too little, so it ray-tests the collider where the bot IS, not where the shooter SAW it — moving bots eat shots (stationary bots hit fine, hence the intermittency). FIX (no wire-schema change): measure one-way latency client-side from the existing keepalive PING/PONG echo (`wsClient.oneWayMs`, EMA-smoothed, clamped ≤250ms), expose `multiplayerHost.viewLagMs()` = interp delay + one-way, and stamp the outbound SHOT ts as `Date.now() − viewLag` in `arenaRuntime` so the server rewinds to the rendered pose. `PROTOCOL_VERSION=1`; BOT_HP=5/BODY=3/HEADSHOT=9 unchanged; headshot classification untouched (the ray now actually reaches the collider). (2) EMPTY ARENA on entry: `initBots` attached EVERY bot inside the boss GLB's `.then()`, so the whole roster waited on the 7.6MB `augustink4.glb`. FIX: two-phase attach — regular bots populate the instant the small regular GLB loads; the boss is fetched in PARALLEL and attached when it resolves (falls back to the regular model on failure). (3) AUGUSTINK FLEEING: the boss ran the regular flank-ring + cover-seek AI (orbits a 6-7.6m ring, ducks to cover when pressured) which read as fleeing. FIX (boss-only): a tank branch in `botSim` — the boss skips cover eval, marches straight at the player, and HOLDS at `BOSS_STANDOFF≈3m`, never retreating; regular bots unchanged. BOSS_HP=60/RADIUS/DAMAGE/SPEED unchanged.

### UPDATE-UX — admin "Update Now" client fixes (v0.2.390-alpha) — GitHub-only (no live deploy). The SERVER side of Update Now works (trigger file → root path-watcher → runner resolves latest tag, builds, restarts arena-ws in ~9s → status file); v0.2.389 deployed this way. The BUG was CLIENT-only: the UI stuck on "UPDATING…" and never reflected success. (1) ROBUST POLLING THROUGH RESTART: arena-ws restarts during the ~9s deploy so `GET /mp/admin/update-status` polls fail; the old poller never recovered. `src/main.js` `_pollUpdateStatus` is now a single fixed-cadence (`setInterval`, 500ms) state machine that treats `state:'unavailable'` (connection refused while restarting) as the 'deploying' stage and KEEPS polling with gentle backoff (1s→3s), resuming brisk polls when the server answers; terminal `succeeded`/`failed` only trusted after the NEW run shows progress (or a 4s grace) so a stale prior-deploy status can't short-circuit; 5-min safety timeout. (2) SEXY PROGRESS BAR: `index.html` gains `#update-progress` (animated orange fill + Requested→Building→Deploying→Done step labels, navy track, shimmer on the deploying stage); `_setUpdateProgress()` drives it. (3) AUTO-REFRESH: on `succeeded` the bar fills 100% ("done — reloading <ver>") then `location.reload()` after ~1.5s — no manual hard-refresh. (4) FAILED STATE: shows the message + re-arms the button as "↻ RETRY UPDATE". (5) LATEST 'v' PREFIX: `liveStatusView` now normalises the LATEST cell to carry the `v` (tags source strips it), matching INSTALLED. `PROTOCOL_VERSION=1`; server endpoints unchanged.

### COMBAT-FEEL — three fixes (v0.2.389-alpha) — GitHub-only (no live deploy). (1) AUGUSTINK TOO SHORT: `BOSS_TARGET_HEIGHT` 2.0→3.0m in `src/config.js` (render-only). The boss collider already scales 2× (`radius/BOT_R`=0.8/0.4) so the ~3.2m hitbox now envelopes the taller 3m model — no collider-Y change needed. (2) HEADSHOTS NOT 1-SHOTTING regular bots: at head height the body cap (r0.30) protruded farther than the head sphere (r0.23), so the authoritative analytic ray (`rayVsPeer`) resolved face shots as `body` (3 dmg → 2 shots) not `head` (9 dmg → 1 shot vs BOT_HP=5). FIX: `BOT_HEAD_RADIUS` 0.23→0.30 (== body radius) in BOTH `server/bots/botColliders.js` + `src/engine/physics/bodies.js` (classifier prox backstop auto-follows); head now wins those ties, chest shots stay body. (3) DEATH BLOW-BACK REGRESSION: server-authoritative bots (v0.2.377) dropped the vertical launch arc — the MP death path (`_syncNetBot`) pinned the corpse to ground `fy`; the snapshot carries the horizontal slide but not `_blowY`. FIX: reconstruct the arc client-side from the death clock (9t−7t², matching botSim's 9 m/s launch + −14 gravity) so corpses fly up + back across the arena again. `BOSS_HP=60` verified applied per-bot (no one-shot bug — the "died in one shot" was accumulated distance damage). `PROTOCOL_VERSION=1`, no HP/damage stats changed.

### BOSS-SIZE — Augustink render-size fix (v0.2.388-alpha) — RENDER-ONLY. The boss rendered ~200m ("bigger than the arena") and `BOSS_TARGET_HEIGHT` had no visible effect. Root cause: `src/botModel.js` `init()` measured natural height via `Box3.setFromObject` on a rigged SkinnedMesh whose Armature root carries a 0.01 Blender cm-export scale — `setFromObject` reports the collapsed node box (~0.017m) while the animated mesh renders at its LOCAL geometry size (~1.66m, inverse-bind cancels the armature scale), so `s=target/naturalH≈120` → ~200m boss. FIX: measure geometry-only LOCAL bounds (true rendered height), scale `s=BOSS_TARGET_HEIGHT/trueHeight≈1.20` → ~2.0m, feet planted via `-gMinY*s`, mirroring the peer-avatar path in `arenaRuntime.js`. NO combat changes: `BOSS_HP=60` (tank), `BOSS_RADIUS`/collider/damage, `BOT_BODY_RADIUS`/`BOT_HEAD_RADIUS`, regular-bot rendering all untouched; `PROTOCOL_VERSION=1`, single-player byte-identical apart from the correctly-sized boss.

### UPD-2 — version/update box + admin server-side "Update Now" (v0.2.387-alpha) — three pillars. (1) TAGS check fixes the live 404: the live update-check now reads the GitHub `tags` endpoint (`RELEASE_SOURCE.tagsUrl` + a dedicated `fetchLatestTag` adapter mapping tag `{name}`→`{tag_name}`) instead of `releases/latest` (this repo ships tags, not GitHub Releases); cache key bumped `torii.updateCheck.v1`→`v2`. (2) `arena-ws` gains admin-gated HTTP endpoints: `POST /mp/admin/update` (session token whose pubkey === `QUEST_ADMIN_NPUB` hex + a FRESH signed intent event `torii-quest:update-now:<nonce>`, created_at ≤120s → writes ONE atomic request file to `/opt/torii-quest/mp/update-requests/`, single-flight, never runs a shell, never accepts a client ref), `GET /mp/admin/update-status` (admin-gated), `GET /mp/admin/update-capability` (PUBLIC). A separate root systemd runner (torii-suite) consumes the request and resolves the latest tag itself. (3) Client UI: the Version box shows Installed/Latest/status and, for the logged-in admin, an "Update Now" button (auto-update installed → sign intent → POST → poll status → hard reload on success) or a disabled button + copy-command fallback (auto-update not installed). New pure modules `src/engine/crypto/npub.js` (npub→hex) + `src/engine/update/adminUpdateClient.js` + `server/auth/adminUpdate.js`. `PROTOCOL_VERSION=1`.

### COMBAT-FEEL — Augustink shrink + hitbox forgiveness (v0.2.386-alpha) — `BOSS_TARGET_HEIGHT` 2.5→2.0m (render-only, boss combat stats unchanged); bot hit capsule widened ~+15% in parity across `server/bots/botColliders.js` + `src/engine/physics/bodies.js` (`BOT_BODY_RADIUS` 0.26→0.30, `BOT_HEAD_RADIUS` 0.20→0.23; centres 0.76/1.55 unchanged, foot stays planted) so shots that visually hit the model's arms/shoulders now register. No damage/HP/zone/parity-lock/lag-comp changes; `PROTOCOL_VERSION=1`.

### COMBAT — bot lag-compensation for player→bot shots (v0.2.385-alpha) — server now rewinds each bot to the shot ts before ray-testing, mirroring the peer lag-comp path, so shots landed on the ~100ms-old rendered (moving) bot register. New pure `server/bots/botSnapshotRing.js` (records `{ts, bots:[{id,x,z,footY,radius,alive}]}` at the sim tick); `arenaBotSim.resolvePlayerShot` accepts the shot ts and rewinds (clamped to the 300ms window, boss collider scaling preserved); `arena-ws.js` passes the same shot ts the peer path uses. No damage/HP/zone/parity changes; `PROTOCOL_VERSION=1`.

### UI / LEADERBOARD / STATS TRUTH PASS (v0.2.384-alpha) — boss render-scale ~2.5m; leaderboard title de-mocked + honest empty state; LOCAL board = every player on this server instance incl. disconnected (ledger `retire()` + reconnect-rekey + `snapshot(32)` cap); homescreen preview + personal stats wired to the authoritative SCORE frame (`EV.SCORE_FRAME`); LOGIN button solid mint CTA; homescreen bolt logo → torii-gate SVG (both places). UI + data-wiring only; combat path untouched.

### COMBAT FIX — player→bot damage now registers (v0.2.383-alpha) — client stale-snapshot clobber fixed; the REAL fix for the v0.2.382-diagnosed miss

> **COMBAT FIX — 2026-07-14 (v0.2.383-alpha):** The REAL fix for player→bot "shots not registering / bots not dying". Server was resolving hits correctly (v0.2.382 diagnostic confirmed `botHit=3@t=4.95 dy≈1.67`). Bug was CLIENT-SIDE in `src/bots.js` `_syncNetBot`: `applyBotHit`/`applyBotKill` set `bot.state.hp`/`alive` immediately, but did NOT update `botNetState`'s `b.hp`/`b.alive` (owned by BOT_STATE snapshots), so the next frame re-sampled the STALE ~67ms-old snapshot and clobbered them (`st.hp = pose.hp` → HP back to full; `st.alive = true` → un-kills). hp/alive are latest-snapshot (not interpolated), so it was a pure event-vs-snapshot staleness race. Fix (Option A): new `botNetState.applyHit(id,hp)`/`applyKill(id)` fold the event into `b.hp`/`b.alive` (kill snaps) so `sample()` is event-authoritative — no clobber; position stays interpolated. Respawn (alive false→true) still snaps in; single-player (`_syncBot`), bot→player, boss unchanged; `[SHOT-RESOLVE]` diagnostic retained. New race tests in `tests/multiplayer/bot-net-state.test.js`; 7 v0.2.382 combat tests stay green. `PROTOCOL_VERSION=1`.

### COMBAT — player→bot damage diagnostic (v0.2.382-alpha) — DIAGNOSTIC-ONLY; reported miss not reproducible headlessly, awaiting live logs

> **COMBAT DIAGNOSTIC — 2026-07-14 (v0.2.382-alpha):** Investigated a reported one-directional break (player→bot shots not registering while bot→player works). Traced the full vertical coordinate chain and proved headlessly (`resolvePlayerShot` HITS torso+head for regular AND boss across shooter positions) that the peer capsule footY (`pos.y−EYE 1.7`), the bot collider footY (`sampleArenaHeight`), and the client's visual bot foot all resolve to the SAME world-Y, and the `MSG.SHOT` wire codec is y-pass-through — so the spec's hypothesised ~0.8m frame mismatch does NOT reproduce. Per the spec decision gate, shipped NO speculative fix; enriched the ≤1/sec `[SHOT-RESOLVE]` log in `server/arena-ws.js` with `originY`/`nearBot`/`botFootY`/`dy` (nearest-bot foot via new `arenaBotSim.nearestBotDiag`) so the maintainer can grep `journalctl -u torii-arena-ws` while shooting bots, plus regression tests `tests/multiplayer/player-bot-combat.test.js`. `PROTOCOL_VERSION=1`, single-player byte-identical. **NEXT:** collect a live `[SHOT-RESOLVE]` line while shooting a bot and report `dy` back to pin the real frame delta.

### BOSS — Augustink server-authoritative BOSS bot (v0.2.381-alpha) — per-bot stat archetype (kind/name/HP~60/slow/high-damage/bigger capsule) on the existing server-authoritative bot roster; 1 boss/arena, own GLB + nameplate, synced to all players, single-player byte-identical except the boss is present

> **AUGUSTINK BOSS — 2026-07-14 (v0.2.381-alpha):** Brought back Augustink as a BOSS bot layered on the v0.2.377 server-authoritative bot roster (NOT a new AI). New per-bot stat profile stamped at spawn (`kind` 'regular'|'boss', name, hp/maxHp, speed, damage, radius, shootCdBase) in the pure brain `src/engine/entities/botSim.js`; `BOSS_*` constants in `src/config.js` (COUNT 1, HP 60, SPEED 1.0, DAMAGE 14, RADIUS 0.8, SHOOT_CD 3.5, NAME 'Augustink', TARGET_HEIGHT 3.2). `spawnAll` spawns `(count−BOSS_COUNT)` regulars + BOSS_COUNT boss at the last indices; with BOSS_COUNT=0 the roster is byte-identical. Server (`server/bots/arenaBotSim.js`) spawns the boss, resolves per-bot HP/damage, grows the hit capsule via `buildBotColliders(...,scale)`, and stamps additive `kind`/`name`/`scale` on the boss BOT_STATE row only (regular rows unchanged, `PROTOCOL_VERSION=1`); `server/arena-ws.js` carries the shooter's per-bot damage on bot→player fire. Client interpolator `botNetState.js` threads kind/name/scale (defaults regular/1×). Render: `src/botModel.js` gains a lazy-loaded (NOT precached) `augustink4.glb` boss template auto-scaled to ~3.2m with an "Augustink" nameplate + full-rate death anim; `src/bots.js` attaches the boss model kind-aware in MP + single-player. New tests: `tests/multiplayer/boss-bot.test.js`.

### LEADERBOARD — Live in-arena leaderboard wired end-to-end (v0.2.380-alpha) — server live SCORE broadcast + toggleable overlay (L/Tab) with LOCAL live tallies + GLOBAL relay read + opt-in publish

> **LIVE IN-ARENA LEADERBOARD — 2026-07-14 (v0.2.380-alpha):** Wired the existing (tested/pure) leaderboard scaffolding live end-to-end. (1) SERVER — `server/arena-ws.js` now broadcasts the authoritative `SCORE` frame DURING play (immediately after each player-kill `addKill`, plus a ~5s `SCORE_TICK_MS` `setInterval` gated on `SCORE_ENABLED`), not only on session close; `broadcastScoreFrame()` no-ops on empty tallies. (2) CLIENT — new pure `src/engine/multiplayer/arenaLeaderboard.js` (`talliesToCurrentEvents` + `createArenaLeaderboard`, all impure edges injected — document/fetchGlobal/onPublish/canPublish) mounted as a toggleable in-arena overlay bound to L / Tab in `src/arenaRuntime.js`; `mp_score` frames feed the LOCAL tab as the DEFAULT view (0 prompts, session-scoped, works with NO signer). (3) PUBLISH — the overlay footer proxies the existing `#leaderboard-publish-btn` (`_publishMyScore`) so the opt-in NIP-07 publish is one click, never auto. (4) GLOBAL tab reads signed score events back via `leaderboardRelayRead` + `fanoutReq` (read-only, 0 prompts, graceful empty/offline). Additive on `PROTOCOL_VERSION=1`. New tests: `tests/multiplayer/arena-leaderboard.test.js` + `tests/multiplayer/live-score-broadcast.test.js`.

### PERF — DPR cap + adaptive quality tier + bloom gate + perf HUD (v0.2.379-alpha) — GPU fill-rate reduction, gameplay byte-identical

> **PERFORMANCE BUILD — 2026-07-14 (v0.2.379-alpha):** GPU-side lag reduction for two-browser playtests + weaker hardware (diagnosis: GPU fill-rate, not network/server). (1) Main renderer DPR cap lowered 2→1.5 in `src/scene.js` (matches the mirror cap). (2) NEW `src/engine/render/qualityTier.js` — pure, no-per-frame-alloc rolling frame-time monitor (60-frame ring) with HIGH (DPR 1.5, bloom on) / NORMAL (1.25, on) / LOW (1.0, bloom off) tiers; steps DOWN when avg FPS <45 sustained ~2s, UP when >55 sustained ~3s (hysteresis + stall guard); on change re-applies `setPixelRatio`/`setSize`/`composer.setSize` + toggles `bloomPass.enabled`. (3) Bloom gate — LOW disables the UnrealBloom pass. (4) NEW `src/engine/render/perfHud.js` — debug DOM overlay behind `window.__toriiPerf` (throttled ~250ms; FPS/frame-ms/draws/tris/DPR/tier/bot+peer counts; zero cost when off). Wired into `arenaRuntime.update()` (tier before `renderFrame`, `renderer.info` + HUD after). Single-player + MP identical; gameplay byte-identical; `PROTOCOL_VERSION` stays 1. New `tests/render/qualityTier.test.js` + `perfHud.test.js`.

### HOTFIX — Combat hits + bot targeting (v0.2.378-alpha) — force-authoritative MP, world-space bot shots, target hysteresis

> **COMBAT/TARGETING HOTFIX — 2026-07-13 (v0.2.378-alpha):** Three field-reported breakages after the v0.2.377 deploy. (1) `MP_MODE` could be a stale `advisory` in the live `.env`, so the SHOT case skipped `resolveAndBroadcast` and NO player→peer or player→bot damage resolved. `advisory` is retired (the client dropped inbound HIT in v0.2.374); `server/arena-ws.js` now FORCES `MP_MODE='authoritative'` in code (env read only to warn), and `resolveAndBroadcast` runs unconditionally on every SHOT. (2) Bot shot origin used a raw sim-local `y=EYE_Y≈0.9` as an ABSOLUTE world height, so on the raised island the bot→player ray started below the floor and missed. The pure `shotCallback` now takes a 3rd `target` arg (player world-eye); both callers (`src/bots.js`, `server/bots/arenaBotSim.js`) lift the origin to `footY+EYE_Y` and re-aim `normalize(target−worldOrigin)`. (3) Bot targeting was sticky — it wouldn't drop a far target for one right next to it. `botSim` now tracks a per-bot target by stable key and switches only when a rival is very close (≤8m and nearer) or materially nearer (<70% of the held distance); single-player (id-less, one player) never switches → byte-identical. Plus a rate-limited `[SHOT-RESOLVE]` diagnostic (~1/s/shooter) on the resolve pipeline. `PROTOCOL_VERSION` stays 1.

### BOT-MILESTONE-2 — Server-authoritative bots (v0.2.377-alpha) — bots now sync across all players in MP

> **SERVER-AUTHORITATIVE BOTS — 2026-07-13 (v0.2.377-alpha):** Chunk 2 makes the extracted bot brain server-authoritative. `botSim.tick(dt, players[])` now accepts the live player roster with per-bot nearest-eligible (in-fence, non-NAP) target selection; single-player passes a 1-element array so behavior is byte-identical. New pure server modules under `server/bots/`: `headlessLos.js` (2D segment-vs-AABB LOS at eye height over CRATES+OBSTACLES — NO Rapier; terrain LOS deferred), `botColliders.js` (ray-vs-bot capsule/head), `arenaBotSim.js` (wraps `createBotSim` with headless LOS + terrain deps). `server/arena-ws.js` runs the sim on a fixed ~20Hz tick against authed sessions, broadcasts `BOT_STATE` (~15Hz throttled) + immediate `BOT_SHOT`/`BOT_HIT`/`BOT_KILL`, resolves player→bot (nearest hit across peers AND bots) and bot→player damage (reuses the peer HIT/KILL path with a synthetic `bot` shooter id). Late-join/reconnect gets a full bot snapshot on finishAuth. Clients are RENDER-ONLY in MP: `src/engine/entities/botNetState.js` buffers+interpolates (SNAP on spawn/kill/respawn), `src/bots.js` net mode stops the local AI + ignores local damage, `arenaRuntime.js` wires the `mp_bot*` events. Additive on PROTOCOL_VERSION=1. Chunk 2 of the bot milestone.

### BOT-MILESTONE-1 — Extract the pure headless bot AI (v0.2.376-alpha) — source-only refactor, no behavior change

> **PURE BOT AI EXTRACTION — 2026-07-13 (v0.2.376-alpha):** The single-player bot AI/sim was tangled into `src/bots.js` alongside THREE, the BotModel GLB, audio, Rapier, `raycastService` and LOD. Extracted the brain into a new PURE module `src/engine/entities/botSim.js` (`createBotSim(deps)` → `{ bots, spawnAll, tick, hitBot, killBot, revive }`) with ZERO render/audio/physics imports — everything it needs (line-of-sight, ground height, coastline clamp/containment, the precomputed arena boxes + cover points, config, the spawn-reject disc, and a `shotCallback`) is injected. `src/bots.js` is now a thin wrapper: each `bots[]` entry pairs a BotModel + Rapier colliders with a reference to its pure sim `state`, mirrors `alive`/`hp`/`pos` for the combat raycast, and applies the kill side-effects (`state.kills++`, sats, `emit`) the brain deliberately does NOT own. `initBots`/`tickBots`/`hitBot` keep their exact signatures; `arenaRuntime.js` untouched. New `tests/multiplayer/bot-sim.test.js` (16) exercises the brain in plain node with injected fakes; all existing bot tests stay green. Chunk 1 of the bot milestone.

### MP-AUTH-1 — Session tokens: "1 sign at login, 0 signs in-game" (v0.2.377-alpha) — awaiting Suite redeploy (bump TORII_QUEST_REF → v0.2.379-alpha) + real-browser retest

> **SESSION-TOKEN AUTH — 2026-07-13 (v0.2.375-alpha):** Arena auth used a per-session NIP-42 (kind:22242) challenge, which is anti-replay so it CANNOT be cached — every arena entry and every reconnect re-prompted the NIP-07 signer. Combined with the score-publish sign and the login presence-publish sign, players saw ~5 signer prompts. Replaced with a one-time login sign that mints a server-issued opaque session token. **SERVER:** new pure `server/auth/sessionTokens.js` (challenge issue/verify one-time-use + TTL 60s; token issue/verify TTL 8h; stores ONLY sha256(token), never the raw token) + two plain-HTTP endpoints in `server/arena-ws.js` (`GET /mp/auth-challenge`, `POST /mp/session` — verifies a NIP-98 kind:27235 login event via the shared `verifyNostrEventSig`, returns `{token, npub}`); a new `finishAuth()` unifies the WELCOME/JOIN path for both the AUTH_TOKEN and NIP-42 branches. **CLIENT:** new `src/engine/multiplayer/sessionAuth.js` (HTTP-base derived from the same MP_WS_PATH mount as the WS URL — never a hard-coded `/mp`; sessionStorage token store; one-sign login flow); `src/nostr.js` login signs once then falls back to `getPublicKey()`; `wsClient.js` sends the additive `AUTH_TOKEN` frame when a token is stored and only calls `signAuth` (NIP-42) as a fallback; a rejected token (AUTH_FAIL) self-clears and reconnects onto the NIP-42 path. **PRESENCE:** dropped the auto presence-publish on `NOSTR_LOGIN` (presence is WS-roster only now). In-game score signs were already zero (`createScoreReporter` is never invoked; the manual "PUBLISH MY SCORE" button is user-initiated and left intact). `PROTOCOL_VERSION` stays 1 (AUTH_TOKEN is additive; old clients drop it). **Source-only — LIVE is unaffected until a Suite redeploy bumps `TORII_QUEST_REF` to v0.2.375-alpha; do NOT touch the VPS.** Not closed until a real-browser retest shows exactly one signer prompt at login and zero on arena entry/reconnect/death.

### QA-MP-COMBAT-1 — Peer combat wired client-side (v0.2.374-alpha): players can now damage + kill each other (server-authoritative); awaiting Suite redeploy + real-browser two-npub retest

> **PEER-COMBAT WIRING — 2026-07-13 (v0.2.374-alpha):** The server had server-authoritative combat (`MP_MODE='authoritative'`: `SHOT` → `resolveAndBroadcast` → `HIT`/`KILL`) but the client never sent `SHOT` and never handled inbound `HIT`/`KILL`, so players could not hurt each other. Fixed by wiring two seams in `src/arenaRuntime.js`, both delegating to a new pure `src/engine/multiplayer/peerCombat.js` (kept testable, out of the three-heavy runtime). **OUTBOUND** — the `EV.SHOOT` handler now sends `_mp.sendShot({origin,dir,ts})` for every arena shot when `_mp.selfId` is set and the player is inside the arena (`shouldSendShot` gate); it sends the AIM ray (`aimOrigin`/`aimDir`, camera-through-crosshair) so server hit-detection matches what the shooter saw, falling back to the muzzle `origin`/`dir` (`buildShotPayload`). The server no-ops when the shot hits no peer. **INBOUND** — `_mpEmit` delegates `mp_shot`/`mp_hit`/`mp_kill` to `createPeerCombat`: `mp_shot` is VISUAL ONLY (muzzle spark + short tracer at the peer origin via `fx.js`; skips our own echo; NO local hit detection), `mp_hit` applies `takeDamage(dmg)` only when `targetId === selfId` and flashes the crosshair when we are the shooter (wire field `id`), `mp_kill` runs the transition-guarded `killPlayer()` when `victimId === selfId` (increments `deaths` once — no double-count vs `takeDamage`) and scores `state.kills++` + a killfeed line when `shooterId === selfId`. Local bot hits remain a SEPARATE client-side path — a shot may both hit a bot locally AND resolve a peer hit server-side; that double path is expected (not deduped). NO wire-protocol change. Verified: check green, build green, tests green; new `tests/multiplayer/peer-combat.test.js` covers the outbound gate/payload + inbound gating. Bots stay client-side (a separate MP milestone; NOT bundled). **Source-only — LIVE v0.2.370-alpha has no peer combat until a Suite redeploy that includes v0.2.374 (bundles v0.2.371 DRACOLoader + v0.2.372 keepalive + v0.2.373 avatar transform + this); the maintainer must bump the Suite Quest ref before a redeploy (`--base=/quest/`) picks it up; do NOT touch the VPS while the Continuum session is active.** Not closed until a real-browser two-npub retest shows one player damaging + killing the other with correct HUD/killfeed/respawn.

### QA-MP-AVATAR-1 — Peer avatars render high in the sky, backwards, and T-posed — root cause FOUND+FIXED v0.2.373-alpha (peer avatarLoader returned the raw `gltf.scene` — no feet offset, no π facing, no AnimationMixer; awaiting Suite redeploy + real-browser retest)

> **PEER-AVATAR TRANSFORM UPDATE — 2026-07-13 (v0.2.373-alpha):** After the v0.2.371 DRACOLoader fix peers became visible, but rendered ~4-5 body-heights UP, FACING 180° away, and in a static T-POSE (bind pose). Root cause: the peer `avatarLoader` in `src/arenaRuntime.js` (~line 416-431) resolved the RAW `gltf.scene` with none of the setup `playerModel.js`/`botModel.js` apply. (1) HEIGHT — chiefmonkey6.glb geometry sits above its origin (large `gMinY`); `playerModel.js:111-112` corrects with `position.y = -gMinY - 1.7`, but the peer loader skipped it, so the model floated at its authored height (and `sendMove`, `arenaRuntime.js:235-237`, broadcasts eye-height Y ≈ 1.7). (2) FACING — the GLB is authored facing +Z, game-forward is −Z; `playerModel.js:115` sets `rotation.y = Math.PI`, skipped here → 180° backwards. (3) ANIMATION — no `THREE.AnimationMixer` (cf. `playerModel.js:163-174`, `botModel.js:100-113`) → bind-pose T-pose; chiefmonkey6 IDLE clip is `Idle_03`. **FIXED in v0.2.373-alpha** (mirror `botModel.js` template cache + `playerModel.js` transform): load chiefmonkey6.glb ONCE into a module-level template (scene + clips + geometry `gMinY`), then per peer `SkeletonUtils.clone` into a wrapper `THREE.Group` with `model.position.y = -gMinY - 1.7`, `model.rotation.y = Math.PI`, opaque materials + `frustumCulled=false`, and an `AnimationMixer` playing `Idle_03` (looped; falls back to `animations[0]` + `console.warn`) ticked once to leave bind-pose. The wrapper exposes `obj.update(dt) → mixer.update(dt)`; `remoteAvatars.js` `tick(renderTime)` now derives a clamped wall-clock `dt` (skips first tick) and calls `entry.obj.update(dt)` per frame. NO movement-based clip switching (IDLE only; WALK-on-move noted MP-1.5); NO `sendMove`/wire change. Verified: check green, build green, tests green; new `remote-avatars.test.js` cases. **Source-only — LIVE v0.2.370-alpha keeps peers mis-transformed until a Suite redeploy that includes v0.2.373 (bundles v0.2.371 DRACOLoader + v0.2.372 keepalive + this fix); the maintainer must bump the Suite Quest ref to v0.2.373-alpha (or the merge commit) before a redeploy (`--base=/quest/`) will include it; do NOT touch the VPS while the Continuum session is active.** Not closed until a real-browser two-npub retest shows both avatars on the ground, facing correctly, and idling.

### QA-MP-AUTHLOOP-1 — Repeating NIP-07 signer prompt on the pause/idle screen — root cause FOUND+FIXED v0.2.372-alpha (no client keepalive → 60s server idle-drop → reconnect + re-auth loop; awaiting Suite redeploy + real-browser retest)

> **PAUSE-SCREEN AUTH-LOOP UPDATE — 2026-07-13 (v0.2.372-alpha):** On the pause/idle screen the client sends no MOVE frames, so its WS connection goes completely silent. The server idle-drops silent sessions after 60s (`IDLE_DISCONNECT_MS = 60_000` in `server/arena-ws.js`) and never initiates its own PING, while `src/engine/multiplayer/wsClient.js` only sent a PONG in *reply* to a server PING — which never arrives. So a paused session was dropped at ~60s → `ws.onclose` → `_scheduleReconnect` → reconnect → fresh `HELLO` + NIP-42 challenge → `signAuth` → **another NIP-07 signer prompt**, looping every ~60s while paused. **FIXED in v0.2.372-alpha** (client-side, minimal): `wsClient.js` now sends a keepalive `{ t: MSG.PING, ts: now() }` every 25s (`KEEPALIVE_MS`) while CONNECTED — `_startKeepalive()` fires in the WELCOME handler and `_stopKeepalive()` clears it in `disconnect()` and `onclose`, via the injectable `setTimeoutFn`/`clearTimeoutFn` seams (recursive setTimeout, single-instance guard). NO server change and NO session-token/auth-caching scheme (NIP-42 challenges are per-session + anti-replay, `arena-ws.js:206`); a *genuine* network drop still reconnects + re-auths — a code comment records the future hardening (server-issued session token). `wsClient.js` already sits on the `tools/regression-check.mjs` [3] setTimeout allowlist. New keepalive unit tests in `tests/multiplayer/ws-client-state.test.js`. Verified: check green, build green, tests green. **Source-only — LIVE v0.2.370-alpha keeps looping until a Suite redeploy that includes v0.2.372 (bundles the v0.2.371 DRACOLoader peer-visibility fix + this keepalive fix); the maintainer must bump the Suite Quest ref to v0.2.372-alpha (or the merge commit) before a redeploy (Suite builds `--base=/quest/`) will include it; do NOT touch the VPS while the Continuum session is active.** Not closed until a real-browser retest on the redeployed build shows the pause screen holding a session past 60s with no re-prompt.

### QA-MP-BLOCKER-1 / H4 — Two players can’t see each other in the live arena — root cause FOUND+FIXED v0.2.371-alpha (peer-avatar GLTFLoader was missing a DRACOLoader; awaiting Suite redeploy + real-browser retest)

> **PEER-VISIBILITY UPDATE — 2026-07-13 (v0.2.371-alpha):** With the v0.2.370 base-aware fix DEPLOYED, anonymous ENTER ARENA now BOOTS on chiefmonkey.art/quest/ (CSP freeze cleared), but **two players still cannot see each other** in the arena. Root cause: `src/arenaRuntime.js` (~line 397, MP wiring seam) created the peer-avatar `GLTFLoader` (`_mpGltf`) WITHOUT a `DRACOLoader` — the ONLY loader touching `chiefmonkey6.glb` that lacked one (`playerModel.js:84-87`, `botModel.js:29-31`, `arena.js:178-181`, `firstPersonBody.js:35-38` all set one). `chiefmonkey6.glb` is Draco-compressed, so `_mpGltf.load(assetUrl('/chiefmonkey6.glb'))` REJECTED (Draco mesh can't decode) for every peer → `remoteAvatars.upsert()` caught the reject and DELETED the roster entry (`remoteAvatars.js:73-77`) → peer avatars never rendered. Wire-level peer discovery already works (`peerJoin`→`roster.upsert` at `multiplayerHost.js:138`; verified at protocol level below), so this was purely the loader. **FIXED in v0.2.371-alpha** (minimal, mirroring `playerModel.js:84-87`): import `DRACOLoader` alongside `GLTFLoader`, then `const _mpDraco = new DRACOLoader(); _mpDraco.setDecoderPath(assetUrl('/draco/')); const _mpGltf = new GLTFLoader(); _mpGltf.setDRACOLoader(_mpDraco);`. Also added `console.warn('[mp] avatar_load_error', peer?.id, err)` in the load-error handler so future GLB failures are visible; NO capsule fallback (kept targeted). No wire/protocol/gameplay/physics/CSP/Nostr change. Verified: check green, build green, tests green. **Source-only — LIVE v0.2.370-alpha stays peer-invisible until a Suite redeploy that includes v0.2.371; the maintainer must bump the Suite Quest ref to v0.2.371-alpha (or the merge commit) before a redeploy (Suite builds `--base=/quest/`) will include it; do NOT touch the VPS while the Continuum session is active.** H4 NOT closed until a real-browser two-npub retest on the redeployed build shows both avatars in-world.

### QA-ARENA-FREEZE-1 — ENTER ARENA froze the session under the `/quest/` mount — root cause FOUND+FIXED v0.2.370-alpha (base-aware pinned-entry import; awaiting Suite redeploy + real-browser retest)

> **REAL-BROWSER UPDATE — 2026-07-13 (v0.2.370-alpha):** On chiefmonkey.art/quest/ (v0.2.369-alpha, Brave + Firefox, two authenticated npubs) the pre-auth menu stayed responsive but clicking **ENTER ARENA froze the session** before any scene/canvas/frame — the arena never booted (Rapier ruled out: a direct import/init of the built Rapier module succeeds). Root cause: the vite CSP plugin (`vite.config.js`) emitted the pinned-entry URL **root-relative** (`/assets/torii-entry.js?v=<stamp>`) for BOTH the inline bootstrap `import()` and every chunk's back-reference import of the entry; under `--base=/quest/` those 404 at host root, and because `arenaRuntime.js` statically imports the entry, ENTER ARENA's `import('./arenaRuntime.js')` graph load REJECTED → arena never started. **FIXED in v0.2.370-alpha**: the plugin now captures the resolved deploy base (`configResolved`) and emits `${base}assets/torii-entry.js?v=<stamp>` (`/quest/…` on the Suite, `/…` at root) via one `entryUrl(base)` helper, applied to the inline import, the base-agnostic removal regexes, and the chunk rewrite; `tools/regression-check.mjs` [16] base-agnostic; NEW deterministic regression `tests/quest-base-entry.test.js` does a real `--base=/quest/` build and asserts every `torii-entry.js?v=` URL carries the `/quest/` base, is byte-identical, and no static entry `<script>` survives. Verified: `--base=/quest/` build emits `/quest/assets/torii-entry.js?v=<stamp>` everywhere; puppeteer ENTER-ARENA repro on a clean `/quest/` server boots the arena (frames advance, no "Failed to fetch dynamically imported module"); 2269/2269 tests, checks green. **Source-only fix — LIVE v0.2.366-alpha stays broken until a Suite redeploy that includes v0.2.370; the Suite installer is pinned at `TORII_QUEST_REF=v0.2.367-alpha`, so the maintainer must first bump the Suite Quest ref to v0.2.370-alpha (or the merge commit) before a redeploy (Suite builds `--base=/quest/`) will include it; do NOT touch the VPS while the Continuum session is active.** The v0.2.369 assetUrl fix is untouched. Non-causal follow-ups explicitly NOT fixed here: the pre-arena root `/sw.js` 404 and the GitHub releases/latest 404 — neither blocks the arena boot.

### QA-MP-BLOCKER-1 — Two peers can’t see each other in live arena — peer discovery VERIFIED at protocol level 2026-07-13; root-relative asset-path root cause FOUND+FIXED v0.2.369-alpha (graphics + peer-avatar visibility; awaiting Suite redeploy + real-browser retest)

> **REAL-BROWSER UPDATE — 2026-07-13 (v0.2.369-alpha):** A two-npub real-browser test (Firefox + Brave) on chiefmonkey.art/quest/ found the arena loads and peers auth/join, but **no `.glb` models load** (bots = capsule pills) and **peer avatars never appear** (players can’t see one another) — the peer-avatar `avatarLoader` has no fallback on GLB failure. Root cause: all `GLTFLoader.load('/foo.glb')`, `setDecoderPath('/draco/')`, `TextureLoader.load('/bitcoin-b.png')` in `src/` were **root-relative**; under the `/quest/` mount they resolve to host root and 404 (confirmed: root 404, `/quest/` 200). **FIXED in v0.2.369-alpha**: new `src/assetUrl.js` helper (`${import.meta.env.BASE_URL}${name}`, mirroring `audio.js`) applied across 8 source files; `tools/regression-check.mjs` [16] hardened to require `assetUrl('/draco/')` + FAIL on any bare root-relative `.load('/...glb|png|…')`. Verified: `--base=/quest/` build inlines `/quest/` + passes GLB args to helper; 2264/2264 tests, 20/20 checks. **Source-only fix — LIVE v0.2.366-alpha stays broken until a Suite redeploy that includes v0.2.369; the Suite installer is pinned at `TORII_QUEST_REF=v0.2.367-alpha`, so the maintainer must first bump the Suite Quest ref to v0.2.369-alpha (or the merge commit) before a redeploy (Suite builds `--base=/quest/`) will include it; do NOT touch the VPS while the Continuum session is active).** H4 NOT closed until a real-browser retest on the redeployed build shows both avatars in-world. SW follow-up (non-blocking): `public/sw.js` registration + precache are root-relative and don’t register under `/quest/`; game loads via network regardless.

> **TEST RESULT — 2026-07-13 (Computer session): peer discovery verified at the protocol level on chiefmonkey.art/quest/.** A simulated two-npub live test against the production install confirmed the wire-level handshake and cross-client peer visibility:
> - Two isolated browser contexts, each performing a real NIP-07 nostr-login with a distinct burner npub (A: `npub1prenm3latj270…`, pubkey `08f33dc7fd…`; B: `npub15m4vrrexahn…`, pubkey `a6eac18f26…`), each opened `wss://chiefmonkey.art/mp`.
> - Both received `HELLO` (`serverVersion: v0.2.366-alpha`, `protocolVersion: 1`), sent `AUTH` (NIP-01 kind-22242, BIP-340 schnorr, challenge-bound via `window.nostr.signEvent`), and were accepted by the server (WELCOME returned).
> - **B's `WELCOME.roster` contained A** → peer B sees the already-present peer A on join.
> - **A received a `JOIN` frame announcing B** (`{id, npub, pos:[0,0,0], rot, character}`) → cross-client peer visibility confirmed (server fan-out works).
> - **Confirmed working:** auth gate, roster fan-out, JOIN broadcast on the live install.
> - **NOT confirmed in this test:** the full in-world arena render + roster visualisation via the app's normal `ENTER ARENA` → `boot()` path. Under headless software-WebGL (swiftshader) the arena's render loop starves the async NIP-07 callback, so `signEvent` does not resolve before the 10s `AUTH_TIMEOUT_MS` when driven through the full 3D flow; the test therefore drove the AUTH handshake directly in-page (same `wss://chiefmonkey.art/mp`, same `window.nostr` shim) to isolate the protocol path. This is a **headless test-environment limitation, not proven to be free of a product-side rendering/roster-visualisation issue (H4)** — re-verify the full ENTER→in-world path on real GPU hardware before closing H4.
> - H2 (MP not eagerly loaded) and H3 (silent AUTH rejection) are not implicated by this test (both peers authed and received WELCOME/JOIN), but were not directly exercised through the ENTER button either.
> - The original pplx.app symptom was solely the broken pplx.app backend sandbox (still 503), now bypassed by the SHC VPS install at chiefmonkey.art.

> **STATUS UPDATE — 2026-07-11 09:13 BST (Computer session):** Root cause is **NOT** a client-side bug (H1–H4). The published site's **backend sandbox is down** while the static frontend is healthy — that split is what produces the silent-WS symptom.
> - **Frontend live & healthy:** `GET /` → HTTP 200, version string reads `v0.2.366-alpha` (static S3).
> - **Backend DOWN:** `GET /port/5000/` → HTTP 503 then full 12s timeout (was 503 on 2026-07-10, worsened to timeout by 2026-07-11 09:09). Arena WS server process is not running.
> - **WS symptom reproduced & explained:** WS upgrade OPENs at the proxy layer on both `/port/5000/mp` and `/mp` (HTTP 101) but **zero bytes ever arrive** — no HELLO, no close, 15s silence. This is the signature of a dead backend behind a live proxy, NOT a client URL/auth/roster failure.
> - **Server code is healthy:** `node dist/server/arena-ws.cjs` boots locally and sends `HELLO` in 9ms: `{"t":"HELLO","challenge":"…","serverVersion":"v0.2.366-alpha","protocolVersion":1}`. No crash-on-startup.
> - **H1 ELIMINATED:** the live `arenaRuntime-DsJt_cka.js` split chunk has the `__PORT_5000__` sentinel **correctly rewritten** to `port/5000` (0× `__PORT_5000__`, 1× `port/5000`). The S3-upload rewrite DOES reach code-split chunks. `resolveUrl()` runtime detection (`!PORT_SENTINEL.startsWith('__')`) works as designed; the client correctly dials `wss://quest-torii.pplx.app/port/5000/mp`.
> - **Fix is trivial once infra recovers:** a single `deploy_website` + `publish_website` (site_id `93507979-679f-4aac-949d-20a4a33d7352`, port 5000, `node server/arena-ws.cjs`) brings the backend up. Re-smoke expects `HELLO` with `serverVersion: v0.2.366-alpha`. The two-npub test should then pass since the arena server will actually be running.
> - **Blocker is the pplx-tool bridge:** every `pplx-tool` call returns `503 "tool bridge is not connected"` (describe ×3 + actual `deploy_website`). Diagnostic `9a174646-325a-4e00-a21b-f71fcb8dc4c1`. Down ~32h+ since 2026-07-10 ~00:30 BST. A fresh Computer session did NOT get a fresh bridge worker — this is a platform-side outage, not a per-session stuck worker. Maintainer to file Perplexity support; do not burn credits retrying.

**Reported:** Friday July 10, 2026 — 10:03 BST by maintainer.
**Repro:** Open `https://quest-torii.pplx.app` in two different browsers, sign in with two different npubs, both reach the arena. Neither peer sees the other. No hits register.
**Live version at repro time:** v0.2.365-alpha (MP-1.5 shipped, MP-3 not yet published due to pplx-tool bridge outage).

**Investigation so far (in-thread, before session handover):**
1. Live WS handshake works — `wss://quest-torii.pplx.app/port/5000/mp` returns `{"t":"HELLO","challenge":"…","serverVersion":"v0.2.365-alpha","protocolVersion":1}`. Server is up.
2. Server logic looks correct in source (`server/arena-ws.js`): sends `WELCOME + roster` to new peer, broadcasts `JOIN` to others (lines 259–262).
3. `MP_ENABLED = true` in `src/config.js:88`.
4. Client URL construction lives in `src/engine/multiplayer/multiplayerHost.js:resolveUrl()` — uses `__PORT_5000__` sentinel that should be rewritten to `port/5000` at S3 upload.
5. **Suspicious finding (NOW EXPLAINED — not a bug):** the deployed main entry bundle at `https://quest-torii.pplx.app/assets/torii-entry.js` (131 KB) contains ZERO occurrences of `__PORT_5000__`, `PORT_SENTINEL`, `port/5000`, `MP_WS_PATH`, or `resolveUrl`. This is **expected and correct** — the sentinel and `resolveUrl()` live in the code-split `assets/arenaRuntime-DsJt_cka.js` chunk, NOT the entry bundle. Verified 2026-07-11: the live arenaRuntime chunk HAS the sentinel rewritten to `port/5000` (0× literal, 1× `port/5000`). See STATUS UPDATE above — H1 eliminated.

**Hypotheses to check (in the new session, AFTER publishing v0.2.366-alpha):**
- **H1: ELIMINATED 2026-07-11.** Sentinel IS rewritten in the arenaRuntime chunk (verified on live). `deploy_website`/`publish_website` DO rewrite code-split chunks, not just the entry bundle. `!PORT_SENTINEL.startsWith('__')` evaluates TRUE in production; client dials `wss://quest-torii.pplx.app/port/5000/mp` correctly. Not the cause.
- **H2:** MP code not eagerly loaded — `multiplayerHost.start()` may not fire on first sign-in for one of the browsers. Check `mp_disabled` / `mp_no_url` emits.
- **H3:** AUTH failure after HELLO — signAuth flow may reject the challenge silently. Server logs (arena-ws `log.info`) not visible from client; would need to add a client-side event listener on `mp_auth_error` and surface it in the HUD.
- **H4:** WELCOME arriving but roster not being applied to the scene — `roster.upsert` at `multiplayerHost.js` might not be visualising peers even when the wire is fine.

**Debug plan for new session (order):**
1. FIRST: publish v0.2.366-alpha (bridge should be fresh in new session). Bug may already be fixed by an MP-3 change; if not, MP-3's leaderboard tile will give a better peer-visibility signal.
2. Re-run the two-tab test on v0.2.366-alpha.
3. If still broken: fetch `https://quest-torii.pplx.app/assets/arenaRuntime-*.js` and grep for `__PORT_5000__` / `port/5000`. If sentinel is literal, that's H1 — fix is either (a) inline the sentinel in the entry bundle or (b) add a runtime fallback that dials `wss://<host>/port/5000/mp` when a same-origin `/mp` connection fails to WELCOME within 3s.
4. Open DevTools on both browsers, filter WS frames, confirm HELLO → AUTH → WELCOME sequence. If AUTH is silently dropping, add a HUD toast for `mp_auth_error`.
5. If AUTH is fine and WELCOME arrives with a non-empty roster: bug is in the roster → scene visualisation path (H4). Grep `arenaRuntime.js` for `roster.upsert` handling.

**Updated guidance (2026-07-11):** Root cause is the **down backend sandbox**, not H1–H4. Do NOT chase client-side URL/auth/roster fixes — they are verified working (sentinel rewritten, server sends HELLO locally). The ONLY fix needed is republishing so the backend comes up, which is blocked by the pplx-tool bridge outage (diagnostic `9a174646`). Once the bridge recovers: `deploy_website` + `publish_website` → re-smoke (expect `HELLO` serverVersion `v0.2.366-alpha`) → re-run two-npub test → expected PASS. H2/H3/H4 need only be revisited if the backend is up, HELLO flows, and peers STILL can't see each other.

---

**v0.2.366-alpha shipping MP-3** — Nostr score/leaderboard, Option A (client-signed). Server accumulates per-peer `{kills, deaths, damage}` in `server/combat/scoreLedger.js` (in-memory, ephemeral) and emits authoritative `SCORE{sessionId(16-hex), endedAt, tallies[]}` frames on peer disconnect to all authed peers (departing peer receives before close). Wire is additive on `PROTOCOL_VERSION=1`. Each client signs ONLY its own row via `window.nostr` (nip07) and publishes as `kind:30078#d=torii-quest` (current, replaceable) + `kind:1#t=torii-quest-score` (durable history) to configured relays. `src/engine/multiplayer/leaderboardAgg.js` aggregates lifetime tallies by dedupe (pubkey, sessionId); `src/ui/leaderboardPanel.js` renders top-20; dashboard tile shows top-5. LocalStorage dedupe key `tq.mp3.published:{sessionId}:{endedAt}`. Empty-row guard skips publish for scoreless matches. `SCORE_ENABLED=true` env default; server-side scoring not required in advisory rollback. MP-3.1 (WoT co-occurrence seed) queued as follow-up, not this shipment.

**v0.2.365-alpha shipped MP-1.5** — arena-ws bundled into `dist/server/arena-ws.cjs` (esbuild, `ws` external) and runs INSIDE the pplx.app published sandbox. `arena-ws.js` now binds `0.0.0.0:$PORT` (default 5000), attaches WSS to `/mp` on an HTTP server (with `/healthz` endpoint), and accepts URLs of shape `/mp` OR `/port/5000/mp` (proxy rewrite). Client `multiplayerHost.resolveUrl` uses the `__PORT_5000__` sentinel — `deploy_website` rewrites it to `port/5000` at S3 upload time so the browser dials `wss://quest-torii.pplx.app/port/5000/mp`. `MP_ENABLED = true` at build (per-instance runtime toggle preserved). VPS path (Caddy + systemd on `/opt/torii-arena-ws`, VPS_INSTALL.md §16) still works — env `PORT=8787 HOST=127.0.0.1` restores the pre-1.5 shape.

**v0.2.364-alpha shipped MP-2** — server-authoritative hit resolution on the same wire (`PROTOCOL_VERSION=1`, additive `RESPAWN` only). Server keeps snapshot rings per peer, rewinds up to `LAG_COMP_MS=300`, resolves ray-vs-capsule/head-sphere itself, applies damage from a parity-locked table (head=9, body=3), broadcasts `HIT`/`KILL` to ALL, and issues `RESPAWN` to the victim after `RESPAWN_MS=3000`. Client stops emitting `HIT`. One-flag rollback via `MP_MODE=advisory` in the systemd unit (see VPS_INSTALL.md §16.6). Same behaviour gate (`MP_ENABLED=false`) as MP-1. Next multiplayer slice is MP-3 (spec queued): richer respawn UX + kill-feed HUD + server-authoritative KILL attribution.

**v0.2.363-alpha shipped MP-1** — WebSocket multiplayer (advisory hit detection). Behind `MP_ENABLED = false`; flip in Instance Settings → Multiplayer once Caddy `/mp` + arena-ws systemd unit are up (VPS_INSTALL.md §16). Superseded by MP-2 above; MP-1 semantics remain reachable via `MP_MODE=advisory`.
Live site (primary): [chiefmonkey.art/quest/](https://chiefmonkey.art/quest/) (SHC VPS, Torii Suite install) · secondary (broken backend): [torii-quest.pplx.app](https://torii-quest.pplx.app)

## Deployment conventions — chiefmonkey.art (Torii Suite install)

Quest is live on `chiefmonkey.art` via the **Torii Suite** installer (`torii-suite` v0.7.0-alpha), NOT the ad-hoc Caddy bundle from earlier sessions. Future agents/deployment runs must inherit this state without manual re-discovery:

- **Frontend mount:** Quest is served at the path prefix **`/quest/`** (NOT the domain root). Root `/` currently 404s (no launcher/claim-gate mounted yet — deferred to the v0.8.0 plan). Hit `https://chiefmonkey.art/quest/` for the game.
- **Multiplayer WS:** the arena WebSocket is at the **root-level, same-origin `/mp`** path → `wss://chiefmonkey.art/mp`. There is no `/port/5000/mp` sentinel on the VPS path (that sentinel is pplx.app-sandbox-only). The client `multiplayerHost.resolveUrl()` resolves to `wss://chiefmonkey.art/mp` on the VPS origin.
- **Backend daemon:** `arena-ws` runs as the hardened systemd unit **`torii-arena-ws.service`** (`User=torii-quest`, bound to `127.0.0.1:8788`, `NoNewPrivileges`, `ProtectSystem=strict`). nginx proxies `location /mp` → `127.0.0.1:8788` with the WS upgrade.
- **nginx layout:** apps mount as path prefixes under one domain via fragments in `/opt/torii/nginx-fragments/<app>.conf`, included by the main torii.conf HTTPS `server{}` block. Quest static at `/quest/`, Quest `/mp` proxy in `quest-mp.conf`. **The shared parent `/opt/torii` stays `root:root 0755` (world-traversable)** so nginx www-data can traverse to siblings — the v0.2.30 fix for the permission regression that 404'd sibling apps. NEVER re-own or re-mode an existing parent.
- **Sibling apps on the same VPS:** Continuum at `/continuum/` (agent `torii-continuum-agent.service`, `127.0.0.1:8787`, API at `/api/`), Plebeian at `/plebeian/`. The Continuum session is actively managing onboarding on this shared host — do NOT touch the shared nginx/parent-dir config while that work is in flight.
- **Suite installer (source of truth for installs):** `curl -fsSL https://raw.githubusercontent.com/ChiefmonkeyArt/torii-suite/v0.7.0-alpha/bootstrap.sh | sudo bash`. Pinned refs: `TORII_QUEST_REF=v0.2.367-alpha`, `TORII_CONTINUUM_REF=v0.2.14-alpha`. Backups under `/var/backups/torii-<app>-<ts>`.
- **Cosmetic note:** the live `arena-ws` advertises `serverVersion: v0.2.366-alpha` in HELLO frames (the live build predates the v0.2.369-alpha source bump); the pinned Suite tag is v0.2.367-alpha. No functional impact.

Source of truth for Torii Quest tasks.

> **Doc structure (v0.2.352-alpha refresh):** the per-project convention is now `torii-quest-{strategy,todo,progress,handoff}.md`. Torii Quest uses `torii-continuum-*` (when written); Torii DE uses `torii-de-*` (when written). The old `todo.md` legacy shim was deleted; historical struck completed markers moved into `## Historical completed (archive)` at the end of this file. Tools have been updated to reference the new names (`docConsistency`, `build-continuum`, `mdPatch`, `regression-check`).

## Scope

Torii Quest is the game app.
Torii Quest is a separate dashboard app and must stay on `torii-continuum-todo.md`.
Nostr Arena is archival only and must not be used as the active queue for Quest.

This todo is reprioritised around the lean MVP only:
- Keep the freedom-tech loop clear and demoable.
- Keep shooter work maintenance-only unless a bug is demo-breaking.
- Keep BotAgent / runtime separation work only where it improves Quest vs Continuum boundaries or prevents instability.
- `NOSTR_ARENA_MASTER_TODO.md` is deleted; `torii-quest-todo.md` is a legacy tooling shim only. Do not use either as an active queue.
- Defer broader polish, most live-promotion work, and larger redesign passes to Milestone 2 and Milestone 3, while keeping security gates visible until the related live features are promoted.


## Milestone 1 — MVP Proof of Concept Torii Quest

Goal: ship the smallest clean proof-of-concept that demonstrates the Torii Quest loop.

### Current job status
- PAUSED: Oversight dashboard progress UX work
- State captured in HANDOFF / NEXT_ACTION_STATE.
- Replacement slices:
  - Finish the Continuum click-through mockup path for the MVP demo (1–3h)
  - Show clearer progress state in the oversight dashboard (1–3h)
  - Add heartbeat / partial-progress feedback for long-running bot work (1–3h)

### Active MVP tasks
- Keep the four MVP proof slices visible and coherent as one loop: Gateway, Product, Leaderboard, Update.
- Keep the title-screen / entry / in-world flow stable so the loop can be demoed end-to-end without silent failure.
- Keep the current safe gateway path intact: preview, intent, consent copy, dry-run plan, same-origin executor seam, and controlled host transport seam.
- Complete the move of the Torii jump gate / gateway experience into the far-right corner of the NAP zone (DONE v0.2.245–v0.2.250) and now **promote the gateway to the live n2n hop**: see `torii-quest-strategy.md` → "15-Hour Proof-of-Concept Route" item 2. Phased — P0 presence/who's-online (live relay read + publish our own world-presence event) → P1 signed travel-request/confirm handshake (NIP-07, SEC-2) → P2 cross-host jump carrying npub (SEC-3) → P3 two-instance interop proof. The placeholder second-zone idea is RETIRED in favour of the real cross-world hop described in `GATEWAY_PROTOCOL.md` §6.
- Keep BotAgent / separation work only where it sharpens the Torii Quest vs Torii Quest app boundary or prevents demo-breaking instability.
- Keep docs / handoff / code-index upkeep only at the minimum needed to stop agent confusion and preserve clean app separation.
- Stand up a safe assistant-editable .md pipeline for torii-quest-todo.md / torii-continuum-todo.md so todo, handoff, and progress updates can be made on the fly without manual copy-editing. **BUILT v0.2.259** — `tools/mdPatch.mjs` (mdPatch-2): whitelist now covers quest-todo / continuum-todo / todo / progress / HANDOFF; per-file capability map (HANDOFF append-only); new `note` action appends a timestamped live bullet under a per-file default heading. `npm run md:patch`.
- Stand up a thin Continuum click-through mockup for the MVP loop (read-only dashboard path, no admin actions), so the freedom-tech loop can be demonstrated with oversight.

### MVP constraints
- Shooter is maintenance-only.
- No combat-feel polish unless demo-breaking.
- No broad rewrites.
- No speculative architecture work.
- No dashboard redesign work in the Quest MVP queue.
- No live network execution unless already required for the safe MVP proof path.

## Milestone 2 — Post-MVP functional expansion

Goal: promote selected proof surfaces into richer, more functional experiences after the MVP is proven.

### Deferred to Milestone 2
- ~~Real leaderboard publish via NIP-07 / relay write.~~ **LANDED v0.2.285-alpha** — the relay write is promoted to live (`src/engine/leaderboard/livePublish.js`): a finalised run score is signed via NIP-07 (`window.nostr.signEvent`) and fanned out to the configured RELAYS through `fanoutPublish`, reusing the existing relay pool. The write goes THROUGH the SEC-1 `publishGate` (never around): it publishes ONLY when the player explicitly consents AND the gate returns `trust: 'crypto-verified'` — no consent, a non-crypto-verified verdict, a forged signer, or a not-logged-in state all FAIL CLOSED (no relay write). The title-screen leaderboard card gained a "PUBLISH MY SCORE" button (armed only when logged in) with publishing / published / failed status feedback. CSP `connect-src` already allows all four relays, so no CSP change was needed. Tests: `tests/leaderboard-live-publish.test.js` (consented + crypto-verified → relay write with the right event shape; no consent → no write; unverified gate verdict → no write; not-logged-in → no sign; zero-accept fan-out → failure; NIP-07 rejection captured) — the accept path uses a REAL BIP-340 signature through the REAL gate so the crypto floor is not bypassed.
- ~~Richer product interaction beyond read-only proof.~~ **LANDED v0.2.349-alpha** — the read-only product PROOF surface (the inert `productPreview`/`productPanel` card a traveller reaches through a gateway: `actionable:false`, `readOnly:true`, no actions) is promoted to a real "click-into" detail + one lightweight LOCAL interaction. New `src/engine/components/productDetail.js` re-shapes the validated product into a render-ready DETAIL view-model (full sanitised description, image, seller proof, price, reward, marketplace LINK as text-only) plus a SAVE interaction descriptor; new `src/engine/components/savedProducts.js` is a pure-by-injection, npub-keyed `localStorage` save store (`torii.savedProducts.v1`). The traveller's verified Nostr identity (`state.nostrPubkey`, the hex64 set for both logged-in operators AND verified P2 arrivals) keys the save bucket — an anon/failed-closed arrival sees a DISABLED save with a "connect a Nostr identity to save" prompt (read-only). The save is a CLIENT-SIDE write only — NOT a relay write / sign / payment / checkout — so it needs no consent gate (`local:true`, `actionable:false`, `readOnly:true` preserved; commerce stays out-of-band on the marketplace). No gate was downgraded (S1/SEC-1/SEC-3/consent/P2 arrival/leaderboard intact) and CSP was untouched (no new origins). Hostile listing markup is stripped (control chars + angle brackets) before display. Saves are isolated per identity, capped at 100/identity (oldest dropped), and fail closed on missing/secured/malformed storage. Tests: `tests/product-interaction.test.js` (detail view-model for verified hex+npub / SAVED label / anon read-only / sanitisation / invalid→UNAVAILABLE; save round-trip + per-identity isolation + toggle + anon-fails-closed-nothing-written + invalid-key + MAX cap + never-throws; SDK exposure at the experimental tier).
- ~~Real GitHub fetch for update-check.~~ **LANDED v0.2.285-alpha** — the title-screen UPDATE card now self-reports its TRUE standing against the latest published release instead of a hard-coded `v0.2.999-alpha` fixture. New `src/engine/update/liveUpdateCheck.js` orchestrates the existing host-only fetch seam (`githubReleaseSource.fetchLatestRelease`): a fresh localStorage probe (15-min TTL) short-circuits the network; otherwise ONE read-only GET to `https://api.github.com/.../releases/latest` via the injected `window.fetch`, parsed + semver-compared against the installed `VERSION` to surface up-to-date / behind-by-N / ahead. It FAILS CLOSED to "unable to check" on any network error / rate-limit / 404 / malformed payload — the card never breaks — and never auto-updates (`actionable:false`). CSP `connect-src` gained `https://api.github.com` (`tools/csp.mjs`, asserted in `npm run check`). Tests: `tests/live-update-check.test.js` (newer → behind-by-N; equal → up-to-date; runtime newer → ahead; network/404/no-fetcher → graceful unable; fresh cache → no 2nd fetch) — pure delta/cache helpers + the async probe, fetch mocked at the seam.
- Further live gateway promotion beyond the current safe proof path.
- ~~Richer in-world proof-surface / mesh promotion.~~ **LANDED v0.2.289-alpha** — the in-world GATEWAY PORTAL marker (the minimal stand-in: two flat torus rings + a faint cylinder beam + a spinning octahedron core) is promoted to a coherent torii-gate form that visibly "wakes" as the traveller nears. The pure render plan `src/engine/gateway/portalMeshPlan.js` (bumped to `PORTAL_MESH_PLAN_VERSION = 2`) now emits 8 inert parts — the two range-aligned rings, two `±z` pillars, a top `kasagi` + lower `nuki` box lintel (the namesake gate frame), the beam, and the core — and flags the gate frame + core `approach:true` for a host-driven glow. The browser adapter `portalMesh.js` gained `box` geometry support + `setPortalApproach(intensity)`, a scalar-only emissive setter that mutates the pre-cached approach materials (`_approachMats`/`_approachBase`) with NO per-frame allocation. A NEW pure, node-safe view-model `src/engine/gateway/portalApproach.js` grades planar (x,z) proximity into idle → approaching → ready, emitting a normalised closeness `t`, a glow `intensity`, and a phase prompt; its `ready` threshold EQUALS the trigger range so it never arms/confirms a hop (the v0.2.181 `portalTrigger` stays the sole proximity→confirm authority). `arenaRuntime.js` drives the glow each frame from a cached `_portalPos`/`_portalRange` (no allocation). DISPLAY-ONLY + INERT preserved (every part `readOnly:true`, `actionable:false`, all five inert flags false). R2 boundary held (all THREE stays in the arena path; the shell never imports it) and R4 shell-leanness untouched. No bundle growth (existing three primitives only, no new assets). No gate downgraded (S1/SEC-1/SEC-3/consent/P2 arrival/leaderboard/R2/R4 intact). Tests: `tests/portal-approach.test.js` (phase grading + parity-with-trigger, monotonic closeness, planar y-ignoring distance, degraded-input idle, range default, SDK exposure) + expanded `tests/portal-mesh-plan.test.js` (8-part torii frame, approach-flag set, scalar glow setter).
- ~~Additional BotAgent runtime migration or deeper engine cleanup that is not needed for clean app separation or MVP stability.~~ **LANDED v0.2.349-alpha** — the highest-value app-boundary cleanup in this area was a residual R1 leak: `handoffControlPanel` (`src/engine/status/handoffControlPanel.js`) — a Continuum/build-only project-oversight surface with NO game-runtime importer (consumed only by `toriiQuestData.js` + build tools + tests) — was still re-exported through the RUNTIME SDK barrel `src/sdk/index.js` (`export * as handoffControlPanel` + a `SDK_SURFACE` entry). Because `export * as` is tree-shake-hostile and the runtime barrel is reachable from the app bundle (via `proofSurfaceCheck.js` → `toriiDebug` → `arenaRuntime`), that re-export dragged the panel's strings into the runtime chunk. It now lives ONLY in the dedicated BUILD-ONLY dashboard barrel `src/sdk/dashboard.js` (`DASHBOARD_SURFACE.handoffControlPanel`, experimental tier), completing R1's Quest-runtime vs Continuum-dashboard barrel split. Semantics identical (pure read-only module unchanged; build tools/tests now import it from the dashboard barrel). **Regression guard added:** `tests/app-boundary.test.js` (7 tests) pins the boundary structurally — the runtime barrel re-exports NO Continuum dashboard-only surface (`continuum`/`hostRouteSmoke`/`updateFlowSmoke`/`handoffControlPanel`), the dashboard barrel re-exports them all at the experimental tier, the runtime barrel + Quest runtime entries (`main.js`/`arenaRuntime.js`/`bots.js`) import no Continuum dashboard/host module, and the BotAgent decision seam (`engine/entities/bot-agent.js`) stays pure (single `config.js` import; no THREE/Rapier/DOM/dashboard reach). **Follow-up (deferred, bigger, not MVP-needed):** migrating the stateful BotAgent runtime (tick/shoot/cooldown/blowback/death/respawn, currently inline in `src/bots.js`) behind the pure `bot-agent.js` seam + wiring `decideActions()` once it is allocation-free — a behaviour-touching change out of scope for this thin refactor.
- Further player-boundary / FSM cleanup that is not directly required for the MVP.
- **Character Forge (ADR-0082)** — validator-first character-creation pipeline. Do NOT build an in-house generation model; orchestrate external generators (Meshy/Tripo/Hunyuan3D) via routstr/Cashu and own the interoperability contract. **GROUNDWORK LANDED v0.2.718**: `src/engine/character/{skeleton,rigAssessment,characterManifest,characterEvent}.js` (canonical skeleton contract + auto-rig assessment + `torii.character` manifest validator + kind-35100 character-event parser) + the "Character" settings tab (`src/engine/settings/characterForgePanel.js`). **Follow-up slices:** (1) live relay read — **LANDED v0.2.719** (`characterRelayRead.js` + `fetchOwnCharacter()`; the Character tab auto-seats an existing kind-35100 character); (2) create round-trip — **LANDED v0.2.720** (`characterPresets.js` + `publishCharacter()` + `uploadBlossom()`; presets → sign → publish, plus the Blossom upload path); (3) automatic mesh loading (player path) — **LANDED v0.2.721** (`characterMesh.js` + `setCustomMeshUrl()`; the player's own mesh loads from Blossom at login); (4) peer-avatar mesh loading — **LANDED v0.2.722** (mesh hash broadcast through the MP `character` field → Blossom URL → peer avatar); (5) texture/sticker generation via routstr; (6) external mesh generation gated by the validator + auto-rig. Tests: `tests/character-{skeleton,manifest,event,event-build,relay-read,presets,publish,mesh}.test.js`, `tests/rig-assessment.test.js`, `tests/character-forge-panel.test.js`.

### Standing security gates for live promotion
- SEC-1 leaderboard publish gate — BUILT v0.2.257 (`src/engine/leaderboard/publishGate.js`): a pure, node-safe structural gate (signer match + event shape + score validity + abuse ceilings + consent + topic tag) wired opt-in into the publisher adapter so a relay write can never run ungated. **CRYPTO UPGRADE LANDED v0.2.277** — the gate now runs real BIP-340 schnorr verification (reusing the shared `src/engine/crypto/nostrSig.js` verifier, moved there from `gateway/`): structural checks are a fast pre-flight, then `verifyNostrEventSig(event)` must pass; a tampered/wrong-key/stub-signed event fails closed and only a full pass returns `trust: 'crypto-verified'` (no structure-only trusted path remains). **LIVE RELAY WRITE PROMOTED v0.2.285** — the actual NIP-07 sign + relay fan-out now ships (`src/engine/leaderboard/livePublish.js`, wired into the title-screen leaderboard card). It runs strictly behind this gate: a consented, `crypto-verified` finalised score is the only thing that reaches a relay; everything else fails closed.
- SEC-2 handoff verification gate — BUILT v0.2.252 (`src/engine/gateway/handoffVerify.js`): structural accept-verification (host match + request reference + traveller addressing + https spawn) wired into the handshake controller.
- SEC-3 product URL hardening gate — BUILT v0.2.253 (`src/engine/gateway/urlHarden.js`): scheme/host allowlist + private-host rejection + traveller-npub append, wired into `_executeJump` so no URL becomes navigable until it clears.

### Live gateway promotion — Milestone 2 (n2n hop)
- **P1 — signed travel-request/confirm handshake — LANDED (NIP-07, SEC-2/S1).** Crypto-verified accept arms the hop; the controller only arms on `trust === 'crypto-verified'`.
- **P2 + P3 — cross-host n2n hop carrying npub + two-instance interop harness — LANDED v0.2.275-alpha (security).** The traveller's verified npub now survives the cross-host jump and is re-verified on the DESTINATION host before seating, so a player arrives on host B seated as their real nostr identity rather than fresh anon — and an unverified/tampered/impostor hop FAILS CLOSED to anon.
  - **Host-side seating gate (`src/engine/gateway/handoffArrival.js`, NEW):** `readArrivingTraveller(url)` parses the `torii-traveller` spawn-URL npub (HINT only, forgeable); `verifyArrival({arrivingPubkey, request, expectedHostPubkey})` seats ONLY when the traveller's SIGNED travel request (a) is present with its signed envelope, (b) was authored by the arriving npub, (c) is addressed to this host, and (d) passes BIP-340 schnorr verification — returning `trust: 'crypto-verified'` only on a full pass; `seatArrivalDecision(verdict)` fails closed to `{identity:null, anon:true}` on any mismatch. S1 gates seating, not just the hop.
  - **`extractTravelRequest` (MODIFIED):** now carries the signed envelope (`id`, `sig`, `signed{pubkey,created_at,kind,tags,content}`) so the host can recompute the NIP-01 id and verify the request signature on arrival.
  - **`handshakeController.js` (MODIFIED):** accept-time re-verifies the incoming request (refuses to accept a forged/unsigned request — `request-unverified`), records accepted travellers in-session, and exposes `admitArrival(url, opts)` that reads the inbound npub, requires a configured host identity, re-verifies (in-session record or relay re-read), and returns `{seated, npub, trust, anon}`.
  - **`main.js` arrival seam (MODIFIED):** on load reads the inbound npub + host identity (`window.__toriiHostPubkey` / `<meta name="torii-host-pubkey">`), only seats ANONYMOUS sessions (never hijacks a logged-in operator), re-reads the signed request from relays, and on a crypto-verified verdict seats the arriving identity (sets `state.nostrPubkey`/`nostrName`, `setOurPubkey`, emits `NOSTR_LOGIN`, re-renders the gateway card).
  - **P3 two-instance interop harness (`tests/gateway-interop.test.js`, NEW, 5 tests):** two independent handshake-controller instances (A=traveller, B=host), each with its own BIP-340 keypair, over ONE shared in-memory relay — drives the full live flow A requests → B accepts → A arms → A jumps carrying its npub → B seats A by verified npub; plus cold-load B (no session memory) re-reads the signed request from the relay and still seats; impostor URL → anon; tampered request → refused; forged incoming request can never be ACCEPTED. Arrival unit gate locked in `tests/handoff-arrival.test.js` (NEW, 10 tests). Suite: **122 → 124 files, 1858 tests green**; `npm run check` 16/16 GREEN.
  - **Residual gap / follow-up for the LIVE two-host test:** the spawn-URL npub is not yet bound to a fresh per-hop signature — seating relies on a valid signed travel request existing on the relay (authored by the arriving npub, addressed to the host). A live two-host run should confirm CSP allows the hop target host and exercise the relay re-read path under real network latency. SEC-gated live-relay writes stay parked behind a manual deploy.

These are not Milestone 1 delivery tasks unless live promotion is explicitly being advanced, but they are also not ordinary backlog items to forget. Keep them visible as standing gates that must be cleared before the related live features are promoted.

### Instance access model & write authority (Milestone 2 theme)

Owner-set design constraint (v0.2.357-alpha, refined v0.2.358-alpha): travel between gates / instances is PUBLIC BY DEFAULT — any npub can resolve a listed gateway and arrive at any listed instance without invite or allow-list. The instance OWNER can narrow arrival on a per-instance basis to `public` (default) / `follows-only` ("follow me to enter") / `whitelist` (admin-curated allow-list) / `invite-only` (per-visitor invite). Visiting npubs are READ-ONLY by default — they can move, play the arena, visit the nap zone, browse the shop, and buy things (commerce stays out-of-band on the marketplace, unchanged) — but they cannot mutate world state.

Instance-admin surfaces live on a per-instance settings page reachable from the Quest Home Screen (the title screen) via a small `⚙ INSTANCE SETTINGS` corner link. The link is visible ONLY to the logged-in instance admin (operator's npub === deployment's host pubkey via the existing `_hostIdentity()` / `window.__toriiHostPubkey` seam). The settings page will grow to hold identity / appearance / moderation / write-authority controls; **Access** is the first section.

- **ACC-1 — Public arrival default + admin-settings shell + Access placeholder — LANDED v0.2.358-alpha (ACC).** The title-screen Instance Settings shell exists: `⚙ INSTANCE SETTINGS` corner link (`#instance-settings-link`, next to the existing project-dashboard link) is hidden by default and unhides only when the operator's logged-in npub matches the deployment's configured host pubkey. Clicking opens an inert overlay (`#instance-settings-panel`) rendered from the pure `src/engine/ui/instanceSettings.js` view-model. One real section — **Access** — shows `Arrival: Public` (read-only) + a "Coming soon" list of the three future modes (`follow-me` / `whitelist` / `invite-only`). A second `More coming soon` section marks that identity / appearance / moderation / write-authority will land here next. Nothing on the page changes the world: no gate change, no relay call, no navigation, no sign; SEC-1/SEC-2/SEC-3 untouched. Visibility rule is `isInstanceAdmin({ operatorPubkey, hostPubkey })` (fail-closed on any malformed / mismatched / anon input). Tests: 23 new cases in `tests/instance-settings.test.js` covering visibility rule, model shape, renderer HTML output, escaping of hostile input, case-insensitive hex normalisation, defence-in-depth on missing/hidden model, close-button data-action hook.
- **ACC-2a — `follows-only` arrival mode: follow-graph query + policy check (Milestone 2, deferred).** Wire the `follows-only` mode: relay-read the follow graph (kind 3 / NIP-02), decide semantics (default "visitor follows owner" — the "follow me to enter" pattern the owner asked for; keep visitor-follows-owner / owner-follows-visitor / mutual as a config knob), cache the follow set, and enforce it in the arrival policy check that runs AFTER SEC-2 crypto verify. Includes the small relaxation of `resolveHandoffSpawn` so `expectedPlayerPubkey` becomes optional on the public arrival path (SEC-2 still verifies the envelope's own claimed pubkey; only the "host names a specific expected traveller" requirement relaxes). Fail-closed default preserved.
- **ACC-2b — `whitelist` + `invite-only` arrival modes: allow-list state + policy check (Milestone 2, deferred; one slice — they share the same admin-curates-a-list plumbing).** Data model for the per-instance allow-list (owner-signed list event, or a per-instance NIP-51 list), owner-facing UI in the Access section to add/remove members, policy check that runs AFTER SEC-2 crypto verify and admits only listed npubs. `invite-only` is a subset shape of the same list plumbing (per-visitor entries, potentially time-bounded / single-use); land as one slice with a mode toggle rather than duplicate the storage.
- **ACC-3 — Write authority via owner-curated admin group — DEFERRED WHOLE-SLICE (Milestone 2, big).** By default a visiting npub can NEVER change anything in an instance. Write capability (place / edit / remove world objects, change zone config, moderate, etc.) is gated by an OWNER-CURATED ADMIN GROUP the owner maintains per-instance. This is a full slice — data model for admin groups, owner UX to add/remove members (rendered under the Instance Settings page from ACC-1), per-action write-gate enforcement across every mutating surface (world objects, zone config, shop listing edits if any land, moderation), and audit — and is NOT required for the public-travel / read-only-visit default to be usable. Track as its own slice; do NOT bundle into ACC-2a / ACC-2b. Read-only visitor default holds until this lands.

These are Milestone 2 access-model items and are not Milestone 1 delivery tasks. Keep them visible so they are not forgotten once the live n2n hop is being advanced.

## Audit follow-ups (v0.2.259 optimization pass)

Remaining items from the v0.2.259 code audit (`torii-quest-audit-v0.2.259.md`). Already landed in v0.2.260: E1 (vitest pool=threads, 28.7s→2.7s), GLB1+GLB2 (−66% assets, DRACOLoader), R3 (SW precache trim), S5 (relay fan-out), S2 (port hardening), repo hygiene. Landed in v0.2.261: **R1** (dashboard SDK split — app chunk −19.4% raw / −19.7% gzip). Landed in v0.2.263: **S1** (real BIP-340 schnorr verify in the gateway handoff). Landed in v0.2.264: **R2** (lazy-load THREE behind ENTER ARENA). Landed in v0.2.265: **E2** (foundation profile → `vitest --changed`). Landed in v0.2.266: **S4 + S3** (vendor Draco locally + CSP via HTTP header, no gstatic — the last third-party runtime origin is gone). Landed in v0.2.267: **E3** (split the three monolith test files along their `describe` blocks — 108→122 files, 1843 tests unchanged). Landed in v0.2.268: **R4** (next barrel-leak audit — audited, NO actionable leaks: `shellReport.js` ~52 KB + the `sdk/index.js` 54-export barrel are both arena-path-only, the shell's 35 first-paint modules are all genuine pre-arena flow; shell `torii-entry.js` unchanged at 103.64 KB / 35.66 KB gzip, R2 boundary intact).

Each item is sized as a single thin slice. Keep them in priority order; pick one per session.

- **S1 — schnorr verify in handoffVerify — LANDED v0.2.263 (security).** Replaced structural-only SEC-2 with a real BIP-340 signature verification. The host's accept is a NIP-01 nostr event; `verifyHandoff()` now recomputes the event id and runs `schnorr.verify(sig, idBytes, hostPubkey)` (new `src/engine/gateway/nostrSig.js`), failing closed on a missing/forged/tampered signature and returning `trust: 'crypto-verified'` only on a full pass. The handshake controller arms the hop ONLY on `crypto-verified`. Adds the project's **first runtime crypto dependency** (`@noble/curves` + transitive `@noble/hashes`); app chunk +30.5 KB raw / +12.5 KB gzip (262→292 KB raw). Tests: 5 new crypto cases in `tests/travel-request.test.js` (valid pass, tampered body, wrong-key, missing sig, unsigned-no-longer-arms) + handshake-controller arming reworked to real schnorr sigs.
- **R2 — lazy-load THREE behind ENTER ARENA — LANDED v0.2.264 (perf).** Split `main.js` into a three-free shell (title screen / gateway cards / previews / login / char select / zone-route notice) + a new `src/arenaRuntime.js` that owns every three-dependent surface (scene/renderer, arena geometry, the game loop, players/bots/weapons/physics viewmodels, the in-world portal mesh + ToriiDebug). The shell `await import('./arenaRuntime.js')`s ONLY inside the ENTER ARENA handler, so `three-vendor` is now a dynamic-import target (no longer in `index.html`'s preload set, no static import in the app chunk). The title-screen n2n handshake + presence polling moved from the game loop to a three-free shell rAF ticker so it keeps running before/after the arena boots. **Bundle:** app/shell chunk 292.4 KB → 103.6 KB raw (96.0 → 35.6 KB gzip, −64.6% raw / −62.9% gzip); new `arenaRuntime` chunk 190.9 KB (62.8 KB gzip); `three-vendor` 625.1 KB raw / 160.6 KB gzip is no longer paid on first paint — it loads on demand after ENTER ARENA (first-paint JS drops ~625 KB raw / ~161 KB gzip). Tests: all 1843 green (boot-order + portal-placement + entry-flow contracts repointed at `arenaRuntime.js`; `main.js` now asserted to never statically import `scene.js`).
- **E2 — replace foundation profile with `vitest --changed origin/main` — LANDED v0.2.265 (DX).** `npm run test:foundation` no longer runs the hand-curated file list — it now runs `vitest run --changed origin/main --passWithNoTests`, i.e. only the suites whose import graph is touched by the diff vs `origin/main` (vitest's related-graph semantics), so there is no manual list to keep in sync as the suite grows. `--passWithNoTests` makes a clean tree (or a checkout where `origin/main` is absent) a graceful no-op (exit 0, "No test files found") rather than a failure. The curated foundation set is preserved as `npm run test:foundation:list` (still backed by `tools/testProfiles.mjs` → it remains the source of the dashboard/handoff profile counts and the validate-against-disk guard); `test:fast` (tiny innermost core) and the full-suite gate (`npm test` / `npm run test:release`) are unchanged. Dashboard "Tests" label updated to reflect the new behaviour.
- **S4 — vendor Draco decoder under public/draco/ — LANDED v0.2.266 (security/CSP).** The three DRACOLoader decoder (matching `three` v0.184.0, copied from `node_modules/three/examples/jsm/libs/draco/gltf/`: `draco_decoder.js` 512 KB, `draco_decoder.wasm` 192 KB, `draco_wasm_wrapper.js` 58 KB) is now vendored at `public/draco/` and served same-origin. All 7 `setDecoderPath('https://www.gstatic.com/draco/…')` call sites (`playerModel`, `arena`, `botModel`, `napNpc`, `firstPersonBody`, `weapons` + the `optimize-glbs.mjs` doc) now point at `/draco/`. `gstatic.com` is gone from the CSP entirely — no third-party runtime origin remains (decoder fetch is covered by `connect-src 'self'`; the decoder's blob worker by `worker-src 'self' blob:`). The DRACOLoader stays on the arena path (R2 boundary intact, not pulled into the shell).
- **S3 — move CSP from meta tag to HTTP header — LANDED v0.2.266 (security).** CSP is no longer a `<meta http-equiv>` tag — it ships as an HTTP response header from a single source of truth (`tools/csp.mjs` → `CSP_VALUE`), emitted in three places that cannot drift: `dist/_headers` (static host, written by a new vite plugin at build), the `vite preview` server, and the Caddy/Nginx blocks in `VPS_INSTALL.md`. `script-src` uses `'strict-dynamic'` + a **sha256 of the single trusted classic inline bootstrap** (`'self'`/`blob:` kept as the CSP-Level-2 fallback). At build the plugin removes the static entry `<script>` tag and has the bootstrap `import('/assets/torii-entry.js')` (entry filename pinned via `entryFileNames`), so `strict-dynamic` propagates trust to the entry + every lazy chunk (three-vendor, rapier, arenaRuntime) with no per-build hash churn. `npm run check` block [16] fails on: a meta CSP in index.html or dist, any gstatic reference, missing Draco files / wrong `setDecoderPath`, a CSP missing `strict-dynamic`/inline-sha/required directives, a `dist/_headers` mismatch, or a recomputed inline-script sha that doesn't match `INLINE_SCRIPT_SHA256`.
- **E3 — split the three biggest test files — LANDED v0.2.267 (DX).** The three monoliths were split along their top-level `describe` blocks, semantics unchanged: `torii-quest-dashboard.test.js` (1633 lines / 149 tests / 27 describes) → 10 focused files (`.helpers/.model/.render/.health/.milestones/.readiness/.validation/.approval/.clickthrough/.sdk`); `next-action-state.test.js` (377 lines / 28 tests / 4 describes) → 4 files (`.constants/.assembly/.formatters/.version`, sharing `tests/_next-action-state-helpers.js`); `md-patch.test.js` (424 lines / 50 tests / 11 describes) → 3 files (`.resolve/.transforms/.apply`, sharing `tests/_md-patch-helpers.js`). Suite: **108 files → 122 files, 1843 tests unchanged**. The `CURRENT_TEST_STATUS`/`DEFAULT_TEST_STATUS` file-count drift guard was bumped 108→122; `test:foundation`/`test:foundation:list` were unaffected (the curated `foundation` list never referenced any of the three). `npm run check` stays 16/16 green.
- **R4 — next barrel-leak audit — AUDITED, NO LEAKS FOUND (shell clean post-R2) v0.2.268 (perf).** Swept the shell entry (`src/main.js` static import graph, with the `arenaRuntime.js` dynamic import excluded as the deferral boundary) for barrel leaks and oversight strings at first paint. **No actionable leak — no code change.** (a) `shellReport.js` (51,822 B) is reached ONLY by `proofSurfaceCheck.js` + `toriiDebug.js`, both arena-path (`toriiDebug` ← `arenaRuntime.js`; `proofSurfaceCheck` ← proofSurface render/gate ← `arena.js`) — confirmed by a static BFS that does NOT reach it from the shell AND by its unique fixture string `npub1demo0gateway0report…` being present in `arenaRuntime-*.js` (1×) and ABSENT from `torii-entry.js` (0×). (b) The sole re-export barrel `src/sdk/index.js` (54 re-exports) is imported ONLY by `proofSurfaceCheck.js` — arena-only, never reached from the shell. (c) No shell module is a non-first-paint heavyweight: the 35 shell-reachable modules (258 KB src → 103.64 KB / 35.66 KB gzip) are all genuine pre-arena flow (gateway/nostr/consent/hud/state/zoneRoute). (d) No non-three module is duplicated across shell/arena. (e) The only shell demo-fixtures are tiny (~60 B) preview fallbacks in `main.js`. **Bundle UNCHANGED (audit only): shell `torii-entry.js` 103.64 KB raw / 35.66 KB gzip (before == after); `arenaRuntime` 193.17 KB / 63.86 KB gzip; `three-vendor` 625.07 KB / 160.55 KB gzip still deferred (R2 intact).** `npm run check` 16/16 GREEN; `npx vitest run` 1843/1843 across 122 files. Full write-up in `R4-implementation-report.md`.

## Milestone 3 — Post-MVP polish and redesign

Goal: improve visual quality, readability, and feel once the MVP loop is already working.

### Deferred to Milestone 3
- Shooter feel polish.
- Mesh / material polish.
- Broader UX refinement.
- Broader bot behaviour polish.
- Larger Continuum redesign follow-up work such as DASHBOARD-LAYOUT-1 richer cards / tables / denser visual redesign.
- Any nonessential visual or presentation improvements that do not change the MVP proof.

## Milestone 4 — Visuals, gameplay, environment & nostr/bitcoin depth

Goal: lift the game from a functional MVP into a distinctive, replayable arena shooter that leans into the cyberpunk-feudal + bitcoin/nostr identity. Each item is a thin vertical slice with a clear green checkpoint.

### Visuals
- ~~M4-V1 — HUD legibility pass~~ **LANDED** — top-right scoreboard stats have pill-shaped backgrounds with backdrop blur, text-shadow, 28px bold values; top-left TORII QUEST logo has safe-area padding + gradient + text-shadow (v0.2.400). Crosshair has color-state glow, killfeed + ammo display functional.
- ~~M4-V2 — Post-processing bloom~~ **LANDED v0.2.400** — `EffectComposer` + `UnrealBloomPass` wired in `src/scene.js`; pure tuning in `src/engine/bloomPlan.js` (strength 0.72, radius 0.33, threshold 0.86); gated by `qualityTier.js` (ON for HIGH/NORMAL, OFF for LOW).
- M4-V3 — Dynamic muzzle/impact lighting: gunfire and bot hits cast short-lived point-light flashes. Adds punch to currently-static arena lighting.
- M4-V4 — Player character model rig/animation pass: fix jagged collar/neck polygons visible in third-person; clean rig + idle/move animations (Blender/Meshy workflow).
  - M4-V4a — IN-CODE normals/material hygiene (LANDED): guarded `computeVertexNormals` (only if normals absent) + force `flatShading=false` on player GLB loads (`playerModel.js`, `firstPersonBody.js`). Rules out the normals/material hypothesis. Animation/AnimationMixer was already complete (no work needed).
  - M4-V4b — ASSET WORK (deferred to Blender/Meshy, owner-driven): the headless FP body GLB (`chiefmonkey-headless.glb`) has an uncapped neck stump; `chiefmonkey6.glb` may have coarse collar topology. Re-export both GLBs from Blender with a capped neck + clean collar topology + smooth vertex normals. No code change can repair torn/uncapped mesh geometry.
  - NOTE: the original paused-third-person "jagged collar/neck" screenshot was the headless body's open neck stump showing through fly mode before the `setFlyHidden` fix (v0.2.345). Normal play only shows the headless FP arms (layer 2); the full body appears only in the wall mirror. So the visible defect is already hidden in gameplay; V4b is cosmetic polish on the mirror/headless asset.

### Gameplay
- M4-G1 — Bot AI upgrade: obstacle avoidance + cover-seeking + flanking + 2-3 difficulty tiers. Replaces current direct-position-mutation + simple proximity clamp. Biggest single gameplay lever.
- M4-G2 — Weapon variety: 2-3 weapons (pistol / rifle / shotgun) with distinct range, damage, recoil, and spread. Combines with fly-mode verticality + 21m ceiling.
- M4-G3 — Pickups: health, ammo, and sats drops from bots. Gives the arena a movement loop and a reason not to camp the safe zone.
- M4-G4 — Verticality-aware arena design: elevated platforms, rooftop cover, risk/reward of flying vs. grounded, built around the fly + 21m ceiling + safe-zone ruleset.

### Environment
- M4-E1 — Day/night cycle (or dusk mode): slow sky/lighting cycle that changes combat visibility and mood; ties into the aurora sky work.
- M4-E2 — Water interaction: splash VFX + audio on beach/water entry, shallow-wading movement penalty. Brings the outside-the-fence dead space to life.
- M4-E3 — Torii gate as a functional portal: make the east-fence gate gap a real gateway to another zone/biome (the namesake gateway metaphor).
- M4-E4 — Ambient soundscape: layered bed (wind, water, distant thunder, bird calls matching the V-shaped flocks) + spatial combat audio.

### Nostr / Bitcoin (differentiator)
- M4-N1 — Nostr-powered live leaderboards: kill counts / high scores signed and broadcast over nostr relays; verifiable, decentralized, no central DB. Builds on the existing NIP-07 leaderboard publish (M2, SEC-1 gate).
- M4-N2 — SATS economy: bots drop sats, sats buy weapons/upgrades, optional small ante/betting mechanic for rounds. Ties bitcoin into actual mechanics, not just flavor.
- M4-N3 — Player identity via nostr keys: log in with an npub, carry loadout/score across sessions and eventually across instances.

### Priority order (suggested)
1. M4-V1 HUD legibility (fast, observed problem)
2. M4-V2 bloom post-processing (huge visual payoff, cheap)
3. M4-G1 bot AI / cover (biggest gameplay lever)
4. M4-V4 character model rig pass
5. M4-G2 weapon variety, M4-G3 pickups, then the rest in priority order.

## Backlog: Level 2 Platform Architecture

- [ ] Draft Level 2 platform architecture proposal: separate Torii engine (gate + runtime + networking) from world content (arena, NAP zone, game mode). Users can delete/replace the arena, build custom worlds (racing, gallery, shop), and changes survive updates. Only the Torii gate is permanent. Pluggable game mode interface.

## Torii Quest ToDo - Working rules

- Prefer thin vertical slices over polish traps.
- Break every task into small chunks that can usually be finished in a few hours, not open-ended multi-day jobs.
- Prefer thin vertical slices with a clear green checkpoint, visible outcome, and one obvious next step.
- Avoid long-running jobs that stall flow, hide risk, or delay feedback; if a task starts to sprawl, split it again before continuing.
- Each chunk should produce meaningful progress on the MVP proof, app separation, safety, or demo stability.
- End each chunk in a handoff-safe state: code green, tests updated where relevant, docs/indexes touched, and the next safe task easy to pick up.
- If a job cannot be described as a few-hour slice with a concrete finish line, it is too big and must be broken down first.
- Every change should improve clarity, safety, testability, or separation when practical.
- Keep Quest and Continuum task lists separate.
- If a task does not directly help the MVP proof, clean app separation, or demo stability, defer it.
- When a current job becomes too large, pause it safely: commit or checkpoint all green work, record the current state in HANDOFF / NEXT_ACTION_STATE, then replace it with a list of 1–3 hour follow-up slices.
- Never abandon partially-completed work; always capture its state before changing the active job.

### Bot / progress handling

- Pause any bot-related work that does not directly improve MVP stability, app boundaries, or player feedback.
- Do not let long-running BotAgent jobs block progress reports or dashboard updates; report partial landings as soon as they are green.
- Prefer short, observable slices (1–3 hours) that show visible changes in bot behaviour, load, or safety.

## Historical completed (archive)

These items are DONE and kept only as struck markers for the Continuum dashboard's completed-task count. Details are in `torii-quest-progress.md` and git history.

- ~~ARS-1~~ ~~Debug dump / handoff snapshot (`engine/debug/snapshot.js`). DONE v0.2.130.~~
- ~~ARS-2~~ ~~Physics interaction API (`engine/physics/interactions.js`). DONE v0.2.130.~~
- ~~ARS-3~~ ~~Rapier raycast service (`engine/physics/raycastService.js`), migration complete v0.2.133.~~
- ~~ARS-5~~ ~~SDK/API skeleton (`src/sdk/index.js`). DONE v0.2.131.~~
- ~~ARS-7~~ ~~Handoff template (`torii-quest-handoff.md`). DONE v0.2.130.~~
- ~~TESTPROF-1~~ ~~Test profile system (`tools/testProfiles.mjs`). DONE v0.2.173.~~
