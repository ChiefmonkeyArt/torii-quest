# Torii Quest ToDo

Current version: `v0.2.362-alpha`
Live site: [torii-quest.pplx.app](https://torii-quest.pplx.app)

Source of truth for Torii Quest tasks.

> **Doc structure (v0.2.352-alpha refresh):** the per-project convention is now `torii-quest-{strategy,todo,progress,handoff}.md`. Torii Quest uses `torii-continuum-*` (when written); Torii DE uses `torii-de-*` (when written). The old `todo.md` legacy shim was deleted; historical struck completed markers moved into `## Historical completed (archive)` at the end of this file. Tools have been updated to reference the new names (`docConsistency`, `build-continuum`, `mdPatch`, `regression-check`).

## Scope

Torii Quest is the game app.
Torii Quest is a separate dashboard app and must stay on `torii-continuum-todo.md`.
Nostr Arena is archival only and must not be used as the active queue for Quest.

This todo is reprioritised around the lean MVP only:
- Keep the freedom-tech loop clear and demoable.
- Keep shooter work maintenance-only unless a bug is demo-breaking.
- Keep BotAgent / runtime separation work only where it improves Quest vs Continuum boundaries or prevents instability.
- `NOSTR_ARENA_MASTER_TODO.md` is deleted; `torii-quest-todo.md` is a legacy tooling shim only. Do not use either as an active queue.
- Defer broader polish, most live-promotion work, and larger redesign passes to Milestone 2 and Milestone 3, while keeping security gates visible until the related live features are promoted.


## Milestone 1 — MVP Proof of Concept Torii Quest

Goal: ship the smallest clean proof-of-concept that demonstrates the Torii Quest loop.

### Current job status
- PAUSED: Oversight dashboard progress UX work
- State captured in HANDOFF / NEXT_ACTION_STATE.
- Replacement slices:
  - Finish the Continuum click-through mockup path for the MVP demo (1–3h)
  - Show clearer progress state in the oversight dashboard (1–3h)
  - Add heartbeat / partial-progress feedback for long-running bot work (1–3h)

### Active MVP tasks
- Keep the four MVP proof slices visible and coherent as one loop: Gateway, Product, Leaderboard, Update.
- Keep the title-screen / entry / in-world flow stable so the loop can be demoed end-to-end without silent failure.
- Keep the current safe gateway path intact: preview, intent, consent copy, dry-run plan, same-origin executor seam, and controlled host transport seam.
- Complete the move of the Torii jump gate / gateway experience into the far-right corner of the NAP zone (DONE v0.2.245–v0.2.250) and now **promote the gateway to the live n2n hop**: see `torii-quest-strategy.md` → "15-Hour Proof-of-Concept Route" item 2. Phased — P0 presence/who's-online (live relay read + publish our own world-presence event) → P1 signed travel-request/confirm handshake (NIP-07, SEC-2) → P2 cross-host jump carrying npub (SEC-3) → P3 two-instance interop proof. The placeholder second-zone idea is RETIRED in favour of the real cross-world hop described in `GATEWAY_PROTOCOL.md` §6.
- Keep BotAgent / separation work only where it sharpens the Torii Quest vs Torii Quest app boundary or prevents demo-breaking instability.
- Keep docs / handoff / code-index upkeep only at the minimum needed to stop agent confusion and preserve clean app separation.
- Stand up a safe assistant-editable .md pipeline for torii-quest-todo.md / torii-continuum-todo.md so todo, handoff, and progress updates can be made on the fly without manual copy-editing. **BUILT v0.2.259** — `tools/mdPatch.mjs` (mdPatch-2): whitelist now covers quest-todo / continuum-todo / todo / progress / HANDOFF; per-file capability map (HANDOFF append-only); new `note` action appends a timestamped live bullet under a per-file default heading. `npm run md:patch`.
- Stand up a thin Continuum click-through mockup for the MVP loop (read-only dashboard path, no admin actions), so the freedom-tech loop can be demonstrated with oversight.

### MVP constraints
- Shooter is maintenance-only.
- No combat-feel polish unless demo-breaking.
- No broad rewrites.
- No speculative architecture work.
- No dashboard redesign work in the Quest MVP queue.
- No live network execution unless already required for the safe MVP proof path.

## Milestone 2 — Post-MVP functional expansion

Goal: promote selected proof surfaces into richer, more functional experiences after the MVP is proven.

### Deferred to Milestone 2
- ~~Real leaderboard publish via NIP-07 / relay write.~~ **LANDED v0.2.285-alpha** — the relay write is promoted to live (`src/engine/leaderboard/livePublish.js`): a finalised run score is signed via NIP-07 (`window.nostr.signEvent`) and fanned out to the configured RELAYS through `fanoutPublish`, reusing the existing relay pool. The write goes THROUGH the SEC-1 `publishGate` (never around): it publishes ONLY when the player explicitly consents AND the gate returns `trust: 'crypto-verified'` — no consent, a non-crypto-verified verdict, a forged signer, or a not-logged-in state all FAIL CLOSED (no relay write). The title-screen leaderboard card gained a "PUBLISH MY SCORE" button (armed only when logged in) with publishing / published / failed status feedback. CSP `connect-src` already allows all four relays, so no CSP change was needed. Tests: `tests/leaderboard-live-publish.test.js` (consented + crypto-verified → relay write with the right event shape; no consent → no write; unverified gate verdict → no write; not-logged-in → no sign; zero-accept fan-out → failure; NIP-07 rejection captured) — the accept path uses a REAL BIP-340 signature through the REAL gate so the crypto floor is not bypassed.
- ~~Richer product interaction beyond read-only proof.~~ **LANDED v0.2.349-alpha** — the read-only product PROOF surface (the inert `productPreview`/`productPanel` card a traveller reaches through a gateway: `actionable:false`, `readOnly:true`, no actions) is promoted to a real "click-into" detail + one lightweight LOCAL interaction. New `src/engine/components/productDetail.js` re-shapes the validated product into a render-ready DETAIL view-model (full sanitised description, image, seller proof, price, reward, marketplace LINK as text-only) plus a SAVE interaction descriptor; new `src/engine/components/savedProducts.js` is a pure-by-injection, npub-keyed `localStorage` save store (`torii.savedProducts.v1`). The traveller's verified Nostr identity (`state.nostrPubkey`, the hex64 set for both logged-in operators AND verified P2 arrivals) keys the save bucket — an anon/failed-closed arrival sees a DISABLED save with a "connect a Nostr identity to save" prompt (read-only). The save is a CLIENT-SIDE write only — NOT a relay write / sign / payment / checkout — so it needs no consent gate (`local:true`, `actionable:false`, `readOnly:true` preserved; commerce stays out-of-band on the marketplace). No gate was downgraded (S1/SEC-1/SEC-3/consent/P2 arrival/leaderboard intact) and CSP was untouched (no new origins). Hostile listing markup is stripped (control chars + angle brackets) before display. Saves are isolated per identity, capped at 100/identity (oldest dropped), and fail closed on missing/secured/malformed storage. Tests: `tests/product-interaction.test.js` (detail view-model for verified hex+npub / SAVED label / anon read-only / sanitisation / invalid→UNAVAILABLE; save round-trip + per-identity isolation + toggle + anon-fails-closed-nothing-written + invalid-key + MAX cap + never-throws; SDK exposure at the experimental tier).
- ~~Real GitHub fetch for update-check.~~ **LANDED v0.2.285-alpha** — the title-screen UPDATE card now self-reports its TRUE standing against the latest published release instead of a hard-coded `v0.2.999-alpha` fixture. New `src/engine/update/liveUpdateCheck.js` orchestrates the existing host-only fetch seam (`githubReleaseSource.fetchLatestRelease`): a fresh localStorage probe (15-min TTL) short-circuits the network; otherwise ONE read-only GET to `https://api.github.com/.../releases/latest` via the injected `window.fetch`, parsed + semver-compared against the installed `VERSION` to surface up-to-date / behind-by-N / ahead. It FAILS CLOSED to "unable to check" on any network error / rate-limit / 404 / malformed payload — the card never breaks — and never auto-updates (`actionable:false`). CSP `connect-src` gained `https://api.github.com` (`tools/csp.mjs`, asserted in `npm run check`). Tests: `tests/live-update-check.test.js` (newer → behind-by-N; equal → up-to-date; runtime newer → ahead; network/404/no-fetcher → graceful unable; fresh cache → no 2nd fetch) — pure delta/cache helpers + the async probe, fetch mocked at the seam.
- Further live gateway promotion beyond the current safe proof path.
- ~~Richer in-world proof-surface / mesh promotion.~~ **LANDED v0.2.289-alpha** — the in-world GATEWAY PORTAL marker (the minimal stand-in: two flat torus rings + a faint cylinder beam + a spinning octahedron core) is promoted to a coherent torii-gate form that visibly "wakes" as the traveller nears. The pure render plan `src/engine/gateway/portalMeshPlan.js` (bumped to `PORTAL_MESH_PLAN_VERSION = 2`) now emits 8 inert parts — the two range-aligned rings, two `±z` pillars, a top `kasagi` + lower `nuki` box lintel (the namesake gate frame), the beam, and the core — and flags the gate frame + core `approach:true` for a host-driven glow. The browser adapter `portalMesh.js` gained `box` geometry support + `setPortalApproach(intensity)`, a scalar-only emissive setter that mutates the pre-cached approach materials (`_approachMats`/`_approachBase`) with NO per-frame allocation. A NEW pure, node-safe view-model `src/engine/gateway/portalApproach.js` grades planar (x,z) proximity into idle → approaching → ready, emitting a normalised closeness `t`, a glow `intensity`, and a phase prompt; its `ready` threshold EQUALS the trigger range so it never arms/confirms a hop (the v0.2.181 `portalTrigger` stays the sole proximity→confirm authority). `arenaRuntime.js` drives the glow each frame from a cached `_portalPos`/`_portalRange` (no allocation). DISPLAY-ONLY + INERT preserved (every part `readOnly:true`, `actionable:false`, all five inert flags false). R2 boundary held (all THREE stays in the arena path; the shell never imports it) and R4 shell-leanness untouched. No bundle growth (existing three primitives only, no new assets). No gate downgraded (S1/SEC-1/SEC-3/consent/P2 arrival/leaderboard/R2/R4 intact). Tests: `tests/portal-approach.test.js` (phase grading + parity-with-trigger, monotonic closeness, planar y-ignoring distance, degraded-input idle, range default, SDK exposure) + expanded `tests/portal-mesh-plan.test.js` (8-part torii frame, approach-flag set, scalar glow setter).
- ~~Additional BotAgent runtime migration or deeper engine cleanup that is not needed for clean app separation or MVP stability.~~ **LANDED v0.2.349-alpha** — the highest-value app-boundary cleanup in this area was a residual R1 leak: `handoffControlPanel` (`src/engine/status/handoffControlPanel.js`) — a Continuum/build-only project-oversight surface with NO game-runtime importer (consumed only by `toriiQuestData.js` + build tools + tests) — was still re-exported through the RUNTIME SDK barrel `src/sdk/index.js` (`export * as handoffControlPanel` + a `SDK_SURFACE` entry). Because `export * as` is tree-shake-hostile and the runtime barrel is reachable from the app bundle (via `proofSurfaceCheck.js` → `toriiDebug` → `arenaRuntime`), that re-export dragged the panel's strings into the runtime chunk. It now lives ONLY in the dedicated BUILD-ONLY dashboard barrel `src/sdk/dashboard.js` (`DASHBOARD_SURFACE.handoffControlPanel`, experimental tier), completing R1's Quest-runtime vs Continuum-dashboard barrel split. Semantics identical (pure read-only module unchanged; build tools/tests now import it from the dashboard barrel). **Regression guard added:** `tests/app-boundary.test.js` (7 tests) pins the boundary structurally — the runtime barrel re-exports NO Continuum dashboard-only surface (`continuum`/`hostRouteSmoke`/`updateFlowSmoke`/`handoffControlPanel`), the dashboard barrel re-exports them all at the experimental tier, the runtime barrel + Quest runtime entries (`main.js`/`arenaRuntime.js`/`bots.js`) import no Continuum dashboard/host module, and the BotAgent decision seam (`engine/entities/bot-agent.js`) stays pure (single `config.js` import; no THREE/Rapier/DOM/dashboard reach). **Follow-up (deferred, bigger, not MVP-needed):** migrating the stateful BotAgent runtime (tick/shoot/cooldown/blowback/death/respawn, currently inline in `src/bots.js`) behind the pure `bot-agent.js` seam + wiring `decideActions()` once it is allocation-free — a behaviour-touching change out of scope for this thin refactor.
- Further player-boundary / FSM cleanup that is not directly required for the MVP.

### Standing security gates for live promotion
- SEC-1 leaderboard publish gate — BUILT v0.2.257 (`src/engine/leaderboard/publishGate.js`): a pure, node-safe structural gate (signer match + event shape + score validity + abuse ceilings + consent + topic tag) wired opt-in into the publisher adapter so a relay write can never run ungated. **CRYPTO UPGRADE LANDED v0.2.277** — the gate now runs real BIP-340 schnorr verification (reusing the shared `src/engine/crypto/nostrSig.js` verifier, moved there from `gateway/`): structural checks are a fast pre-flight, then `verifyNostrEventSig(event)` must pass; a tampered/wrong-key/stub-signed event fails closed and only a full pass returns `trust: 'crypto-verified'` (no structure-only trusted path remains). **LIVE RELAY WRITE PROMOTED v0.2.285** — the actual NIP-07 sign + relay fan-out now ships (`src/engine/leaderboard/livePublish.js`, wired into the title-screen leaderboard card). It runs strictly behind this gate: a consented, `crypto-verified` finalised score is the only thing that reaches a relay; everything else fails closed.
- SEC-2 handoff verification gate — BUILT v0.2.252 (`src/engine/gateway/handoffVerify.js`): structural accept-verification (host match + request reference + traveller addressing + https spawn) wired into the handshake controller.
- SEC-3 product URL hardening gate — BUILT v0.2.253 (`src/engine/gateway/urlHarden.js`): scheme/host allowlist + private-host rejection + traveller-npub append, wired into `_executeJump` so no URL becomes navigable until it clears.

### Live gateway promotion — Milestone 2 (n2n hop)
- **P1 — signed travel-request/confirm handshake — LANDED (NIP-07, SEC-2/S1).** Crypto-verified accept arms the hop; the controller only arms on `trust === 'crypto-verified'`.
- **P2 + P3 — cross-host n2n hop carrying npub + two-instance interop harness — LANDED v0.2.275-alpha (security).** The traveller's verified npub now survives the cross-host jump and is re-verified on the DESTINATION host before seating, so a player arrives on host B seated as their real nostr identity rather than fresh anon — and an unverified/tampered/impostor hop FAILS CLOSED to anon.
  - **Host-side seating gate (`src/engine/gateway/handoffArrival.js`, NEW):** `readArrivingTraveller(url)` parses the `torii-traveller` spawn-URL npub (HINT only, forgeable); `verifyArrival({arrivingPubkey, request, expectedHostPubkey})` seats ONLY when the traveller's SIGNED travel request (a) is present with its signed envelope, (b) was authored by the arriving npub, (c) is addressed to this host, and (d) passes BIP-340 schnorr verification — returning `trust: 'crypto-verified'` only on a full pass; `seatArrivalDecision(verdict)` fails closed to `{identity:null, anon:true}` on any mismatch. S1 gates seating, not just the hop.
  - **`extractTravelRequest` (MODIFIED):** now carries the signed envelope (`id`, `sig`, `signed{pubkey,created_at,kind,tags,content}`) so the host can recompute the NIP-01 id and verify the request signature on arrival.
  - **`handshakeController.js` (MODIFIED):** accept-time re-verifies the incoming request (refuses to accept a forged/unsigned request — `request-unverified`), records accepted travellers in-session, and exposes `admitArrival(url, opts)` that reads the inbound npub, requires a configured host identity, re-verifies (in-session record or relay re-read), and returns `{seated, npub, trust, anon}`.
  - **`main.js` arrival seam (MODIFIED):** on load reads the inbound npub + host identity (`window.__toriiHostPubkey` / `<meta name="torii-host-pubkey">`), only seats ANONYMOUS sessions (never hijacks a logged-in operator), re-reads the signed request from relays, and on a crypto-verified verdict seats the arriving identity (sets `state.nostrPubkey`/`nostrName`, `setOurPubkey`, emits `NOSTR_LOGIN`, re-renders the gateway card).
  - **P3 two-instance interop harness (`tests/gateway-interop.test.js`, NEW, 5 tests):** two independent handshake-controller instances (A=traveller, B=host), each with its own BIP-340 keypair, over ONE shared in-memory relay — drives the full live flow A requests → B accepts → A arms → A jumps carrying its npub → B seats A by verified npub; plus cold-load B (no session memory) re-reads the signed request from the relay and still seats; impostor URL → anon; tampered request → refused; forged incoming request can never be ACCEPTED. Arrival unit gate locked in `tests/handoff-arrival.test.js` (NEW, 10 tests). Suite: **122 → 124 files, 1858 tests green**; `npm run check` 16/16 GREEN.
  - **Residual gap / follow-up for the LIVE two-host test:** the spawn-URL npub is not yet bound to a fresh per-hop signature — seating relies on a valid signed travel request existing on the relay (authored by the arriving npub, addressed to the host). A live two-host run should confirm CSP allows the hop target host and exercise the relay re-read path under real network latency. SEC-gated live-relay writes stay parked behind a manual deploy.

These are not Milestone 1 delivery tasks unless live promotion is explicitly being advanced, but they are also not ordinary backlog items to forget. Keep them visible as standing gates that must be cleared before the related live features are promoted.

### Instance access model & write authority (Milestone 2 theme)

Owner-set design constraint (v0.2.357-alpha, refined v0.2.358-alpha): travel between gates / instances is PUBLIC BY DEFAULT — any npub can resolve a listed gateway and arrive at any listed instance without invite or allow-list. The instance OWNER can narrow arrival on a per-instance basis to `public` (default) / `follows-only` ("follow me to enter") / `whitelist` (admin-curated allow-list) / `invite-only` (per-visitor invite). Visiting npubs are READ-ONLY by default — they can move, play the arena, visit the nap zone, browse the shop, and buy things (commerce stays out-of-band on the marketplace, unchanged) — but they cannot mutate world state.

Instance-admin surfaces live on a per-instance settings page reachable from the Quest Home Screen (the title screen) via a small `⚙ INSTANCE SETTINGS` corner link. The link is visible ONLY to the logged-in instance admin (operator's npub === deployment's host pubkey via the existing `_hostIdentity()` / `window.__toriiHostPubkey` seam). The settings page will grow to hold identity / appearance / moderation / write-authority controls; **Access** is the first section.

- **ACC-1 — Public arrival default + admin-settings shell + Access placeholder — LANDED v0.2.358-alpha (ACC).** The title-screen Instance Settings shell exists: `⚙ INSTANCE SETTINGS` corner link (`#instance-settings-link`, next to the existing project-dashboard link) is hidden by default and unhides only when the operator's logged-in npub matches the deployment's configured host pubkey. Clicking opens an inert overlay (`#instance-settings-panel`) rendered from the pure `src/engine/ui/instanceSettings.js` view-model. One real section — **Access** — shows `Arrival: Public` (read-only) + a "Coming soon" list of the three future modes (`follow-me` / `whitelist` / `invite-only`). A second `More coming soon` section marks that identity / appearance / moderation / write-authority will land here next. Nothing on the page changes the world: no gate change, no relay call, no navigation, no sign; SEC-1/SEC-2/SEC-3 untouched. Visibility rule is `isInstanceAdmin({ operatorPubkey, hostPubkey })` (fail-closed on any malformed / mismatched / anon input). Tests: 23 new cases in `tests/instance-settings.test.js` covering visibility rule, model shape, renderer HTML output, escaping of hostile input, case-insensitive hex normalisation, defence-in-depth on missing/hidden model, close-button data-action hook.
- **ACC-2a — `follows-only` arrival mode: follow-graph query + policy check (Milestone 2, deferred).** Wire the `follows-only` mode: relay-read the follow graph (kind 3 / NIP-02), decide semantics (default "visitor follows owner" — the "follow me to enter" pattern the owner asked for; keep visitor-follows-owner / owner-follows-visitor / mutual as a config knob), cache the follow set, and enforce it in the arrival policy check that runs AFTER SEC-2 crypto verify. Includes the small relaxation of `resolveHandoffSpawn` so `expectedPlayerPubkey` becomes optional on the public arrival path (SEC-2 still verifies the envelope's own claimed pubkey; only the "host names a specific expected traveller" requirement relaxes). Fail-closed default preserved.
- **ACC-2b — `whitelist` + `invite-only` arrival modes: allow-list state + policy check (Milestone 2, deferred; one slice — they share the same admin-curates-a-list plumbing).** Data model for the per-instance allow-list (owner-signed list event, or a per-instance NIP-51 list), owner-facing UI in the Access section to add/remove members, policy check that runs AFTER SEC-2 crypto verify and admits only listed npubs. `invite-only` is a subset shape of the same list plumbing (per-visitor entries, potentially time-bounded / single-use); land as one slice with a mode toggle rather than duplicate the storage.
- **ACC-3 — Write authority via owner-curated admin group — DEFERRED WHOLE-SLICE (Milestone 2, big).** By default a visiting npub can NEVER change anything in an instance. Write capability (place / edit / remove world objects, change zone config, moderate, etc.) is gated by an OWNER-CURATED ADMIN GROUP the owner maintains per-instance. This is a full slice — data model for admin groups, owner UX to add/remove members (rendered under the Instance Settings page from ACC-1), per-action write-gate enforcement across every mutating surface (world objects, zone config, shop listing edits if any land, moderation), and audit — and is NOT required for the public-travel / read-only-visit default to be usable. Track as its own slice; do NOT bundle into ACC-2a / ACC-2b. Read-only visitor default holds until this lands.

These are Milestone 2 access-model items and are not Milestone 1 delivery tasks. Keep them visible so they are not forgotten once the live n2n hop is being advanced.

## Audit follow-ups (v0.2.259 optimization pass)

Remaining items from the v0.2.259 code audit (`torii-quest-audit-v0.2.259.md`). Already landed in v0.2.260: E1 (vitest pool=threads, 28.7s→2.7s), GLB1+GLB2 (−66% assets, DRACOLoader), R3 (SW precache trim), S5 (relay fan-out), S2 (port hardening), repo hygiene. Landed in v0.2.261: **R1** (dashboard SDK split — app chunk −19.4% raw / −19.7% gzip). Landed in v0.2.263: **S1** (real BIP-340 schnorr verify in the gateway handoff). Landed in v0.2.264: **R2** (lazy-load THREE behind ENTER ARENA). Landed in v0.2.265: **E2** (foundation profile → `vitest --changed`). Landed in v0.2.266: **S4 + S3** (vendor Draco locally + CSP via HTTP header, no gstatic — the last third-party runtime origin is gone). Landed in v0.2.267: **E3** (split the three monolith test files along their `describe` blocks — 108→122 files, 1843 tests unchanged). Landed in v0.2.268: **R4** (next barrel-leak audit — audited, NO actionable leaks: `shellReport.js` ~52 KB + the `sdk/index.js` 54-export barrel are both arena-path-only, the shell's 35 first-paint modules are all genuine pre-arena flow; shell `torii-entry.js` unchanged at 103.64 KB / 35.66 KB gzip, R2 boundary intact).

Each item is sized as a single thin slice. Keep them in priority order; pick one per session.

- **S1 — schnorr verify in handoffVerify — LANDED v0.2.263 (security).** Replaced structural-only SEC-2 with a real BIP-340 signature verification. The host's accept is a NIP-01 nostr event; `verifyHandoff()` now recomputes the event id and runs `schnorr.verify(sig, idBytes, hostPubkey)` (new `src/engine/gateway/nostrSig.js`), failing closed on a missing/forged/tampered signature and returning `trust: 'crypto-verified'` only on a full pass. The handshake controller arms the hop ONLY on `crypto-verified`. Adds the project's **first runtime crypto dependency** (`@noble/curves` + transitive `@noble/hashes`); app chunk +30.5 KB raw / +12.5 KB gzip (262→292 KB raw). Tests: 5 new crypto cases in `tests/travel-request.test.js` (valid pass, tampered body, wrong-key, missing sig, unsigned-no-longer-arms) + handshake-controller arming reworked to real schnorr sigs.
- **R2 — lazy-load THREE behind ENTER ARENA — LANDED v0.2.264 (perf).** Split `main.js` into a three-free shell (title screen / gateway cards / previews / login / char select / zone-route notice) + a new `src/arenaRuntime.js` that owns every three-dependent surface (scene/renderer, arena geometry, the game loop, players/bots/weapons/physics viewmodels, the in-world portal mesh + ToriiDebug). The shell `await import('./arenaRuntime.js')`s ONLY inside the ENTER ARENA handler, so `three-vendor` is now a dynamic-import target (no longer in `index.html`'s preload set, no static import in the app chunk). The title-screen n2n handshake + presence polling moved from the game loop to a three-free shell rAF ticker so it keeps running before/after the arena boots. **Bundle:** app/shell chunk 292.4 KB → 103.6 KB raw (96.0 → 35.6 KB gzip, −64.6% raw / −62.9% gzip); new `arenaRuntime` chunk 190.9 KB (62.8 KB gzip); `three-vendor` 625.1 KB raw / 160.6 KB gzip is no longer paid on first paint — it loads on demand after ENTER ARENA (first-paint JS drops ~625 KB raw / ~161 KB gzip). Tests: all 1843 green (boot-order + portal-placement + entry-flow contracts repointed at `arenaRuntime.js`; `main.js` now asserted to never statically import `scene.js`).
- **E2 — replace foundation profile with `vitest --changed origin/main` — LANDED v0.2.265 (DX).** `npm run test:foundation` no longer runs the hand-curated file list — it now runs `vitest run --changed origin/main --passWithNoTests`, i.e. only the suites whose import graph is touched by the diff vs `origin/main` (vitest's related-graph semantics), so there is no manual list to keep in sync as the suite grows. `--passWithNoTests` makes a clean tree (or a checkout where `origin/main` is absent) a graceful no-op (exit 0, "No test files found") rather than a failure. The curated foundation set is preserved as `npm run test:foundation:list` (still backed by `tools/testProfiles.mjs` → it remains the source of the dashboard/handoff profile counts and the validate-against-disk guard); `test:fast` (tiny innermost core) and the full-suite gate (`npm test` / `npm run test:release`) are unchanged. Dashboard "Tests" label updated to reflect the new behaviour.
- **S4 — vendor Draco decoder under public/draco/ — LANDED v0.2.266 (security/CSP).** The three DRACOLoader decoder (matching `three` v0.184.0, copied from `node_modules/three/examples/jsm/libs/draco/gltf/`: `draco_decoder.js` 512 KB, `draco_decoder.wasm` 192 KB, `draco_wasm_wrapper.js` 58 KB) is now vendored at `public/draco/` and served same-origin. All 7 `setDecoderPath('https://www.gstatic.com/draco/…')` call sites (`playerModel`, `arena`, `botModel`, `napNpc`, `firstPersonBody`, `weapons` + the `optimize-glbs.mjs` doc) now point at `/draco/`. `gstatic.com` is gone from the CSP entirely — no third-party runtime origin remains (decoder fetch is covered by `connect-src 'self'`; the decoder's blob worker by `worker-src 'self' blob:`). The DRACOLoader stays on the arena path (R2 boundary intact, not pulled into the shell).
- **S3 — move CSP from meta tag to HTTP header — LANDED v0.2.266 (security).** CSP is no longer a `<meta http-equiv>` tag — it ships as an HTTP response header from a single source of truth (`tools/csp.mjs` → `CSP_VALUE`), emitted in three places that cannot drift: `dist/_headers` (static host, written by a new vite plugin at build), the `vite preview` server, and the Caddy/Nginx blocks in `VPS_INSTALL.md`. `script-src` uses `'strict-dynamic'` + a **sha256 of the single trusted classic inline bootstrap** (`'self'`/`blob:` kept as the CSP-Level-2 fallback). At build the plugin removes the static entry `<script>` tag and has the bootstrap `import('/assets/torii-entry.js')` (entry filename pinned via `entryFileNames`), so `strict-dynamic` propagates trust to the entry + every lazy chunk (three-vendor, rapier, arenaRuntime) with no per-build hash churn. `npm run check` block [16] fails on: a meta CSP in index.html or dist, any gstatic reference, missing Draco files / wrong `setDecoderPath`, a CSP missing `strict-dynamic`/inline-sha/required directives, a `dist/_headers` mismatch, or a recomputed inline-script sha that doesn't match `INLINE_SCRIPT_SHA256`.
- **E3 — split the three biggest test files — LANDED v0.2.267 (DX).** The three monoliths were split along their top-level `describe` blocks, semantics unchanged: `torii-quest-dashboard.test.js` (1633 lines / 149 tests / 27 describes) → 10 focused files (`.helpers/.model/.render/.health/.milestones/.readiness/.validation/.approval/.clickthrough/.sdk`); `next-action-state.test.js` (377 lines / 28 tests / 4 describes) → 4 files (`.constants/.assembly/.formatters/.version`, sharing `tests/_next-action-state-helpers.js`); `md-patch.test.js` (424 lines / 50 tests / 11 describes) → 3 files (`.resolve/.transforms/.apply`, sharing `tests/_md-patch-helpers.js`). Suite: **108 files → 122 files, 1843 tests unchanged**. The `CURRENT_TEST_STATUS`/`DEFAULT_TEST_STATUS` file-count drift guard was bumped 108→122; `test:foundation`/`test:foundation:list` were unaffected (the curated `foundation` list never referenced any of the three). `npm run check` stays 16/16 green.
- **R4 — next barrel-leak audit — AUDITED, NO LEAKS FOUND (shell clean post-R2) v0.2.268 (perf).** Swept the shell entry (`src/main.js` static import graph, with the `arenaRuntime.js` dynamic import excluded as the deferral boundary) for barrel leaks and oversight strings at first paint. **No actionable leak — no code change.** (a) `shellReport.js` (51,822 B) is reached ONLY by `proofSurfaceCheck.js` + `toriiDebug.js`, both arena-path (`toriiDebug` ← `arenaRuntime.js`; `proofSurfaceCheck` ← proofSurface render/gate ← `arena.js`) — confirmed by a static BFS that does NOT reach it from the shell AND by its unique fixture string `npub1demo0gateway0report…` being present in `arenaRuntime-*.js` (1×) and ABSENT from `torii-entry.js` (0×). (b) The sole re-export barrel `src/sdk/index.js` (54 re-exports) is imported ONLY by `proofSurfaceCheck.js` — arena-only, never reached from the shell. (c) No shell module is a non-first-paint heavyweight: the 35 shell-reachable modules (258 KB src → 103.64 KB / 35.66 KB gzip) are all genuine pre-arena flow (gateway/nostr/consent/hud/state/zoneRoute). (d) No non-three module is duplicated across shell/arena. (e) The only shell demo-fixtures are tiny (~60 B) preview fallbacks in `main.js`. **Bundle UNCHANGED (audit only): shell `torii-entry.js` 103.64 KB raw / 35.66 KB gzip (before == after); `arenaRuntime` 193.17 KB / 63.86 KB gzip; `three-vendor` 625.07 KB / 160.55 KB gzip still deferred (R2 intact).** `npm run check` 16/16 GREEN; `npx vitest run` 1843/1843 across 122 files. Full write-up in `R4-implementation-report.md`.

## Milestone 3 — Post-MVP polish and redesign

Goal: improve visual quality, readability, and feel once the MVP loop is already working.

### Deferred to Milestone 3
- Shooter feel polish.
- Mesh / material polish.
- Broader UX refinement.
- Broader bot behaviour polish.
- Larger Continuum redesign follow-up work such as DASHBOARD-LAYOUT-1 richer cards / tables / denser visual redesign.
- Any nonessential visual or presentation improvements that do not change the MVP proof.

## Milestone 4 — Visuals, gameplay, environment & nostr/bitcoin depth

Goal: lift the game from a functional MVP into a distinctive, replayable arena shooter that leans into the cyberpunk-feudal + bitcoin/nostr identity. Each item is a thin vertical slice with a clear green checkpoint.

### Visuals
- M4-V1 — HUD legibility pass: bump top-right stats (SATS/KILLS/HP) font size + contrast, add text-shadow / background pills, fix the top-left TORII QUEST logo crop. (Fast win; problem observed across multiple verification rounds.)
- M4-V2 — Post-processing bloom: add a Three.js `EffectComposer` UnrealBloom pass so the neon fence, aurora, cyan paths, and torii gate read as actually luminous. Cheap, high visual payoff.
- M4-V3 — Dynamic muzzle/impact lighting: gunfire and bot hits cast short-lived point-light flashes. Adds punch to currently-static arena lighting.
- M4-V4 — Player character model rig/animation pass: fix jagged collar/neck polygons visible in third-person; clean rig + idle/move animations (Blender/Meshy workflow).
  - M4-V4a — IN-CODE normals/material hygiene (LANDED): guarded `computeVertexNormals` (only if normals absent) + force `flatShading=false` on player GLB loads (`playerModel.js`, `firstPersonBody.js`). Rules out the normals/material hypothesis. Animation/AnimationMixer was already complete (no work needed).
  - M4-V4b — ASSET WORK (deferred to Blender/Meshy, owner-driven): the headless FP body GLB (`chiefmonkey-headless.glb`) has an uncapped neck stump; `chiefmonkey6.glb` may have coarse collar topology. Re-export both GLBs from Blender with a capped neck + clean collar topology + smooth vertex normals. No code change can repair torn/uncapped mesh geometry.
  - NOTE: the original paused-third-person "jagged collar/neck" screenshot was the headless body's open neck stump showing through fly mode before the `setFlyHidden` fix (v0.2.345). Normal play only shows the headless FP arms (layer 2); the full body appears only in the wall mirror. So the visible defect is already hidden in gameplay; V4b is cosmetic polish on the mirror/headless asset.

### Gameplay
- M4-G1 — Bot AI upgrade: obstacle avoidance + cover-seeking + flanking + 2-3 difficulty tiers. Replaces current direct-position-mutation + simple proximity clamp. Biggest single gameplay lever.
- M4-G2 — Weapon variety: 2-3 weapons (pistol / rifle / shotgun) with distinct range, damage, recoil, and spread. Combines with fly-mode verticality + 21m ceiling.
- M4-G3 — Pickups: health, ammo, and sats drops from bots. Gives the arena a movement loop and a reason not to camp the safe zone.
- M4-G4 — Verticality-aware arena design: elevated platforms, rooftop cover, risk/reward of flying vs. grounded, built around the fly + 21m ceiling + safe-zone ruleset.

### Environment
- M4-E1 — Day/night cycle (or dusk mode): slow sky/lighting cycle that changes combat visibility and mood; ties into the aurora sky work.
- M4-E2 — Water interaction: splash VFX + audio on beach/water entry, shallow-wading movement penalty. Brings the outside-the-fence dead space to life.
- M4-E3 — Torii gate as a functional portal: make the east-fence gate gap a real gateway to another zone/biome (the namesake gateway metaphor).
- M4-E4 — Ambient soundscape: layered bed (wind, water, distant thunder, bird calls matching the V-shaped flocks) + spatial combat audio.

### Nostr / Bitcoin (differentiator)
- M4-N1 — Nostr-powered live leaderboards: kill counts / high scores signed and broadcast over nostr relays; verifiable, decentralized, no central DB. Builds on the existing NIP-07 leaderboard publish (M2, SEC-1 gate).
- M4-N2 — SATS economy: bots drop sats, sats buy weapons/upgrades, optional small ante/betting mechanic for rounds. Ties bitcoin into actual mechanics, not just flavor.
- M4-N3 — Player identity via nostr keys: log in with an npub, carry loadout/score across sessions and eventually across instances.

### Priority order (suggested)
1. M4-V1 HUD legibility (fast, observed problem)
2. M4-V2 bloom post-processing (huge visual payoff, cheap)
3. M4-G1 bot AI / cover (biggest gameplay lever)
4. M4-V4 character model rig pass
5. M4-G2 weapon variety, M4-G3 pickups, then the rest in priority order.

## Torii Quest ToDo - Working rules

- Prefer thin vertical slices over polish traps.
- Break every task into small chunks that can usually be finished in a few hours, not open-ended multi-day jobs.
- Prefer thin vertical slices with a clear green checkpoint, visible outcome, and one obvious next step.
- Avoid long-running jobs that stall flow, hide risk, or delay feedback; if a task starts to sprawl, split it again before continuing.
- Each chunk should produce meaningful progress on the MVP proof, app separation, safety, or demo stability.
- End each chunk in a handoff-safe state: code green, tests updated where relevant, docs/indexes touched, and the next safe task easy to pick up.
- If a job cannot be described as a few-hour slice with a concrete finish line, it is too big and must be broken down first.
- Every change should improve clarity, safety, testability, or separation when practical.
- Keep Quest and Continuum task lists separate.
- If a task does not directly help the MVP proof, clean app separation, or demo stability, defer it.
- When a current job becomes too large, pause it safely: commit or checkpoint all green work, record the current state in HANDOFF / NEXT_ACTION_STATE, then replace it with a list of 1–3 hour follow-up slices.
- Never abandon partially-completed work; always capture its state before changing the active job.

### Bot / progress handling

- Pause any bot-related work that does not directly improve MVP stability, app boundaries, or player feedback.
- Do not let long-running BotAgent jobs block progress reports or dashboard updates; report partial landings as soon as they are green.
- Prefer short, observable slices (1–3 hours) that show visible changes in bot behaviour, load, or safety.

## Historical completed (archive)

These items are DONE and kept only as struck markers for the Continuum dashboard's completed-task count. Details are in `torii-quest-progress.md` and git history.

- ~~ARS-1~~ ~~Debug dump / handoff snapshot (`engine/debug/snapshot.js`). DONE v0.2.130.~~
- ~~ARS-2~~ ~~Physics interaction API (`engine/physics/interactions.js`). DONE v0.2.130.~~
- ~~ARS-3~~ ~~Rapier raycast service (`engine/physics/raycastService.js`), migration complete v0.2.133.~~
- ~~ARS-5~~ ~~SDK/API skeleton (`src/sdk/index.js`). DONE v0.2.131.~~
- ~~ARS-7~~ ~~Handoff template (`torii-quest-handoff.md`). DONE v0.2.130.~~
- ~~TESTPROF-1~~ ~~Test profile system (`tools/testProfiles.mjs`). DONE v0.2.173.~~
